
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Lospec = (props: BadgeProps) => <Badge name="Lospec" {...props} backgroundColor="#EAEAEA" />

export default Lospec
