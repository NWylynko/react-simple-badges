
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Threejs = (props: BadgeProps) => <Badge name="Three.js" backgroundColor="#000000" {...props} />

export default Threejs
