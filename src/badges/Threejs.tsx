
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Threejs = (props: BadgeProps) => <Badge name="Three.js" {...props} backgroundColor="#000000" />

export default Threejs
