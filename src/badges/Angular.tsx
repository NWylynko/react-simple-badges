
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Angular = (props: BadgeProps) => <Badge name="Angular" {...props} backgroundColor="#DD0031" />

export default Angular
