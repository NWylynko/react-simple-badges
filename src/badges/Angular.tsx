
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Angular = (props: BadgeProps) => <Badge name="Angular" backgroundColor="#DD0031" {...props} />

export default Angular
