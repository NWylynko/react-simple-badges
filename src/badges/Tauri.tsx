
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Tauri = (props: BadgeProps) => <Badge name="Tauri" backgroundColor="#FFC131" {...props} />

export default Tauri
