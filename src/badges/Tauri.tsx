
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Tauri = (props: BadgeProps) => <Badge name="Tauri" {...props} backgroundColor="#FFC131" />

export default Tauri
