
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Warp = (props: BadgeProps) => <Badge name="Warp" {...props} backgroundColor="#01A4FF" />

export default Warp
