
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Billboard = (props: BadgeProps) => <Badge name="Billboard" {...props} backgroundColor="#000000" />

export default Billboard
