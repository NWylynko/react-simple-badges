
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Billboard = (props: BadgeProps) => <Badge name="Billboard" backgroundColor="#000000" {...props} />

export default Billboard
