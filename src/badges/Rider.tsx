
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Rider = (props: BadgeProps) => <Badge name="Rider" {...props} backgroundColor="#000000" />

export default Rider
