
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Rider = (props: BadgeProps) => <Badge name="Rider" backgroundColor="#000000" {...props} />

export default Rider
