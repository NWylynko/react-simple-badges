
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CarThrottle = (props: BadgeProps) => <Badge name="Car Throttle" {...props} backgroundColor="#FF9C42" />

export default CarThrottle
