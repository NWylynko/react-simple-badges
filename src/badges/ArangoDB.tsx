
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ArangoDB = (props: BadgeProps) => <Badge name="ArangoDB" {...props} backgroundColor="#DDE072" />

export default ArangoDB
