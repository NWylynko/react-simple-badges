
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const ArangoDB = (props: BadgeProps) => <Badge name="ArangoDB" backgroundColor="#DDE072" {...props} />

export default ArangoDB
