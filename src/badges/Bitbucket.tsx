
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Bitbucket = (props: BadgeProps) => <Badge name="Bitbucket" backgroundColor="#0052CC" {...props} />

export default Bitbucket
