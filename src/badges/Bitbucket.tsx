
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Bitbucket = (props: BadgeProps) => <Badge name="Bitbucket" {...props} backgroundColor="#0052CC" />

export default Bitbucket
