
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GNU = (props: BadgeProps) => <Badge name="GNU" {...props} backgroundColor="#A42E2B" />

export default GNU
