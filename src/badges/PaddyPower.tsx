
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PaddyPower = (props: BadgeProps) => <Badge name="Paddy Power" {...props} backgroundColor="#004833" />

export default PaddyPower
