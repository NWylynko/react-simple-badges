
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Discourse = (props: BadgeProps) => <Badge name="Discourse" {...props} backgroundColor="#000000" />

export default Discourse
