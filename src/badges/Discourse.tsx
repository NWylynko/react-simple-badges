
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Discourse = (props: BadgeProps) => <Badge name="Discourse" backgroundColor="#000000" {...props} />

export default Discourse
