
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const InVision = (props: BadgeProps) => <Badge name="InVision" {...props} backgroundColor="#FF3366" />

export default InVision
