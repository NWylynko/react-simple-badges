
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Statamic = (props: BadgeProps) => <Badge name="Statamic" {...props} backgroundColor="#FF269E" />

export default Statamic
