
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Statamic = (props: BadgeProps) => <Badge name="Statamic" backgroundColor="#FF269E" {...props} />

export default Statamic
