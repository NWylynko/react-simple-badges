
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Vimeo = (props: BadgeProps) => <Badge name="Vimeo" {...props} backgroundColor="#1AB7EA" />

export default Vimeo
