
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const FourTwo = (props: BadgeProps) => <Badge name="42" {...props} backgroundColor="#000000" />

export default FourTwo
