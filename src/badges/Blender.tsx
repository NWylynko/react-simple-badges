
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Blender = (props: BadgeProps) => <Badge name="Blender" backgroundColor="#F5792A" {...props} />

export default Blender
