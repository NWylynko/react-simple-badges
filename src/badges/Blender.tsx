
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Blender = (props: BadgeProps) => <Badge name="Blender" {...props} backgroundColor="#F5792A" />

export default Blender
