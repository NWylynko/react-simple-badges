
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Etsy = (props: BadgeProps) => <Badge name="Etsy" {...props} backgroundColor="#F16521" />

export default Etsy
