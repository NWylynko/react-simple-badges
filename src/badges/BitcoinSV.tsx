
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const BitcoinSV = (props: BadgeProps) => <Badge name="Bitcoin SV" {...props} backgroundColor="#EAB300" />

export default BitcoinSV
