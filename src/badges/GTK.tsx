
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const GTK = (props: BadgeProps) => <Badge name="GTK" backgroundColor="#7FE719" {...props} />

export default GTK
