
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GTK = (props: BadgeProps) => <Badge name="GTK" {...props} backgroundColor="#7FE719" />

export default GTK
