
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Iceland = (props: BadgeProps) => <Badge name="Iceland" {...props} backgroundColor="#CC092F" />

export default Iceland
