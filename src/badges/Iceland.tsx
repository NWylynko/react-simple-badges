
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Iceland = (props: BadgeProps) => <Badge name="Iceland" backgroundColor="#CC092F" {...props} />

export default Iceland
