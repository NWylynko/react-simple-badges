
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Splunk = (props: BadgeProps) => <Badge name="Splunk" {...props} backgroundColor="#000000" />

export default Splunk
