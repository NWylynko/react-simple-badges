
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Splunk = (props: BadgeProps) => <Badge name="Splunk" backgroundColor="#000000" {...props} />

export default Splunk
