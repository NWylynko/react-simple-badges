
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Budibase = (props: BadgeProps) => <Badge name="Budibase" {...props} backgroundColor="#000000" />

export default Budibase
