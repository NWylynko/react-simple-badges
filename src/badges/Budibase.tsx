
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Budibase = (props: BadgeProps) => <Badge name="Budibase" backgroundColor="#000000" {...props} />

export default Budibase
