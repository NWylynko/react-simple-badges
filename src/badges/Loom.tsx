
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Loom = (props: BadgeProps) => <Badge name="Loom" backgroundColor="#625DF5" {...props} />

export default Loom
