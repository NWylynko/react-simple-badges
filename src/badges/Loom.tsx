
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Loom = (props: BadgeProps) => <Badge name="Loom" {...props} backgroundColor="#625DF5" />

export default Loom
