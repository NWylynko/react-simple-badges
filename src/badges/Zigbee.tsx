
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Zigbee = (props: BadgeProps) => <Badge name="Zigbee" backgroundColor="#EB0443" {...props} />

export default Zigbee
