
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Zigbee = (props: BadgeProps) => <Badge name="Zigbee" {...props} backgroundColor="#EB0443" />

export default Zigbee
