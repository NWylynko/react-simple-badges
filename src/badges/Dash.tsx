
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Dash = (props: BadgeProps) => <Badge name="Dash" {...props} backgroundColor="#008DE4" />

export default Dash
