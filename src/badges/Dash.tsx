
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Dash = (props: BadgeProps) => <Badge name="Dash" backgroundColor="#008DE4" {...props} />

export default Dash
