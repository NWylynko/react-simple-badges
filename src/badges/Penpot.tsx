
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Penpot = (props: BadgeProps) => <Badge name="Penpot" backgroundColor="#000000" {...props} />

export default Penpot
