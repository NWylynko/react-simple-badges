
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const DoorDash = (props: BadgeProps) => <Badge name="DoorDash" {...props} backgroundColor="#FF3008" />

export default DoorDash
