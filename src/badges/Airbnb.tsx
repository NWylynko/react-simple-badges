
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Airbnb = (props: BadgeProps) => <Badge name="Airbnb" {...props} backgroundColor="#FF5A5F" />

export default Airbnb
