
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AlpineLinux = (props: BadgeProps) => <Badge name="Alpine Linux" {...props} backgroundColor="#0D597F" />

export default AlpineLinux
