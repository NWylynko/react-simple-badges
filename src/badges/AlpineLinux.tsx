
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const AlpineLinux = (props: BadgeProps) => <Badge name="Alpine Linux" backgroundColor="#0D597F" {...props} />

export default AlpineLinux
