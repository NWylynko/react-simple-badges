
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MacOS = (props: BadgeProps) => <Badge name="macOS" {...props} backgroundColor="#000000" />

export default MacOS
