
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const MacOS = (props: BadgeProps) => <Badge name="macOS" backgroundColor="#000000" {...props} />

export default MacOS
