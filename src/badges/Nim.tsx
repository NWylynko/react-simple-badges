
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Nim = (props: BadgeProps) => <Badge name="Nim" {...props} backgroundColor="#FFE953" />

export default Nim
