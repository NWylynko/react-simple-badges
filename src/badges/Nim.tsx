
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Nim = (props: BadgeProps) => <Badge name="Nim" backgroundColor="#FFE953" {...props} />

export default Nim
