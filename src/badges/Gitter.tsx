
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Gitter = (props: BadgeProps) => <Badge name="Gitter" {...props} backgroundColor="#ED1965" />

export default Gitter
