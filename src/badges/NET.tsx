
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const NET = (props: BadgeProps) => <Badge name=".NET" backgroundColor="#512BD4" {...props} />

export default NET
