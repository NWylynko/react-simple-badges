
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const NET = (props: BadgeProps) => <Badge name=".NET" {...props} backgroundColor="#512BD4" />

export default NET
