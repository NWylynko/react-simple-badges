
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Yahoo = (props: BadgeProps) => <Badge name="Yahoo!" {...props} backgroundColor="#6001D2" />

export default Yahoo
