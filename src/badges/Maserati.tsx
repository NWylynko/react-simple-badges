
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Maserati = (props: BadgeProps) => <Badge name="Maserati" {...props} backgroundColor="#0C2340" />

export default Maserati
