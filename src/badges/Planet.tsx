
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Planet = (props: BadgeProps) => <Badge name="Planet" backgroundColor="#009DB1" {...props} />

export default Planet
