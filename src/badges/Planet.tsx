
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Planet = (props: BadgeProps) => <Badge name="Planet" {...props} backgroundColor="#009DB1" />

export default Planet
