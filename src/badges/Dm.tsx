
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Dm = (props: BadgeProps) => <Badge name="dm" backgroundColor="#002878" {...props} />

export default Dm
