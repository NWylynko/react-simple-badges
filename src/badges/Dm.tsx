
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Dm = (props: BadgeProps) => <Badge name="dm" {...props} backgroundColor="#002878" />

export default Dm
