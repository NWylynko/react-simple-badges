
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Electron = (props: BadgeProps) => <Badge name="Electron" {...props} backgroundColor="#47848F" />

export default Electron
