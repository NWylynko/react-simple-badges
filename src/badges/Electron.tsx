
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Electron = (props: BadgeProps) => <Badge name="Electron" backgroundColor="#47848F" {...props} />

export default Electron
