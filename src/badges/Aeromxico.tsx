
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Aeromxico = (props: BadgeProps) => <Badge name="Aeroméxico" {...props} backgroundColor="#0B2343" />

export default Aeromxico
