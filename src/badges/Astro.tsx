
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Astro = (props: BadgeProps) => <Badge name="Astro" backgroundColor="#FF5D01" {...props} />

export default Astro
