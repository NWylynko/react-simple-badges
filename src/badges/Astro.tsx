
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Astro = (props: BadgeProps) => <Badge name="Astro" {...props} backgroundColor="#FF5D01" />

export default Astro
