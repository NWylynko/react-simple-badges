
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Jira = (props: BadgeProps) => <Badge name="Jira" {...props} backgroundColor="#0052CC" />

export default Jira
