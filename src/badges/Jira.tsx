
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Jira = (props: BadgeProps) => <Badge name="Jira" backgroundColor="#0052CC" {...props} />

export default Jira
