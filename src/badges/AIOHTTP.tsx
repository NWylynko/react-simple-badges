
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AIOHTTP = (props: BadgeProps) => <Badge name="AIOHTTP" {...props} backgroundColor="#2C5BB4" />

export default AIOHTTP
