
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Kitsu = (props: BadgeProps) => <Badge name="Kitsu" {...props} backgroundColor="#FD755C" />

export default Kitsu
