
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Vulkan = (props: BadgeProps) => <Badge name="Vulkan" backgroundColor="#AC162C" {...props} />

export default Vulkan
