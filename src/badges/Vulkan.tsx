
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Vulkan = (props: BadgeProps) => <Badge name="Vulkan" {...props} backgroundColor="#AC162C" />

export default Vulkan
