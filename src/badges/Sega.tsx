
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Sega = (props: BadgeProps) => <Badge name="Sega" {...props} backgroundColor="#0089CF" />

export default Sega
