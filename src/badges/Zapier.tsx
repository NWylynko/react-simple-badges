
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Zapier = (props: BadgeProps) => <Badge name="Zapier" {...props} backgroundColor="#FF4A00" />

export default Zapier
