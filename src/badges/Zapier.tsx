
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Zapier = (props: BadgeProps) => <Badge name="Zapier" backgroundColor="#FF4A00" {...props} />

export default Zapier
