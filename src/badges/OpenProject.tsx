
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const OpenProject = (props: BadgeProps) => <Badge name="OpenProject" {...props} backgroundColor="#0770B8" />

export default OpenProject
