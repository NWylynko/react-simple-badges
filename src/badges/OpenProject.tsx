
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const OpenProject = (props: BadgeProps) => <Badge name="OpenProject" backgroundColor="#0770B8" {...props} />

export default OpenProject
