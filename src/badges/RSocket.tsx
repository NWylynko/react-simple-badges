
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const RSocket = (props: BadgeProps) => <Badge name="RSocket" {...props} backgroundColor="#EF0092" />

export default RSocket
