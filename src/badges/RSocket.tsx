
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const RSocket = (props: BadgeProps) => <Badge name="RSocket" backgroundColor="#EF0092" {...props} />

export default RSocket
