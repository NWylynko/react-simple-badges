
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Rollupjs = (props: BadgeProps) => <Badge name="rollup.js" {...props} backgroundColor="#EC4A3F" />

export default Rollupjs
