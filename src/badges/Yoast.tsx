
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Yoast = (props: BadgeProps) => <Badge name="Yoast" backgroundColor="#A4286A" {...props} />

export default Yoast
