
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Yoast = (props: BadgeProps) => <Badge name="Yoast" {...props} backgroundColor="#A4286A" />

export default Yoast
