
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Dependabot = (props: BadgeProps) => <Badge name="Dependabot" backgroundColor="#025E8C" {...props} />

export default Dependabot
