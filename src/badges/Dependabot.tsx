
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Dependabot = (props: BadgeProps) => <Badge name="Dependabot" {...props} backgroundColor="#025E8C" />

export default Dependabot
