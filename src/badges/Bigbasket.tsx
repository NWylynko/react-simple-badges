
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Bigbasket = (props: BadgeProps) => <Badge name="bigbasket" backgroundColor="#A5CD39" {...props} />

export default Bigbasket
