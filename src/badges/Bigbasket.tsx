
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Bigbasket = (props: BadgeProps) => <Badge name="bigbasket" {...props} backgroundColor="#A5CD39" />

export default Bigbasket
