
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Dataverse = (props: BadgeProps) => <Badge name="Dataverse" {...props} backgroundColor="#088142" />

export default Dataverse
