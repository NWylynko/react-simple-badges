
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Dataverse = (props: BadgeProps) => <Badge name="Dataverse" backgroundColor="#088142" {...props} />

export default Dataverse
