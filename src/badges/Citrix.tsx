
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Citrix = (props: BadgeProps) => <Badge name="Citrix" backgroundColor="#452170" {...props} />

export default Citrix
