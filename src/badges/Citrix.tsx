
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Citrix = (props: BadgeProps) => <Badge name="Citrix" {...props} backgroundColor="#452170" />

export default Citrix
