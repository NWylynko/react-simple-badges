
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Dell = (props: BadgeProps) => <Badge name="Dell" {...props} backgroundColor="#007DB8" />

export default Dell
