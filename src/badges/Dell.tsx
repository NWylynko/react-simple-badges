
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Dell = (props: BadgeProps) => <Badge name="Dell" backgroundColor="#007DB8" {...props} />

export default Dell
