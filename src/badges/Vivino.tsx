
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Vivino = (props: BadgeProps) => <Badge name="Vivino" {...props} backgroundColor="#AA1329" />

export default Vivino
