
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Chef = (props: BadgeProps) => <Badge name="Chef" {...props} backgroundColor="#F09820" />

export default Chef
