
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Chef = (props: BadgeProps) => <Badge name="Chef" backgroundColor="#F09820" {...props} />

export default Chef
