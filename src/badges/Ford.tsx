
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Ford = (props: BadgeProps) => <Badge name="Ford" {...props} backgroundColor="#00274E" />

export default Ford
