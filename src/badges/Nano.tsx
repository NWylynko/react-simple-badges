
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Nano = (props: BadgeProps) => <Badge name="Nano" {...props} backgroundColor="#4A90E2" />

export default Nano
