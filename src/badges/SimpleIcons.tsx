
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const SimpleIcons = (props: BadgeProps) => <Badge name="Simple Icons" backgroundColor="#111111" {...props} />

export default SimpleIcons
