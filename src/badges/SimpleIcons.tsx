
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SimpleIcons = (props: BadgeProps) => <Badge name="Simple Icons" {...props} backgroundColor="#111111" />

export default SimpleIcons
