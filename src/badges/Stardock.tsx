
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Stardock = (props: BadgeProps) => <Badge name="Stardock" backgroundColor="#004B8D" {...props} />

export default Stardock
