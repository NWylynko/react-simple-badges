
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Stardock = (props: BadgeProps) => <Badge name="Stardock" {...props} backgroundColor="#004B8D" />

export default Stardock
