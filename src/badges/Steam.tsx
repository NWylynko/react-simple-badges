
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Steam = (props: BadgeProps) => <Badge name="Steam" backgroundColor="#000000" {...props} />

export default Steam
