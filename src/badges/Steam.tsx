
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Steam = (props: BadgeProps) => <Badge name="Steam" {...props} backgroundColor="#000000" />

export default Steam
