
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Prisma = (props: BadgeProps) => <Badge name="Prisma" {...props} backgroundColor="#2D3748" />

export default Prisma
