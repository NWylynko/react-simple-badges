
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Prisma = (props: BadgeProps) => <Badge name="Prisma" backgroundColor="#2D3748" {...props} />

export default Prisma
