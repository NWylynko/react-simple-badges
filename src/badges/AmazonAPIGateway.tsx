
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AmazonAPIGateway = (props: BadgeProps) => <Badge name="Amazon API Gateway" {...props} backgroundColor="#FF4F8B" />

export default AmazonAPIGateway
