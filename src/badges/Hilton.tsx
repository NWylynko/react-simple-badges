
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Hilton = (props: BadgeProps) => <Badge name="Hilton" {...props} backgroundColor="#124D97" />

export default Hilton
