
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Opsgenie = (props: BadgeProps) => <Badge name="Opsgenie" {...props} backgroundColor="#172B4D" />

export default Opsgenie
