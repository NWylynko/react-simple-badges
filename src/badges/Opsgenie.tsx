
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Opsgenie = (props: BadgeProps) => <Badge name="Opsgenie" backgroundColor="#172B4D" {...props} />

export default Opsgenie
