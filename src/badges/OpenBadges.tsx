
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const OpenBadges = (props: BadgeProps) => <Badge name="Open Badges" {...props} backgroundColor="#073B5A" />

export default OpenBadges
