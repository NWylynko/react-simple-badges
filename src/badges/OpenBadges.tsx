
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const OpenBadges = (props: BadgeProps) => <Badge name="Open Badges" backgroundColor="#073B5A" {...props} />

export default OpenBadges
