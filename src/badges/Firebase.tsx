
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Firebase = (props: BadgeProps) => <Badge name="Firebase" {...props} backgroundColor="#FFCA28" />

export default Firebase
