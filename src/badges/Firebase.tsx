
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Firebase = (props: BadgeProps) => <Badge name="Firebase" backgroundColor="#FFCA28" {...props} />

export default Firebase
