
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const JSON = (props: BadgeProps) => <Badge name="JSON" backgroundColor="#000000" {...props} />

export default JSON
