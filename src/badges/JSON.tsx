
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const JSON = (props: BadgeProps) => <Badge name="JSON" {...props} backgroundColor="#000000" />

export default JSON
