
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Pepsi = (props: BadgeProps) => <Badge name="Pepsi" {...props} backgroundColor="#2151A1" />

export default Pepsi
