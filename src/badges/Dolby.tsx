
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Dolby = (props: BadgeProps) => <Badge name="Dolby" {...props} backgroundColor="#000000" />

export default Dolby
