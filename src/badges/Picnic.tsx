
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Picnic = (props: BadgeProps) => <Badge name="Picnic" {...props} backgroundColor="#E1171E" />

export default Picnic
