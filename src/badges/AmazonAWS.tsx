
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AmazonAWS = (props: BadgeProps) => <Badge name="Amazon AWS" {...props} backgroundColor="#232F3E" />

export default AmazonAWS
