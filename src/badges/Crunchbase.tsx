
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Crunchbase = (props: BadgeProps) => <Badge name="Crunchbase" {...props} backgroundColor="#0288D1" />

export default Crunchbase
