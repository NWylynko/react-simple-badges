
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Crunchbase = (props: BadgeProps) => <Badge name="Crunchbase" backgroundColor="#0288D1" {...props} />

export default Crunchbase
