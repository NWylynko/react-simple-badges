
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CentOS = (props: BadgeProps) => <Badge name="CentOS" {...props} backgroundColor="#262577" />

export default CentOS
