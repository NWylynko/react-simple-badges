
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SumoLogic = (props: BadgeProps) => <Badge name="Sumo Logic" {...props} backgroundColor="#000099" />

export default SumoLogic
