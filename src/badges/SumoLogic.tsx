
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const SumoLogic = (props: BadgeProps) => <Badge name="Sumo Logic" backgroundColor="#000099" {...props} />

export default SumoLogic
