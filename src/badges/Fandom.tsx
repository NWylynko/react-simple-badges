
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Fandom = (props: BadgeProps) => <Badge name="Fandom" {...props} backgroundColor="#FA005A" />

export default Fandom
