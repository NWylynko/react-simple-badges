
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Fandom = (props: BadgeProps) => <Badge name="Fandom" backgroundColor="#FA005A" {...props} />

export default Fandom
