
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Nodemon = (props: BadgeProps) => <Badge name="Nodemon" {...props} backgroundColor="#76D04B" />

export default Nodemon
