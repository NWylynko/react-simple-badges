
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Nodemon = (props: BadgeProps) => <Badge name="Nodemon" backgroundColor="#76D04B" {...props} />

export default Nodemon
