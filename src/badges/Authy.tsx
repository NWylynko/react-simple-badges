
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Authy = (props: BadgeProps) => <Badge name="Authy" {...props} backgroundColor="#EC1C24" />

export default Authy
