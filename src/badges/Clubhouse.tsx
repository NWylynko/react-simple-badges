
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Clubhouse = (props: BadgeProps) => <Badge name="Clubhouse" {...props} backgroundColor="#6515DD" />

export default Clubhouse
