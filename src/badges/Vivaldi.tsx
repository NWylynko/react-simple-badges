
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Vivaldi = (props: BadgeProps) => <Badge name="Vivaldi" {...props} backgroundColor="#EF3939" />

export default Vivaldi
