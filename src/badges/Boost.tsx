
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Boost = (props: BadgeProps) => <Badge name="Boost" backgroundColor="#F7901E" {...props} />

export default Boost
