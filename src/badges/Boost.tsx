
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Boost = (props: BadgeProps) => <Badge name="Boost" {...props} backgroundColor="#F7901E" />

export default Boost
