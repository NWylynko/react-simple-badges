
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Numba = (props: BadgeProps) => <Badge name="Numba" backgroundColor="#00A3E0" {...props} />

export default Numba
