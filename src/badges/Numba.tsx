
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Numba = (props: BadgeProps) => <Badge name="Numba" {...props} backgroundColor="#00A3E0" />

export default Numba
