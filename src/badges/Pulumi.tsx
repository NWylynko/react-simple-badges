
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Pulumi = (props: BadgeProps) => <Badge name="Pulumi" {...props} backgroundColor="#8A3391" />

export default Pulumi
