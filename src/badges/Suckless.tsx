
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Suckless = (props: BadgeProps) => <Badge name="suckless" {...props} backgroundColor="#1177AA" />

export default Suckless
