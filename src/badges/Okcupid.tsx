
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Okcupid = (props: BadgeProps) => <Badge name="okcupid" {...props} backgroundColor="#0500BE" />

export default Okcupid
