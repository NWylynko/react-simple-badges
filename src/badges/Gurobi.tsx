
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Gurobi = (props: BadgeProps) => <Badge name="Gurobi" backgroundColor="#EE3524" {...props} />

export default Gurobi
