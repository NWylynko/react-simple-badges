
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Gurobi = (props: BadgeProps) => <Badge name="Gurobi" {...props} backgroundColor="#EE3524" />

export default Gurobi
