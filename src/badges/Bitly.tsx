
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Bitly = (props: BadgeProps) => <Badge name="Bitly" backgroundColor="#EE6123" {...props} />

export default Bitly
