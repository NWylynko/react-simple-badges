
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Bitly = (props: BadgeProps) => <Badge name="Bitly" {...props} backgroundColor="#EE6123" />

export default Bitly
