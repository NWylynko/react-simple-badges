
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MediaTek = (props: BadgeProps) => <Badge name="MediaTek" {...props} backgroundColor="#EC9430" />

export default MediaTek
