
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Yale = (props: BadgeProps) => <Badge name="Yale" {...props} backgroundColor="#FFD900" />

export default Yale
