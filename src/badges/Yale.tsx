
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Yale = (props: BadgeProps) => <Badge name="Yale" backgroundColor="#FFD900" {...props} />

export default Yale
