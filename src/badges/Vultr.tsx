
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Vultr = (props: BadgeProps) => <Badge name="Vultr" {...props} backgroundColor="#007BFC" />

export default Vultr
