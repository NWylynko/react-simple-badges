
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Vultr = (props: BadgeProps) => <Badge name="Vultr" backgroundColor="#007BFC" {...props} />

export default Vultr
