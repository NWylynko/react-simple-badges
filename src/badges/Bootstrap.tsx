
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Bootstrap = (props: BadgeProps) => <Badge name="Bootstrap" backgroundColor="#7952B3" {...props} />

export default Bootstrap
