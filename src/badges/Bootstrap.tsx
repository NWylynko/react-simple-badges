
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Bootstrap = (props: BadgeProps) => <Badge name="Bootstrap" {...props} backgroundColor="#7952B3" />

export default Bootstrap
