
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Bitwig = (props: BadgeProps) => <Badge name="Bitwig" backgroundColor="#FF5A00" {...props} />

export default Bitwig
