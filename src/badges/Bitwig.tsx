
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Bitwig = (props: BadgeProps) => <Badge name="Bitwig" {...props} backgroundColor="#FF5A00" />

export default Bitwig
