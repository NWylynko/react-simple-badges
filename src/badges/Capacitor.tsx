
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Capacitor = (props: BadgeProps) => <Badge name="Capacitor" backgroundColor="#119EFF" {...props} />

export default Capacitor
