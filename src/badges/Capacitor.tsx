
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Capacitor = (props: BadgeProps) => <Badge name="Capacitor" {...props} backgroundColor="#119EFF" />

export default Capacitor
