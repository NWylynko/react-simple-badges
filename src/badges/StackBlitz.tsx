
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const StackBlitz = (props: BadgeProps) => <Badge name="StackBlitz" {...props} backgroundColor="#1269D3" />

export default StackBlitz
