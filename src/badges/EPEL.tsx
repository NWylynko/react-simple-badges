
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const EPEL = (props: BadgeProps) => <Badge name="EPEL" {...props} backgroundColor="#FC0000" />

export default EPEL
