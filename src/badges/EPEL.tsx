
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const EPEL = (props: BadgeProps) => <Badge name="EPEL" backgroundColor="#FC0000" {...props} />

export default EPEL
