
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Publons = (props: BadgeProps) => <Badge name="Publons" backgroundColor="#336699" {...props} />

export default Publons
