
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Publons = (props: BadgeProps) => <Badge name="Publons" {...props} backgroundColor="#336699" />

export default Publons
