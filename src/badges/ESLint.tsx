
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ESLint = (props: BadgeProps) => <Badge name="ESLint" {...props} backgroundColor="#4B32C3" />

export default ESLint
