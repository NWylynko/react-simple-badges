
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const ESLint = (props: BadgeProps) => <Badge name="ESLint" backgroundColor="#4B32C3" {...props} />

export default ESLint
