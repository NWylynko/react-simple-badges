
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TUI = (props: BadgeProps) => <Badge name="TUI" {...props} backgroundColor="#D40E14" />

export default TUI
