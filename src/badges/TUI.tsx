
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const TUI = (props: BadgeProps) => <Badge name="TUI" backgroundColor="#D40E14" {...props} />

export default TUI
