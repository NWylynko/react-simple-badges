
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const LandRover = (props: BadgeProps) => <Badge name="Land Rover" backgroundColor="#005A2B" {...props} />

export default LandRover
