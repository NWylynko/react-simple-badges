
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const LandRover = (props: BadgeProps) => <Badge name="Land Rover" {...props} backgroundColor="#005A2B" />

export default LandRover
