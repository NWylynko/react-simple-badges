
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const ZWave = (props: BadgeProps) => <Badge name="Z-Wave" backgroundColor="#1B365D" {...props} />

export default ZWave
