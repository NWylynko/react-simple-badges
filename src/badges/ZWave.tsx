
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ZWave = (props: BadgeProps) => <Badge name="Z-Wave" {...props} backgroundColor="#1B365D" />

export default ZWave
