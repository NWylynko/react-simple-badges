
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Express = (props: BadgeProps) => <Badge name="Express" {...props} backgroundColor="#000000" />

export default Express
