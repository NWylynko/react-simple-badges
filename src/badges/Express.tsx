
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Express = (props: BadgeProps) => <Badge name="Express" backgroundColor="#000000" {...props} />

export default Express
