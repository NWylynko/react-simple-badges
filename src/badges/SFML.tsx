
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SFML = (props: BadgeProps) => <Badge name="SFML" {...props} backgroundColor="#8CC445" />

export default SFML
