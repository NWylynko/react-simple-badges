
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const SFML = (props: BadgeProps) => <Badge name="SFML" backgroundColor="#8CC445" {...props} />

export default SFML
