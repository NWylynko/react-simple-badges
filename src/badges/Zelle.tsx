
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Zelle = (props: BadgeProps) => <Badge name="Zelle" {...props} backgroundColor="#6D1ED4" />

export default Zelle
