
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const BBC = (props: BadgeProps) => <Badge name="BBC" {...props} backgroundColor="#000000" />

export default BBC
