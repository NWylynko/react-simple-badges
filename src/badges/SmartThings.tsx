
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SmartThings = (props: BadgeProps) => <Badge name="SmartThings" {...props} backgroundColor="#15BFFF" />

export default SmartThings
