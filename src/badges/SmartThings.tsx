
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const SmartThings = (props: BadgeProps) => <Badge name="SmartThings" backgroundColor="#15BFFF" {...props} />

export default SmartThings
