
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Fourchan = (props: BadgeProps) => <Badge name="4chan" {...props} backgroundColor="#006600" />

export default Fourchan
