
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Rossmann = (props: BadgeProps) => <Badge name="Rossmann" backgroundColor="#C3002D" {...props} />

export default Rossmann
