
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Rossmann = (props: BadgeProps) => <Badge name="Rossmann" {...props} backgroundColor="#C3002D" />

export default Rossmann
