
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Spectrum = (props: BadgeProps) => <Badge name="Spectrum" {...props} backgroundColor="#7B16FF" />

export default Spectrum
