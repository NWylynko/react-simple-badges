
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Spectrum = (props: BadgeProps) => <Badge name="Spectrum" backgroundColor="#7B16FF" {...props} />

export default Spectrum
