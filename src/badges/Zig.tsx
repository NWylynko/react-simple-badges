
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Zig = (props: BadgeProps) => <Badge name="Zig" {...props} backgroundColor="#F7A41D" />

export default Zig
