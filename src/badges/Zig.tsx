
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Zig = (props: BadgeProps) => <Badge name="Zig" backgroundColor="#F7A41D" {...props} />

export default Zig
