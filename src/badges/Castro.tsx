
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Castro = (props: BadgeProps) => <Badge name="Castro" {...props} backgroundColor="#00B265" />

export default Castro
