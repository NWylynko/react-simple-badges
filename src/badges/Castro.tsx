
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Castro = (props: BadgeProps) => <Badge name="Castro" backgroundColor="#00B265" {...props} />

export default Castro
