
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Linkfire = (props: BadgeProps) => <Badge name="Linkfire" {...props} backgroundColor="#FF3850" />

export default Linkfire
