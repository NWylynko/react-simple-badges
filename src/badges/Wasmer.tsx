
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Wasmer = (props: BadgeProps) => <Badge name="Wasmer" {...props} backgroundColor="#4946DD" />

export default Wasmer
