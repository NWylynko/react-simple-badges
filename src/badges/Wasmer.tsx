
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Wasmer = (props: BadgeProps) => <Badge name="Wasmer" backgroundColor="#4946DD" {...props} />

export default Wasmer
