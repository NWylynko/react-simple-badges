
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Mapbox = (props: BadgeProps) => <Badge name="Mapbox" backgroundColor="#000000" {...props} />

export default Mapbox
