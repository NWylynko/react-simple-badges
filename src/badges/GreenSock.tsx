
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GreenSock = (props: BadgeProps) => <Badge name="GreenSock" {...props} backgroundColor="#88CE02" />

export default GreenSock
