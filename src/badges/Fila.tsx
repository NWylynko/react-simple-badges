
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Fila = (props: BadgeProps) => <Badge name="Fila" {...props} backgroundColor="#03234C" />

export default Fila
