
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Fila = (props: BadgeProps) => <Badge name="Fila" backgroundColor="#03234C" {...props} />

export default Fila
