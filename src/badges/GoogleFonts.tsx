
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GoogleFonts = (props: BadgeProps) => <Badge name="Google Fonts" {...props} backgroundColor="#4285F4" />

export default GoogleFonts
