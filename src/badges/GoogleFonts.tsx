
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const GoogleFonts = (props: BadgeProps) => <Badge name="Google Fonts" backgroundColor="#4285F4" {...props} />

export default GoogleFonts
