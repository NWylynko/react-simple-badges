
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const AMP = (props: BadgeProps) => <Badge name="AMP" backgroundColor="#005AF0" {...props} />

export default AMP
