
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AMP = (props: BadgeProps) => <Badge name="AMP" {...props} backgroundColor="#005AF0" />

export default AMP
