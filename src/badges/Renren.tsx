
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Renren = (props: BadgeProps) => <Badge name="Renren" {...props} backgroundColor="#217DC6" />

export default Renren
