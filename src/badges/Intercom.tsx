
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Intercom = (props: BadgeProps) => <Badge name="Intercom" {...props} backgroundColor="#6AFDEF" />

export default Intercom
