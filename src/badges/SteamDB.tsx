
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const SteamDB = (props: BadgeProps) => <Badge name="SteamDB" backgroundColor="#000000" {...props} />

export default SteamDB
