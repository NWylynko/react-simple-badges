
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SteamDB = (props: BadgeProps) => <Badge name="SteamDB" {...props} backgroundColor="#000000" />

export default SteamDB
