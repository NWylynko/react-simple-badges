
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Notist = (props: BadgeProps) => <Badge name="Notist" {...props} backgroundColor="#333333" />

export default Notist
