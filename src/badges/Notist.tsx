
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Notist = (props: BadgeProps) => <Badge name="Notist" backgroundColor="#333333" {...props} />

export default Notist
