
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Messenger = (props: BadgeProps) => <Badge name="Messenger" backgroundColor="#00B2FF" {...props} />

export default Messenger
