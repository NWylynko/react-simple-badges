
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Messenger = (props: BadgeProps) => <Badge name="Messenger" {...props} backgroundColor="#00B2FF" />

export default Messenger
