
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GameJolt = (props: BadgeProps) => <Badge name="Game Jolt" {...props} backgroundColor="#CCFF00" />

export default GameJolt
