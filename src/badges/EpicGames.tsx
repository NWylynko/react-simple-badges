
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const EpicGames = (props: BadgeProps) => <Badge name="Epic Games" {...props} backgroundColor="#313131" />

export default EpicGames
