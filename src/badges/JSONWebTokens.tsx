
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const JSONWebTokens = (props: BadgeProps) => <Badge name="JSON Web Tokens" {...props} backgroundColor="#000000" />

export default JSONWebTokens
