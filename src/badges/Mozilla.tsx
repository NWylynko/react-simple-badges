
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Mozilla = (props: BadgeProps) => <Badge name="Mozilla" {...props} backgroundColor="#000000" />

export default Mozilla
