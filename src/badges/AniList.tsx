
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AniList = (props: BadgeProps) => <Badge name="AniList" {...props} backgroundColor="#02A9FF" />

export default AniList
