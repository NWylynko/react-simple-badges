
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AWSAmplify = (props: BadgeProps) => <Badge name="AWS Amplify" {...props} backgroundColor="#FF9900" />

export default AWSAmplify
