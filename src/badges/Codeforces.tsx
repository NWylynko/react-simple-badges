
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Codeforces = (props: BadgeProps) => <Badge name="Codeforces" {...props} backgroundColor="#1F8ACB" />

export default Codeforces
