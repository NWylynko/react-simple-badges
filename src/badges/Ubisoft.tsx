
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Ubisoft = (props: BadgeProps) => <Badge name="Ubisoft" backgroundColor="#000000" {...props} />

export default Ubisoft
