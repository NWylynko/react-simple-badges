
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Ubisoft = (props: BadgeProps) => <Badge name="Ubisoft" {...props} backgroundColor="#000000" />

export default Ubisoft
