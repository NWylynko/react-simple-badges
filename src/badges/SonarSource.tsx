
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SonarSource = (props: BadgeProps) => <Badge name="SonarSource" {...props} backgroundColor="#CB3032" />

export default SonarSource
