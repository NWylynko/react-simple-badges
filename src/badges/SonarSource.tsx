
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const SonarSource = (props: BadgeProps) => <Badge name="SonarSource" backgroundColor="#CB3032" {...props} />

export default SonarSource
