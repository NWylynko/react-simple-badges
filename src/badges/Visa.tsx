
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Visa = (props: BadgeProps) => <Badge name="Visa" {...props} backgroundColor="#1A1F71" />

export default Visa
