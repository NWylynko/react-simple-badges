
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Personio = (props: BadgeProps) => <Badge name="Personio" backgroundColor="#FFFFFF" {...props} />

export default Personio
