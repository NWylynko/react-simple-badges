
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Personio = (props: BadgeProps) => <Badge name="Personio" {...props} backgroundColor="#FFFFFF" />

export default Personio
