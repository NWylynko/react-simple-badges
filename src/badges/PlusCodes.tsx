
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PlusCodes = (props: BadgeProps) => <Badge name="Plus Codes" {...props} backgroundColor="#4285F4" />

export default PlusCodes
