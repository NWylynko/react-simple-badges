
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Radar = (props: BadgeProps) => <Badge name="Radar" {...props} backgroundColor="#007AFF" />

export default Radar
