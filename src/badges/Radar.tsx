
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Radar = (props: BadgeProps) => <Badge name="Radar" backgroundColor="#007AFF" {...props} />

export default Radar
