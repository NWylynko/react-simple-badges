
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ScrumAlliance = (props: BadgeProps) => <Badge name="Scrum Alliance" {...props} backgroundColor="#009FDA" />

export default ScrumAlliance
