
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const AngelList = (props: BadgeProps) => <Badge name="AngelList" backgroundColor="#000000" {...props} />

export default AngelList
