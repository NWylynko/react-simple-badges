
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AngelList = (props: BadgeProps) => <Badge name="AngelList" {...props} backgroundColor="#000000" />

export default AngelList
