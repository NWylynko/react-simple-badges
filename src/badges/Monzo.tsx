
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Monzo = (props: BadgeProps) => <Badge name="Monzo" backgroundColor="#14233C" {...props} />

export default Monzo
