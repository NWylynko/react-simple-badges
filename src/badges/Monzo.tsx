
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Monzo = (props: BadgeProps) => <Badge name="Monzo" {...props} backgroundColor="#14233C" />

export default Monzo
