
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AWSLambda = (props: BadgeProps) => <Badge name="AWS Lambda" {...props} backgroundColor="#FF9900" />

export default AWSLambda
