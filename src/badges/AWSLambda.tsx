
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const AWSLambda = (props: BadgeProps) => <Badge name="AWS Lambda" backgroundColor="#FF9900" {...props} />

export default AWSLambda
