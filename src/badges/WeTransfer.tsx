
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const WeTransfer = (props: BadgeProps) => <Badge name="WeTransfer" backgroundColor="#409FFF" {...props} />

export default WeTransfer
