
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const WeTransfer = (props: BadgeProps) => <Badge name="WeTransfer" {...props} backgroundColor="#409FFF" />

export default WeTransfer
