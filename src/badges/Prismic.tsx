
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Prismic = (props: BadgeProps) => <Badge name="Prismic" {...props} backgroundColor="#5163BA" />

export default Prismic
