
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Prismic = (props: BadgeProps) => <Badge name="Prismic" backgroundColor="#5163BA" {...props} />

export default Prismic
