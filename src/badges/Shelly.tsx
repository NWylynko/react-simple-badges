
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Shelly = (props: BadgeProps) => <Badge name="Shelly" {...props} backgroundColor="#4495D1" />

export default Shelly
