
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const LGTM = (props: BadgeProps) => <Badge name="LGTM" {...props} backgroundColor="#FFFFFF" />

export default LGTM
