
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PremierLeague = (props: BadgeProps) => <Badge name="Premier League" {...props} backgroundColor="#360D3A" />

export default PremierLeague
