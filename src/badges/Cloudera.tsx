
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Cloudera = (props: BadgeProps) => <Badge name="Cloudera" backgroundColor="#F96702" {...props} />

export default Cloudera
