
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Cloudera = (props: BadgeProps) => <Badge name="Cloudera" {...props} backgroundColor="#F96702" />

export default Cloudera
