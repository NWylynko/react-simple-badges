
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Tmux = (props: BadgeProps) => <Badge name="tmux" {...props} backgroundColor="#1BB91F" />

export default Tmux
