
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Tmux = (props: BadgeProps) => <Badge name="tmux" backgroundColor="#1BB91F" {...props} />

export default Tmux
