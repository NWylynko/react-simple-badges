
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const CoffeeScript = (props: BadgeProps) => <Badge name="CoffeeScript" backgroundColor="#2F2625" {...props} />

export default CoffeeScript
