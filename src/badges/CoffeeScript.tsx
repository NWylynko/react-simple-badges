
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CoffeeScript = (props: BadgeProps) => <Badge name="CoffeeScript" {...props} backgroundColor="#2F2625" />

export default CoffeeScript
