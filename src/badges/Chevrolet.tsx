
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Chevrolet = (props: BadgeProps) => <Badge name="Chevrolet" {...props} backgroundColor="#CD9834" />

export default Chevrolet
