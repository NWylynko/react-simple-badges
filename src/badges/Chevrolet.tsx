
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Chevrolet = (props: BadgeProps) => <Badge name="Chevrolet" backgroundColor="#CD9834" {...props} />

export default Chevrolet
