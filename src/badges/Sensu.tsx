
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Sensu = (props: BadgeProps) => <Badge name="Sensu" {...props} backgroundColor="#89C967" />

export default Sensu
