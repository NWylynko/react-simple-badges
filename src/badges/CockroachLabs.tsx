
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CockroachLabs = (props: BadgeProps) => <Badge name="Cockroach Labs" {...props} backgroundColor="#6933FF" />

export default CockroachLabs
