
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const OCaml = (props: BadgeProps) => <Badge name="OCaml" {...props} backgroundColor="#EC6813" />

export default OCaml
