
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const OCaml = (props: BadgeProps) => <Badge name="OCaml" backgroundColor="#EC6813" {...props} />

export default OCaml
