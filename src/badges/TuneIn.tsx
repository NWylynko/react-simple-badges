
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const TuneIn = (props: BadgeProps) => <Badge name="TuneIn" backgroundColor="#14D8CC" {...props} />

export default TuneIn
