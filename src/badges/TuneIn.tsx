
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TuneIn = (props: BadgeProps) => <Badge name="TuneIn" {...props} backgroundColor="#14D8CC" />

export default TuneIn
