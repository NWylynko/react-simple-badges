
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Microgenetics = (props: BadgeProps) => <Badge name="Microgenetics" backgroundColor="#FF0000" {...props} />

export default Microgenetics
