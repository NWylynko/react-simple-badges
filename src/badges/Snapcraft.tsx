
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Snapcraft = (props: BadgeProps) => <Badge name="Snapcraft" {...props} backgroundColor="#82BEA0" />

export default Snapcraft
