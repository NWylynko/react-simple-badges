
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Snapcraft = (props: BadgeProps) => <Badge name="Snapcraft" backgroundColor="#82BEA0" {...props} />

export default Snapcraft
