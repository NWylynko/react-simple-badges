
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const NestJS = (props: BadgeProps) => <Badge name="NestJS" {...props} backgroundColor="#E0234E" />

export default NestJS
