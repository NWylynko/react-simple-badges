
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const NestJS = (props: BadgeProps) => <Badge name="NestJS" backgroundColor="#E0234E" {...props} />

export default NestJS
