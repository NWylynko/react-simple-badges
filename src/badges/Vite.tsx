
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Vite = (props: BadgeProps) => <Badge name="Vite" {...props} backgroundColor="#646CFF" />

export default Vite
