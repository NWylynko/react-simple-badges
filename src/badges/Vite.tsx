
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Vite = (props: BadgeProps) => <Badge name="Vite" backgroundColor="#646CFF" {...props} />

export default Vite
