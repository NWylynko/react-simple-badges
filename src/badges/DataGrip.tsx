
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const DataGrip = (props: BadgeProps) => <Badge name="DataGrip" {...props} backgroundColor="#000000" />

export default DataGrip
