
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const DataGrip = (props: BadgeProps) => <Badge name="DataGrip" backgroundColor="#000000" {...props} />

export default DataGrip
