
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Lens = (props: BadgeProps) => <Badge name="Lens" {...props} backgroundColor="#3D90CE" />

export default Lens
