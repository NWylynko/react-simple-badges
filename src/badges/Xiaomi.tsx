
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Xiaomi = (props: BadgeProps) => <Badge name="Xiaomi" {...props} backgroundColor="#FF6900" />

export default Xiaomi
