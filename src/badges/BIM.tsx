
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const BIM = (props: BadgeProps) => <Badge name="BIM" backgroundColor="#EB1928" {...props} />

export default BIM
