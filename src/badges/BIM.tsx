
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const BIM = (props: BadgeProps) => <Badge name="BIM" {...props} backgroundColor="#EB1928" />

export default BIM
