
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PurgeCSS = (props: BadgeProps) => <Badge name="PurgeCSS" {...props} backgroundColor="#14161A" />

export default PurgeCSS
