
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Jabber = (props: BadgeProps) => <Badge name="Jabber" {...props} backgroundColor="#CC0000" />

export default Jabber
