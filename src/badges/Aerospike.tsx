
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Aerospike = (props: BadgeProps) => <Badge name="Aerospike" backgroundColor="#C41E25" {...props} />

export default Aerospike
