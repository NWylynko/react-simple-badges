
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Aerospike = (props: BadgeProps) => <Badge name="Aerospike" {...props} backgroundColor="#C41E25" />

export default Aerospike
