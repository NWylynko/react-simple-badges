
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Microbit = (props: BadgeProps) => <Badge name="micro:bit" {...props} backgroundColor="#00ED00" />

export default Microbit
