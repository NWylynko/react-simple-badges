
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Microbit = (props: BadgeProps) => <Badge name="micro:bit" backgroundColor="#00ED00" {...props} />

export default Microbit
