
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Libuv = (props: BadgeProps) => <Badge name="libuv" {...props} backgroundColor="#403C3D" />

export default Libuv
