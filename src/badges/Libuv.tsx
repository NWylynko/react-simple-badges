
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Libuv = (props: BadgeProps) => <Badge name="libuv" backgroundColor="#403C3D" {...props} />

export default Libuv
