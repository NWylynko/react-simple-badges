
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Pihole = (props: BadgeProps) => <Badge name="Pi-hole" backgroundColor="#96060C" {...props} />

export default Pihole
