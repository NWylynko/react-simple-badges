
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Pihole = (props: BadgeProps) => <Badge name="Pi-hole" {...props} backgroundColor="#96060C" />

export default Pihole
