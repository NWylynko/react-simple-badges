
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MTA = (props: BadgeProps) => <Badge name="MTA" {...props} backgroundColor="#0039A6" />

export default MTA
