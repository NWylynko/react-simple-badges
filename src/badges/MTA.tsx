
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const MTA = (props: BadgeProps) => <Badge name="MTA" backgroundColor="#0039A6" {...props} />

export default MTA
