
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Hootsuite = (props: BadgeProps) => <Badge name="Hootsuite" {...props} backgroundColor="#143059" />

export default Hootsuite
