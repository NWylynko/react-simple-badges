
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Nodejs = (props: BadgeProps) => <Badge name="Node.js" backgroundColor="#339933" {...props} />

export default Nodejs
