
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Nodejs = (props: BadgeProps) => <Badge name="Node.js" {...props} backgroundColor="#339933" />

export default Nodejs
