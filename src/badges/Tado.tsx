
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Tado = (props: BadgeProps) => <Badge name="tado°" backgroundColor="#FFA900" {...props} />

export default Tado
