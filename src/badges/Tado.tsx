
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Tado = (props: BadgeProps) => <Badge name="tado°" {...props} backgroundColor="#FFA900" />

export default Tado
