
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const ROS = (props: BadgeProps) => <Badge name="ROS" backgroundColor="#22314E" {...props} />

export default ROS
