
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ROS = (props: BadgeProps) => <Badge name="ROS" {...props} backgroundColor="#22314E" />

export default ROS
