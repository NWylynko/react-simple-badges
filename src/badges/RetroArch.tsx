
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const RetroArch = (props: BadgeProps) => <Badge name="RetroArch" backgroundColor="#000000" {...props} />

export default RetroArch
