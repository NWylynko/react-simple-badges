
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const RetroArch = (props: BadgeProps) => <Badge name="RetroArch" {...props} backgroundColor="#000000" />

export default RetroArch
