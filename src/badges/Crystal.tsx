
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Crystal = (props: BadgeProps) => <Badge name="Crystal" {...props} backgroundColor="#000000" />

export default Crystal
