
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Crystal = (props: BadgeProps) => <Badge name="Crystal" backgroundColor="#000000" {...props} />

export default Crystal
