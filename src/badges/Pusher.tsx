
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Pusher = (props: BadgeProps) => <Badge name="Pusher" {...props} backgroundColor="#300D4F" />

export default Pusher
