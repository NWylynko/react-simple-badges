
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Pusher = (props: BadgeProps) => <Badge name="Pusher" backgroundColor="#300D4F" {...props} />

export default Pusher
