
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const HBO = (props: BadgeProps) => <Badge name="HBO" {...props} backgroundColor="#000000" />

export default HBO
