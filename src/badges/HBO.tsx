
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const HBO = (props: BadgeProps) => <Badge name="HBO" backgroundColor="#000000" {...props} />

export default HBO
