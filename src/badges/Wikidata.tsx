
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Wikidata = (props: BadgeProps) => <Badge name="Wikidata" backgroundColor="#006699" {...props} />

export default Wikidata
