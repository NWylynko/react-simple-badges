
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Wikidata = (props: BadgeProps) => <Badge name="Wikidata" {...props} backgroundColor="#006699" />

export default Wikidata
