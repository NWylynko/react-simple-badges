
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const SurrealDB = (props: BadgeProps) => <Badge name="SurrealDB" backgroundColor="#FF00A0" {...props} />

export default SurrealDB
