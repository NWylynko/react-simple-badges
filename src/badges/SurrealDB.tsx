
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SurrealDB = (props: BadgeProps) => <Badge name="SurrealDB" {...props} backgroundColor="#FF00A0" />

export default SurrealDB
