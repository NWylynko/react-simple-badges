
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Metabase = (props: BadgeProps) => <Badge name="Metabase" {...props} backgroundColor="#509EE3" />

export default Metabase
