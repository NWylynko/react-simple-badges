
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Metabase = (props: BadgeProps) => <Badge name="Metabase" backgroundColor="#509EE3" {...props} />

export default Metabase
