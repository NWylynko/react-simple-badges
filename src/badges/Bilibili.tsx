
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Bilibili = (props: BadgeProps) => <Badge name="Bilibili" {...props} backgroundColor="#00A1D6" />

export default Bilibili
