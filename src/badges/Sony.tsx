
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Sony = (props: BadgeProps) => <Badge name="Sony" backgroundColor="#FFFFFF" {...props} />

export default Sony
