
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Sony = (props: BadgeProps) => <Badge name="Sony" {...props} backgroundColor="#FFFFFF" />

export default Sony
