
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Prometheus = (props: BadgeProps) => <Badge name="Prometheus" {...props} backgroundColor="#E6522C" />

export default Prometheus
