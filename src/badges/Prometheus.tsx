
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Prometheus = (props: BadgeProps) => <Badge name="Prometheus" backgroundColor="#E6522C" {...props} />

export default Prometheus
