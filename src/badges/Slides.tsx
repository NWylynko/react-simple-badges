
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Slides = (props: BadgeProps) => <Badge name="Slides" backgroundColor="#E4637C" {...props} />

export default Slides
