
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Slides = (props: BadgeProps) => <Badge name="Slides" {...props} backgroundColor="#E4637C" />

export default Slides
