
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const R = (props: BadgeProps) => <Badge name="R" backgroundColor="#276DC3" {...props} />

export default R
