
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const R = (props: BadgeProps) => <Badge name="R" {...props} backgroundColor="#276DC3" />

export default R
