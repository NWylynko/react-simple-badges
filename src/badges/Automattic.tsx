
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Automattic = (props: BadgeProps) => <Badge name="Automattic" {...props} backgroundColor="#3499CD" />

export default Automattic
