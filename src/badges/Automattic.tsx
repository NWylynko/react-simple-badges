
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Automattic = (props: BadgeProps) => <Badge name="Automattic" backgroundColor="#3499CD" {...props} />

export default Automattic
