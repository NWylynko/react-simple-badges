
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Osu = (props: BadgeProps) => <Badge name="osu!" {...props} backgroundColor="#FF66AA" />

export default Osu
