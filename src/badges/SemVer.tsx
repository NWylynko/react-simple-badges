
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SemVer = (props: BadgeProps) => <Badge name="SemVer" {...props} backgroundColor="#3F4551" />

export default SemVer
