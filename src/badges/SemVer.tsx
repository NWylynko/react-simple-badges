
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const SemVer = (props: BadgeProps) => <Badge name="SemVer" backgroundColor="#3F4551" {...props} />

export default SemVer
