
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const TPLink = (props: BadgeProps) => <Badge name="TP-Link" backgroundColor="#4ACBD6" {...props} />

export default TPLink
