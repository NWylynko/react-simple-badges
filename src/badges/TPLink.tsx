
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TPLink = (props: BadgeProps) => <Badge name="TP-Link" {...props} backgroundColor="#4ACBD6" />

export default TPLink
