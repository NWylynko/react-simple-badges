
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Realm = (props: BadgeProps) => <Badge name="Realm" backgroundColor="#39477F" {...props} />

export default Realm
