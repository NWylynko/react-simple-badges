
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Realm = (props: BadgeProps) => <Badge name="Realm" {...props} backgroundColor="#39477F" />

export default Realm
