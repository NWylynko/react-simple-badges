
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Upstash = (props: BadgeProps) => <Badge name="Upstash" backgroundColor="#00E9A3" {...props} />

export default Upstash
