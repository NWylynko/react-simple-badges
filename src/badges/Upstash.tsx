
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Upstash = (props: BadgeProps) => <Badge name="Upstash" {...props} backgroundColor="#00E9A3" />

export default Upstash
