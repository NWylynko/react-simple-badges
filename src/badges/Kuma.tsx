
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Kuma = (props: BadgeProps) => <Badge name="Kuma" {...props} backgroundColor="#290B53" />

export default Kuma
