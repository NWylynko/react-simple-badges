
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Solidity = (props: BadgeProps) => <Badge name="Solidity" {...props} backgroundColor="#363636" />

export default Solidity
