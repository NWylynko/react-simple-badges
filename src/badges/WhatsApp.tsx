
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const WhatsApp = (props: BadgeProps) => <Badge name="WhatsApp" {...props} backgroundColor="#25D366" />

export default WhatsApp
