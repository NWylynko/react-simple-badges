
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MDX = (props: BadgeProps) => <Badge name="MDX" {...props} backgroundColor="#1B1F24" />

export default MDX
