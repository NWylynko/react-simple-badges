
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const MDX = (props: BadgeProps) => <Badge name="MDX" backgroundColor="#1B1F24" {...props} />

export default MDX
