
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const LiveJournal = (props: BadgeProps) => <Badge name="LiveJournal" backgroundColor="#00B0EA" {...props} />

export default LiveJournal
