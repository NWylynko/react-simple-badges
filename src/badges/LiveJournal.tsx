
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const LiveJournal = (props: BadgeProps) => <Badge name="LiveJournal" {...props} backgroundColor="#00B0EA" />

export default LiveJournal
