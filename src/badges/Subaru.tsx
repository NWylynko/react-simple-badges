
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Subaru = (props: BadgeProps) => <Badge name="Subaru" backgroundColor="#013C74" {...props} />

export default Subaru
