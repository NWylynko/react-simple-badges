
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Subaru = (props: BadgeProps) => <Badge name="Subaru" {...props} backgroundColor="#013C74" />

export default Subaru
