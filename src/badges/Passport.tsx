
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Passport = (props: BadgeProps) => <Badge name="Passport" backgroundColor="#34E27A" {...props} />

export default Passport
