
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Passport = (props: BadgeProps) => <Badge name="Passport" {...props} backgroundColor="#34E27A" />

export default Passport
