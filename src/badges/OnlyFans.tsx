
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const OnlyFans = (props: BadgeProps) => <Badge name="OnlyFans" {...props} backgroundColor="#00AFF0" />

export default OnlyFans
