
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const WordPress = (props: BadgeProps) => <Badge name="WordPress" {...props} backgroundColor="#21759B" />

export default WordPress
