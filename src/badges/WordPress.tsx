
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const WordPress = (props: BadgeProps) => <Badge name="WordPress" backgroundColor="#21759B" {...props} />

export default WordPress
