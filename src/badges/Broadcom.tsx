
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Broadcom = (props: BadgeProps) => <Badge name="Broadcom" backgroundColor="#CC092F" {...props} />

export default Broadcom
