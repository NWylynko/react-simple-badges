
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Broadcom = (props: BadgeProps) => <Badge name="Broadcom" {...props} backgroundColor="#CC092F" />

export default Broadcom
