
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Files = (props: BadgeProps) => <Badge name="Files" {...props} backgroundColor="#4285F4" />

export default Files
