
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Files = (props: BadgeProps) => <Badge name="Files" backgroundColor="#4285F4" {...props} />

export default Files
