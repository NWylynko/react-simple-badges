
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Prezi = (props: BadgeProps) => <Badge name="Prezi" {...props} backgroundColor="#3181FF" />

export default Prezi
