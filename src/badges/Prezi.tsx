
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Prezi = (props: BadgeProps) => <Badge name="Prezi" backgroundColor="#3181FF" {...props} />

export default Prezi
