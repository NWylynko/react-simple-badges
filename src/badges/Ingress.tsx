
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Ingress = (props: BadgeProps) => <Badge name="Ingress" {...props} backgroundColor="#783CBD" />

export default Ingress
