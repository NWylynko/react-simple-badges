
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Ingress = (props: BadgeProps) => <Badge name="Ingress" backgroundColor="#783CBD" {...props} />

export default Ingress
