
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Adminer = (props: BadgeProps) => <Badge name="Adminer" backgroundColor="#34567C" {...props} />

export default Adminer
