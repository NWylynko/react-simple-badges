
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Adminer = (props: BadgeProps) => <Badge name="Adminer" {...props} backgroundColor="#34567C" />

export default Adminer
