
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ProtonDB = (props: BadgeProps) => <Badge name="ProtonDB" {...props} backgroundColor="#F50057" />

export default ProtonDB
