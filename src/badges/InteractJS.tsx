
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const InteractJS = (props: BadgeProps) => <Badge name="InteractJS" backgroundColor="#2599ED" {...props} />

export default InteractJS
