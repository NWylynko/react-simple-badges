
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const InteractJS = (props: BadgeProps) => <Badge name="InteractJS" {...props} backgroundColor="#2599ED" />

export default InteractJS
