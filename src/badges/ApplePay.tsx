
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const ApplePay = (props: BadgeProps) => <Badge name="Apple Pay" backgroundColor="#000000" {...props} />

export default ApplePay
