
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ApplePay = (props: BadgeProps) => <Badge name="Apple Pay" {...props} backgroundColor="#000000" />

export default ApplePay
