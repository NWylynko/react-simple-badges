
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const FutureLearn = (props: BadgeProps) => <Badge name="FutureLearn" {...props} backgroundColor="#DE00A5" />

export default FutureLearn
