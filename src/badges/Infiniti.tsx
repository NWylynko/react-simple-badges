
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Infiniti = (props: BadgeProps) => <Badge name="Infiniti" backgroundColor="#000000" {...props} />

export default Infiniti
