
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Infiniti = (props: BadgeProps) => <Badge name="Infiniti" {...props} backgroundColor="#000000" />

export default Infiniti
