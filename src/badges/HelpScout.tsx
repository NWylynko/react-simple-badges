
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const HelpScout = (props: BadgeProps) => <Badge name="Help Scout" {...props} backgroundColor="#1292EE" />

export default HelpScout
