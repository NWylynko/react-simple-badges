
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AppleNews = (props: BadgeProps) => <Badge name="Apple News" {...props} backgroundColor="#FD415E" />

export default AppleNews
