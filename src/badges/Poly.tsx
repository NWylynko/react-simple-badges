
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Poly = (props: BadgeProps) => <Badge name="Poly" {...props} backgroundColor="#EB3C00" />

export default Poly
