
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Poly = (props: BadgeProps) => <Badge name="Poly" backgroundColor="#EB3C00" {...props} />

export default Poly
