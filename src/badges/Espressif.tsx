
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Espressif = (props: BadgeProps) => <Badge name="Espressif" {...props} backgroundColor="#E7352C" />

export default Espressif
