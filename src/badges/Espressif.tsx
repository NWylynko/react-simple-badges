
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Espressif = (props: BadgeProps) => <Badge name="Espressif" backgroundColor="#E7352C" {...props} />

export default Espressif
