
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Lada = (props: BadgeProps) => <Badge name="Lada" {...props} backgroundColor="#ED6B21" />

export default Lada
