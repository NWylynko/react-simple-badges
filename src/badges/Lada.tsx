
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Lada = (props: BadgeProps) => <Badge name="Lada" backgroundColor="#ED6B21" {...props} />

export default Lada
