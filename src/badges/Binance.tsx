
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Binance = (props: BadgeProps) => <Badge name="Binance" {...props} backgroundColor="#F0B90B" />

export default Binance
