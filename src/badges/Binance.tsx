
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Binance = (props: BadgeProps) => <Badge name="Binance" backgroundColor="#F0B90B" {...props} />

export default Binance
