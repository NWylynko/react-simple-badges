
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Ducati = (props: BadgeProps) => <Badge name="Ducati" {...props} backgroundColor="#CC0000" />

export default Ducati
