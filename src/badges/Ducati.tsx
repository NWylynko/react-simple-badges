
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Ducati = (props: BadgeProps) => <Badge name="Ducati" backgroundColor="#CC0000" {...props} />

export default Ducati
