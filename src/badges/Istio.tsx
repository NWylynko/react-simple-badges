
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Istio = (props: BadgeProps) => <Badge name="Istio" backgroundColor="#466BB0" {...props} />

export default Istio
