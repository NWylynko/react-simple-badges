
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Istio = (props: BadgeProps) => <Badge name="Istio" {...props} backgroundColor="#466BB0" />

export default Istio
