
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Argo = (props: BadgeProps) => <Badge name="Argo" {...props} backgroundColor="#EF7B4D" />

export default Argo
