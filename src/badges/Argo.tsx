
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Argo = (props: BadgeProps) => <Badge name="Argo" backgroundColor="#EF7B4D" {...props} />

export default Argo
