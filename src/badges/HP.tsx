
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const HP = (props: BadgeProps) => <Badge name="HP" {...props} backgroundColor="#0096D6" />

export default HP
