
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CondaForge = (props: BadgeProps) => <Badge name="Conda-Forge" {...props} backgroundColor="#000000" />

export default CondaForge
