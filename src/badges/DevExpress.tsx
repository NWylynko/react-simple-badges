
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const DevExpress = (props: BadgeProps) => <Badge name="DevExpress" {...props} backgroundColor="#FF7200" />

export default DevExpress
