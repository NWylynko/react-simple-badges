
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const DevExpress = (props: BadgeProps) => <Badge name="DevExpress" backgroundColor="#FF7200" {...props} />

export default DevExpress
