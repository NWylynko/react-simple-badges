
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Pandas = (props: BadgeProps) => <Badge name="pandas" {...props} backgroundColor="#150458" />

export default Pandas
