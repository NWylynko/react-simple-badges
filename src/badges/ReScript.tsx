
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const ReScript = (props: BadgeProps) => <Badge name="ReScript" backgroundColor="#E6484F" {...props} />

export default ReScript
