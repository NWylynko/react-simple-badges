
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ReScript = (props: BadgeProps) => <Badge name="ReScript" {...props} backgroundColor="#E6484F" />

export default ReScript
