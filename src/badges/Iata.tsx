
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Iata = (props: BadgeProps) => <Badge name="Iata" {...props} backgroundColor="#004E81" />

export default Iata
