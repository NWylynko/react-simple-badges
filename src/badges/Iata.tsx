
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Iata = (props: BadgeProps) => <Badge name="Iata" backgroundColor="#004E81" {...props} />

export default Iata
