
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Kubuntu = (props: BadgeProps) => <Badge name="Kubuntu" {...props} backgroundColor="#0079C1" />

export default Kubuntu
