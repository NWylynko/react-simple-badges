
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Kubuntu = (props: BadgeProps) => <Badge name="Kubuntu" backgroundColor="#0079C1" {...props} />

export default Kubuntu
