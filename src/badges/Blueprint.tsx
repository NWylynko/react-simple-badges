
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Blueprint = (props: BadgeProps) => <Badge name="Blueprint" backgroundColor="#137CBD" {...props} />

export default Blueprint
