
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Blueprint = (props: BadgeProps) => <Badge name="Blueprint" {...props} backgroundColor="#137CBD" />

export default Blueprint
