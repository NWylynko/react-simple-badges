
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Fastify = (props: BadgeProps) => <Badge name="Fastify" backgroundColor="#000000" {...props} />

export default Fastify
