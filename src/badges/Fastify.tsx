
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Fastify = (props: BadgeProps) => <Badge name="Fastify" {...props} backgroundColor="#000000" />

export default Fastify
