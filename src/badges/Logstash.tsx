
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Logstash = (props: BadgeProps) => <Badge name="Logstash" backgroundColor="#005571" {...props} />

export default Logstash
