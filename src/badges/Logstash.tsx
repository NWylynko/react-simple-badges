
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Logstash = (props: BadgeProps) => <Badge name="Logstash" {...props} backgroundColor="#005571" />

export default Logstash
