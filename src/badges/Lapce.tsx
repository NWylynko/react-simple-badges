
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Lapce = (props: BadgeProps) => <Badge name="Lapce" {...props} backgroundColor="#3B82F6" />

export default Lapce
