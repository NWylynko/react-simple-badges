
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Lapce = (props: BadgeProps) => <Badge name="Lapce" backgroundColor="#3B82F6" {...props} />

export default Lapce
