
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const MariaDB = (props: BadgeProps) => <Badge name="MariaDB" backgroundColor="#003545" {...props} />

export default MariaDB
