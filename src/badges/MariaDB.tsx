
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MariaDB = (props: BadgeProps) => <Badge name="MariaDB" {...props} backgroundColor="#003545" />

export default MariaDB
