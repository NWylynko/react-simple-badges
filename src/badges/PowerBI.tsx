
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PowerBI = (props: BadgeProps) => <Badge name="Power BI" {...props} backgroundColor="#F2C811" />

export default PowerBI
