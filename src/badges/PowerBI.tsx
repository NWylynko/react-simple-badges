
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const PowerBI = (props: BadgeProps) => <Badge name="Power BI" backgroundColor="#F2C811" {...props} />

export default PowerBI
