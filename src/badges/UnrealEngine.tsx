
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const UnrealEngine = (props: BadgeProps) => <Badge name="Unreal Engine" {...props} backgroundColor="#0E1128" />

export default UnrealEngine
