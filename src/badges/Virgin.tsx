
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Virgin = (props: BadgeProps) => <Badge name="Virgin" backgroundColor="#E10A0A" {...props} />

export default Virgin
