
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Virgin = (props: BadgeProps) => <Badge name="Virgin" {...props} backgroundColor="#E10A0A" />

export default Virgin
