
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const SAP = (props: BadgeProps) => <Badge name="SAP" backgroundColor="#0FAAFF" {...props} />

export default SAP
