
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SAP = (props: BadgeProps) => <Badge name="SAP" {...props} backgroundColor="#0FAAFF" />

export default SAP
