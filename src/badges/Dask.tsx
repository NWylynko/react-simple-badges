
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Dask = (props: BadgeProps) => <Badge name="Dask" {...props} backgroundColor="#FDA061" />

export default Dask
