
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Dask = (props: BadgeProps) => <Badge name="Dask" backgroundColor="#FDA061" {...props} />

export default Dask
