
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const KinoPoisk = (props: BadgeProps) => <Badge name="KinoPoisk" {...props} backgroundColor="#FF6600" />

export default KinoPoisk
