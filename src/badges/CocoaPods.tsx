
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CocoaPods = (props: BadgeProps) => <Badge name="CocoaPods" {...props} backgroundColor="#EE3322" />

export default CocoaPods
