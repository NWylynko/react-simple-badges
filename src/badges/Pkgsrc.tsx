
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Pkgsrc = (props: BadgeProps) => <Badge name="pkgsrc" backgroundColor="#FF6600" {...props} />

export default Pkgsrc
