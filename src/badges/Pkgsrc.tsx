
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Pkgsrc = (props: BadgeProps) => <Badge name="pkgsrc" {...props} backgroundColor="#FF6600" />

export default Pkgsrc
