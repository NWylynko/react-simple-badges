
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Swiper = (props: BadgeProps) => <Badge name="Swiper" backgroundColor="#6332F6" {...props} />

export default Swiper
