
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Swiper = (props: BadgeProps) => <Badge name="Swiper" {...props} backgroundColor="#6332F6" />

export default Swiper
