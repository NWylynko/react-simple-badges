
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ZincSearch = (props: BadgeProps) => <Badge name="ZincSearch" {...props} backgroundColor="#5BA37F" />

export default ZincSearch
