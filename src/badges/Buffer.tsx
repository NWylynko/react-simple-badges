
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Buffer = (props: BadgeProps) => <Badge name="Buffer" {...props} backgroundColor="#231F20" />

export default Buffer
