
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Elementary = (props: BadgeProps) => <Badge name="elementary" {...props} backgroundColor="#64BAFF" />

export default Elementary
