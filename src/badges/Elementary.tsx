
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Elementary = (props: BadgeProps) => <Badge name="elementary" backgroundColor="#64BAFF" {...props} />

export default Elementary
