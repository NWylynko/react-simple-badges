
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Librariesio = (props: BadgeProps) => <Badge name="Libraries.io" {...props} backgroundColor="#337AB7" />

export default Librariesio
