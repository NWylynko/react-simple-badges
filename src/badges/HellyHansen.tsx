
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const HellyHansen = (props: BadgeProps) => <Badge name="Helly Hansen" backgroundColor="#DA2128" {...props} />

export default HellyHansen
