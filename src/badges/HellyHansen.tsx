
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const HellyHansen = (props: BadgeProps) => <Badge name="Helly Hansen" {...props} backgroundColor="#DA2128" />

export default HellyHansen
