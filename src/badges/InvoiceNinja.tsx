
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const InvoiceNinja = (props: BadgeProps) => <Badge name="Invoice Ninja" {...props} backgroundColor="#000000" />

export default InvoiceNinja
