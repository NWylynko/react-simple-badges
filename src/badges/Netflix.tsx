
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Netflix = (props: BadgeProps) => <Badge name="Netflix" {...props} backgroundColor="#E50914" />

export default Netflix
