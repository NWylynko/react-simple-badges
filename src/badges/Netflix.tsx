
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Netflix = (props: BadgeProps) => <Badge name="Netflix" backgroundColor="#E50914" {...props} />

export default Netflix
