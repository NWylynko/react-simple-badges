
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Canonical = (props: BadgeProps) => <Badge name="Canonical" backgroundColor="#77216F" {...props} />

export default Canonical
