
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Canonical = (props: BadgeProps) => <Badge name="Canonical" {...props} backgroundColor="#77216F" />

export default Canonical
