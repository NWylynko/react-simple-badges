
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TheAlgorithms = (props: BadgeProps) => <Badge name="The Algorithms" {...props} backgroundColor="#00BCB4" />

export default TheAlgorithms
