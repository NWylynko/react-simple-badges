
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PetsatHome = (props: BadgeProps) => <Badge name="Pets at Home" {...props} backgroundColor="#4BA840" />

export default PetsatHome
