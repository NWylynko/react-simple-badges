
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Jest = (props: BadgeProps) => <Badge name="Jest" {...props} backgroundColor="#C21325" />

export default Jest
