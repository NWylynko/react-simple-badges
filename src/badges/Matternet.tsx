
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Matternet = (props: BadgeProps) => <Badge name="Matternet" {...props} backgroundColor="#261C29" />

export default Matternet
