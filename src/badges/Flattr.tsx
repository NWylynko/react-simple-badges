
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Flattr = (props: BadgeProps) => <Badge name="Flattr" {...props} backgroundColor="#000000" />

export default Flattr
