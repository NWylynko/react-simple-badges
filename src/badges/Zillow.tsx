
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Zillow = (props: BadgeProps) => <Badge name="Zillow" {...props} backgroundColor="#006AFF" />

export default Zillow
