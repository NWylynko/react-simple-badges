
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Zillow = (props: BadgeProps) => <Badge name="Zillow" backgroundColor="#006AFF" {...props} />

export default Zillow
