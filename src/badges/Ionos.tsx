
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Ionos = (props: BadgeProps) => <Badge name="Ionos" {...props} backgroundColor="#003D8F" />

export default Ionos
