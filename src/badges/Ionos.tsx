
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Ionos = (props: BadgeProps) => <Badge name="Ionos" backgroundColor="#003D8F" {...props} />

export default Ionos
