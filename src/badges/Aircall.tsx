
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Aircall = (props: BadgeProps) => <Badge name="Aircall" {...props} backgroundColor="#00B388" />

export default Aircall
