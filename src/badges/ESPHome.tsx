
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const ESPHome = (props: BadgeProps) => <Badge name="ESPHome" backgroundColor="#000000" {...props} />

export default ESPHome
