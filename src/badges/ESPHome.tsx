
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ESPHome = (props: BadgeProps) => <Badge name="ESPHome" {...props} backgroundColor="#000000" />

export default ESPHome
