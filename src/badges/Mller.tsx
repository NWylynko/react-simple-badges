
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Mller = (props: BadgeProps) => <Badge name="Müller" backgroundColor="#F46519" {...props} />

export default Mller
