
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Sennheiser = (props: BadgeProps) => <Badge name="Sennheiser" backgroundColor="#000000" {...props} />

export default Sennheiser
