
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Sennheiser = (props: BadgeProps) => <Badge name="Sennheiser" {...props} backgroundColor="#000000" />

export default Sennheiser
