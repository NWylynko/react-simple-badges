
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Helm = (props: BadgeProps) => <Badge name="Helm" {...props} backgroundColor="#0F1689" />

export default Helm
