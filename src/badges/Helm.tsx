
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Helm = (props: BadgeProps) => <Badge name="Helm" backgroundColor="#0F1689" {...props} />

export default Helm
