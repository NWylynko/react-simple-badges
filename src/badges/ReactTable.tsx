
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const ReactTable = (props: BadgeProps) => <Badge name="React Table" backgroundColor="#FF4154" {...props} />

export default ReactTable
