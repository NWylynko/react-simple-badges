
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ReactTable = (props: BadgeProps) => <Badge name="React Table" {...props} backgroundColor="#FF4154" />

export default ReactTable
