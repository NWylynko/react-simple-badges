
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Mendeley = (props: BadgeProps) => <Badge name="Mendeley" backgroundColor="#9D1620" {...props} />

export default Mendeley
