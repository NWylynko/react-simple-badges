
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Mendeley = (props: BadgeProps) => <Badge name="Mendeley" {...props} backgroundColor="#9D1620" />

export default Mendeley
