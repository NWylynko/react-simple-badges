
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Naver = (props: BadgeProps) => <Badge name="Naver" {...props} backgroundColor="#03C75A" />

export default Naver
