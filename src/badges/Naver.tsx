
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Naver = (props: BadgeProps) => <Badge name="Naver" backgroundColor="#03C75A" {...props} />

export default Naver
