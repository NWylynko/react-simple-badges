
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Clyp = (props: BadgeProps) => <Badge name="Clyp" backgroundColor="#3CBDB1" {...props} />

export default Clyp
