
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Clyp = (props: BadgeProps) => <Badge name="Clyp" {...props} backgroundColor="#3CBDB1" />

export default Clyp
