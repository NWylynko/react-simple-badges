
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Gerrit = (props: BadgeProps) => <Badge name="Gerrit" backgroundColor="#EEEEEE" {...props} />

export default Gerrit
