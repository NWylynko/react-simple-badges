
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Gerrit = (props: BadgeProps) => <Badge name="Gerrit" {...props} backgroundColor="#EEEEEE" />

export default Gerrit
