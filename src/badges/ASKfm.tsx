
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ASKfm = (props: BadgeProps) => <Badge name="ASKfm" {...props} backgroundColor="#DB3552" />

export default ASKfm
