
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Konami = (props: BadgeProps) => <Badge name="Konami" {...props} backgroundColor="#B60014" />

export default Konami
