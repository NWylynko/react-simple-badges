
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const BitcoinCash = (props: BadgeProps) => <Badge name="Bitcoin Cash" backgroundColor="#0AC18E" {...props} />

export default BitcoinCash
