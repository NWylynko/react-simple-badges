
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const BitcoinCash = (props: BadgeProps) => <Badge name="Bitcoin Cash" {...props} backgroundColor="#0AC18E" />

export default BitcoinCash
