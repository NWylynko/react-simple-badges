
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MetrodeMadrid = (props: BadgeProps) => <Badge name="Metro de Madrid" {...props} backgroundColor="#255E9C" />

export default MetrodeMadrid
