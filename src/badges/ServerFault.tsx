
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ServerFault = (props: BadgeProps) => <Badge name="Server Fault" {...props} backgroundColor="#E7282D" />

export default ServerFault
