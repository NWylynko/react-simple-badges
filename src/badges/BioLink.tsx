
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const BioLink = (props: BadgeProps) => <Badge name="Bio Link" {...props} backgroundColor="#000000" />

export default BioLink
