
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const BioLink = (props: BadgeProps) => <Badge name="Bio Link" backgroundColor="#000000" {...props} />

export default BioLink
