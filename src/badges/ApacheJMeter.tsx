
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ApacheJMeter = (props: BadgeProps) => <Badge name="Apache JMeter" {...props} backgroundColor="#D22128" />

export default ApacheJMeter
