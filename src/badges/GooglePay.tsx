
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GooglePay = (props: BadgeProps) => <Badge name="Google Pay" {...props} backgroundColor="#4285F4" />

export default GooglePay
