
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Flyway = (props: BadgeProps) => <Badge name="Flyway" {...props} backgroundColor="#CC0200" />

export default Flyway
