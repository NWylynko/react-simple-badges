
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Supermicro = (props: BadgeProps) => <Badge name="Supermicro" {...props} backgroundColor="#151F6D" />

export default Supermicro
