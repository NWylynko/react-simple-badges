
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const AirPlayVideo = (props: BadgeProps) => <Badge name="AirPlay Video" backgroundColor="#000000" {...props} />

export default AirPlayVideo
