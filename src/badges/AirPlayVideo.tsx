
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AirPlayVideo = (props: BadgeProps) => <Badge name="AirPlay Video" {...props} backgroundColor="#000000" />

export default AirPlayVideo
