
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Gulp = (props: BadgeProps) => <Badge name="gulp" {...props} backgroundColor="#CF4647" />

export default Gulp
