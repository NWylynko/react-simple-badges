
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Gulp = (props: BadgeProps) => <Badge name="gulp" backgroundColor="#CF4647" {...props} />

export default Gulp
