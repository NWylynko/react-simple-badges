
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Airbus = (props: BadgeProps) => <Badge name="Airbus" backgroundColor="#00205B" {...props} />

export default Airbus
