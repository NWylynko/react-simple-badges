
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Airbus = (props: BadgeProps) => <Badge name="Airbus" {...props} backgroundColor="#00205B" />

export default Airbus
