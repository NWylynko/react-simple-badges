
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Framer = (props: BadgeProps) => <Badge name="Framer" backgroundColor="#0055FF" {...props} />

export default Framer
