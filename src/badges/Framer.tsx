
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Framer = (props: BadgeProps) => <Badge name="Framer" {...props} backgroundColor="#0055FF" />

export default Framer
