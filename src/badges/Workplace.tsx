
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Workplace = (props: BadgeProps) => <Badge name="Workplace" backgroundColor="#4326C4" {...props} />

export default Workplace
