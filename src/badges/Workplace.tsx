
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Workplace = (props: BadgeProps) => <Badge name="Workplace" {...props} backgroundColor="#4326C4" />

export default Workplace
