
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Jenkins = (props: BadgeProps) => <Badge name="Jenkins" backgroundColor="#D24939" {...props} />

export default Jenkins
