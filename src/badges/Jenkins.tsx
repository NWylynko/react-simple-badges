
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Jenkins = (props: BadgeProps) => <Badge name="Jenkins" {...props} backgroundColor="#D24939" />

export default Jenkins
