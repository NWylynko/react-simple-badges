
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Tomorrowland = (props: BadgeProps) => <Badge name="Tomorrowland" {...props} backgroundColor="#000000" />

export default Tomorrowland
