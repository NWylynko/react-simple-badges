
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TheIrishTimes = (props: BadgeProps) => <Badge name="The Irish Times" {...props} backgroundColor="#000000" />

export default TheIrishTimes
