
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MEGA = (props: BadgeProps) => <Badge name="MEGA" {...props} backgroundColor="#D9272E" />

export default MEGA
