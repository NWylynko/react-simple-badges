
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Flickr = (props: BadgeProps) => <Badge name="Flickr" backgroundColor="#0063DC" {...props} />

export default Flickr
