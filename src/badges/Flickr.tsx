
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Flickr = (props: BadgeProps) => <Badge name="Flickr" {...props} backgroundColor="#0063DC" />

export default Flickr
