
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Circle = (props: BadgeProps) => <Badge name="Circle" backgroundColor="#8669AE" {...props} />

export default Circle
