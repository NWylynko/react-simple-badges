
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Circle = (props: BadgeProps) => <Badge name="Circle" {...props} backgroundColor="#8669AE" />

export default Circle
