
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const HEY = (props: BadgeProps) => <Badge name="HEY" {...props} backgroundColor="#5522FA" />

export default HEY
