
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Coop = (props: BadgeProps) => <Badge name="Co-op" {...props} backgroundColor="#00B1E7" />

export default Coop
