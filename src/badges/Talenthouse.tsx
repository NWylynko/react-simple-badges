
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Talenthouse = (props: BadgeProps) => <Badge name="Talenthouse" backgroundColor="#FFFFFF" {...props} />

export default Talenthouse
