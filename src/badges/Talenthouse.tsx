
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Talenthouse = (props: BadgeProps) => <Badge name="Talenthouse" {...props} backgroundColor="#FFFFFF" />

export default Talenthouse
