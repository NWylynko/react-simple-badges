
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Glassdoor = (props: BadgeProps) => <Badge name="Glassdoor" {...props} backgroundColor="#0CAA41" />

export default Glassdoor
