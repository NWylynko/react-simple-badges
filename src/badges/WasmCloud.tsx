
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const WasmCloud = (props: BadgeProps) => <Badge name="wasmCloud" {...props} backgroundColor="#00BC8E" />

export default WasmCloud
