
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const WasmCloud = (props: BadgeProps) => <Badge name="wasmCloud" backgroundColor="#00BC8E" {...props} />

export default WasmCloud
