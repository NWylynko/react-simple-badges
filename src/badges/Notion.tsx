
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Notion = (props: BadgeProps) => <Badge name="Notion" backgroundColor="#000000" {...props} />

export default Notion
