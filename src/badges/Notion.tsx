
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Notion = (props: BadgeProps) => <Badge name="Notion" {...props} backgroundColor="#000000" />

export default Notion
