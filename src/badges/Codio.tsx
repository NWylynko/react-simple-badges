
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Codio = (props: BadgeProps) => <Badge name="Codio" {...props} backgroundColor="#4574E0" />

export default Codio
