
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Codio = (props: BadgeProps) => <Badge name="Codio" backgroundColor="#4574E0" {...props} />

export default Codio
