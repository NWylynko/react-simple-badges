
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const RenovateBot = (props: BadgeProps) => <Badge name="RenovateBot" backgroundColor="#1A1F6C" {...props} />

export default RenovateBot
