
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const RenovateBot = (props: BadgeProps) => <Badge name="RenovateBot" {...props} backgroundColor="#1A1F6C" />

export default RenovateBot
