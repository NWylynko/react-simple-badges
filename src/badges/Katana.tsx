
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Katana = (props: BadgeProps) => <Badge name="Katana" {...props} backgroundColor="#000000" />

export default Katana
