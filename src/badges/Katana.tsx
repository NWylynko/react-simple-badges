
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Katana = (props: BadgeProps) => <Badge name="Katana" backgroundColor="#000000" {...props} />

export default Katana
