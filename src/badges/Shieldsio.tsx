
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Shieldsio = (props: BadgeProps) => <Badge name="Shields.io" {...props} backgroundColor="#000000" />

export default Shieldsio
