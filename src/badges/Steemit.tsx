
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Steemit = (props: BadgeProps) => <Badge name="Steemit" backgroundColor="#06D6A9" {...props} />

export default Steemit
