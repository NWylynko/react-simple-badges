
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Steemit = (props: BadgeProps) => <Badge name="Steemit" {...props} backgroundColor="#06D6A9" />

export default Steemit
