
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Fiat = (props: BadgeProps) => <Badge name="Fiat" backgroundColor="#941711" {...props} />

export default Fiat
