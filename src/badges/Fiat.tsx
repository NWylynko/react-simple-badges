
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Fiat = (props: BadgeProps) => <Badge name="Fiat" {...props} backgroundColor="#941711" />

export default Fiat
