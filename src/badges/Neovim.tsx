
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Neovim = (props: BadgeProps) => <Badge name="Neovim" {...props} backgroundColor="#57A143" />

export default Neovim
