
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Neovim = (props: BadgeProps) => <Badge name="Neovim" backgroundColor="#57A143" {...props} />

export default Neovim
