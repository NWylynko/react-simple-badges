
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const SlideShare = (props: BadgeProps) => <Badge name="SlideShare" backgroundColor="#008ED2" {...props} />

export default SlideShare
