
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SlideShare = (props: BadgeProps) => <Badge name="SlideShare" {...props} backgroundColor="#008ED2" />

export default SlideShare
