
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Instatus = (props: BadgeProps) => <Badge name="Instatus" {...props} backgroundColor="#4EE3C2" />

export default Instatus
