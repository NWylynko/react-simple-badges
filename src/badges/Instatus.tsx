
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Instatus = (props: BadgeProps) => <Badge name="Instatus" backgroundColor="#4EE3C2" {...props} />

export default Instatus
