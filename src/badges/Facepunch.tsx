
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Facepunch = (props: BadgeProps) => <Badge name="Facepunch" backgroundColor="#EC1C24" {...props} />

export default Facepunch
