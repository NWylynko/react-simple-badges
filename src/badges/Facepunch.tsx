
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Facepunch = (props: BadgeProps) => <Badge name="Facepunch" {...props} backgroundColor="#EC1C24" />

export default Facepunch
