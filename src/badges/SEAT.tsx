
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SEAT = (props: BadgeProps) => <Badge name="SEAT" {...props} backgroundColor="#33302E" />

export default SEAT
