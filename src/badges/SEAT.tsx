
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const SEAT = (props: BadgeProps) => <Badge name="SEAT" backgroundColor="#33302E" {...props} />

export default SEAT
