
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ApacheAirflow = (props: BadgeProps) => <Badge name="Apache Airflow" {...props} backgroundColor="#017CEE" />

export default ApacheAirflow
