
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Namebase = (props: BadgeProps) => <Badge name="Namebase" backgroundColor="#0068FF" {...props} />

export default Namebase
