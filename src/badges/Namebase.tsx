
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Namebase = (props: BadgeProps) => <Badge name="Namebase" {...props} backgroundColor="#0068FF" />

export default Namebase
