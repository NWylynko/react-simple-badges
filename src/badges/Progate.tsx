
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Progate = (props: BadgeProps) => <Badge name="Progate" {...props} backgroundColor="#380953" />

export default Progate
