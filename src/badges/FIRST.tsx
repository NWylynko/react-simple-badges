
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const FIRST = (props: BadgeProps) => <Badge name="FIRST" {...props} backgroundColor="#0066B3" />

export default FIRST
