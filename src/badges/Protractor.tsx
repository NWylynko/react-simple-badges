
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Protractor = (props: BadgeProps) => <Badge name="Protractor" backgroundColor="#ED163A" {...props} />

export default Protractor
