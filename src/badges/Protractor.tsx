
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Protractor = (props: BadgeProps) => <Badge name="Protractor" {...props} backgroundColor="#ED163A" />

export default Protractor
