
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const StopStalk = (props: BadgeProps) => <Badge name="StopStalk" {...props} backgroundColor="#536DFE" />

export default StopStalk
