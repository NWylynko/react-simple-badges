
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const StopStalk = (props: BadgeProps) => <Badge name="StopStalk" backgroundColor="#536DFE" {...props} />

export default StopStalk
