
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Hugo = (props: BadgeProps) => <Badge name="Hugo" {...props} backgroundColor="#FF4088" />

export default Hugo
