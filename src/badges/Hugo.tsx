
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Hugo = (props: BadgeProps) => <Badge name="Hugo" backgroundColor="#FF4088" {...props} />

export default Hugo
