
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Pastebin = (props: BadgeProps) => <Badge name="Pastebin" backgroundColor="#02456C" {...props} />

export default Pastebin
