
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Pastebin = (props: BadgeProps) => <Badge name="Pastebin" {...props} backgroundColor="#02456C" />

export default Pastebin
