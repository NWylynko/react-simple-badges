
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Nette = (props: BadgeProps) => <Badge name="Nette" {...props} backgroundColor="#3484D2" />

export default Nette
