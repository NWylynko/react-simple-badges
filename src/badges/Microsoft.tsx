
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Microsoft = (props: BadgeProps) => <Badge name="Microsoft" {...props} backgroundColor="#5E5E5E" />

export default Microsoft
