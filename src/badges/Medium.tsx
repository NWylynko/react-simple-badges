
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Medium = (props: BadgeProps) => <Badge name="Medium" backgroundColor="#000000" {...props} />

export default Medium
