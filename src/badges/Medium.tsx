
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Medium = (props: BadgeProps) => <Badge name="Medium" {...props} backgroundColor="#000000" />

export default Medium
