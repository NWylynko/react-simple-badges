
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PyScaffold = (props: BadgeProps) => <Badge name="PyScaffold" {...props} backgroundColor="#005CA0" />

export default PyScaffold
