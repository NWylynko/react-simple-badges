
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const PyScaffold = (props: BadgeProps) => <Badge name="PyScaffold" backgroundColor="#005CA0" {...props} />

export default PyScaffold
