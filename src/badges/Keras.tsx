
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Keras = (props: BadgeProps) => <Badge name="Keras" {...props} backgroundColor="#D00000" />

export default Keras
