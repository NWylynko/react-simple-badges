
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Keras = (props: BadgeProps) => <Badge name="Keras" backgroundColor="#D00000" {...props} />

export default Keras
