
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Tsnode = (props: BadgeProps) => <Badge name="ts-node" {...props} backgroundColor="#3178C6" />

export default Tsnode
