
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Zorin = (props: BadgeProps) => <Badge name="Zorin" {...props} backgroundColor="#0CC1F3" />

export default Zorin
