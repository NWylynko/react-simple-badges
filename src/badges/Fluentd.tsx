
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Fluentd = (props: BadgeProps) => <Badge name="Fluentd" {...props} backgroundColor="#0E83C8" />

export default Fluentd
