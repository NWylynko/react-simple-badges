
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Deepin = (props: BadgeProps) => <Badge name="deepin" {...props} backgroundColor="#007CFF" />

export default Deepin
