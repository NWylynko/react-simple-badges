
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Deepin = (props: BadgeProps) => <Badge name="deepin" backgroundColor="#007CFF" {...props} />

export default Deepin
