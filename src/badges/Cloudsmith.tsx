
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Cloudsmith = (props: BadgeProps) => <Badge name="Cloudsmith" {...props} backgroundColor="#187EB6" />

export default Cloudsmith
