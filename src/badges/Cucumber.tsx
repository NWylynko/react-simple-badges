
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Cucumber = (props: BadgeProps) => <Badge name="Cucumber" {...props} backgroundColor="#23D96C" />

export default Cucumber
