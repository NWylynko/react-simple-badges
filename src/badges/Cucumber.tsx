
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Cucumber = (props: BadgeProps) => <Badge name="Cucumber" backgroundColor="#23D96C" {...props} />

export default Cucumber
