
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Aparat = (props: BadgeProps) => <Badge name="Aparat" backgroundColor="#ED145B" {...props} />

export default Aparat
