
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Aparat = (props: BadgeProps) => <Badge name="Aparat" {...props} backgroundColor="#ED145B" />

export default Aparat
