
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const OpenCV = (props: BadgeProps) => <Badge name="OpenCV" backgroundColor="#5C3EE8" {...props} />

export default OpenCV
