
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const OpenCV = (props: BadgeProps) => <Badge name="OpenCV" {...props} backgroundColor="#5C3EE8" />

export default OpenCV
