
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const JetBrains = (props: BadgeProps) => <Badge name="JetBrains" {...props} backgroundColor="#000000" />

export default JetBrains
