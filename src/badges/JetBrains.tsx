
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const JetBrains = (props: BadgeProps) => <Badge name="JetBrains" backgroundColor="#000000" {...props} />

export default JetBrains
