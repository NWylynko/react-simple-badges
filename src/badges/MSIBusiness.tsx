
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MSIBusiness = (props: BadgeProps) => <Badge name="MSI Business" {...props} backgroundColor="#9A8555" />

export default MSIBusiness
