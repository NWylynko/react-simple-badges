
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const MSIBusiness = (props: BadgeProps) => <Badge name="MSI Business" backgroundColor="#9A8555" {...props} />

export default MSIBusiness
