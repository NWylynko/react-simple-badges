
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Ubuntu = (props: BadgeProps) => <Badge name="Ubuntu" backgroundColor="#E95420" {...props} />

export default Ubuntu
