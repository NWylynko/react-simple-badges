
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Ubuntu = (props: BadgeProps) => <Badge name="Ubuntu" {...props} backgroundColor="#E95420" />

export default Ubuntu
