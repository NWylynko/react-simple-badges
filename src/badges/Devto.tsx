
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Devto = (props: BadgeProps) => <Badge name="dev.to" {...props} backgroundColor="#0A0A0A" />

export default Devto
