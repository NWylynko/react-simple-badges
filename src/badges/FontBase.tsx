
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const FontBase = (props: BadgeProps) => <Badge name="FontBase" {...props} backgroundColor="#3D03A7" />

export default FontBase
