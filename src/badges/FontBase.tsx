
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const FontBase = (props: BadgeProps) => <Badge name="FontBase" backgroundColor="#3D03A7" {...props} />

export default FontBase
