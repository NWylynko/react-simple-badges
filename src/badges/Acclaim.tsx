
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Acclaim = (props: BadgeProps) => <Badge name="Acclaim" {...props} backgroundColor="#26689A" />

export default Acclaim
