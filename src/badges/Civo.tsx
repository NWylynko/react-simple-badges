
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Civo = (props: BadgeProps) => <Badge name="Civo" backgroundColor="#239DFF" {...props} />

export default Civo
