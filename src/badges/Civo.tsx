
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Civo = (props: BadgeProps) => <Badge name="Civo" {...props} backgroundColor="#239DFF" />

export default Civo
