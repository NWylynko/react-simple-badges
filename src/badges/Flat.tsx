
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Flat = (props: BadgeProps) => <Badge name="Flat" {...props} backgroundColor="#3481FE" />

export default Flat
