
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Flat = (props: BadgeProps) => <Badge name="Flat" backgroundColor="#3481FE" {...props} />

export default Flat
