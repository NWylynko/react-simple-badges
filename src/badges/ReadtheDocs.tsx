
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const ReadtheDocs = (props: BadgeProps) => <Badge name="Read the Docs" backgroundColor="#8CA1AF" {...props} />

export default ReadtheDocs
