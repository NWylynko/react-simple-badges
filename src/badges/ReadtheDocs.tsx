
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ReadtheDocs = (props: BadgeProps) => <Badge name="Read the Docs" {...props} backgroundColor="#8CA1AF" />

export default ReadtheDocs
