
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Chocolatey = (props: BadgeProps) => <Badge name="Chocolatey" {...props} backgroundColor="#80B5E3" />

export default Chocolatey
