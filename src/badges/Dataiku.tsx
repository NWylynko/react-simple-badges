
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Dataiku = (props: BadgeProps) => <Badge name="Dataiku" {...props} backgroundColor="#2AB1AC" />

export default Dataiku
