
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const NumPy = (props: BadgeProps) => <Badge name="NumPy" {...props} backgroundColor="#013243" />

export default NumPy
