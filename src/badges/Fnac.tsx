
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Fnac = (props: BadgeProps) => <Badge name="Fnac" {...props} backgroundColor="#E1A925" />

export default Fnac
