
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ByteDance = (props: BadgeProps) => <Badge name="ByteDance" {...props} backgroundColor="#3C8CFF" />

export default ByteDance
