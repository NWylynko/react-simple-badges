
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Barclays = (props: BadgeProps) => <Badge name="Barclays" backgroundColor="#00AEEF" {...props} />

export default Barclays
