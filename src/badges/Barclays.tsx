
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Barclays = (props: BadgeProps) => <Badge name="Barclays" {...props} backgroundColor="#00AEEF" />

export default Barclays
