
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GameDeveloper = (props: BadgeProps) => <Badge name="Game Developer" {...props} backgroundColor="#E60012" />

export default GameDeveloper
