
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Adobe = (props: BadgeProps) => <Badge name="Adobe" {...props} backgroundColor="#FF0000" />

export default Adobe
