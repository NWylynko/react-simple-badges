
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Adobe = (props: BadgeProps) => <Badge name="Adobe" backgroundColor="#FF0000" {...props} />

export default Adobe
