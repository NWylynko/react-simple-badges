
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Instapaper = (props: BadgeProps) => <Badge name="Instapaper" backgroundColor="#1F1F1F" {...props} />

export default Instapaper
