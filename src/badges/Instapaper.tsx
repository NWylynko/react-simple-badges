
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Instapaper = (props: BadgeProps) => <Badge name="Instapaper" {...props} backgroundColor="#1F1F1F" />

export default Instapaper
