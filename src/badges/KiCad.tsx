
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const KiCad = (props: BadgeProps) => <Badge name="KiCad" backgroundColor="#314CB0" {...props} />

export default KiCad
