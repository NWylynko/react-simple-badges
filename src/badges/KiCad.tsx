
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const KiCad = (props: BadgeProps) => <Badge name="KiCad" {...props} backgroundColor="#314CB0" />

export default KiCad
