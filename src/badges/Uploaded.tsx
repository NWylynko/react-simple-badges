
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Uploaded = (props: BadgeProps) => <Badge name="Uploaded" backgroundColor="#0E70CB" {...props} />

export default Uploaded
