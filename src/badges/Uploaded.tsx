
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Uploaded = (props: BadgeProps) => <Badge name="Uploaded" {...props} backgroundColor="#0E70CB" />

export default Uploaded
