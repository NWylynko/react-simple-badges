
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Tasmota = (props: BadgeProps) => <Badge name="Tasmota" {...props} backgroundColor="#1FA3EC" />

export default Tasmota
