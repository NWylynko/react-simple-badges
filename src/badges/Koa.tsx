
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Koa = (props: BadgeProps) => <Badge name="Koa" {...props} backgroundColor="#33333D" />

export default Koa
