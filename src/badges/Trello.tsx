
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Trello = (props: BadgeProps) => <Badge name="Trello" {...props} backgroundColor="#0052CC" />

export default Trello
