
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const WizzAir = (props: BadgeProps) => <Badge name="Wizz Air" {...props} backgroundColor="#C6007E" />

export default WizzAir
