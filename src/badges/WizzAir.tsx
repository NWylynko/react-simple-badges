
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const WizzAir = (props: BadgeProps) => <Badge name="Wizz Air" backgroundColor="#C6007E" {...props} />

export default WizzAir
