
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const YouTube = (props: BadgeProps) => <Badge name="YouTube" backgroundColor="#FF0000" {...props} />

export default YouTube
