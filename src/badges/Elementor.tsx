
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Elementor = (props: BadgeProps) => <Badge name="Elementor" {...props} backgroundColor="#92003B" />

export default Elementor
