
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Qi = (props: BadgeProps) => <Badge name="Qi" {...props} backgroundColor="#000000" />

export default Qi
