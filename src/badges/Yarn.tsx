
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Yarn = (props: BadgeProps) => <Badge name="Yarn" {...props} backgroundColor="#2C8EBB" />

export default Yarn
