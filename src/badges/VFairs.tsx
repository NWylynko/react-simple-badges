
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const VFairs = (props: BadgeProps) => <Badge name="vFairs" {...props} backgroundColor="#EF4678" />

export default VFairs
