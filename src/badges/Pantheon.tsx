
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Pantheon = (props: BadgeProps) => <Badge name="Pantheon" {...props} backgroundColor="#FFDC28" />

export default Pantheon
