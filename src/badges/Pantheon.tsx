
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Pantheon = (props: BadgeProps) => <Badge name="Pantheon" backgroundColor="#FFDC28" {...props} />

export default Pantheon
