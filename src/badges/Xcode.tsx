
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Xcode = (props: BadgeProps) => <Badge name="Xcode" backgroundColor="#147EFB" {...props} />

export default Xcode
