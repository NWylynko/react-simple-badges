
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Xcode = (props: BadgeProps) => <Badge name="Xcode" {...props} backgroundColor="#147EFB" />

export default Xcode
