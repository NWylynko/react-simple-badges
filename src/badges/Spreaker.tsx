
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Spreaker = (props: BadgeProps) => <Badge name="Spreaker" {...props} backgroundColor="#F5C300" />

export default Spreaker
