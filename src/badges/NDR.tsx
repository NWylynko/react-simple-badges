
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const NDR = (props: BadgeProps) => <Badge name="NDR" {...props} backgroundColor="#0C1754" />

export default NDR
