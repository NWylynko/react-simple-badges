
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Pimcore = (props: BadgeProps) => <Badge name="Pimcore" {...props} backgroundColor="#6428B4" />

export default Pimcore
