
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SpeakerDeck = (props: BadgeProps) => <Badge name="Speaker Deck" {...props} backgroundColor="#009287" />

export default SpeakerDeck
