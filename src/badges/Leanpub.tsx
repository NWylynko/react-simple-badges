
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Leanpub = (props: BadgeProps) => <Badge name="Leanpub" {...props} backgroundColor="#FFFFFF" />

export default Leanpub
