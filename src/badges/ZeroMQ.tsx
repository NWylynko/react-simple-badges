
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ZeroMQ = (props: BadgeProps) => <Badge name="ZeroMQ" {...props} backgroundColor="#DF0000" />

export default ZeroMQ
