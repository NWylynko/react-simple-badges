
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const ZeroMQ = (props: BadgeProps) => <Badge name="ZeroMQ" backgroundColor="#DF0000" {...props} />

export default ZeroMQ
