
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Photobucket = (props: BadgeProps) => <Badge name="Photobucket" {...props} backgroundColor="#0672CB" />

export default Photobucket
