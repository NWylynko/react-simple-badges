
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const LeaderPrice = (props: BadgeProps) => <Badge name="Leader Price" backgroundColor="#E50005" {...props} />

export default LeaderPrice
