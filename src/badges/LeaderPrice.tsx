
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const LeaderPrice = (props: BadgeProps) => <Badge name="Leader Price" {...props} backgroundColor="#E50005" />

export default LeaderPrice
