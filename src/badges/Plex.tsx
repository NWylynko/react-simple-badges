
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Plex = (props: BadgeProps) => <Badge name="Plex" backgroundColor="#EBAF00" {...props} />

export default Plex
