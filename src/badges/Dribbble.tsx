
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Dribbble = (props: BadgeProps) => <Badge name="Dribbble" {...props} backgroundColor="#EA4C89" />

export default Dribbble
