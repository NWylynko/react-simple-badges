
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Stellar = (props: BadgeProps) => <Badge name="Stellar" {...props} backgroundColor="#7D00FF" />

export default Stellar
