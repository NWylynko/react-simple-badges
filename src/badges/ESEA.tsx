
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ESEA = (props: BadgeProps) => <Badge name="ESEA" {...props} backgroundColor="#0E9648" />

export default ESEA
