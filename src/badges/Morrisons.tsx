
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Morrisons = (props: BadgeProps) => <Badge name="Morrisons" {...props} backgroundColor="#007531" />

export default Morrisons
