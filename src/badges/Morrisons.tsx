
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Morrisons = (props: BadgeProps) => <Badge name="Morrisons" backgroundColor="#007531" {...props} />

export default Morrisons
