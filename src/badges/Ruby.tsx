
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Ruby = (props: BadgeProps) => <Badge name="Ruby" {...props} backgroundColor="#CC342D" />

export default Ruby
