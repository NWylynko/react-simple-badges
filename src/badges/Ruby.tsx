
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Ruby = (props: BadgeProps) => <Badge name="Ruby" backgroundColor="#CC342D" {...props} />

export default Ruby
