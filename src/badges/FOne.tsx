
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const FOne = (props: BadgeProps) => <Badge name="F1" {...props} backgroundColor="#E10600" />

export default FOne
