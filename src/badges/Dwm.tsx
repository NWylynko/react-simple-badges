
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Dwm = (props: BadgeProps) => <Badge name="dwm" {...props} backgroundColor="#1177AA" />

export default Dwm
