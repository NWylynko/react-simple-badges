
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Streamlit = (props: BadgeProps) => <Badge name="Streamlit" {...props} backgroundColor="#FF4B4B" />

export default Streamlit
