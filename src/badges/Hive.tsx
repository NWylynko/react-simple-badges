
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Hive = (props: BadgeProps) => <Badge name="Hive" backgroundColor="#FF7A00" {...props} />

export default Hive
