
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Hive = (props: BadgeProps) => <Badge name="Hive" {...props} backgroundColor="#FF7A00" />

export default Hive
