
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const StarlingBank = (props: BadgeProps) => <Badge name="Starling Bank" backgroundColor="#6935D3" {...props} />

export default StarlingBank
