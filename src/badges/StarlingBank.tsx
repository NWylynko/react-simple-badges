
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const StarlingBank = (props: BadgeProps) => <Badge name="Starling Bank" {...props} backgroundColor="#6935D3" />

export default StarlingBank
