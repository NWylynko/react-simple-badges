
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Subversion = (props: BadgeProps) => <Badge name="Subversion" backgroundColor="#809CC9" {...props} />

export default Subversion
