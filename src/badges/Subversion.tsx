
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Subversion = (props: BadgeProps) => <Badge name="Subversion" {...props} backgroundColor="#809CC9" />

export default Subversion
