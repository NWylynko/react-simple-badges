
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Payoneer = (props: BadgeProps) => <Badge name="Payoneer" {...props} backgroundColor="#FF4800" />

export default Payoneer
