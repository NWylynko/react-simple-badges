
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Payoneer = (props: BadgeProps) => <Badge name="Payoneer" backgroundColor="#FF4800" {...props} />

export default Payoneer
