
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const XSplit = (props: BadgeProps) => <Badge name="XSplit" backgroundColor="#0095DE" {...props} />

export default XSplit
