
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const XSplit = (props: BadgeProps) => <Badge name="XSplit" {...props} backgroundColor="#0095DE" />

export default XSplit
