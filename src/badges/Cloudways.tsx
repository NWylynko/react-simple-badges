
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Cloudways = (props: BadgeProps) => <Badge name="Cloudways" {...props} backgroundColor="#2C39BD" />

export default Cloudways
