
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Battlenet = (props: BadgeProps) => <Badge name="Battle.net" backgroundColor="#148EFF" {...props} />

export default Battlenet
