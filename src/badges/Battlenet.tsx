
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Battlenet = (props: BadgeProps) => <Badge name="Battle.net" {...props} backgroundColor="#148EFF" />

export default Battlenet
