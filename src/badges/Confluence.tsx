
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Confluence = (props: BadgeProps) => <Badge name="Confluence" {...props} backgroundColor="#172B4D" />

export default Confluence
