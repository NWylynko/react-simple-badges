
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const XMPP = (props: BadgeProps) => <Badge name="XMPP" {...props} backgroundColor="#002B5C" />

export default XMPP
