
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const XMPP = (props: BadgeProps) => <Badge name="XMPP" backgroundColor="#002B5C" {...props} />

export default XMPP
