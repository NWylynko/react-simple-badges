
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Mumble = (props: BadgeProps) => <Badge name="Mumble" {...props} backgroundColor="#FFFFFF" />

export default Mumble
