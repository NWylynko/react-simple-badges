
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Mumble = (props: BadgeProps) => <Badge name="Mumble" backgroundColor="#FFFFFF" {...props} />

export default Mumble
