
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Icinga = (props: BadgeProps) => <Badge name="Icinga" backgroundColor="#06062C" {...props} />

export default Icinga
