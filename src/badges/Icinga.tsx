
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Icinga = (props: BadgeProps) => <Badge name="Icinga" {...props} backgroundColor="#06062C" />

export default Icinga
