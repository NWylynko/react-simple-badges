
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TinyLetter = (props: BadgeProps) => <Badge name="TinyLetter" {...props} backgroundColor="#ED1C24" />

export default TinyLetter
