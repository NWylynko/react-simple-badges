
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Douban = (props: BadgeProps) => <Badge name="Douban" backgroundColor="#2D963D" {...props} />

export default Douban
