
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Douban = (props: BadgeProps) => <Badge name="Douban" {...props} backgroundColor="#007722" />

export default Douban
