
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PUBG = (props: BadgeProps) => <Badge name="PUBG" {...props} backgroundColor="#FEAB02" />

export default PUBG
