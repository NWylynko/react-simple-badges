
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const PUBG = (props: BadgeProps) => <Badge name="PUBG" backgroundColor="#FEAB02" {...props} />

export default PUBG
