
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Momenteo = (props: BadgeProps) => <Badge name="Momenteo" {...props} backgroundColor="#5A6AB1" />

export default Momenteo
