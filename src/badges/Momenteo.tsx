
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Momenteo = (props: BadgeProps) => <Badge name="Momenteo" backgroundColor="#5A6AB1" {...props} />

export default Momenteo
