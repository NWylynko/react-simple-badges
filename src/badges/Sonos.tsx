
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Sonos = (props: BadgeProps) => <Badge name="Sonos" {...props} backgroundColor="#000000" />

export default Sonos
