
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Sonos = (props: BadgeProps) => <Badge name="Sonos" backgroundColor="#000000" {...props} />

export default Sonos
