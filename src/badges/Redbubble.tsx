
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Redbubble = (props: BadgeProps) => <Badge name="Redbubble" {...props} backgroundColor="#E41321" />

export default Redbubble
