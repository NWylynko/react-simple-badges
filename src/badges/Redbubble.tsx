
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Redbubble = (props: BadgeProps) => <Badge name="Redbubble" backgroundColor="#E41321" {...props} />

export default Redbubble
