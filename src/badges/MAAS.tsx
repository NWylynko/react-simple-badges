
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MAAS = (props: BadgeProps) => <Badge name="MAAS" {...props} backgroundColor="#E95420" />

export default MAAS
