
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Quantcast = (props: BadgeProps) => <Badge name="Quantcast" {...props} backgroundColor="#000000" />

export default Quantcast
