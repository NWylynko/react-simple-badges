
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const NativeScript = (props: BadgeProps) => <Badge name="NativeScript" {...props} backgroundColor="#3655FF" />

export default NativeScript
