
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const NativeScript = (props: BadgeProps) => <Badge name="NativeScript" backgroundColor="#3655FF" {...props} />

export default NativeScript
