
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GoogleScholar = (props: BadgeProps) => <Badge name="Google Scholar" {...props} backgroundColor="#4285F4" />

export default GoogleScholar
