
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Opel = (props: BadgeProps) => <Badge name="Opel" {...props} backgroundColor="#F7FF14" />

export default Opel
