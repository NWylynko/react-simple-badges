
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Go = (props: BadgeProps) => <Badge name="Go" {...props} backgroundColor="#00ADD8" />

export default Go
