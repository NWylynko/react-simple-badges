
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Go = (props: BadgeProps) => <Badge name="Go" backgroundColor="#00ADD8" {...props} />

export default Go
