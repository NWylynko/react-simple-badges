
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const JBL = (props: BadgeProps) => <Badge name="JBL" {...props} backgroundColor="#FF3300" />

export default JBL
