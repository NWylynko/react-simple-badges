
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Known = (props: BadgeProps) => <Badge name="Known" {...props} backgroundColor="#333333" />

export default Known
