
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Known = (props: BadgeProps) => <Badge name="Known" backgroundColor="#333333" {...props} />

export default Known
