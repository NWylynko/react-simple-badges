
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const UPS = (props: BadgeProps) => <Badge name="UPS" backgroundColor="#150400" {...props} />

export default UPS
