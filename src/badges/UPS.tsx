
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const UPS = (props: BadgeProps) => <Badge name="UPS" {...props} backgroundColor="#150400" />

export default UPS
