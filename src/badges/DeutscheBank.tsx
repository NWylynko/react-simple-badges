
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const DeutscheBank = (props: BadgeProps) => <Badge name="Deutsche Bank" {...props} backgroundColor="#0018A8" />

export default DeutscheBank
