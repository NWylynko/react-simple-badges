
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Krita = (props: BadgeProps) => <Badge name="Krita" {...props} backgroundColor="#3BABFF" />

export default Krita
