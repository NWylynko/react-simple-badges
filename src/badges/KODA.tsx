
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const KODA = (props: BadgeProps) => <Badge name="ŠKODA" {...props} backgroundColor="#4BA82E" />

export default KODA
