
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GameWatch = (props: BadgeProps) => <Badge name="Game & Watch" {...props} backgroundColor="#000000" />

export default GameWatch
