
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Boots = (props: BadgeProps) => <Badge name="Boots" {...props} backgroundColor="#05054B" />

export default Boots
