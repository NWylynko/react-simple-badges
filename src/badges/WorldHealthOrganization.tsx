
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const WorldHealthOrganization = (props: BadgeProps) => <Badge name="World Health Organization" backgroundColor="#0093D5" {...props} />

export default WorldHealthOrganization
