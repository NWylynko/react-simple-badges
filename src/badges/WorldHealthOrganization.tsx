
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const WorldHealthOrganization = (props: BadgeProps) => <Badge name="World Health Organization" {...props} backgroundColor="#0093D5" />

export default WorldHealthOrganization
