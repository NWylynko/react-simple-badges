
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const XAML = (props: BadgeProps) => <Badge name="XAML" {...props} backgroundColor="#0C54C2" />

export default XAML
