
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const XAML = (props: BadgeProps) => <Badge name="XAML" backgroundColor="#0C54C2" {...props} />

export default XAML
