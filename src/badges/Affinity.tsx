
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Affinity = (props: BadgeProps) => <Badge name="Affinity" backgroundColor="#222324" {...props} />

export default Affinity
