
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Affinity = (props: BadgeProps) => <Badge name="Affinity" {...props} backgroundColor="#222324" />

export default Affinity
