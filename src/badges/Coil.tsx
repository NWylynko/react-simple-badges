
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Coil = (props: BadgeProps) => <Badge name="Coil" {...props} backgroundColor="#000000" />

export default Coil
