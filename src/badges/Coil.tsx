
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Coil = (props: BadgeProps) => <Badge name="Coil" backgroundColor="#000000" {...props} />

export default Coil
