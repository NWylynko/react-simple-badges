
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const V = (props: BadgeProps) => <Badge name="V" {...props} backgroundColor="#5D87BF" />

export default V
