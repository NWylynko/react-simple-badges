
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const OctaneRender = (props: BadgeProps) => <Badge name="Octane Render" backgroundColor="#000000" {...props} />

export default OctaneRender
