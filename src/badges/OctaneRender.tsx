
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const OctaneRender = (props: BadgeProps) => <Badge name="Octane Render" {...props} backgroundColor="#000000" />

export default OctaneRender
