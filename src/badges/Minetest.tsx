
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Minetest = (props: BadgeProps) => <Badge name="Minetest" {...props} backgroundColor="#53AC56" />

export default Minetest
