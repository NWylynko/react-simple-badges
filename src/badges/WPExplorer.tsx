
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const WPExplorer = (props: BadgeProps) => <Badge name="WPExplorer" {...props} backgroundColor="#2563EB" />

export default WPExplorer
