
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GoogleColab = (props: BadgeProps) => <Badge name="Google Colab" {...props} backgroundColor="#F9AB00" />

export default GoogleColab
