
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Flipboard = (props: BadgeProps) => <Badge name="Flipboard" {...props} backgroundColor="#E12828" />

export default Flipboard
