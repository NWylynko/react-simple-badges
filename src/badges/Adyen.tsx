
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Adyen = (props: BadgeProps) => <Badge name="Adyen" {...props} backgroundColor="#0ABF53" />

export default Adyen
