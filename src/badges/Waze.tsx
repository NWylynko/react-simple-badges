
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Waze = (props: BadgeProps) => <Badge name="Waze" {...props} backgroundColor="#33CCFF" />

export default Waze
