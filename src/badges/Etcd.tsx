
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Etcd = (props: BadgeProps) => <Badge name="etcd" backgroundColor="#419EDA" {...props} />

export default Etcd
