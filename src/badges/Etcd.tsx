
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Etcd = (props: BadgeProps) => <Badge name="etcd" {...props} backgroundColor="#419EDA" />

export default Etcd
