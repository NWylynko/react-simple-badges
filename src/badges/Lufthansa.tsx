
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Lufthansa = (props: BadgeProps) => <Badge name="Lufthansa" {...props} backgroundColor="#05164D" />

export default Lufthansa
