
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Lufthansa = (props: BadgeProps) => <Badge name="Lufthansa" backgroundColor="#05164D" {...props} />

export default Lufthansa
