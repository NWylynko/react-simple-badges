
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GoogleAds = (props: BadgeProps) => <Badge name="Google Ads" {...props} backgroundColor="#4285F4" />

export default GoogleAds
