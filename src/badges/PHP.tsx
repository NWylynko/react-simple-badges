
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PHP = (props: BadgeProps) => <Badge name="PHP" {...props} backgroundColor="#777BB4" />

export default PHP
