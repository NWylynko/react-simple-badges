
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const PHP = (props: BadgeProps) => <Badge name="PHP" backgroundColor="#777BB4" {...props} />

export default PHP
