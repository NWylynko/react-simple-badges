
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Kahoot = (props: BadgeProps) => <Badge name="Kahoot!" backgroundColor="#46178F" {...props} />

export default Kahoot
