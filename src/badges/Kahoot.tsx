
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Kahoot = (props: BadgeProps) => <Badge name="Kahoot!" {...props} backgroundColor="#46178F" />

export default Kahoot
