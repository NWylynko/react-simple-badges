
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const ChartMogul = (props: BadgeProps) => <Badge name="ChartMogul" backgroundColor="#13324B" {...props} />

export default ChartMogul
