
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ChartMogul = (props: BadgeProps) => <Badge name="ChartMogul" {...props} backgroundColor="#13324B" />

export default ChartMogul
