
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Sequelize = (props: BadgeProps) => <Badge name="Sequelize" backgroundColor="#52B0E7" {...props} />

export default Sequelize
