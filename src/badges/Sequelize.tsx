
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Sequelize = (props: BadgeProps) => <Badge name="Sequelize" {...props} backgroundColor="#52B0E7" />

export default Sequelize
