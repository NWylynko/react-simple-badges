
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TheNorthFace = (props: BadgeProps) => <Badge name="The North Face" {...props} backgroundColor="#000000" />

export default TheNorthFace
