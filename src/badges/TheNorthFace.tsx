
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const TheNorthFace = (props: BadgeProps) => <Badge name="The North Face" backgroundColor="#000000" {...props} />

export default TheNorthFace
