
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const IBMCloud = (props: BadgeProps) => <Badge name="IBM Cloud" backgroundColor="#1261FE" {...props} />

export default IBMCloud
