
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Honey = (props: BadgeProps) => <Badge name="Honey" {...props} backgroundColor="#FF6801" />

export default Honey
