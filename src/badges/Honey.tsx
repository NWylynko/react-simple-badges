
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Honey = (props: BadgeProps) => <Badge name="Honey" backgroundColor="#FF6801" {...props} />

export default Honey
