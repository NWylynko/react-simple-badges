
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Foursquare = (props: BadgeProps) => <Badge name="Foursquare" {...props} backgroundColor="#3333FF" />

export default Foursquare
