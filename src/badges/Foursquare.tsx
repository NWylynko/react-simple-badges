
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Foursquare = (props: BadgeProps) => <Badge name="Foursquare" backgroundColor="#3333FF" {...props} />

export default Foursquare
