
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Pagekit = (props: BadgeProps) => <Badge name="Pagekit" {...props} backgroundColor="#212121" />

export default Pagekit
