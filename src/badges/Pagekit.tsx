
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Pagekit = (props: BadgeProps) => <Badge name="Pagekit" backgroundColor="#212121" {...props} />

export default Pagekit
