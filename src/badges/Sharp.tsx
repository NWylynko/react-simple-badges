
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Sharp = (props: BadgeProps) => <Badge name="sharp" {...props} backgroundColor="#99CC00" />

export default Sharp
