
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Sharp = (props: BadgeProps) => <Badge name="sharp" backgroundColor="#99CC00" {...props} />

export default Sharp
