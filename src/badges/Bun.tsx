
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Bun = (props: BadgeProps) => <Badge name="Bun" {...props} backgroundColor="#000000" />

export default Bun
