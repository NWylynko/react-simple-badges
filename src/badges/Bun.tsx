
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Bun = (props: BadgeProps) => <Badge name="Bun" backgroundColor="#000000" {...props} />

export default Bun
