
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Blockchaincom = (props: BadgeProps) => <Badge name="Blockchain.com" {...props} backgroundColor="#121D33" />

export default Blockchaincom
