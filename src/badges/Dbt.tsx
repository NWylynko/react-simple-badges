
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Dbt = (props: BadgeProps) => <Badge name="dbt" {...props} backgroundColor="#FF694B" />

export default Dbt
