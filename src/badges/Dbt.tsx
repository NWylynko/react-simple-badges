
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Dbt = (props: BadgeProps) => <Badge name="dbt" backgroundColor="#FF694B" {...props} />

export default Dbt
