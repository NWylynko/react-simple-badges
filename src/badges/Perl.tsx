
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Perl = (props: BadgeProps) => <Badge name="Perl" backgroundColor="#39457E" {...props} />

export default Perl
