
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Perl = (props: BadgeProps) => <Badge name="Perl" {...props} backgroundColor="#39457E" />

export default Perl
