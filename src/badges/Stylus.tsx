
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Stylus = (props: BadgeProps) => <Badge name="Stylus" backgroundColor="#333333" {...props} />

export default Stylus
