
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Stylus = (props: BadgeProps) => <Badge name="Stylus" {...props} backgroundColor="#333333" />

export default Stylus
