
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const FreeNAS = (props: BadgeProps) => <Badge name="FreeNAS" {...props} backgroundColor="#343434" />

export default FreeNAS
