
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const RuboCop = (props: BadgeProps) => <Badge name="RuboCop" {...props} backgroundColor="#000000" />

export default RuboCop
