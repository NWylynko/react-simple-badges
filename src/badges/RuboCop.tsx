
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const RuboCop = (props: BadgeProps) => <Badge name="RuboCop" backgroundColor="#000000" {...props} />

export default RuboCop
