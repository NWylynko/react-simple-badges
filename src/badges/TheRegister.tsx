
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TheRegister = (props: BadgeProps) => <Badge name="The Register" {...props} backgroundColor="#FF0000" />

export default TheRegister
