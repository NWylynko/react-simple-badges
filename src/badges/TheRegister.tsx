
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const TheRegister = (props: BadgeProps) => <Badge name="The Register" backgroundColor="#FF0000" {...props} />

export default TheRegister
