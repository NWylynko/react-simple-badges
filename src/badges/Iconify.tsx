
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Iconify = (props: BadgeProps) => <Badge name="Iconify" {...props} backgroundColor="#1769AA" />

export default Iconify
