
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Iconify = (props: BadgeProps) => <Badge name="Iconify" backgroundColor="#1769AA" {...props} />

export default Iconify
