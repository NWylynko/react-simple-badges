
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Presto = (props: BadgeProps) => <Badge name="Presto" {...props} backgroundColor="#5890FF" />

export default Presto
