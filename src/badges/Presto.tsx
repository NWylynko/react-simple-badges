
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Presto = (props: BadgeProps) => <Badge name="Presto" backgroundColor="#5890FF" {...props} />

export default Presto
