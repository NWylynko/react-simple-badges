
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const OpenNebula = (props: BadgeProps) => <Badge name="OpenNebula" {...props} backgroundColor="#0097C2" />

export default OpenNebula
