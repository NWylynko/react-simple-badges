
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MXLinux = (props: BadgeProps) => <Badge name="MX Linux" {...props} backgroundColor="#000000" />

export default MXLinux
