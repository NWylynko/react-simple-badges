
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const MXLinux = (props: BadgeProps) => <Badge name="MX Linux" backgroundColor="#000000" {...props} />

export default MXLinux
