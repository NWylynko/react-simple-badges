
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Bentley = (props: BadgeProps) => <Badge name="Bentley" {...props} backgroundColor="#333333" />

export default Bentley
