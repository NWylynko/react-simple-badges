
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const DSAutomobiles = (props: BadgeProps) => <Badge name="DS Automobiles" {...props} backgroundColor="#1D1717" />

export default DSAutomobiles
