
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Wikipedia = (props: BadgeProps) => <Badge name="Wikipedia" {...props} backgroundColor="#000000" />

export default Wikipedia
