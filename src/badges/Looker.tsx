
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Looker = (props: BadgeProps) => <Badge name="Looker" {...props} backgroundColor="#4285F4" />

export default Looker
