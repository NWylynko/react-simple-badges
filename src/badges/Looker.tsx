
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Looker = (props: BadgeProps) => <Badge name="Looker" backgroundColor="#4285F4" {...props} />

export default Looker
