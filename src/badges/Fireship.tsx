
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Fireship = (props: BadgeProps) => <Badge name="Fireship" {...props} backgroundColor="#EB844E" />

export default Fireship
