
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Volvo = (props: BadgeProps) => <Badge name="Volvo" backgroundColor="#003057" {...props} />

export default Volvo
