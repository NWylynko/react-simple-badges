
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Volvo = (props: BadgeProps) => <Badge name="Volvo" {...props} backgroundColor="#003057" />

export default Volvo
