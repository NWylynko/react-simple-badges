
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GoogleCloud = (props: BadgeProps) => <Badge name="Google Cloud" {...props} backgroundColor="#4285F4" />

export default GoogleCloud
