
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Burton = (props: BadgeProps) => <Badge name="Burton" backgroundColor="#000000" {...props} />

export default Burton
