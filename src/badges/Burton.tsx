
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Burton = (props: BadgeProps) => <Badge name="Burton" {...props} backgroundColor="#000000" />

export default Burton
