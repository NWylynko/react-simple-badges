
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Corsair = (props: BadgeProps) => <Badge name="Corsair" {...props} backgroundColor="#000000" />

export default Corsair
