
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Corsair = (props: BadgeProps) => <Badge name="Corsair" backgroundColor="#000000" {...props} />

export default Corsair
