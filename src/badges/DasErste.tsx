
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const DasErste = (props: BadgeProps) => <Badge name="Das Erste" {...props} backgroundColor="#001A4B" />

export default DasErste
