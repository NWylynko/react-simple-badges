
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Webcomponentsorg = (props: BadgeProps) => <Badge name="webcomponents.org" {...props} backgroundColor="#29ABE2" />

export default Webcomponentsorg
