
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Webcomponentsorg = (props: BadgeProps) => <Badge name="webcomponents.org" backgroundColor="#29ABE2" {...props} />

export default Webcomponentsorg
