
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ZDF = (props: BadgeProps) => <Badge name="ZDF" {...props} backgroundColor="#FA7D19" />

export default ZDF
