
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const ZDF = (props: BadgeProps) => <Badge name="ZDF" backgroundColor="#FA7D19" {...props} />

export default ZDF
