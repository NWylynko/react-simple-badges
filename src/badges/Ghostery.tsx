
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Ghostery = (props: BadgeProps) => <Badge name="Ghostery" {...props} backgroundColor="#00AEF0" />

export default Ghostery
