
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ChatBot = (props: BadgeProps) => <Badge name="ChatBot" {...props} backgroundColor="#FFD000" />

export default ChatBot
