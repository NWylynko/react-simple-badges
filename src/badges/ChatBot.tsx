
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const ChatBot = (props: BadgeProps) => <Badge name="ChatBot" backgroundColor="#FFD000" {...props} />

export default ChatBot
