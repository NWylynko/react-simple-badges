
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const SaltProject = (props: BadgeProps) => <Badge name="Salt Project" backgroundColor="#57BCAD" {...props} />

export default SaltProject
