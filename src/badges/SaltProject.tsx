
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SaltProject = (props: BadgeProps) => <Badge name="Salt Project" {...props} backgroundColor="#57BCAD" />

export default SaltProject
