
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GSK = (props: BadgeProps) => <Badge name="GSK" {...props} backgroundColor="#F36633" />

export default GSK
