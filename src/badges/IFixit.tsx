
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const IFixit = (props: BadgeProps) => <Badge name="iFixit" {...props} backgroundColor="#0071CE" />

export default IFixit
