
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const HumbleBundle = (props: BadgeProps) => <Badge name="Humble Bundle" {...props} backgroundColor="#CC2929" />

export default HumbleBundle
