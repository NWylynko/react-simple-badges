
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const HumbleBundle = (props: BadgeProps) => <Badge name="Humble Bundle" backgroundColor="#CC2929" {...props} />

export default HumbleBundle
