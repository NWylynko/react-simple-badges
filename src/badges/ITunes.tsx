
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ITunes = (props: BadgeProps) => <Badge name="iTunes" {...props} backgroundColor="#FB5BC5" />

export default ITunes
