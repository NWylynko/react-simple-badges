
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Egghead = (props: BadgeProps) => <Badge name="egghead" {...props} backgroundColor="#FCFBFA" />

export default Egghead
