
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Glitch = (props: BadgeProps) => <Badge name="Glitch" backgroundColor="#3333FF" {...props} />

export default Glitch
