
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Glitch = (props: BadgeProps) => <Badge name="Glitch" {...props} backgroundColor="#3333FF" />

export default Glitch
