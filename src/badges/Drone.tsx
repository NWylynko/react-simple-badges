
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Drone = (props: BadgeProps) => <Badge name="Drone" {...props} backgroundColor="#212121" />

export default Drone
