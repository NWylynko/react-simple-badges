
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Drone = (props: BadgeProps) => <Badge name="Drone" backgroundColor="#212121" {...props} />

export default Drone
