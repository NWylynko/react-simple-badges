
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Puppet = (props: BadgeProps) => <Badge name="Puppet" backgroundColor="#FFAE1A" {...props} />

export default Puppet
