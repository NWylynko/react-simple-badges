
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Puppet = (props: BadgeProps) => <Badge name="Puppet" {...props} backgroundColor="#FFAE1A" />

export default Puppet
