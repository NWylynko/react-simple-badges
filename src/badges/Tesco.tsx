
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Tesco = (props: BadgeProps) => <Badge name="Tesco" {...props} backgroundColor="#00539F" />

export default Tesco
