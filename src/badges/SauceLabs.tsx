
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SauceLabs = (props: BadgeProps) => <Badge name="Sauce Labs" {...props} backgroundColor="#E2231A" />

export default SauceLabs
