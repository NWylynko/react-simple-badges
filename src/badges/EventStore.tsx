
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const EventStore = (props: BadgeProps) => <Badge name="Event Store" {...props} backgroundColor="#5AB552" />

export default EventStore
