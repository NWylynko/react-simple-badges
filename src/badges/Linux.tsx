
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Linux = (props: BadgeProps) => <Badge name="Linux" {...props} backgroundColor="#FCC624" />

export default Linux
