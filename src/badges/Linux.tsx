
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Linux = (props: BadgeProps) => <Badge name="Linux" backgroundColor="#FCC624" {...props} />

export default Linux
