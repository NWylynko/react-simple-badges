
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Paperspace = (props: BadgeProps) => <Badge name="Paperspace" {...props} backgroundColor="#000000" />

export default Paperspace
