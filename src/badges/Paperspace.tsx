
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Paperspace = (props: BadgeProps) => <Badge name="Paperspace" backgroundColor="#000000" {...props} />

export default Paperspace
