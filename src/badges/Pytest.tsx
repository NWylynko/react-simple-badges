
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Pytest = (props: BadgeProps) => <Badge name="Pytest" backgroundColor="#0A9EDC" {...props} />

export default Pytest
