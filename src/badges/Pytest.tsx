
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Pytest = (props: BadgeProps) => <Badge name="Pytest" {...props} backgroundColor="#0A9EDC" />

export default Pytest
