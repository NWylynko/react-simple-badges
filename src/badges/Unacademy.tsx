
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Unacademy = (props: BadgeProps) => <Badge name="Unacademy" {...props} backgroundColor="#08BD80" />

export default Unacademy
