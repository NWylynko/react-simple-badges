
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Unacademy = (props: BadgeProps) => <Badge name="Unacademy" backgroundColor="#08BD80" {...props} />

export default Unacademy
