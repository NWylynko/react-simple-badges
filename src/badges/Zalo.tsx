
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Zalo = (props: BadgeProps) => <Badge name="Zalo" {...props} backgroundColor="#0068FF" />

export default Zalo
