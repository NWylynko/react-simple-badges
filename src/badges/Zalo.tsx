
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Zalo = (props: BadgeProps) => <Badge name="Zalo" backgroundColor="#0068FF" {...props} />

export default Zalo
