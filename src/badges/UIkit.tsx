
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const UIkit = (props: BadgeProps) => <Badge name="UIkit" {...props} backgroundColor="#2396F3" />

export default UIkit
