
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const UIkit = (props: BadgeProps) => <Badge name="UIkit" backgroundColor="#2396F3" {...props} />

export default UIkit
