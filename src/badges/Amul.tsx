
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Amul = (props: BadgeProps) => <Badge name="Amul" backgroundColor="#ED1D24" {...props} />

export default Amul
