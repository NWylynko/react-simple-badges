
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Amul = (props: BadgeProps) => <Badge name="Amul" {...props} backgroundColor="#ED1D24" />

export default Amul
