
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Cisco = (props: BadgeProps) => <Badge name="Cisco" {...props} backgroundColor="#1BA0D7" />

export default Cisco
