
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Cisco = (props: BadgeProps) => <Badge name="Cisco" backgroundColor="#1BA0D7" {...props} />

export default Cisco
