
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Shopware = (props: BadgeProps) => <Badge name="Shopware" {...props} backgroundColor="#189EFF" />

export default Shopware
