
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const BandLab = (props: BadgeProps) => <Badge name="BandLab" {...props} backgroundColor="#F12C18" />

export default BandLab
