
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Pinterest = (props: BadgeProps) => <Badge name="Pinterest" {...props} backgroundColor="#BD081C" />

export default Pinterest
