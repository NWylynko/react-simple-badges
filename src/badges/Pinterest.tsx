
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Pinterest = (props: BadgeProps) => <Badge name="Pinterest" backgroundColor="#BD081C" {...props} />

export default Pinterest
