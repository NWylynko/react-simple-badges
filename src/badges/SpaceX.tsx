
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const SpaceX = (props: BadgeProps) => <Badge name="SpaceX" backgroundColor="#000000" {...props} />

export default SpaceX
