
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SpaceX = (props: BadgeProps) => <Badge name="SpaceX" {...props} backgroundColor="#000000" />

export default SpaceX
