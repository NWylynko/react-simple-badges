
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Formstack = (props: BadgeProps) => <Badge name="Formstack" {...props} backgroundColor="#21B573" />

export default Formstack
