
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Marketo = (props: BadgeProps) => <Badge name="Marketo" {...props} backgroundColor="#5C4C9F" />

export default Marketo
