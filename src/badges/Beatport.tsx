
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Beatport = (props: BadgeProps) => <Badge name="Beatport" backgroundColor="#01FF95" {...props} />

export default Beatport
