
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Beatport = (props: BadgeProps) => <Badge name="Beatport" {...props} backgroundColor="#01FF95" />

export default Beatport
