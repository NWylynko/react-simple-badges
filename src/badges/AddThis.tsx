
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AddThis = (props: BadgeProps) => <Badge name="AddThis" {...props} backgroundColor="#FF6550" />

export default AddThis
