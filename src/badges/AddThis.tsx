
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const AddThis = (props: BadgeProps) => <Badge name="AddThis" backgroundColor="#FF6550" {...props} />

export default AddThis
