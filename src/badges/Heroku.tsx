
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Heroku = (props: BadgeProps) => <Badge name="Heroku" {...props} backgroundColor="#430098" />

export default Heroku
