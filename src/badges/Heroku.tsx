
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Heroku = (props: BadgeProps) => <Badge name="Heroku" backgroundColor="#430098" {...props} />

export default Heroku
