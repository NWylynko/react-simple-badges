
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Algorand = (props: BadgeProps) => <Badge name="Algorand" backgroundColor="#000000" {...props} />

export default Algorand
