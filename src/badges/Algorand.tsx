
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Algorand = (props: BadgeProps) => <Badge name="Algorand" {...props} backgroundColor="#000000" />

export default Algorand
