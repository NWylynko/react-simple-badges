
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Wacom = (props: BadgeProps) => <Badge name="Wacom" backgroundColor="#000000" {...props} />

export default Wacom
