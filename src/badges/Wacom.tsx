
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Wacom = (props: BadgeProps) => <Badge name="Wacom" {...props} backgroundColor="#000000" />

export default Wacom
