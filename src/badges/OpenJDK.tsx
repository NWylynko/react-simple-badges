
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const OpenJDK = (props: BadgeProps) => <Badge name="OpenJDK" backgroundColor="#FFFFFF" {...props} />

export default OpenJDK
