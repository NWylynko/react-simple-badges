
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const OpenJDK = (props: BadgeProps) => <Badge name="OpenJDK" {...props} backgroundColor="#FFFFFF" />

export default OpenJDK
