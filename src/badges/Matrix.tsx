
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Matrix = (props: BadgeProps) => <Badge name="Matrix" {...props} backgroundColor="#000000" />

export default Matrix
