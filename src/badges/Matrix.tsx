
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Matrix = (props: BadgeProps) => <Badge name="Matrix" backgroundColor="#000000" {...props} />

export default Matrix
