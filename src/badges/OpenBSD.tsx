
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const OpenBSD = (props: BadgeProps) => <Badge name="OpenBSD" {...props} backgroundColor="#F2CA30" />

export default OpenBSD
