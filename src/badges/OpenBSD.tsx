
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const OpenBSD = (props: BadgeProps) => <Badge name="OpenBSD" backgroundColor="#F2CA30" {...props} />

export default OpenBSD
