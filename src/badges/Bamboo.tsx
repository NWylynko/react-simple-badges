
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Bamboo = (props: BadgeProps) => <Badge name="Bamboo" backgroundColor="#0052CC" {...props} />

export default Bamboo
