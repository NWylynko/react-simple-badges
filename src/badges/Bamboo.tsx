
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Bamboo = (props: BadgeProps) => <Badge name="Bamboo" {...props} backgroundColor="#0052CC" />

export default Bamboo
