
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const YouTubeGaming = (props: BadgeProps) => <Badge name="YouTube Gaming" {...props} backgroundColor="#FF0000" />

export default YouTubeGaming
