
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Ludwig = (props: BadgeProps) => <Badge name="Ludwig" {...props} backgroundColor="#FFFFFF" />

export default Ludwig
