
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Ludwig = (props: BadgeProps) => <Badge name="Ludwig" backgroundColor="#FFFFFF" {...props} />

export default Ludwig
