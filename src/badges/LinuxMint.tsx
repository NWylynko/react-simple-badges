
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const LinuxMint = (props: BadgeProps) => <Badge name="Linux Mint" backgroundColor="#87CF3E" {...props} />

export default LinuxMint
