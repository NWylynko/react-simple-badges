
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const LinuxMint = (props: BadgeProps) => <Badge name="Linux Mint" {...props} backgroundColor="#87CF3E" />

export default LinuxMint
