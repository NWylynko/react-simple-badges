
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const FastAPI = (props: BadgeProps) => <Badge name="FastAPI" backgroundColor="#009688" {...props} />

export default FastAPI
