
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const FastAPI = (props: BadgeProps) => <Badge name="FastAPI" {...props} backgroundColor="#009688" />

export default FastAPI
