
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Zalando = (props: BadgeProps) => <Badge name="Zalando" backgroundColor="#FF6900" {...props} />

export default Zalando
