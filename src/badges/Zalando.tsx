
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Zalando = (props: BadgeProps) => <Badge name="Zalando" {...props} backgroundColor="#FF6900" />

export default Zalando
