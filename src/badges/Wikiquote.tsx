
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Wikiquote = (props: BadgeProps) => <Badge name="Wikiquote" backgroundColor="#006699" {...props} />

export default Wikiquote
