
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Wikiquote = (props: BadgeProps) => <Badge name="Wikiquote" {...props} backgroundColor="#006699" />

export default Wikiquote
