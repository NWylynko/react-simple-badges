
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Arm = (props: BadgeProps) => <Badge name="Arm" {...props} backgroundColor="#0091BD" />

export default Arm
