
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Arm = (props: BadgeProps) => <Badge name="Arm" backgroundColor="#0091BD" {...props} />

export default Arm
