
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Geocaching = (props: BadgeProps) => <Badge name="Geocaching" {...props} backgroundColor="#00874D" />

export default Geocaching
