
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Geocaching = (props: BadgeProps) => <Badge name="Geocaching" backgroundColor="#00874D" {...props} />

export default Geocaching
