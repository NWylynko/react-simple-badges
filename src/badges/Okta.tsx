
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Okta = (props: BadgeProps) => <Badge name="Okta" {...props} backgroundColor="#007DC1" />

export default Okta
