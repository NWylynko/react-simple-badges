
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CodinGame = (props: BadgeProps) => <Badge name="CodinGame" {...props} backgroundColor="#F2BB13" />

export default CodinGame
