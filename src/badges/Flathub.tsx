
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Flathub = (props: BadgeProps) => <Badge name="Flathub" {...props} backgroundColor="#4A86CF" />

export default Flathub
