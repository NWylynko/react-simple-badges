
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Tide = (props: BadgeProps) => <Badge name="Tide" {...props} backgroundColor="#4050FB" />

export default Tide
