
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Tide = (props: BadgeProps) => <Badge name="Tide" backgroundColor="#4050FB" {...props} />

export default Tide
