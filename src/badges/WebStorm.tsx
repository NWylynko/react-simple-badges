
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const WebStorm = (props: BadgeProps) => <Badge name="WebStorm" {...props} backgroundColor="#000000" />

export default WebStorm
