
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const WebStorm = (props: BadgeProps) => <Badge name="WebStorm" backgroundColor="#000000" {...props} />

export default WebStorm
