
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const EyeEm = (props: BadgeProps) => <Badge name="EyeEm" {...props} backgroundColor="#000000" />

export default EyeEm
