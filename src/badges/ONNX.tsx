
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const ONNX = (props: BadgeProps) => <Badge name="ONNX" backgroundColor="#005CED" {...props} />

export default ONNX
