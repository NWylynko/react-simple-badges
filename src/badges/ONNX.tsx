
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ONNX = (props: BadgeProps) => <Badge name="ONNX" {...props} backgroundColor="#005CED" />

export default ONNX
