
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Cognizant = (props: BadgeProps) => <Badge name="Cognizant" backgroundColor="#1A4CA1" {...props} />

export default Cognizant
