
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Cognizant = (props: BadgeProps) => <Badge name="Cognizant" {...props} backgroundColor="#1A4CA1" />

export default Cognizant
