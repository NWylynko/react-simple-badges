
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const DVC = (props: BadgeProps) => <Badge name="DVC" backgroundColor="#13ADC7" {...props} />

export default DVC
