
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const DVC = (props: BadgeProps) => <Badge name="DVC" {...props} backgroundColor="#13ADC7" />

export default DVC
