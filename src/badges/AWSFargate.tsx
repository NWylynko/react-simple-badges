
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AWSFargate = (props: BadgeProps) => <Badge name="AWS Fargate" {...props} backgroundColor="#FF9900" />

export default AWSFargate
