
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const AWSFargate = (props: BadgeProps) => <Badge name="AWS Fargate" backgroundColor="#FF9900" {...props} />

export default AWSFargate
