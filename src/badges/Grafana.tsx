
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Grafana = (props: BadgeProps) => <Badge name="Grafana" backgroundColor="#F46800" {...props} />

export default Grafana
