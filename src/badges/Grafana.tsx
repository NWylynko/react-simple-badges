
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Grafana = (props: BadgeProps) => <Badge name="Grafana" {...props} backgroundColor="#F46800" />

export default Grafana
