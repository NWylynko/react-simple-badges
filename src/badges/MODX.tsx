
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const MODX = (props: BadgeProps) => <Badge name="MODX" backgroundColor="#102C53" {...props} />

export default MODX
