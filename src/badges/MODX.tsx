
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MODX = (props: BadgeProps) => <Badge name="MODX" {...props} backgroundColor="#102C53" />

export default MODX
