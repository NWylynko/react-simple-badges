
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const OnStar = (props: BadgeProps) => <Badge name="OnStar" {...props} backgroundColor="#003D7D" />

export default OnStar
