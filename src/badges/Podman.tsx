
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Podman = (props: BadgeProps) => <Badge name="Podman" backgroundColor="#892CA0" {...props} />

export default Podman
