
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Podman = (props: BadgeProps) => <Badge name="Podman" {...props} backgroundColor="#892CA0" />

export default Podman
