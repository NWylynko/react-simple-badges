
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Slack = (props: BadgeProps) => <Badge name="Slack" backgroundColor="#4A154B" {...props} />

export default Slack
