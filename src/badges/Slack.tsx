
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Slack = (props: BadgeProps) => <Badge name="Slack" {...props} backgroundColor="#4A154B" />

export default Slack
