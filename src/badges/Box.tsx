
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Box = (props: BadgeProps) => <Badge name="Box" {...props} backgroundColor="#0061D5" />

export default Box
