
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Box = (props: BadgeProps) => <Badge name="Box" backgroundColor="#0061D5" {...props} />

export default Box
