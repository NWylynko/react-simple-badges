
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Indeed = (props: BadgeProps) => <Badge name="Indeed" {...props} backgroundColor="#003A9B" />

export default Indeed
