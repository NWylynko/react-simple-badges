
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Indeed = (props: BadgeProps) => <Badge name="Indeed" backgroundColor="#003A9B" {...props} />

export default Indeed
