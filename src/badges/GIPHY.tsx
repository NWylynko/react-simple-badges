
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GIPHY = (props: BadgeProps) => <Badge name="GIPHY" {...props} backgroundColor="#FF6666" />

export default GIPHY
