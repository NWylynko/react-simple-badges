
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Coderwall = (props: BadgeProps) => <Badge name="Coderwall" backgroundColor="#3E8DCC" {...props} />

export default Coderwall
