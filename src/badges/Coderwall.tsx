
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Coderwall = (props: BadgeProps) => <Badge name="Coderwall" {...props} backgroundColor="#3E8DCC" />

export default Coderwall
