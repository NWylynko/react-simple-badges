
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SingleStore = (props: BadgeProps) => <Badge name="SingleStore" {...props} backgroundColor="#AA00FF" />

export default SingleStore
