
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Minds = (props: BadgeProps) => <Badge name="Minds" {...props} backgroundColor="#FED12F" />

export default Minds
