
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Airbyte = (props: BadgeProps) => <Badge name="Airbyte" {...props} backgroundColor="#615EFF" />

export default Airbyte
