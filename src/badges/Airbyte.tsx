
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Airbyte = (props: BadgeProps) => <Badge name="Airbyte" backgroundColor="#615EFF" {...props} />

export default Airbyte
