
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Dgraph = (props: BadgeProps) => <Badge name="Dgraph" {...props} backgroundColor="#E50695" />

export default Dgraph
