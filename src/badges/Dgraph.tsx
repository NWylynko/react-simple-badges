
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Dgraph = (props: BadgeProps) => <Badge name="Dgraph" backgroundColor="#E50695" {...props} />

export default Dgraph
