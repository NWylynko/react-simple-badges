
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Byjus = (props: BadgeProps) => <Badge name="Byju's" {...props} backgroundColor="#813588" />

export default Byjus
