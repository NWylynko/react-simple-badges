
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Bower = (props: BadgeProps) => <Badge name="Bower" backgroundColor="#EF5734" {...props} />

export default Bower
