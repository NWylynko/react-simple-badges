
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Bower = (props: BadgeProps) => <Badge name="Bower" {...props} backgroundColor="#EF5734" />

export default Bower
