
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const StubHub = (props: BadgeProps) => <Badge name="StubHub" {...props} backgroundColor="#003168" />

export default StubHub
