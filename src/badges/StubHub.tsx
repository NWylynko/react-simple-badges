
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const StubHub = (props: BadgeProps) => <Badge name="StubHub" backgroundColor="#003168" {...props} />

export default StubHub
