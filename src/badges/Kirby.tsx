
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Kirby = (props: BadgeProps) => <Badge name="Kirby" {...props} backgroundColor="#000000" />

export default Kirby
