
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Kirby = (props: BadgeProps) => <Badge name="Kirby" backgroundColor="#000000" {...props} />

export default Kirby
