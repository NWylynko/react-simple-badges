
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Norco = (props: BadgeProps) => <Badge name="Norco" {...props} backgroundColor="#00FF00" />

export default Norco
