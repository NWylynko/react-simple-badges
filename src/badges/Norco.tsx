
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Norco = (props: BadgeProps) => <Badge name="Norco" backgroundColor="#00FF00" {...props} />

export default Norco
