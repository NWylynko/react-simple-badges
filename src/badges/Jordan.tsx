
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Jordan = (props: BadgeProps) => <Badge name="Jordan" {...props} backgroundColor="#000000" />

export default Jordan
