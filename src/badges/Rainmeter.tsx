
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Rainmeter = (props: BadgeProps) => <Badge name="Rainmeter" backgroundColor="#19519B" {...props} />

export default Rainmeter
