
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Rainmeter = (props: BadgeProps) => <Badge name="Rainmeter" {...props} backgroundColor="#19519B" />

export default Rainmeter
