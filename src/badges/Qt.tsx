
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Qt = (props: BadgeProps) => <Badge name="Qt" {...props} backgroundColor="#41CD52" />

export default Qt
