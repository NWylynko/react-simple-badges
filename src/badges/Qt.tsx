
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Qt = (props: BadgeProps) => <Badge name="Qt" backgroundColor="#41CD52" {...props} />

export default Qt
