
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GoogleChat = (props: BadgeProps) => <Badge name="Google Chat" {...props} backgroundColor="#00AC47" />

export default GoogleChat
