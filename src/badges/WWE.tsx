
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const WWE = (props: BadgeProps) => <Badge name="WWE" {...props} backgroundColor="#000000" />

export default WWE
