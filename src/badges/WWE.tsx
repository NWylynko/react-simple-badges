
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const WWE = (props: BadgeProps) => <Badge name="WWE" backgroundColor="#000000" {...props} />

export default WWE
