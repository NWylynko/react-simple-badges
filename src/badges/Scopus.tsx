
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Scopus = (props: BadgeProps) => <Badge name="Scopus" backgroundColor="#E9711C" {...props} />

export default Scopus
