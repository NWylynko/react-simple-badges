
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Scopus = (props: BadgeProps) => <Badge name="Scopus" {...props} backgroundColor="#E9711C" />

export default Scopus
