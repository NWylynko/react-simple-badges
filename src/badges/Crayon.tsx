
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Crayon = (props: BadgeProps) => <Badge name="Crayon" {...props} backgroundColor="#FF6A4C" />

export default Crayon
