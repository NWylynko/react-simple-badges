
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Taobao = (props: BadgeProps) => <Badge name="Taobao" {...props} backgroundColor="#E94F20" />

export default Taobao
