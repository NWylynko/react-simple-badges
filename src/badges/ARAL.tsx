
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ARAL = (props: BadgeProps) => <Badge name="ARAL" {...props} backgroundColor="#0063CB" />

export default ARAL
