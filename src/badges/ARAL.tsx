
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const ARAL = (props: BadgeProps) => <Badge name="ARAL" backgroundColor="#0063CB" {...props} />

export default ARAL
