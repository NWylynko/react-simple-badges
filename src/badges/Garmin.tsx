
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Garmin = (props: BadgeProps) => <Badge name="Garmin" {...props} backgroundColor="#000000" />

export default Garmin
