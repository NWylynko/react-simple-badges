
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Vagrant = (props: BadgeProps) => <Badge name="Vagrant" backgroundColor="#1868F2" {...props} />

export default Vagrant
