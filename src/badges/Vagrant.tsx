
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Vagrant = (props: BadgeProps) => <Badge name="Vagrant" {...props} backgroundColor="#1868F2" />

export default Vagrant
