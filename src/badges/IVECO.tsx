
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const IVECO = (props: BadgeProps) => <Badge name="IVECO" backgroundColor="#004994" {...props} />

export default IVECO
