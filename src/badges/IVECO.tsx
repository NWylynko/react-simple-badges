
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const IVECO = (props: BadgeProps) => <Badge name="IVECO" {...props} backgroundColor="#004994" />

export default IVECO
