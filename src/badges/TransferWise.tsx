
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TransferWise = (props: BadgeProps) => <Badge name="TransferWise" {...props} backgroundColor="#00B9FF" />

export default TransferWise
