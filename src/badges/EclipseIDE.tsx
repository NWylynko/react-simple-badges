
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const EclipseIDE = (props: BadgeProps) => <Badge name="Eclipse IDE" {...props} backgroundColor="#2C2255" />

export default EclipseIDE
