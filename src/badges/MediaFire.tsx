
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const MediaFire = (props: BadgeProps) => <Badge name="MediaFire" backgroundColor="#1299F3" {...props} />

export default MediaFire
