
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Wix = (props: BadgeProps) => <Badge name="Wix" {...props} backgroundColor="#0C6EFC" />

export default Wix
