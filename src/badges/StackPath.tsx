
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const StackPath = (props: BadgeProps) => <Badge name="StackPath" {...props} backgroundColor="#000000" />

export default StackPath
