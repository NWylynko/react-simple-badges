
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const StackPath = (props: BadgeProps) => <Badge name="StackPath" backgroundColor="#000000" {...props} />

export default StackPath
