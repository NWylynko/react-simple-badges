
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Myspace = (props: BadgeProps) => <Badge name="Myspace" {...props} backgroundColor="#030303" />

export default Myspace
