
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Rezgo = (props: BadgeProps) => <Badge name="Rezgo" {...props} backgroundColor="#F76C00" />

export default Rezgo
