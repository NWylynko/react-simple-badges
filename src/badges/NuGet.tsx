
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const NuGet = (props: BadgeProps) => <Badge name="NuGet" {...props} backgroundColor="#004880" />

export default NuGet
