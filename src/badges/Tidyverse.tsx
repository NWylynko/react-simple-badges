
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Tidyverse = (props: BadgeProps) => <Badge name="Tidyverse" backgroundColor="#1A162D" {...props} />

export default Tidyverse
