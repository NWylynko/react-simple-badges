
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Tidyverse = (props: BadgeProps) => <Badge name="Tidyverse" {...props} backgroundColor="#1A162D" />

export default Tidyverse
