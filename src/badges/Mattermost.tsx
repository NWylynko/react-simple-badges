
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Mattermost = (props: BadgeProps) => <Badge name="Mattermost" backgroundColor="#0058CC" {...props} />

export default Mattermost
