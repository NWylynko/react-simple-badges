
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Mattermost = (props: BadgeProps) => <Badge name="Mattermost" {...props} backgroundColor="#0058CC" />

export default Mattermost
