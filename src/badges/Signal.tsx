
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Signal = (props: BadgeProps) => <Badge name="Signal" {...props} backgroundColor="#3A76F0" />

export default Signal
