
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Signal = (props: BadgeProps) => <Badge name="Signal" backgroundColor="#3A76F0" {...props} />

export default Signal
