
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Temporal = (props: BadgeProps) => <Badge name="Temporal" {...props} backgroundColor="#000000" />

export default Temporal
