
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Temporal = (props: BadgeProps) => <Badge name="Temporal" backgroundColor="#000000" {...props} />

export default Temporal
