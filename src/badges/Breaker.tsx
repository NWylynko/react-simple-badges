
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Breaker = (props: BadgeProps) => <Badge name="Breaker" {...props} backgroundColor="#003DAD" />

export default Breaker
