
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Google = (props: BadgeProps) => <Badge name="Google" backgroundColor="#4285F4" {...props} />

export default Google
