
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Google = (props: BadgeProps) => <Badge name="Google" {...props} backgroundColor="#4285F4" />

export default Google
