
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Laravel = (props: BadgeProps) => <Badge name="Laravel" {...props} backgroundColor="#FF2D20" />

export default Laravel
