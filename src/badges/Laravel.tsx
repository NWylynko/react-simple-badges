
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Laravel = (props: BadgeProps) => <Badge name="Laravel" backgroundColor="#FF2D20" {...props} />

export default Laravel
