
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Esbuild = (props: BadgeProps) => <Badge name="esbuild" {...props} backgroundColor="#FFCF00" />

export default Esbuild
