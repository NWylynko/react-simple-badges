
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Esbuild = (props: BadgeProps) => <Badge name="esbuild" backgroundColor="#FFCF00" {...props} />

export default Esbuild
