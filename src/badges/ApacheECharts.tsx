
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ApacheECharts = (props: BadgeProps) => <Badge name="Apache ECharts" {...props} backgroundColor="#AA344D" />

export default ApacheECharts
