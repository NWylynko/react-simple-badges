
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const LINE = (props: BadgeProps) => <Badge name="LINE" backgroundColor="#00C300" {...props} />

export default LINE
