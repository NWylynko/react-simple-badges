
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const LINE = (props: BadgeProps) => <Badge name="LINE" {...props} backgroundColor="#00C300" />

export default LINE
