
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const GoogleLens = (props: BadgeProps) => <Badge name="Google Lens" backgroundColor="#4285F4" {...props} />

export default GoogleLens
