
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GoogleLens = (props: BadgeProps) => <Badge name="Google Lens" {...props} backgroundColor="#4285F4" />

export default GoogleLens
