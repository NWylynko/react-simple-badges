
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const AppSignal = (props: BadgeProps) => <Badge name="AppSignal" backgroundColor="#21375A" {...props} />

export default AppSignal
