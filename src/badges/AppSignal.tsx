
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AppSignal = (props: BadgeProps) => <Badge name="AppSignal" {...props} backgroundColor="#21375A" />

export default AppSignal
