
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const McLaren = (props: BadgeProps) => <Badge name="McLaren" {...props} backgroundColor="#FF0000" />

export default McLaren
