
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const McLaren = (props: BadgeProps) => <Badge name="McLaren" backgroundColor="#FF0000" {...props} />

export default McLaren
