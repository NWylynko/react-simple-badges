
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Wattpad = (props: BadgeProps) => <Badge name="Wattpad" {...props} backgroundColor="#FF500A" />

export default Wattpad
