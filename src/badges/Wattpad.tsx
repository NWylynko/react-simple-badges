
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Wattpad = (props: BadgeProps) => <Badge name="Wattpad" backgroundColor="#FF500A" {...props} />

export default Wattpad
