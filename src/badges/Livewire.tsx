
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Livewire = (props: BadgeProps) => <Badge name="Livewire" {...props} backgroundColor="#4E56A6" />

export default Livewire
