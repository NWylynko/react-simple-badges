
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Livewire = (props: BadgeProps) => <Badge name="Livewire" backgroundColor="#4E56A6" {...props} />

export default Livewire
