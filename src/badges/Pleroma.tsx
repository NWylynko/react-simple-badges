
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Pleroma = (props: BadgeProps) => <Badge name="Pleroma" {...props} backgroundColor="#FBA457" />

export default Pleroma
