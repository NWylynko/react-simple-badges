
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Pleroma = (props: BadgeProps) => <Badge name="Pleroma" backgroundColor="#FBA457" {...props} />

export default Pleroma
