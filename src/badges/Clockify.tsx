
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Clockify = (props: BadgeProps) => <Badge name="Clockify" backgroundColor="#03A9F4" {...props} />

export default Clockify
