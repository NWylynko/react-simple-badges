
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Clockify = (props: BadgeProps) => <Badge name="Clockify" {...props} backgroundColor="#03A9F4" />

export default Clockify
