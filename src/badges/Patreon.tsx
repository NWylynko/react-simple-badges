
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Patreon = (props: BadgeProps) => <Badge name="Patreon" {...props} backgroundColor="#FF424D" />

export default Patreon
