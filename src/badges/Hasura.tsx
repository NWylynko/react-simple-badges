
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Hasura = (props: BadgeProps) => <Badge name="Hasura" {...props} backgroundColor="#1EB4D4" />

export default Hasura
