
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Hasura = (props: BadgeProps) => <Badge name="Hasura" backgroundColor="#1EB4D4" {...props} />

export default Hasura
