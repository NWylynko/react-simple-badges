
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const WhenIWork = (props: BadgeProps) => <Badge name="When I Work" {...props} backgroundColor="#51A33D" />

export default WhenIWork
