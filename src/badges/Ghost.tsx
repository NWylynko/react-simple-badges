
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Ghost = (props: BadgeProps) => <Badge name="Ghost" backgroundColor="#15171A" {...props} />

export default Ghost
