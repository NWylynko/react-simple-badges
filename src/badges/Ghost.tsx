
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Ghost = (props: BadgeProps) => <Badge name="Ghost" {...props} backgroundColor="#15171A" />

export default Ghost
