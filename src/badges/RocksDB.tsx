
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const RocksDB = (props: BadgeProps) => <Badge name="RocksDB" {...props} backgroundColor="#2A2A2A" />

export default RocksDB
