
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Socketio = (props: BadgeProps) => <Badge name="Socket.io" {...props} backgroundColor="#010101" />

export default Socketio
