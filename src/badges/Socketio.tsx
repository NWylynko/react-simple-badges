
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Socketio = (props: BadgeProps) => <Badge name="Socket.io" backgroundColor="#010101" {...props} />

export default Socketio
