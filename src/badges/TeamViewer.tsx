
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TeamViewer = (props: BadgeProps) => <Badge name="TeamViewer" {...props} backgroundColor="#004680" />

export default TeamViewer
