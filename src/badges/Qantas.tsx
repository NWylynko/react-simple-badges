
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Qantas = (props: BadgeProps) => <Badge name="Qantas" {...props} backgroundColor="#E40000" />

export default Qantas
