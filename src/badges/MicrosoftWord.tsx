
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MicrosoftWord = (props: BadgeProps) => <Badge name="Microsoft Word" {...props} backgroundColor="#2B579A" />

export default MicrosoftWord
