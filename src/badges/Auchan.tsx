
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Auchan = (props: BadgeProps) => <Badge name="Auchan" backgroundColor="#D6180B" {...props} />

export default Auchan
