
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Auchan = (props: BadgeProps) => <Badge name="Auchan" {...props} backgroundColor="#D6180B" />

export default Auchan
