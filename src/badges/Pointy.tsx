
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Pointy = (props: BadgeProps) => <Badge name="Pointy" backgroundColor="#009DE0" {...props} />

export default Pointy
