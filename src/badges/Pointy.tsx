
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Pointy = (props: BadgeProps) => <Badge name="Pointy" {...props} backgroundColor="#009DE0" />

export default Pointy
