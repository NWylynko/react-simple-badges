
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Songkick = (props: BadgeProps) => <Badge name="Songkick" {...props} backgroundColor="#F80046" />

export default Songkick
