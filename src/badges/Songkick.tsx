
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Songkick = (props: BadgeProps) => <Badge name="Songkick" backgroundColor="#F80046" {...props} />

export default Songkick
