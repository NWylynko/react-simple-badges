
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SourceEngine = (props: BadgeProps) => <Badge name="Source Engine" {...props} backgroundColor="#F79A10" />

export default SourceEngine
