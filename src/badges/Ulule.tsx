
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Ulule = (props: BadgeProps) => <Badge name="Ulule" {...props} backgroundColor="#18A5D6" />

export default Ulule
