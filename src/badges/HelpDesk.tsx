
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const HelpDesk = (props: BadgeProps) => <Badge name="HelpDesk" {...props} backgroundColor="#FFD000" />

export default HelpDesk
