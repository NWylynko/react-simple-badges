
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const HelpDesk = (props: BadgeProps) => <Badge name="HelpDesk" backgroundColor="#FFD000" {...props} />

export default HelpDesk
