
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MYOB = (props: BadgeProps) => <Badge name="MYOB" {...props} backgroundColor="#6100A5" />

export default MYOB
