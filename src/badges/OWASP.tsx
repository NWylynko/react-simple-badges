
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const OWASP = (props: BadgeProps) => <Badge name="OWASP" {...props} backgroundColor="#000000" />

export default OWASP
