
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const OWASP = (props: BadgeProps) => <Badge name="OWASP" backgroundColor="#000000" {...props} />

export default OWASP
