
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Figma = (props: BadgeProps) => <Badge name="Figma" {...props} backgroundColor="#F24E1E" />

export default Figma
