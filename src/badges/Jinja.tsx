
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Jinja = (props: BadgeProps) => <Badge name="Jinja" {...props} backgroundColor="#B41717" />

export default Jinja
