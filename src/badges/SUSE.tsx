
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const SUSE = (props: BadgeProps) => <Badge name="SUSE" backgroundColor="#0C322C" {...props} />

export default SUSE
