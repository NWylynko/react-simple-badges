
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SUSE = (props: BadgeProps) => <Badge name="SUSE" {...props} backgroundColor="#0C322C" />

export default SUSE
