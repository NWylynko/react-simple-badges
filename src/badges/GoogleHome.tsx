
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const GoogleHome = (props: BadgeProps) => <Badge name="Google Home" backgroundColor="#4285F4" {...props} />

export default GoogleHome
