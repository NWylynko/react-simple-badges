
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GoogleHome = (props: BadgeProps) => <Badge name="Google Home" {...props} backgroundColor="#4285F4" />

export default GoogleHome
