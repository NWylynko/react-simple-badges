
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const RiotGames = (props: BadgeProps) => <Badge name="Riot Games" {...props} backgroundColor="#D32936" />

export default RiotGames
