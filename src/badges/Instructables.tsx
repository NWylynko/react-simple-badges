
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Instructables = (props: BadgeProps) => <Badge name="Instructables" {...props} backgroundColor="#FABF15" />

export default Instructables
