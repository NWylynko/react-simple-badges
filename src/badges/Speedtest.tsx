
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Speedtest = (props: BadgeProps) => <Badge name="Speedtest" {...props} backgroundColor="#141526" />

export default Speedtest
