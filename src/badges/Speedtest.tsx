
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Speedtest = (props: BadgeProps) => <Badge name="Speedtest" backgroundColor="#141526" {...props} />

export default Speedtest
