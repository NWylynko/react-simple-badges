
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Fujitsu = (props: BadgeProps) => <Badge name="Fujitsu" backgroundColor="#FF0000" {...props} />

export default Fujitsu
