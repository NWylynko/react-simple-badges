
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Fujitsu = (props: BadgeProps) => <Badge name="Fujitsu" {...props} backgroundColor="#FF0000" />

export default Fujitsu
