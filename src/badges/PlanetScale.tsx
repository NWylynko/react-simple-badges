
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PlanetScale = (props: BadgeProps) => <Badge name="PlanetScale" {...props} backgroundColor="#000000" />

export default PlanetScale
