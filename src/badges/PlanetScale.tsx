
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const PlanetScale = (props: BadgeProps) => <Badge name="PlanetScale" backgroundColor="#000000" {...props} />

export default PlanetScale
