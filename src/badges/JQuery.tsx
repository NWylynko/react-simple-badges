
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const JQuery = (props: BadgeProps) => <Badge name="jQuery" {...props} backgroundColor="#0769AD" />

export default JQuery
