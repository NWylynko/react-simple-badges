
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const AOL = (props: BadgeProps) => <Badge name="AOL" backgroundColor="#3399FF" {...props} />

export default AOL
