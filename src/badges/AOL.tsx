
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AOL = (props: BadgeProps) => <Badge name="AOL" {...props} backgroundColor="#3399FF" />

export default AOL
