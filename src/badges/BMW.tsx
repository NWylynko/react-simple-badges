
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const BMW = (props: BadgeProps) => <Badge name="BMW" {...props} backgroundColor="#0066B1" />

export default BMW
