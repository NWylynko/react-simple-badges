
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const BMW = (props: BadgeProps) => <Badge name="BMW" backgroundColor="#0066B1" {...props} />

export default BMW
