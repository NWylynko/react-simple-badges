
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const FMOD = (props: BadgeProps) => <Badge name="FMOD" {...props} backgroundColor="#000000" />

export default FMOD
