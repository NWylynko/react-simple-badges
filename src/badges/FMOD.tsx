
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const FMOD = (props: BadgeProps) => <Badge name="FMOD" backgroundColor="#000000" {...props} />

export default FMOD
