
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const VisualStudio = (props: BadgeProps) => <Badge name="Visual Studio" {...props} backgroundColor="#5C2D91" />

export default VisualStudio
