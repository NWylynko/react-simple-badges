
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Pocket = (props: BadgeProps) => <Badge name="Pocket" {...props} backgroundColor="#EF3F56" />

export default Pocket
