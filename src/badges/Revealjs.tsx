
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Revealjs = (props: BadgeProps) => <Badge name="reveal.js" {...props} backgroundColor="#F2E142" />

export default Revealjs
