
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Revealjs = (props: BadgeProps) => <Badge name="reveal.js" backgroundColor="#F2E142" {...props} />

export default Revealjs
