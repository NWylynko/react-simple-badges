
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Fitbit = (props: BadgeProps) => <Badge name="Fitbit" {...props} backgroundColor="#00B0B9" />

export default Fitbit
