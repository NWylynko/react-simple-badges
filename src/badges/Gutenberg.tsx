
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Gutenberg = (props: BadgeProps) => <Badge name="Gutenberg" {...props} backgroundColor="#000000" />

export default Gutenberg
