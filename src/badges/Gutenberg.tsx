
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Gutenberg = (props: BadgeProps) => <Badge name="Gutenberg" backgroundColor="#000000" {...props} />

export default Gutenberg
