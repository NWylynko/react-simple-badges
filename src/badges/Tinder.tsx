
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Tinder = (props: BadgeProps) => <Badge name="Tinder" backgroundColor="#FF6B6B" {...props} />

export default Tinder
