
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Tinder = (props: BadgeProps) => <Badge name="Tinder" {...props} backgroundColor="#FF6B6B" />

export default Tinder
