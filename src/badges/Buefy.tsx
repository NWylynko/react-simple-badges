
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Buefy = (props: BadgeProps) => <Badge name="Buefy" {...props} backgroundColor="#7957D5" />

export default Buefy
