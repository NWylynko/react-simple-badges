
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const CNN = (props: BadgeProps) => <Badge name="CNN" backgroundColor="#CC0000" {...props} />

export default CNN
