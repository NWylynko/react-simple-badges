
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CNN = (props: BadgeProps) => <Badge name="CNN" {...props} backgroundColor="#CC0000" />

export default CNN
