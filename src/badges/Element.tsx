
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Element = (props: BadgeProps) => <Badge name="Element" {...props} backgroundColor="#0DBD8B" />

export default Element
