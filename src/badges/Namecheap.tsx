
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Namecheap = (props: BadgeProps) => <Badge name="Namecheap" backgroundColor="#DE3723" {...props} />

export default Namecheap
