
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Namecheap = (props: BadgeProps) => <Badge name="Namecheap" {...props} backgroundColor="#DE3723" />

export default Namecheap
