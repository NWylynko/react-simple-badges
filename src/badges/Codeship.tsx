
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Codeship = (props: BadgeProps) => <Badge name="Codeship" backgroundColor="#004466" {...props} />

export default Codeship
