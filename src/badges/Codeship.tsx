
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Codeship = (props: BadgeProps) => <Badge name="Codeship" {...props} backgroundColor="#004466" />

export default Codeship
