
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const JiraSoftware = (props: BadgeProps) => <Badge name="Jira Software" {...props} backgroundColor="#0052CC" />

export default JiraSoftware
