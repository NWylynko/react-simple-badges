
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CounterStrike = (props: BadgeProps) => <Badge name="Counter-Strike" {...props} backgroundColor="#000000" />

export default CounterStrike
