
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Vectorworks = (props: BadgeProps) => <Badge name="Vectorworks" backgroundColor="#000000" {...props} />

export default Vectorworks
