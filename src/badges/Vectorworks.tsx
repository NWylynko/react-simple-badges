
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Vectorworks = (props: BadgeProps) => <Badge name="Vectorworks" {...props} backgroundColor="#000000" />

export default Vectorworks
