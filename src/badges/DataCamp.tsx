
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const DataCamp = (props: BadgeProps) => <Badge name="DataCamp" {...props} backgroundColor="#03EF62" />

export default DataCamp
