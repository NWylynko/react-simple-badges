
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const DataCamp = (props: BadgeProps) => <Badge name="DataCamp" backgroundColor="#03EF62" {...props} />

export default DataCamp
