
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Trustpilot = (props: BadgeProps) => <Badge name="Trustpilot" {...props} backgroundColor="#00B67A" />

export default Trustpilot
