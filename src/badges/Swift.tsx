
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Swift = (props: BadgeProps) => <Badge name="Swift" {...props} backgroundColor="#F05138" />

export default Swift
