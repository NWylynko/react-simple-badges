
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Swift = (props: BadgeProps) => <Badge name="Swift" backgroundColor="#F05138" {...props} />

export default Swift
