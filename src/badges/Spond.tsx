
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Spond = (props: BadgeProps) => <Badge name="Spond" backgroundColor="#EE4353" {...props} />

export default Spond
