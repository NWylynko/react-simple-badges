
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Spond = (props: BadgeProps) => <Badge name="Spond" {...props} backgroundColor="#EE4353" />

export default Spond
