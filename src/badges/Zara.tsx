
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Zara = (props: BadgeProps) => <Badge name="Zara" {...props} backgroundColor="#000000" />

export default Zara
