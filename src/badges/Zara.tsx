
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Zara = (props: BadgeProps) => <Badge name="Zara" backgroundColor="#000000" {...props} />

export default Zara
