
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const JSS = (props: BadgeProps) => <Badge name="JSS" {...props} backgroundColor="#F7DF1E" />

export default JSS
