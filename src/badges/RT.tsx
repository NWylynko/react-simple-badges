
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const RT = (props: BadgeProps) => <Badge name="RTÉ" backgroundColor="#00A7B3" {...props} />

export default RT
