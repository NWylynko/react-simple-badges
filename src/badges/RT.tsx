
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const RT = (props: BadgeProps) => <Badge name="RTÉ" {...props} backgroundColor="#00A7B3" />

export default RT
