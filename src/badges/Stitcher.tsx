
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Stitcher = (props: BadgeProps) => <Badge name="Stitcher" backgroundColor="#000000" {...props} />

export default Stitcher
