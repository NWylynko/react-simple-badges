
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Guilded = (props: BadgeProps) => <Badge name="Guilded" {...props} backgroundColor="#F5C400" />

export default Guilded
