
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Guilded = (props: BadgeProps) => <Badge name="Guilded" backgroundColor="#F5C400" {...props} />

export default Guilded
