
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Gitea = (props: BadgeProps) => <Badge name="Gitea" {...props} backgroundColor="#609926" />

export default Gitea
