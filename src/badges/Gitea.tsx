
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Gitea = (props: BadgeProps) => <Badge name="Gitea" backgroundColor="#609926" {...props} />

export default Gitea
