
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Codecov = (props: BadgeProps) => <Badge name="Codecov" backgroundColor="#F01F7A" {...props} />

export default Codecov
