
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Codecov = (props: BadgeProps) => <Badge name="Codecov" {...props} backgroundColor="#F01F7A" />

export default Codecov
