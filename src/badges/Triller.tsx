
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Triller = (props: BadgeProps) => <Badge name="Triller" {...props} backgroundColor="#FF0089" />

export default Triller
