
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Triller = (props: BadgeProps) => <Badge name="Triller" backgroundColor="#FF0089" {...props} />

export default Triller
