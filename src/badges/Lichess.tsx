
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Lichess = (props: BadgeProps) => <Badge name="Lichess" {...props} backgroundColor="#000000" />

export default Lichess
