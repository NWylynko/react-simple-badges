
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Lichess = (props: BadgeProps) => <Badge name="Lichess" backgroundColor="#000000" {...props} />

export default Lichess
