
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MacPaw = (props: BadgeProps) => <Badge name="MacPaw" {...props} backgroundColor="#000000" />

export default MacPaw
