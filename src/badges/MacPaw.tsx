
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const MacPaw = (props: BadgeProps) => <Badge name="MacPaw" backgroundColor="#000000" {...props} />

export default MacPaw
