
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const UFC = (props: BadgeProps) => <Badge name="UFC" {...props} backgroundColor="#D20A0A" />

export default UFC
