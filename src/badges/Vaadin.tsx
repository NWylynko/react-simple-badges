
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Vaadin = (props: BadgeProps) => <Badge name="Vaadin" {...props} backgroundColor="#00B4F0" />

export default Vaadin
