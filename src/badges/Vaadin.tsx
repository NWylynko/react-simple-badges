
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Vaadin = (props: BadgeProps) => <Badge name="Vaadin" backgroundColor="#00B4F0" {...props} />

export default Vaadin
