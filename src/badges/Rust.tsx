
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Rust = (props: BadgeProps) => <Badge name="Rust" {...props} backgroundColor="#000000" />

export default Rust
