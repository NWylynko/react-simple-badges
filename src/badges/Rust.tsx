
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Rust = (props: BadgeProps) => <Badge name="Rust" backgroundColor="#000000" {...props} />

export default Rust
