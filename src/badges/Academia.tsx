
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Academia = (props: BadgeProps) => <Badge name="Academia" {...props} backgroundColor="#41454A" />

export default Academia
