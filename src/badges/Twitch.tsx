
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Twitch = (props: BadgeProps) => <Badge name="Twitch" {...props} backgroundColor="#9146FF" />

export default Twitch
