
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Twitch = (props: BadgeProps) => <Badge name="Twitch" backgroundColor="#9146FF" {...props} />

export default Twitch
