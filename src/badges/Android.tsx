
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Android = (props: BadgeProps) => <Badge name="Android" {...props} backgroundColor="#3DDC84" />

export default Android
