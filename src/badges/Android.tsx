
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Android = (props: BadgeProps) => <Badge name="Android" backgroundColor="#3DDC84" {...props} />

export default Android
