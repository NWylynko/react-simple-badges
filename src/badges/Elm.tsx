
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Elm = (props: BadgeProps) => <Badge name="Elm" backgroundColor="#1293D8" {...props} />

export default Elm
