
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Elm = (props: BadgeProps) => <Badge name="Elm" {...props} backgroundColor="#1293D8" />

export default Elm
