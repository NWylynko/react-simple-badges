
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Harbor = (props: BadgeProps) => <Badge name="Harbor" backgroundColor="#60B932" {...props} />

export default Harbor
