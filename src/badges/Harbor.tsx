
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Harbor = (props: BadgeProps) => <Badge name="Harbor" {...props} backgroundColor="#60B932" />

export default Harbor
