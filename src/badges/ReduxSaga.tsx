
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const ReduxSaga = (props: BadgeProps) => <Badge name="Redux-Saga" backgroundColor="#999999" {...props} />

export default ReduxSaga
