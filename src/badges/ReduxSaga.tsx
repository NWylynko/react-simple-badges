
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ReduxSaga = (props: BadgeProps) => <Badge name="Redux-Saga" {...props} backgroundColor="#999999" />

export default ReduxSaga
