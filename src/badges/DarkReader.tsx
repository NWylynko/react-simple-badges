
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const DarkReader = (props: BadgeProps) => <Badge name="Dark Reader" {...props} backgroundColor="#141E24" />

export default DarkReader
