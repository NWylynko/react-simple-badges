
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Hetzner = (props: BadgeProps) => <Badge name="Hetzner" {...props} backgroundColor="#D50C2D" />

export default Hetzner
