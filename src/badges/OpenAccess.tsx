
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const OpenAccess = (props: BadgeProps) => <Badge name="Open Access" backgroundColor="#F68212" {...props} />

export default OpenAccess
