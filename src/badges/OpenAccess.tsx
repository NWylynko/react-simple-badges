
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const OpenAccess = (props: BadgeProps) => <Badge name="Open Access" {...props} backgroundColor="#F68212" />

export default OpenAccess
