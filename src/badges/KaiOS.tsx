
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const KaiOS = (props: BadgeProps) => <Badge name="KaiOS" backgroundColor="#6F02B5" {...props} />

export default KaiOS
