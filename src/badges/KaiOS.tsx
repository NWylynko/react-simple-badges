
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const KaiOS = (props: BadgeProps) => <Badge name="KaiOS" {...props} backgroundColor="#6F02B5" />

export default KaiOS
