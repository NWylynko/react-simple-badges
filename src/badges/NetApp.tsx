
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const NetApp = (props: BadgeProps) => <Badge name="NetApp" {...props} backgroundColor="#0067C5" />

export default NetApp
