
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const NetApp = (props: BadgeProps) => <Badge name="NetApp" backgroundColor="#0067C5" {...props} />

export default NetApp
