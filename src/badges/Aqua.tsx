
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Aqua = (props: BadgeProps) => <Badge name="Aqua" {...props} backgroundColor="#1904DA" />

export default Aqua
