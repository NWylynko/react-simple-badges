
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Aqua = (props: BadgeProps) => <Badge name="Aqua" backgroundColor="#1904DA" {...props} />

export default Aqua
