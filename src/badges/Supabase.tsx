
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Supabase = (props: BadgeProps) => <Badge name="Supabase" {...props} backgroundColor="#3ECF8E" />

export default Supabase
