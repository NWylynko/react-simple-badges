
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Tableau = (props: BadgeProps) => <Badge name="Tableau" backgroundColor="#E97627" {...props} />

export default Tableau
