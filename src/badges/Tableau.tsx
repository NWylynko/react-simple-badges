
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Tableau = (props: BadgeProps) => <Badge name="Tableau" {...props} backgroundColor="#E97627" />

export default Tableau
