
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Yubico = (props: BadgeProps) => <Badge name="Yubico" backgroundColor="#84BD00" {...props} />

export default Yubico
