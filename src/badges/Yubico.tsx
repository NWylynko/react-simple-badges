
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Yubico = (props: BadgeProps) => <Badge name="Yubico" {...props} backgroundColor="#84BD00" />

export default Yubico
