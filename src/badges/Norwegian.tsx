
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Norwegian = (props: BadgeProps) => <Badge name="Norwegian" backgroundColor="#D81939" {...props} />

export default Norwegian
