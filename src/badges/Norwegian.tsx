
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Norwegian = (props: BadgeProps) => <Badge name="Norwegian" {...props} backgroundColor="#D81939" />

export default Norwegian
