
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Apple = (props: BadgeProps) => <Badge name="Apple" {...props} backgroundColor="#000000" />

export default Apple
