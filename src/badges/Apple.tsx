
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Apple = (props: BadgeProps) => <Badge name="Apple" backgroundColor="#000000" {...props} />

export default Apple
