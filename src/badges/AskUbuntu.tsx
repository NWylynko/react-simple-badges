
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AskUbuntu = (props: BadgeProps) => <Badge name="Ask Ubuntu" {...props} backgroundColor="#DC461D" />

export default AskUbuntu
