
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const AskUbuntu = (props: BadgeProps) => <Badge name="Ask Ubuntu" backgroundColor="#DC461D" {...props} />

export default AskUbuntu
