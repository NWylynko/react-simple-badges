
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const OpenSSL = (props: BadgeProps) => <Badge name="OpenSSL" backgroundColor="#721412" {...props} />

export default OpenSSL
