
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const OpenSSL = (props: BadgeProps) => <Badge name="OpenSSL" {...props} backgroundColor="#721412" />

export default OpenSSL
