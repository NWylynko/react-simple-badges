
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Tether = (props: BadgeProps) => <Badge name="Tether" {...props} backgroundColor="#50AF95" />

export default Tether
