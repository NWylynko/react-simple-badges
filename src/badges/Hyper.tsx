
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Hyper = (props: BadgeProps) => <Badge name="Hyper" {...props} backgroundColor="#000000" />

export default Hyper
