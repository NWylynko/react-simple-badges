
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Hyper = (props: BadgeProps) => <Badge name="Hyper" backgroundColor="#000000" {...props} />

export default Hyper
