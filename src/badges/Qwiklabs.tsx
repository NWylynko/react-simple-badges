
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Qwiklabs = (props: BadgeProps) => <Badge name="Qwiklabs" {...props} backgroundColor="#F5CD0E" />

export default Qwiklabs
