
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const OpenAPIInitiative = (props: BadgeProps) => <Badge name="OpenAPI Initiative" {...props} backgroundColor="#6BA539" />

export default OpenAPIInitiative
