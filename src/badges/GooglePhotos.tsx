
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GooglePhotos = (props: BadgeProps) => <Badge name="Google Photos" {...props} backgroundColor="#4285F4" />

export default GooglePhotos
