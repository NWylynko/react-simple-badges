
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const GooglePhotos = (props: BadgeProps) => <Badge name="Google Photos" backgroundColor="#4285F4" {...props} />

export default GooglePhotos
