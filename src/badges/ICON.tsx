
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const ICON = (props: BadgeProps) => <Badge name="ICON" backgroundColor="#31B8BB" {...props} />

export default ICON
