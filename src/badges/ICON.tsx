
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ICON = (props: BadgeProps) => <Badge name="ICON" {...props} backgroundColor="#31B8BB" />

export default ICON
