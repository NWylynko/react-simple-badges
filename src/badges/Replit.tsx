
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Replit = (props: BadgeProps) => <Badge name="Replit" {...props} backgroundColor="#667881" />

export default Replit
