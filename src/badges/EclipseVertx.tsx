
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const EclipseVertx = (props: BadgeProps) => <Badge name="Eclipse Vert.x" {...props} backgroundColor="#782A90" />

export default EclipseVertx
