
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Roundcube = (props: BadgeProps) => <Badge name="Roundcube" backgroundColor="#37BEFF" {...props} />

export default Roundcube
