
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Roundcube = (props: BadgeProps) => <Badge name="Roundcube" {...props} backgroundColor="#37BEFF" />

export default Roundcube
