
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Reason = (props: BadgeProps) => <Badge name="Reason" {...props} backgroundColor="#DD4B39" />

export default Reason
