
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Reason = (props: BadgeProps) => <Badge name="Reason" backgroundColor="#DD4B39" {...props} />

export default Reason
