
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Razer = (props: BadgeProps) => <Badge name="Razer" {...props} backgroundColor="#00FF00" />

export default Razer
