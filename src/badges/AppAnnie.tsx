
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AppAnnie = (props: BadgeProps) => <Badge name="App Annie" {...props} backgroundColor="#0F2346" />

export default AppAnnie
