
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Dlib = (props: BadgeProps) => <Badge name="Dlib" {...props} backgroundColor="#008000" />

export default Dlib
