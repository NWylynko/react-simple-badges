
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Caffeine = (props: BadgeProps) => <Badge name="Caffeine" {...props} backgroundColor="#0000FF" />

export default Caffeine
