
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const MAN = (props: BadgeProps) => <Badge name="MAN" backgroundColor="#E40045" {...props} />

export default MAN
