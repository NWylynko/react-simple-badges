
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Square = (props: BadgeProps) => <Badge name="Square" {...props} backgroundColor="#3E4348" />

export default Square
