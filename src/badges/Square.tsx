
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Square = (props: BadgeProps) => <Badge name="Square" backgroundColor="#3E4348" {...props} />

export default Square
