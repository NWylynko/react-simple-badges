
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Windows = (props: BadgeProps) => <Badge name="Windows" backgroundColor="#0078D6" {...props} />

export default Windows
