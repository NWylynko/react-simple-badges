
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Windows = (props: BadgeProps) => <Badge name="Windows" {...props} backgroundColor="#0078D6" />

export default Windows
