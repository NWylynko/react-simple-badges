
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Ello = (props: BadgeProps) => <Badge name="Ello" backgroundColor="#000000" {...props} />

export default Ello
