
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Ello = (props: BadgeProps) => <Badge name="Ello" {...props} backgroundColor="#000000" />

export default Ello
