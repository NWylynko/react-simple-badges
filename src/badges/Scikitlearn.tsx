
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Scikitlearn = (props: BadgeProps) => <Badge name="scikit-learn" backgroundColor="#F7931E" {...props} />

export default Scikitlearn
