
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Scikitlearn = (props: BadgeProps) => <Badge name="scikit-learn" {...props} backgroundColor="#F7931E" />

export default Scikitlearn
