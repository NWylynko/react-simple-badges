
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const GIMP = (props: BadgeProps) => <Badge name="GIMP" backgroundColor="#5C5543" {...props} />

export default GIMP
