
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GIMP = (props: BadgeProps) => <Badge name="GIMP" {...props} backgroundColor="#5C5543" />

export default GIMP
