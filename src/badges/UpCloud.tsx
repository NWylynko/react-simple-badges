
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const UpCloud = (props: BadgeProps) => <Badge name="UpCloud" {...props} backgroundColor="#7B00FF" />

export default UpCloud
