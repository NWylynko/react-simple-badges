
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const UpCloud = (props: BadgeProps) => <Badge name="UpCloud" backgroundColor="#7B00FF" {...props} />

export default UpCloud
