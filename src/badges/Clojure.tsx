
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Clojure = (props: BadgeProps) => <Badge name="Clojure" {...props} backgroundColor="#5881D8" />

export default Clojure
