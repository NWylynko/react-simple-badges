
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const LLVM = (props: BadgeProps) => <Badge name="LLVM" {...props} backgroundColor="#262D3A" />

export default LLVM
