
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Prevention = (props: BadgeProps) => <Badge name="Prevention" {...props} backgroundColor="#44C1C5" />

export default Prevention
