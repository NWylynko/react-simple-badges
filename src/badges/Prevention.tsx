
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Prevention = (props: BadgeProps) => <Badge name="Prevention" backgroundColor="#44C1C5" {...props} />

export default Prevention
