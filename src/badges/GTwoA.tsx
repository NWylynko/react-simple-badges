
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const GTwoA = (props: BadgeProps) => <Badge name="G2A" backgroundColor="#F05F00" {...props} />

export default GTwoA
