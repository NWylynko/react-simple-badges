
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GTwoA = (props: BadgeProps) => <Badge name="G2A" {...props} backgroundColor="#F05F00" />

export default GTwoA
