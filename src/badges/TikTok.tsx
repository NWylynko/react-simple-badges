
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TikTok = (props: BadgeProps) => <Badge name="TikTok" {...props} backgroundColor="#000000" />

export default TikTok
