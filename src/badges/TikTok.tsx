
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const TikTok = (props: BadgeProps) => <Badge name="TikTok" backgroundColor="#000000" {...props} />

export default TikTok
