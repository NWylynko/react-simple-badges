
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Git = (props: BadgeProps) => <Badge name="Git" backgroundColor="#F05032" {...props} />

export default Git
