
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Git = (props: BadgeProps) => <Badge name="Git" {...props} backgroundColor="#F05032" />

export default Git
