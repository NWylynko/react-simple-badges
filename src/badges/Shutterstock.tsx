
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Shutterstock = (props: BadgeProps) => <Badge name="Shutterstock" {...props} backgroundColor="#EE2B24" />

export default Shutterstock
