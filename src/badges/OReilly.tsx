
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const OReilly = (props: BadgeProps) => <Badge name="O'Reilly" {...props} backgroundColor="#D3002D" />

export default OReilly
