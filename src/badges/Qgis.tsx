
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Qgis = (props: BadgeProps) => <Badge name="Qgis" {...props} backgroundColor="#589632" />

export default Qgis
