
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const StackOverflow = (props: BadgeProps) => <Badge name="Stack Overflow" {...props} backgroundColor="#F58025" />

export default StackOverflow
