
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const SinaWeibo = (props: BadgeProps) => <Badge name="Sina Weibo" backgroundColor="#E6162D" {...props} />

export default SinaWeibo
