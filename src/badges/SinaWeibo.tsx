
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SinaWeibo = (props: BadgeProps) => <Badge name="Sina Weibo" {...props} backgroundColor="#E6162D" />

export default SinaWeibo
