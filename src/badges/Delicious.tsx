
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Delicious = (props: BadgeProps) => <Badge name="del.icio.us" {...props} backgroundColor="#0000FF" />

export default Delicious
