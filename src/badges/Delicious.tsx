
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Delicious = (props: BadgeProps) => <Badge name="del.icio.us" backgroundColor="#0000FF" {...props} />

export default Delicious
