
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ArtifactHub = (props: BadgeProps) => <Badge name="Artifact Hub" {...props} backgroundColor="#417598" />

export default ArtifactHub
