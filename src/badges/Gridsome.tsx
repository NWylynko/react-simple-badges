
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Gridsome = (props: BadgeProps) => <Badge name="Gridsome" {...props} backgroundColor="#00A672" />

export default Gridsome
