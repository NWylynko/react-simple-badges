
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Gridsome = (props: BadgeProps) => <Badge name="Gridsome" backgroundColor="#00A672" {...props} />

export default Gridsome
