
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PyUp = (props: BadgeProps) => <Badge name="PyUp" {...props} backgroundColor="#9F55FF" />

export default PyUp
