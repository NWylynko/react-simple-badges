
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Chromecast = (props: BadgeProps) => <Badge name="Chromecast" backgroundColor="#999999" {...props} />

export default Chromecast
