
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Chromecast = (props: BadgeProps) => <Badge name="Chromecast" {...props} backgroundColor="#999999" />

export default Chromecast
