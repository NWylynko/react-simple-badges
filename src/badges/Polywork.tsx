
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Polywork = (props: BadgeProps) => <Badge name="Polywork" {...props} backgroundColor="#543DE0" />

export default Polywork
