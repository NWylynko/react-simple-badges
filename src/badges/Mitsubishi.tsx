
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Mitsubishi = (props: BadgeProps) => <Badge name="Mitsubishi" {...props} backgroundColor="#E60012" />

export default Mitsubishi
