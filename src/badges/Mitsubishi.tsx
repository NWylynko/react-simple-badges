
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Mitsubishi = (props: BadgeProps) => <Badge name="Mitsubishi" backgroundColor="#E60012" {...props} />

export default Mitsubishi
