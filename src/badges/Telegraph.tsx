
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Telegraph = (props: BadgeProps) => <Badge name="Telegraph" {...props} backgroundColor="#FAFAFA" />

export default Telegraph
