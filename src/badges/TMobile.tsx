
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const TMobile = (props: BadgeProps) => <Badge name="T-Mobile" backgroundColor="#E20074" {...props} />

export default TMobile
