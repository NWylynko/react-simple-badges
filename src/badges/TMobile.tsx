
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TMobile = (props: BadgeProps) => <Badge name="T-Mobile" {...props} backgroundColor="#E20074" />

export default TMobile
