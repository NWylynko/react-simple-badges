
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Wise = (props: BadgeProps) => <Badge name="Wise" backgroundColor="#00B9FF" {...props} />

export default Wise
