
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Wise = (props: BadgeProps) => <Badge name="Wise" {...props} backgroundColor="#00B9FF" />

export default Wise
