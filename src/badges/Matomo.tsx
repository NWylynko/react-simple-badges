
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Matomo = (props: BadgeProps) => <Badge name="Matomo" {...props} backgroundColor="#3152A0" />

export default Matomo
