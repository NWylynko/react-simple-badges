
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Matomo = (props: BadgeProps) => <Badge name="Matomo" backgroundColor="#3152A0" {...props} />

export default Matomo
