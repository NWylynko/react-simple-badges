
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Shazam = (props: BadgeProps) => <Badge name="Shazam" backgroundColor="#0088FF" {...props} />

export default Shazam
