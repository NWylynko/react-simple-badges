
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Shazam = (props: BadgeProps) => <Badge name="Shazam" {...props} backgroundColor="#0088FF" />

export default Shazam
