
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Hashnode = (props: BadgeProps) => <Badge name="Hashnode" {...props} backgroundColor="#2962FF" />

export default Hashnode
