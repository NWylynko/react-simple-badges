
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Hashnode = (props: BadgeProps) => <Badge name="Hashnode" backgroundColor="#2962FF" {...props} />

export default Hashnode
