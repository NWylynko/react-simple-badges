
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Curl = (props: BadgeProps) => <Badge name="curl" {...props} backgroundColor="#073551" />

export default Curl
