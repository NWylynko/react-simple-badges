
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Curl = (props: BadgeProps) => <Badge name="curl" backgroundColor="#073551" {...props} />

export default Curl
