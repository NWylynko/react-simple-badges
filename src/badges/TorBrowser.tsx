
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TorBrowser = (props: BadgeProps) => <Badge name="Tor Browser" {...props} backgroundColor="#7D4698" />

export default TorBrowser
