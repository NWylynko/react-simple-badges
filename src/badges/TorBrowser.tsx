
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const TorBrowser = (props: BadgeProps) => <Badge name="Tor Browser" backgroundColor="#7D4698" {...props} />

export default TorBrowser
