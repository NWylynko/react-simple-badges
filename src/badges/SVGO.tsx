
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SVGO = (props: BadgeProps) => <Badge name="SVGO" {...props} backgroundColor="#3E7FC1" />

export default SVGO
