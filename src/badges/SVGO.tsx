
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const SVGO = (props: BadgeProps) => <Badge name="SVGO" backgroundColor="#3E7FC1" {...props} />

export default SVGO
