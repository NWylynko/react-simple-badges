
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Slashdot = (props: BadgeProps) => <Badge name="Slashdot" {...props} backgroundColor="#026664" />

export default Slashdot
