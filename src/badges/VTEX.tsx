
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const VTEX = (props: BadgeProps) => <Badge name="VTEX" {...props} backgroundColor="#ED125F" />

export default VTEX
