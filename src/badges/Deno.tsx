
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Deno = (props: BadgeProps) => <Badge name="Deno" {...props} backgroundColor="#000000" />

export default Deno
