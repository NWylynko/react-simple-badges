
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Deno = (props: BadgeProps) => <Badge name="Deno" backgroundColor="#000000" {...props} />

export default Deno
