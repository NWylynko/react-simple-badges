
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Delta = (props: BadgeProps) => <Badge name="Delta" {...props} backgroundColor="#003366" />

export default Delta
