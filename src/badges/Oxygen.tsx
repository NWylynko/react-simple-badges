
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Oxygen = (props: BadgeProps) => <Badge name="Oxygen" {...props} backgroundColor="#3A209E" />

export default Oxygen
