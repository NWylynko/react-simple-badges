
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const StackShare = (props: BadgeProps) => <Badge name="StackShare" {...props} backgroundColor="#0690FA" />

export default StackShare
