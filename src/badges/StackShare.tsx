
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const StackShare = (props: BadgeProps) => <Badge name="StackShare" backgroundColor="#0690FA" {...props} />

export default StackShare
