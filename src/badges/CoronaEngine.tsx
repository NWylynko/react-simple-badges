
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CoronaEngine = (props: BadgeProps) => <Badge name="Corona Engine" {...props} backgroundColor="#F96F29" />

export default CoronaEngine
