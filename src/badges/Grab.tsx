
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Grab = (props: BadgeProps) => <Badge name="Grab" {...props} backgroundColor="#00B14F" />

export default Grab
