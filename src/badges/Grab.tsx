
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Grab = (props: BadgeProps) => <Badge name="Grab" backgroundColor="#00B14F" {...props} />

export default Grab
