
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Revue = (props: BadgeProps) => <Badge name="Revue" backgroundColor="#E15718" {...props} />

export default Revue
