
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Revue = (props: BadgeProps) => <Badge name="Revue" {...props} backgroundColor="#E15718" />

export default Revue
