
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Symantec = (props: BadgeProps) => <Badge name="Symantec" {...props} backgroundColor="#FDB511" />

export default Symantec
