
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Symantec = (props: BadgeProps) => <Badge name="Symantec" backgroundColor="#FDB511" {...props} />

export default Symantec
