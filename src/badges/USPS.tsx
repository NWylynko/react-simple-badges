
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const USPS = (props: BadgeProps) => <Badge name="USPS" {...props} backgroundColor="#333366" />

export default USPS
