
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const QMK = (props: BadgeProps) => <Badge name="QMK" backgroundColor="#333333" {...props} />

export default QMK
