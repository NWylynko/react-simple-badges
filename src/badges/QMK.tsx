
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const QMK = (props: BadgeProps) => <Badge name="QMK" {...props} backgroundColor="#333333" />

export default QMK
