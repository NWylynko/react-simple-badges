
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Conan = (props: BadgeProps) => <Badge name="Conan" {...props} backgroundColor="#6699CB" />

export default Conan
