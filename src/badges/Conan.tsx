
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Conan = (props: BadgeProps) => <Badge name="Conan" backgroundColor="#6699CB" {...props} />

export default Conan
