
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Spinnaker = (props: BadgeProps) => <Badge name="Spinnaker" {...props} backgroundColor="#139BB4" />

export default Spinnaker
