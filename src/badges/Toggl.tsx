
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Toggl = (props: BadgeProps) => <Badge name="Toggl" {...props} backgroundColor="#E01B22" />

export default Toggl
