
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Toggl = (props: BadgeProps) => <Badge name="Toggl" backgroundColor="#E01B22" {...props} />

export default Toggl
