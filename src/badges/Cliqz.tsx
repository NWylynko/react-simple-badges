
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Cliqz = (props: BadgeProps) => <Badge name="Cliqz" {...props} backgroundColor="#00AEF0" />

export default Cliqz
