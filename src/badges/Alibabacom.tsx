
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Alibabacom = (props: BadgeProps) => <Badge name="Alibaba.com" {...props} backgroundColor="#FF6A00" />

export default Alibabacom
