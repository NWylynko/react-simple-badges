
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Moleculer = (props: BadgeProps) => <Badge name="Moleculer" {...props} backgroundColor="#3CAFCE" />

export default Moleculer
