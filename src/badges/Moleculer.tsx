
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Moleculer = (props: BadgeProps) => <Badge name="Moleculer" backgroundColor="#3CAFCE" {...props} />

export default Moleculer
