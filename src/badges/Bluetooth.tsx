
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Bluetooth = (props: BadgeProps) => <Badge name="Bluetooth" backgroundColor="#0082FC" {...props} />

export default Bluetooth
