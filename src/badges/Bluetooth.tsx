
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Bluetooth = (props: BadgeProps) => <Badge name="Bluetooth" {...props} backgroundColor="#0082FC" />

export default Bluetooth
