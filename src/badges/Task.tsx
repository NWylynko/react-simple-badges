
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Task = (props: BadgeProps) => <Badge name="Task" {...props} backgroundColor="#29BEB0" />

export default Task
