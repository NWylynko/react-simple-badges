
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Task = (props: BadgeProps) => <Badge name="Task" backgroundColor="#29BEB0" {...props} />

export default Task
