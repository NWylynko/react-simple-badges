
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Sourcegraph = (props: BadgeProps) => <Badge name="Sourcegraph" backgroundColor="#00CBEC" {...props} />

export default Sourcegraph
