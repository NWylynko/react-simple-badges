
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Sourcegraph = (props: BadgeProps) => <Badge name="Sourcegraph" {...props} backgroundColor="#00CBEC" />

export default Sourcegraph
