
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Terraform = (props: BadgeProps) => <Badge name="Terraform" backgroundColor="#7B42BC" {...props} />

export default Terraform
