
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Terraform = (props: BadgeProps) => <Badge name="Terraform" {...props} backgroundColor="#7B42BC" />

export default Terraform
