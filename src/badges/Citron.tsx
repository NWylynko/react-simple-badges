
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Citron = (props: BadgeProps) => <Badge name="Citroën" {...props} backgroundColor="#6E6E6E" />

export default Citron
