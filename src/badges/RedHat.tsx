
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const RedHat = (props: BadgeProps) => <Badge name="Red Hat" backgroundColor="#EE0000" {...props} />

export default RedHat
