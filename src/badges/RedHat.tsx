
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const RedHat = (props: BadgeProps) => <Badge name="Red Hat" {...props} backgroundColor="#EE0000" />

export default RedHat
