
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SparkAR = (props: BadgeProps) => <Badge name="Spark AR" {...props} backgroundColor="#FF5C83" />

export default SparkAR
