
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ATT = (props: BadgeProps) => <Badge name="AT&T" {...props} backgroundColor="#009FDB" />

export default ATT
