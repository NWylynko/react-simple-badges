
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const ATT = (props: BadgeProps) => <Badge name="AT&T" backgroundColor="#009FDB" {...props} />

export default ATT
