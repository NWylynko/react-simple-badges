
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Stylelint = (props: BadgeProps) => <Badge name="stylelint" backgroundColor="#263238" {...props} />

export default Stylelint
