
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Stylelint = (props: BadgeProps) => <Badge name="stylelint" {...props} backgroundColor="#263238" />

export default Stylelint
