
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Julia = (props: BadgeProps) => <Badge name="Julia" {...props} backgroundColor="#9558B2" />

export default Julia
