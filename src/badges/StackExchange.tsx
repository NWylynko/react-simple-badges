
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const StackExchange = (props: BadgeProps) => <Badge name="Stack Exchange" {...props} backgroundColor="#1E5397" />

export default StackExchange
