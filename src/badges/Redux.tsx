
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Redux = (props: BadgeProps) => <Badge name="Redux" {...props} backgroundColor="#764ABC" />

export default Redux
