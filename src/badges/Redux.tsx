
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Redux = (props: BadgeProps) => <Badge name="Redux" backgroundColor="#764ABC" {...props} />

export default Redux
