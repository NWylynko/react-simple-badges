
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GitKraken = (props: BadgeProps) => <Badge name="GitKraken" {...props} backgroundColor="#179287" />

export default GitKraken
