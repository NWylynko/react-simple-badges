
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Puppeteer = (props: BadgeProps) => <Badge name="Puppeteer" backgroundColor="#40B5A4" {...props} />

export default Puppeteer
