
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Puppeteer = (props: BadgeProps) => <Badge name="Puppeteer" {...props} backgroundColor="#40B5A4" />

export default Puppeteer
