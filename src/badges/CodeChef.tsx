
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const CodeChef = (props: BadgeProps) => <Badge name="CodeChef" backgroundColor="#5B4638" {...props} />

export default CodeChef
