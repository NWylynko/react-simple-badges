
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CodeChef = (props: BadgeProps) => <Badge name="CodeChef" {...props} backgroundColor="#5B4638" />

export default CodeChef
