
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Writeas = (props: BadgeProps) => <Badge name="write.as" backgroundColor="#5BC4EE" {...props} />

export default Writeas
