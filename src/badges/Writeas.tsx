
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Writeas = (props: BadgeProps) => <Badge name="write.as" {...props} backgroundColor="#5BC4EE" />

export default Writeas
