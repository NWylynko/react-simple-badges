
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GoogleFit = (props: BadgeProps) => <Badge name="Google Fit" {...props} backgroundColor="#4285F4" />

export default GoogleFit
