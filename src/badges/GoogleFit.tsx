
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const GoogleFit = (props: BadgeProps) => <Badge name="Google Fit" backgroundColor="#4285F4" {...props} />

export default GoogleFit
