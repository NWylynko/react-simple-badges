
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const VSCodium = (props: BadgeProps) => <Badge name="VSCodium" {...props} backgroundColor="#2F80ED" />

export default VSCodium
