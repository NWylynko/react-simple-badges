
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const TraefikProxy = (props: BadgeProps) => <Badge name="Traefik Proxy" backgroundColor="#24A1C1" {...props} />

export default TraefikProxy
