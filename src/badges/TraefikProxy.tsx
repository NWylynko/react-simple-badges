
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TraefikProxy = (props: BadgeProps) => <Badge name="Traefik Proxy" {...props} backgroundColor="#24A1C1" />

export default TraefikProxy
