
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Nx = (props: BadgeProps) => <Badge name="Nx" {...props} backgroundColor="#143055" />

export default Nx
