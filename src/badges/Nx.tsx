
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Nx = (props: BadgeProps) => <Badge name="Nx" backgroundColor="#143055" {...props} />

export default Nx
