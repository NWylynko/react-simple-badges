
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Vitest = (props: BadgeProps) => <Badge name="Vitest" {...props} backgroundColor="#6E9F18" />

export default Vitest
