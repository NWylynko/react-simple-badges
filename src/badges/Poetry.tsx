
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Poetry = (props: BadgeProps) => <Badge name="Poetry" backgroundColor="#60A5FA" {...props} />

export default Poetry
