
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Poetry = (props: BadgeProps) => <Badge name="Poetry" {...props} backgroundColor="#60A5FA" />

export default Poetry
