
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Ardour = (props: BadgeProps) => <Badge name="Ardour" {...props} backgroundColor="#C61C3E" />

export default Ardour
