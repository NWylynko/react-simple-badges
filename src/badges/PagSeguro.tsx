
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PagSeguro = (props: BadgeProps) => <Badge name="PagSeguro" {...props} backgroundColor="#FFC801" />

export default PagSeguro
