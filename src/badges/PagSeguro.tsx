
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const PagSeguro = (props: BadgeProps) => <Badge name="PagSeguro" backgroundColor="#FFC801" {...props} />

export default PagSeguro
