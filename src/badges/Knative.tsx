
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Knative = (props: BadgeProps) => <Badge name="Knative" {...props} backgroundColor="#0865AD" />

export default Knative
