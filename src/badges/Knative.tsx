
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Knative = (props: BadgeProps) => <Badge name="Knative" backgroundColor="#0865AD" {...props} />

export default Knative
