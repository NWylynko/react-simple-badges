
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const FOX = (props: BadgeProps) => <Badge name="FOX" backgroundColor="#000000" {...props} />

export default FOX
