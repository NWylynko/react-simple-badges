
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const FOX = (props: BadgeProps) => <Badge name="FOX" {...props} backgroundColor="#000000" />

export default FOX
