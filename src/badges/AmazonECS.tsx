
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AmazonECS = (props: BadgeProps) => <Badge name="Amazon ECS" {...props} backgroundColor="#FF9900" />

export default AmazonECS
