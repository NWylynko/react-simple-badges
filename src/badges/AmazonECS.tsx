
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const AmazonECS = (props: BadgeProps) => <Badge name="Amazon ECS" backgroundColor="#FF9900" {...props} />

export default AmazonECS
