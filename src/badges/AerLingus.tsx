
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AerLingus = (props: BadgeProps) => <Badge name="Aer Lingus" {...props} backgroundColor="#006272" />

export default AerLingus
