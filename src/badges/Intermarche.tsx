
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Intermarche = (props: BadgeProps) => <Badge name="Intermarche" {...props} backgroundColor="#E2001A" />

export default Intermarche
