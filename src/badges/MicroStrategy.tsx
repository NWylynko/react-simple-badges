
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MicroStrategy = (props: BadgeProps) => <Badge name="MicroStrategy" {...props} backgroundColor="#D9232E" />

export default MicroStrategy
