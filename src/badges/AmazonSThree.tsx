
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AmazonSThree = (props: BadgeProps) => <Badge name="Amazon S3" {...props} backgroundColor="#569A31" />

export default AmazonSThree
