
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MUI = (props: BadgeProps) => <Badge name="MUI" {...props} backgroundColor="#007FFF" />

export default MUI
