
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const MUI = (props: BadgeProps) => <Badge name="MUI" backgroundColor="#007FFF" {...props} />

export default MUI
