
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Statuspage = (props: BadgeProps) => <Badge name="Statuspage" {...props} backgroundColor="#172B4D" />

export default Statuspage
