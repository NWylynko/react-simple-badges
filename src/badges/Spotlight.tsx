
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Spotlight = (props: BadgeProps) => <Badge name="Spotlight" backgroundColor="#352A71" {...props} />

export default Spotlight
