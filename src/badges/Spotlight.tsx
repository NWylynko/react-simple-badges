
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Spotlight = (props: BadgeProps) => <Badge name="Spotlight" {...props} backgroundColor="#352A71" />

export default Spotlight
