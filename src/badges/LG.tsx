
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const LG = (props: BadgeProps) => <Badge name="LG" backgroundColor="#A50034" {...props} />

export default LG
