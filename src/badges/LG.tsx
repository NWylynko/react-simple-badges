
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const LG = (props: BadgeProps) => <Badge name="LG" {...props} backgroundColor="#A50034" />

export default LG
