
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AffinityPhoto = (props: BadgeProps) => <Badge name="Affinity Photo" {...props} backgroundColor="#7E4DD2" />

export default AffinityPhoto
