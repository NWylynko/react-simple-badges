
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Tutanota = (props: BadgeProps) => <Badge name="Tutanota" {...props} backgroundColor="#840010" />

export default Tutanota
