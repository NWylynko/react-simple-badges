
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Datadog = (props: BadgeProps) => <Badge name="Datadog" backgroundColor="#632CA6" {...props} />

export default Datadog
