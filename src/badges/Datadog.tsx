
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Datadog = (props: BadgeProps) => <Badge name="Datadog" {...props} backgroundColor="#632CA6" />

export default Datadog
