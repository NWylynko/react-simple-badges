
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Django = (props: BadgeProps) => <Badge name="Django" {...props} backgroundColor="#092E20" />

export default Django
