
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Django = (props: BadgeProps) => <Badge name="Django" backgroundColor="#092E20" {...props} />

export default Django
