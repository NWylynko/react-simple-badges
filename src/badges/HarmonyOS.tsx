
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const HarmonyOS = (props: BadgeProps) => <Badge name="HarmonyOS" backgroundColor="#000000" {...props} />

export default HarmonyOS
