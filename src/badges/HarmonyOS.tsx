
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const HarmonyOS = (props: BadgeProps) => <Badge name="HarmonyOS" {...props} backgroundColor="#000000" />

export default HarmonyOS
