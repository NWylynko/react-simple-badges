
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Textpattern = (props: BadgeProps) => <Badge name="Textpattern" {...props} backgroundColor="#FFDA44" />

export default Textpattern
