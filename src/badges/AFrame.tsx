
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AFrame = (props: BadgeProps) => <Badge name="A-Frame" {...props} backgroundColor="#EF2D5E" />

export default AFrame
