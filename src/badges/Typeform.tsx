
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Typeform = (props: BadgeProps) => <Badge name="Typeform" backgroundColor="#262627" {...props} />

export default Typeform
