
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Typeform = (props: BadgeProps) => <Badge name="Typeform" {...props} backgroundColor="#262627" />

export default Typeform
