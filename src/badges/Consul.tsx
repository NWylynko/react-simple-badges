
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Consul = (props: BadgeProps) => <Badge name="Consul" {...props} backgroundColor="#F24C53" />

export default Consul
