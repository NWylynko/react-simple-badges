
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Consul = (props: BadgeProps) => <Badge name="Consul" backgroundColor="#F24C53" {...props} />

export default Consul
