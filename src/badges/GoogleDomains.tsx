
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GoogleDomains = (props: BadgeProps) => <Badge name="Google Domains" {...props} backgroundColor="#4285F4" />

export default GoogleDomains
