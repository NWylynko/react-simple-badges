
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Swagger = (props: BadgeProps) => <Badge name="Swagger" backgroundColor="#85EA2D" {...props} />

export default Swagger
