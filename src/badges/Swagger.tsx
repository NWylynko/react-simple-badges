
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Swagger = (props: BadgeProps) => <Badge name="Swagger" {...props} backgroundColor="#85EA2D" />

export default Swagger
