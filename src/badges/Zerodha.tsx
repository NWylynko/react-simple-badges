
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Zerodha = (props: BadgeProps) => <Badge name="Zerodha" {...props} backgroundColor="#387ED1" />

export default Zerodha
