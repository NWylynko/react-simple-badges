
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ArtStation = (props: BadgeProps) => <Badge name="ArtStation" {...props} backgroundColor="#13AFF0" />

export default ArtStation
