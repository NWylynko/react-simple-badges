
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const ArtStation = (props: BadgeProps) => <Badge name="ArtStation" backgroundColor="#13AFF0" {...props} />

export default ArtStation
