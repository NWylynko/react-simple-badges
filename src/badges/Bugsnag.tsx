
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Bugsnag = (props: BadgeProps) => <Badge name="Bugsnag" {...props} backgroundColor="#4949E4" />

export default Bugsnag
