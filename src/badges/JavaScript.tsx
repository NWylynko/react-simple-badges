
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const JavaScript = (props: BadgeProps) => <Badge name="JavaScript" {...props} backgroundColor="#F7DF1E" />

export default JavaScript
