
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ReactBadge = (props: BadgeProps) => <Badge name="React" {...props} backgroundColor="#61DAFB" />

export default ReactBadge
