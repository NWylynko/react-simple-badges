
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const SublimeText = (props: BadgeProps) => <Badge name="Sublime Text" backgroundColor="#FF9800" {...props} />

export default SublimeText
