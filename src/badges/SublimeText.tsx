
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SublimeText = (props: BadgeProps) => <Badge name="Sublime Text" {...props} backgroundColor="#FF9800" />

export default SublimeText
