
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Zulip = (props: BadgeProps) => <Badge name="Zulip" {...props} backgroundColor="#FFFFFF" />

export default Zulip
