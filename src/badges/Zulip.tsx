
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Zulip = (props: BadgeProps) => <Badge name="Zulip" backgroundColor="#FFFFFF" {...props} />

export default Zulip
