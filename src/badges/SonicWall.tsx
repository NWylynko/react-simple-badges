
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SonicWall = (props: BadgeProps) => <Badge name="SonicWall" {...props} backgroundColor="#FF791A" />

export default SonicWall
