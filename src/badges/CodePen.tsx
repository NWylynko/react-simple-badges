
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const CodePen = (props: BadgeProps) => <Badge name="CodePen" backgroundColor="#000000" {...props} />

export default CodePen
