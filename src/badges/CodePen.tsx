
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CodePen = (props: BadgeProps) => <Badge name="CodePen" {...props} backgroundColor="#000000" />

export default CodePen
