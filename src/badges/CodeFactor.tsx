
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const CodeFactor = (props: BadgeProps) => <Badge name="CodeFactor" backgroundColor="#F44A6A" {...props} />

export default CodeFactor
