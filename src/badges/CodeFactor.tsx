
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CodeFactor = (props: BadgeProps) => <Badge name="CodeFactor" {...props} backgroundColor="#F44A6A" />

export default CodeFactor
