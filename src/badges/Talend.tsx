
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Talend = (props: BadgeProps) => <Badge name="Talend" backgroundColor="#FF6D70" {...props} />

export default Talend
