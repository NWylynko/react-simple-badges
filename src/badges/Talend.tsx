
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Talend = (props: BadgeProps) => <Badge name="Talend" {...props} backgroundColor="#FF6D70" />

export default Talend
