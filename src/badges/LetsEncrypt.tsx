
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const LetsEncrypt = (props: BadgeProps) => <Badge name="Let's Encrypt" backgroundColor="#003A70" {...props} />

export default LetsEncrypt
