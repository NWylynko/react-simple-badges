
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const LetsEncrypt = (props: BadgeProps) => <Badge name="Let’s Encrypt" {...props} backgroundColor="#003A70" />

export default LetsEncrypt
