
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const HCL = (props: BadgeProps) => <Badge name="HCL" backgroundColor="#006BB6" {...props} />

export default HCL
