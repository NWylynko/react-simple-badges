
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const HCL = (props: BadgeProps) => <Badge name="HCL" {...props} backgroundColor="#006BB6" />

export default HCL
