
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const GoLand = (props: BadgeProps) => <Badge name="GoLand" backgroundColor="#000000" {...props} />

export default GoLand
