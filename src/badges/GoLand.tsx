
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GoLand = (props: BadgeProps) => <Badge name="GoLand" {...props} backgroundColor="#000000" />

export default GoLand
