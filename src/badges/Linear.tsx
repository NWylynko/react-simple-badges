
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Linear = (props: BadgeProps) => <Badge name="Linear" {...props} backgroundColor="#5E6AD2" />

export default Linear
