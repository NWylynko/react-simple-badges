
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Linear = (props: BadgeProps) => <Badge name="Linear" backgroundColor="#5E6AD2" {...props} />

export default Linear
