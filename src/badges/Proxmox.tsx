
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Proxmox = (props: BadgeProps) => <Badge name="Proxmox" {...props} backgroundColor="#E57000" />

export default Proxmox
