
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Proxmox = (props: BadgeProps) => <Badge name="Proxmox" backgroundColor="#E57000" {...props} />

export default Proxmox
