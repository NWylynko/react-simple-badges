
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Sparkasse = (props: BadgeProps) => <Badge name="Sparkasse" backgroundColor="#FF0000" {...props} />

export default Sparkasse
