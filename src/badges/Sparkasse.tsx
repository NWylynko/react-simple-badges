
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Sparkasse = (props: BadgeProps) => <Badge name="Sparkasse" {...props} backgroundColor="#FF0000" />

export default Sparkasse
