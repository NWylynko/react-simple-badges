
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Lua = (props: BadgeProps) => <Badge name="Lua" {...props} backgroundColor="#2C2D72" />

export default Lua
