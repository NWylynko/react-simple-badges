
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Spotify = (props: BadgeProps) => <Badge name="Spotify" {...props} backgroundColor="#1DB954" />

export default Spotify
