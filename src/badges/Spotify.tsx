
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Spotify = (props: BadgeProps) => <Badge name="Spotify" backgroundColor="#1DB954" {...props} />

export default Spotify
