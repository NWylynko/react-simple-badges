
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Mercedes = (props: BadgeProps) => <Badge name="Mercedes" {...props} backgroundColor="#242424" />

export default Mercedes
