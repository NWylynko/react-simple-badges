
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Mercedes = (props: BadgeProps) => <Badge name="Mercedes" backgroundColor="#242424" {...props} />

export default Mercedes
