
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const VowpalWabbit = (props: BadgeProps) => <Badge name="Vowpal Wabbit" {...props} backgroundColor="#FF81F9" />

export default VowpalWabbit
