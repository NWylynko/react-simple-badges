
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Dapr = (props: BadgeProps) => <Badge name="Dapr" backgroundColor="#0D2192" {...props} />

export default Dapr
