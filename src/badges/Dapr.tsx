
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Dapr = (props: BadgeProps) => <Badge name="Dapr" {...props} backgroundColor="#0D2192" />

export default Dapr
