
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const STARZ = (props: BadgeProps) => <Badge name="STARZ" {...props} backgroundColor="#000000" />

export default STARZ
