
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Gitpod = (props: BadgeProps) => <Badge name="Gitpod" {...props} backgroundColor="#FFAE33" />

export default Gitpod
