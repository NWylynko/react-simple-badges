
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SemanticWeb = (props: BadgeProps) => <Badge name="Semantic Web" {...props} backgroundColor="#005A9C" />

export default SemanticWeb
