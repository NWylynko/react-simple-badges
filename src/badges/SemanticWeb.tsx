
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const SemanticWeb = (props: BadgeProps) => <Badge name="Semantic Web" backgroundColor="#005A9C" {...props} />

export default SemanticWeb
