
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Skynet = (props: BadgeProps) => <Badge name="Skynet" {...props} backgroundColor="#00C65E" />

export default Skynet
