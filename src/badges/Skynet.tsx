
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Skynet = (props: BadgeProps) => <Badge name="Skynet" backgroundColor="#00C65E" {...props} />

export default Skynet
