
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Emirates = (props: BadgeProps) => <Badge name="Emirates" {...props} backgroundColor="#D71921" />

export default Emirates
