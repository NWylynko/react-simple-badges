
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Flutter = (props: BadgeProps) => <Badge name="Flutter" {...props} backgroundColor="#02569B" />

export default Flutter
