
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const OpenVPN = (props: BadgeProps) => <Badge name="OpenVPN" {...props} backgroundColor="#EA7E20" />

export default OpenVPN
