
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Favro = (props: BadgeProps) => <Badge name="Favro" {...props} backgroundColor="#512DA8" />

export default Favro
