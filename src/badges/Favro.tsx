
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Favro = (props: BadgeProps) => <Badge name="Favro" backgroundColor="#512DA8" {...props} />

export default Favro
