
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const REWE = (props: BadgeProps) => <Badge name="REWE" {...props} backgroundColor="#CC071E" />

export default REWE
