
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Mastercomfig = (props: BadgeProps) => <Badge name="mastercomfig" backgroundColor="#009688" {...props} />

export default Mastercomfig
