
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Mastercomfig = (props: BadgeProps) => <Badge name="mastercomfig" {...props} backgroundColor="#009688" />

export default Mastercomfig
