
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SCPFoundation = (props: BadgeProps) => <Badge name="SCP Foundation" {...props} backgroundColor="#FFFFFF" />

export default SCPFoundation
