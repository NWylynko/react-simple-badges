
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Xbox = (props: BadgeProps) => <Badge name="Xbox" {...props} backgroundColor="#107C10" />

export default Xbox
