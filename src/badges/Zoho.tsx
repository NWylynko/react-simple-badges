
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Zoho = (props: BadgeProps) => <Badge name="Zoho" backgroundColor="#C8202B" {...props} />

export default Zoho
