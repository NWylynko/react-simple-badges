
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Zoho = (props: BadgeProps) => <Badge name="Zoho" {...props} backgroundColor="#C8202B" />

export default Zoho
