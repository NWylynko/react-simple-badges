
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const YouTubeStudio = (props: BadgeProps) => <Badge name="YouTube Studio" {...props} backgroundColor="#FF0000" />

export default YouTubeStudio
