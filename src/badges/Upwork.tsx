
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Upwork = (props: BadgeProps) => <Badge name="Upwork" backgroundColor="#6FDA44" {...props} />

export default Upwork
