
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Upwork = (props: BadgeProps) => <Badge name="Upwork" {...props} backgroundColor="#6FDA44" />

export default Upwork
