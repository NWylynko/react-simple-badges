
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Monero = (props: BadgeProps) => <Badge name="Monero" {...props} backgroundColor="#FF6600" />

export default Monero
