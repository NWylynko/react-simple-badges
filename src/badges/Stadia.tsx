
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Stadia = (props: BadgeProps) => <Badge name="Stadia" {...props} backgroundColor="#CD2640" />

export default Stadia
