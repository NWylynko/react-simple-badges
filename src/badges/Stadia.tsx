
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Stadia = (props: BadgeProps) => <Badge name="Stadia" backgroundColor="#CD2640" {...props} />

export default Stadia
