
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MtrodeParis = (props: BadgeProps) => <Badge name="Métro de Paris" {...props} backgroundColor="#003E95" />

export default MtrodeParis
