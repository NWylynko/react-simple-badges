
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Accenture = (props: BadgeProps) => <Badge name="Accenture" {...props} backgroundColor="#A100FF" />

export default Accenture
