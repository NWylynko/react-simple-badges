
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Accenture = (props: BadgeProps) => <Badge name="Accenture" backgroundColor="#A100FF" {...props} />

export default Accenture
