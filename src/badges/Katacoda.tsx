
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Katacoda = (props: BadgeProps) => <Badge name="Katacoda" {...props} backgroundColor="#F48220" />

export default Katacoda
