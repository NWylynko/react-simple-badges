
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Audible = (props: BadgeProps) => <Badge name="Audible" {...props} backgroundColor="#F8991C" />

export default Audible
