
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Audible = (props: BadgeProps) => <Badge name="Audible" backgroundColor="#F8991C" {...props} />

export default Audible
