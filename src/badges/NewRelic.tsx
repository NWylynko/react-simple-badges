
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const NewRelic = (props: BadgeProps) => <Badge name="New Relic" {...props} backgroundColor="#008C99" />

export default NewRelic
