
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Seagate = (props: BadgeProps) => <Badge name="Seagate" backgroundColor="#6EBE49" {...props} />

export default Seagate
