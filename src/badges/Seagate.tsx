
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Seagate = (props: BadgeProps) => <Badge name="Seagate" {...props} backgroundColor="#6EBE49" />

export default Seagate
