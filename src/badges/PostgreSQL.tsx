
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PostgreSQL = (props: BadgeProps) => <Badge name="PostgreSQL" {...props} backgroundColor="#4169E1" />

export default PostgreSQL
