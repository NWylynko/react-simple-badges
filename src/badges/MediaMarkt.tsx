
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MediaMarkt = (props: BadgeProps) => <Badge name="MediaMarkt" {...props} backgroundColor="#DF0000" />

export default MediaMarkt
