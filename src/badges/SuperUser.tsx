
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const SuperUser = (props: BadgeProps) => <Badge name="Super User" backgroundColor="#38A1CE" {...props} />

export default SuperUser
