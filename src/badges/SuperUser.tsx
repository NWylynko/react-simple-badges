
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SuperUser = (props: BadgeProps) => <Badge name="Super User" {...props} backgroundColor="#38A1CE" />

export default SuperUser
