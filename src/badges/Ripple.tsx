
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Ripple = (props: BadgeProps) => <Badge name="Ripple" {...props} backgroundColor="#0085C0" />

export default Ripple
