
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Storybook = (props: BadgeProps) => <Badge name="Storybook" {...props} backgroundColor="#FF4785" />

export default Storybook
