
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Storybook = (props: BadgeProps) => <Badge name="Storybook" backgroundColor="#FF4785" {...props} />

export default Storybook
