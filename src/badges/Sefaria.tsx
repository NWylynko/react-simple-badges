
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Sefaria = (props: BadgeProps) => <Badge name="Sefaria" backgroundColor="#212E50" {...props} />

export default Sefaria
