
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Sefaria = (props: BadgeProps) => <Badge name="Sefaria" {...props} backgroundColor="#212E50" />

export default Sefaria
