
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Xero = (props: BadgeProps) => <Badge name="Xero" {...props} backgroundColor="#13B5EA" />

export default Xero
