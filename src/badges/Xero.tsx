
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Xero = (props: BadgeProps) => <Badge name="Xero" backgroundColor="#13B5EA" {...props} />

export default Xero
