
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const FreeBSD = (props: BadgeProps) => <Badge name="FreeBSD" backgroundColor="#AB2B28" {...props} />

export default FreeBSD
