
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const FreeBSD = (props: BadgeProps) => <Badge name="FreeBSD" {...props} backgroundColor="#AB2B28" />

export default FreeBSD
