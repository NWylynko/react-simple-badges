
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const KeyCDN = (props: BadgeProps) => <Badge name="KeyCDN" backgroundColor="#047AED" {...props} />

export default KeyCDN
