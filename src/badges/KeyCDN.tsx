
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const KeyCDN = (props: BadgeProps) => <Badge name="KeyCDN" {...props} backgroundColor="#047AED" />

export default KeyCDN
