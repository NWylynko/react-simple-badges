
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const OnePassword = (props: BadgeProps) => <Badge name="1Password" backgroundColor="#0094F5" {...props} />

export default OnePassword
