
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const OnePassword = (props: BadgeProps) => <Badge name="1Password" {...props} backgroundColor="#0094F5" />

export default OnePassword
