
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Claris = (props: BadgeProps) => <Badge name="Claris" {...props} backgroundColor="#000000" />

export default Claris
