
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const IconJar = (props: BadgeProps) => <Badge name="IconJar" {...props} backgroundColor="#16A5F3" />

export default IconJar
