
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Lemmy = (props: BadgeProps) => <Badge name="Lemmy" {...props} backgroundColor="#FFFFFF" />

export default Lemmy
