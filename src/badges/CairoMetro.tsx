
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CairoMetro = (props: BadgeProps) => <Badge name="Cairo Metro" {...props} backgroundColor="#C10C0C" />

export default CairoMetro
