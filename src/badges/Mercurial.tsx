
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Mercurial = (props: BadgeProps) => <Badge name="Mercurial" {...props} backgroundColor="#999999" />

export default Mercurial
