
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const SamsungPay = (props: BadgeProps) => <Badge name="Samsung Pay" backgroundColor="#1428A0" {...props} />

export default SamsungPay
