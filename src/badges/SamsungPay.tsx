
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SamsungPay = (props: BadgeProps) => <Badge name="Samsung Pay" {...props} backgroundColor="#1428A0" />

export default SamsungPay
