
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const EA = (props: BadgeProps) => <Badge name="EA" {...props} backgroundColor="#000000" />

export default EA
