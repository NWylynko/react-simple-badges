
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const EA = (props: BadgeProps) => <Badge name="EA" backgroundColor="#000000" {...props} />

export default EA
