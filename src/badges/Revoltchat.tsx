
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Revoltchat = (props: BadgeProps) => <Badge name="Revolt.chat" {...props} backgroundColor="#FF4655" />

export default Revoltchat
