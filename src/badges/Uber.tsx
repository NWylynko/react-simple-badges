
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Uber = (props: BadgeProps) => <Badge name="Uber" {...props} backgroundColor="#000000" />

export default Uber
