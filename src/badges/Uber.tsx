
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Uber = (props: BadgeProps) => <Badge name="Uber" backgroundColor="#000000" {...props} />

export default Uber
