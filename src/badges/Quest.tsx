
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Quest = (props: BadgeProps) => <Badge name="Quest" {...props} backgroundColor="#FB4F14" />

export default Quest
