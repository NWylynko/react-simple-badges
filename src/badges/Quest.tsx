
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Quest = (props: BadgeProps) => <Badge name="Quest" backgroundColor="#FB4F14" {...props} />

export default Quest
