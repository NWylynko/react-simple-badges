
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Probot = (props: BadgeProps) => <Badge name="Probot" backgroundColor="#00B0D8" {...props} />

export default Probot
