
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Probot = (props: BadgeProps) => <Badge name="Probot" {...props} backgroundColor="#00B0D8" />

export default Probot
