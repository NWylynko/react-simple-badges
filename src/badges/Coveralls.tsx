
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Coveralls = (props: BadgeProps) => <Badge name="Coveralls" backgroundColor="#3F5767" {...props} />

export default Coveralls
