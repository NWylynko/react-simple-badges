
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Coveralls = (props: BadgeProps) => <Badge name="Coveralls" {...props} backgroundColor="#3F5767" />

export default Coveralls
