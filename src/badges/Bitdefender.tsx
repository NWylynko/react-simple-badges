
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Bitdefender = (props: BadgeProps) => <Badge name="Bitdefender" {...props} backgroundColor="#ED1C24" />

export default Bitdefender
