
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Bitdefender = (props: BadgeProps) => <Badge name="Bitdefender" backgroundColor="#ED1C24" {...props} />

export default Bitdefender
