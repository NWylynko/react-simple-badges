
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Pelican = (props: BadgeProps) => <Badge name="Pelican" {...props} backgroundColor="#14A0C4" />

export default Pelican
