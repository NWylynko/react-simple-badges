
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CSSWizardry = (props: BadgeProps) => <Badge name="CSS Wizardry" {...props} backgroundColor="#F43059" />

export default CSSWizardry
