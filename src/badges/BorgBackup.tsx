
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const BorgBackup = (props: BadgeProps) => <Badge name="BorgBackup" backgroundColor="#00DD00" {...props} />

export default BorgBackup
