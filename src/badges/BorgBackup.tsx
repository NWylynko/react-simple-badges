
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const BorgBackup = (props: BadgeProps) => <Badge name="BorgBackup" {...props} backgroundColor="#00DD00" />

export default BorgBackup
