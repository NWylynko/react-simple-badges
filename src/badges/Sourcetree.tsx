
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Sourcetree = (props: BadgeProps) => <Badge name="Sourcetree" backgroundColor="#0052CC" {...props} />

export default Sourcetree
