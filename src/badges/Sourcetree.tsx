
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Sourcetree = (props: BadgeProps) => <Badge name="Sourcetree" {...props} backgroundColor="#0052CC" />

export default Sourcetree
