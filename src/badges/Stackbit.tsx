
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Stackbit = (props: BadgeProps) => <Badge name="Stackbit" backgroundColor="#207BEA" {...props} />

export default Stackbit
