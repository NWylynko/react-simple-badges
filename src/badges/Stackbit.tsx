
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Stackbit = (props: BadgeProps) => <Badge name="Stackbit" {...props} backgroundColor="#207BEA" />

export default Stackbit
