
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Bose = (props: BadgeProps) => <Badge name="Bose" backgroundColor="#000000" {...props} />

export default Bose
