
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Bose = (props: BadgeProps) => <Badge name="Bose" {...props} backgroundColor="#000000" />

export default Bose
