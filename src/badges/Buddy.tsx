
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Buddy = (props: BadgeProps) => <Badge name="Buddy" {...props} backgroundColor="#1A86FD" />

export default Buddy
