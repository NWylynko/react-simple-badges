
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Buddy = (props: BadgeProps) => <Badge name="Buddy" backgroundColor="#1A86FD" {...props} />

export default Buddy
