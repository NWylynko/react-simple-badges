
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AMD = (props: BadgeProps) => <Badge name="AMD" {...props} backgroundColor="#ED1C24" />

export default AMD
