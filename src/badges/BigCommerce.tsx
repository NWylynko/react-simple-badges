
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const BigCommerce = (props: BadgeProps) => <Badge name="BigCommerce" {...props} backgroundColor="#121118" />

export default BigCommerce
