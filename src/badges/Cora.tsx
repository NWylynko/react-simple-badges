
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Cora = (props: BadgeProps) => <Badge name="Cora" {...props} backgroundColor="#E61845" />

export default Cora
