
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Cora = (props: BadgeProps) => <Badge name="Cora" backgroundColor="#E61845" {...props} />

export default Cora
