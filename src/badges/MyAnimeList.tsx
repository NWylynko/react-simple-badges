
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const MyAnimeList = (props: BadgeProps) => <Badge name="MyAnimeList" backgroundColor="#2E51A2" {...props} />

export default MyAnimeList
