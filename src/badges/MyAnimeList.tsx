
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MyAnimeList = (props: BadgeProps) => <Badge name="MyAnimeList" {...props} backgroundColor="#2E51A2" />

export default MyAnimeList
