
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const AdBlock = (props: BadgeProps) => <Badge name="AdBlock" backgroundColor="#F40D12" {...props} />

export default AdBlock
