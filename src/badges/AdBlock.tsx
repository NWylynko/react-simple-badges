
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AdBlock = (props: BadgeProps) => <Badge name="AdBlock" {...props} backgroundColor="#F40D12" />

export default AdBlock
