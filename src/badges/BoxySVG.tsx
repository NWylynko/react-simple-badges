
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const BoxySVG = (props: BadgeProps) => <Badge name="Boxy SVG" {...props} backgroundColor="#3584E3" />

export default BoxySVG
