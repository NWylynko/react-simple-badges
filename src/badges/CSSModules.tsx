
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const CSSModules = (props: BadgeProps) => <Badge name="CSS Modules" backgroundColor="#000000" {...props} />

export default CSSModules
