
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CSSModules = (props: BadgeProps) => <Badge name="CSS Modules" {...props} backgroundColor="#000000" />

export default CSSModules
