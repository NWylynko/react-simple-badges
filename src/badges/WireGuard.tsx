
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const WireGuard = (props: BadgeProps) => <Badge name="WireGuard" {...props} backgroundColor="#88171A" />

export default WireGuard
