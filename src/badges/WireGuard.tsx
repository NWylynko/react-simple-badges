
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const WireGuard = (props: BadgeProps) => <Badge name="WireGuard" backgroundColor="#88171A" {...props} />

export default WireGuard
