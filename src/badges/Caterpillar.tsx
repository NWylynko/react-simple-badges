
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Caterpillar = (props: BadgeProps) => <Badge name="Caterpillar" {...props} backgroundColor="#FFCD11" />

export default Caterpillar
