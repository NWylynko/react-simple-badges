
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Zotero = (props: BadgeProps) => <Badge name="Zotero" backgroundColor="#CC2936" {...props} />

export default Zotero
