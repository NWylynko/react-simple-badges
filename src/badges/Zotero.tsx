
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Zotero = (props: BadgeProps) => <Badge name="Zotero" {...props} backgroundColor="#CC2936" />

export default Zotero
