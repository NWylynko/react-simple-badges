
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const HackClub = (props: BadgeProps) => <Badge name="Hack Club" {...props} backgroundColor="#EC3750" />

export default HackClub
