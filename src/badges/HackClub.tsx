
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const HackClub = (props: BadgeProps) => <Badge name="Hack Club" backgroundColor="#EC3750" {...props} />

export default HackClub
