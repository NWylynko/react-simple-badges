
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Toptal = (props: BadgeProps) => <Badge name="Toptal" backgroundColor="#3863A0" {...props} />

export default Toptal
