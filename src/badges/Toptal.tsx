
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Toptal = (props: BadgeProps) => <Badge name="Toptal" {...props} backgroundColor="#3863A0" />

export default Toptal
