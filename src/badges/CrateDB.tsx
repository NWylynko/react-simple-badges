
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CrateDB = (props: BadgeProps) => <Badge name="CrateDB" {...props} backgroundColor="#009DC7" />

export default CrateDB
