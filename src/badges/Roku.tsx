
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Roku = (props: BadgeProps) => <Badge name="Roku" backgroundColor="#662D91" {...props} />

export default Roku
