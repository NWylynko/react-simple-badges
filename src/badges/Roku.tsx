
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Roku = (props: BadgeProps) => <Badge name="Roku" {...props} backgroundColor="#662D91" />

export default Roku
