
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Wappalyzer = (props: BadgeProps) => <Badge name="Wappalyzer" {...props} backgroundColor="#32067C" />

export default Wappalyzer
