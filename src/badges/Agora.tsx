
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Agora = (props: BadgeProps) => <Badge name="Agora" {...props} backgroundColor="#099DFD" />

export default Agora
