
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Wagtail = (props: BadgeProps) => <Badge name="Wagtail" {...props} backgroundColor="#43B1B0" />

export default Wagtail
