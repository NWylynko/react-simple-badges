
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const PlayCanvas = (props: BadgeProps) => <Badge name="PlayCanvas" backgroundColor="#E05F2C" {...props} />

export default PlayCanvas
