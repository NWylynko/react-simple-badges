
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PlayCanvas = (props: BadgeProps) => <Badge name="PlayCanvas" {...props} backgroundColor="#E05F2C" />

export default PlayCanvas
