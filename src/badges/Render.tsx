
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Render = (props: BadgeProps) => <Badge name="Render" {...props} backgroundColor="#46E3B7" />

export default Render
