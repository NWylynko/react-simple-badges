
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Render = (props: BadgeProps) => <Badge name="Render" backgroundColor="#46E3B7" {...props} />

export default Render
