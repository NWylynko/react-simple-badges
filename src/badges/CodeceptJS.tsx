
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CodeceptJS = (props: BadgeProps) => <Badge name="CodeceptJS" {...props} backgroundColor="#F6E05E" />

export default CodeceptJS
