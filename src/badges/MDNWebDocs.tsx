
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const MDNWebDocs = (props: BadgeProps) => <Badge name="MDN Web Docs" backgroundColor="#000000" {...props} />

export default MDNWebDocs
