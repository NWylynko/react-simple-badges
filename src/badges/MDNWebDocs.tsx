
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MDNWebDocs = (props: BadgeProps) => <Badge name="MDN Web Docs" {...props} backgroundColor="#000000" />

export default MDNWebDocs
