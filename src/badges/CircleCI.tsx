
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CircleCI = (props: BadgeProps) => <Badge name="CircleCI" {...props} backgroundColor="#343434" />

export default CircleCI
