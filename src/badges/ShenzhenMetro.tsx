
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ShenzhenMetro = (props: BadgeProps) => <Badge name="Shenzhen Metro" {...props} backgroundColor="#009943" />

export default ShenzhenMetro
