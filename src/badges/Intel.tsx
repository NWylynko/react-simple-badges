
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Intel = (props: BadgeProps) => <Badge name="Intel" {...props} backgroundColor="#0071C5" />

export default Intel
