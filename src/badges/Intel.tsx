
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Intel = (props: BadgeProps) => <Badge name="Intel" backgroundColor="#0071C5" {...props} />

export default Intel
