
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SatOne = (props: BadgeProps) => <Badge name="Sat.1" {...props} backgroundColor="#047DA3" />

export default SatOne
