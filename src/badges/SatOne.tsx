
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const SatOne = (props: BadgeProps) => <Badge name="Sat.1" backgroundColor="#047DA3" {...props} />

export default SatOne
