
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AmazonPrime = (props: BadgeProps) => <Badge name="Amazon Prime" {...props} backgroundColor="#00A8E1" />

export default AmazonPrime
