
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Reebok = (props: BadgeProps) => <Badge name="Reebok" backgroundColor="#E41D1B" {...props} />

export default Reebok
