
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Reebok = (props: BadgeProps) => <Badge name="Reebok" {...props} backgroundColor="#E41D1B" />

export default Reebok
