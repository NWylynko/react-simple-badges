
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const JenkinsX = (props: BadgeProps) => <Badge name="Jenkins X" {...props} backgroundColor="#73C3D5" />

export default JenkinsX
