
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const FontAwesome = (props: BadgeProps) => <Badge name="Font Awesome" {...props} backgroundColor="#528DD7" />

export default FontAwesome
