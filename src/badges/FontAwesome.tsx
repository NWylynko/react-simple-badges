
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const FontAwesome = (props: BadgeProps) => <Badge name="Font Awesome" backgroundColor="#528DD7" {...props} />

export default FontAwesome
