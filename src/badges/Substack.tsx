
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Substack = (props: BadgeProps) => <Badge name="Substack" backgroundColor="#FF6719" {...props} />

export default Substack
