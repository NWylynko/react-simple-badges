
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Substack = (props: BadgeProps) => <Badge name="Substack" {...props} backgroundColor="#FF6719" />

export default Substack
