
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Firefox = (props: BadgeProps) => <Badge name="Firefox" {...props} backgroundColor="#FF7139" />

export default Firefox
