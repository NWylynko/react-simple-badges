
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Firefox = (props: BadgeProps) => <Badge name="Firefox" backgroundColor="#FF7139" {...props} />

export default Firefox
