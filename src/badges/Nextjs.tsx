
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Nextjs = (props: BadgeProps) => <Badge name="Next.js" backgroundColor="#000000" {...props} />

export default Nextjs
