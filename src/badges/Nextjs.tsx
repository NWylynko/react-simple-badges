
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Nextjs = (props: BadgeProps) => <Badge name="Next.js" {...props} backgroundColor="#000000" />

export default Nextjs
