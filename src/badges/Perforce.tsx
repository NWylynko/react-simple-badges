
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Perforce = (props: BadgeProps) => <Badge name="Perforce" {...props} backgroundColor="#404040" />

export default Perforce
