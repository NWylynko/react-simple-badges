
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Perforce = (props: BadgeProps) => <Badge name="Perforce" backgroundColor="#404040" {...props} />

export default Perforce
