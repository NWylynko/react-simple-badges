
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Gravatar = (props: BadgeProps) => <Badge name="Gravatar" {...props} backgroundColor="#1E8CBE" />

export default Gravatar
