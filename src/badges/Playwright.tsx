
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Playwright = (props: BadgeProps) => <Badge name="Playwright" backgroundColor="#2EAD33" {...props} />

export default Playwright
