
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Playwright = (props: BadgeProps) => <Badge name="Playwright" {...props} backgroundColor="#2EAD33" />

export default Playwright
