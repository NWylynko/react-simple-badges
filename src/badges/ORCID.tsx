
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ORCID = (props: BadgeProps) => <Badge name="ORCID" {...props} backgroundColor="#A6CE39" />

export default ORCID
