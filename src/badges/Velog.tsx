
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Velog = (props: BadgeProps) => <Badge name="Velog" backgroundColor="#20C997" {...props} />

export default Velog
