
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Velog = (props: BadgeProps) => <Badge name="Velog" {...props} backgroundColor="#20C997" />

export default Velog
