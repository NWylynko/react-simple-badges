
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const SailfishOS = (props: BadgeProps) => <Badge name="Sailfish OS" backgroundColor="#053766" {...props} />

export default SailfishOS
