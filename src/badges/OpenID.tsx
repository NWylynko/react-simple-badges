
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const OpenID = (props: BadgeProps) => <Badge name="OpenID" backgroundColor="#F78C40" {...props} />

export default OpenID
