
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const OpenID = (props: BadgeProps) => <Badge name="OpenID" {...props} backgroundColor="#F78C40" />

export default OpenID
