
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Swarm = (props: BadgeProps) => <Badge name="Swarm" {...props} backgroundColor="#FFA633" />

export default Swarm
