
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Swarm = (props: BadgeProps) => <Badge name="Swarm" backgroundColor="#FFA633" {...props} />

export default Swarm
