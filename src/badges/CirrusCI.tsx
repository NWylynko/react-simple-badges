
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CirrusCI = (props: BadgeProps) => <Badge name="Cirrus CI" {...props} backgroundColor="#4051B5" />

export default CirrusCI
