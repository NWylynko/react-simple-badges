
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const CirrusCI = (props: BadgeProps) => <Badge name="Cirrus CI" backgroundColor="#4051B5" {...props} />

export default CirrusCI
