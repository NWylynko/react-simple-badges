
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const MIDI = (props: BadgeProps) => <Badge name="MIDI" backgroundColor="#000000" {...props} />

export default MIDI
