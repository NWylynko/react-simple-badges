
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MIDI = (props: BadgeProps) => <Badge name="MIDI" {...props} backgroundColor="#000000" />

export default MIDI
