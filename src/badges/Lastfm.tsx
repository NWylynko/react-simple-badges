
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Lastfm = (props: BadgeProps) => <Badge name="Last.fm" {...props} backgroundColor="#D51007" />

export default Lastfm
