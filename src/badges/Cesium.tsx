
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Cesium = (props: BadgeProps) => <Badge name="Cesium" {...props} backgroundColor="#6CADDF" />

export default Cesium
