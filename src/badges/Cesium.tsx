
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Cesium = (props: BadgeProps) => <Badge name="Cesium" backgroundColor="#6CADDF" {...props} />

export default Cesium
