
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Xing = (props: BadgeProps) => <Badge name="Xing" {...props} backgroundColor="#006567" />

export default Xing
