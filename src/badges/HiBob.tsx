
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const HiBob = (props: BadgeProps) => <Badge name="Hi Bob" {...props} backgroundColor="#E42C51" />

export default HiBob
