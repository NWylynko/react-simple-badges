
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Dacia = (props: BadgeProps) => <Badge name="Dacia" {...props} backgroundColor="#122AFF" />

export default Dacia
