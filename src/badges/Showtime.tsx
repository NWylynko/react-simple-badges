
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Showtime = (props: BadgeProps) => <Badge name="Showtime" backgroundColor="#B10000" {...props} />

export default Showtime
