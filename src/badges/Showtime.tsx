
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Showtime = (props: BadgeProps) => <Badge name="Showtime" {...props} backgroundColor="#B10000" />

export default Showtime
