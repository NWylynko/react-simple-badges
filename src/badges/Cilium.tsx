
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Cilium = (props: BadgeProps) => <Badge name="Cilium" {...props} backgroundColor="#F8C517" />

export default Cilium
