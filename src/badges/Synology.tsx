
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Synology = (props: BadgeProps) => <Badge name="Synology" {...props} backgroundColor="#B5B5B6" />

export default Synology
