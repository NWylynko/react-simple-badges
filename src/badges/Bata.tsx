
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Bata = (props: BadgeProps) => <Badge name="Bata" backgroundColor="#DD282E" {...props} />

export default Bata
