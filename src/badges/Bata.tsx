
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Bata = (props: BadgeProps) => <Badge name="Bata" {...props} backgroundColor="#DD282E" />

export default Bata
