
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Infosys = (props: BadgeProps) => <Badge name="Infosys" backgroundColor="#007CC3" {...props} />

export default Infosys
