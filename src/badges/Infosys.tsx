
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Infosys = (props: BadgeProps) => <Badge name="Infosys" {...props} backgroundColor="#007CC3" />

export default Infosys
