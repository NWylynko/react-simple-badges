
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Flood = (props: BadgeProps) => <Badge name="Flood" backgroundColor="#4285F4" {...props} />

export default Flood
