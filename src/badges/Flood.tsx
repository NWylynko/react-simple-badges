
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Flood = (props: BadgeProps) => <Badge name="Flood" {...props} backgroundColor="#4285F4" />

export default Flood
