
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const VOX = (props: BadgeProps) => <Badge name="VOX" backgroundColor="#DA074A" {...props} />

export default VOX
