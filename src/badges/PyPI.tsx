
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const PyPI = (props: BadgeProps) => <Badge name="PyPI" backgroundColor="#3775A9" {...props} />

export default PyPI
