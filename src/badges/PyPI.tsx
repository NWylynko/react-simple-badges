
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PyPI = (props: BadgeProps) => <Badge name="PyPI" {...props} backgroundColor="#3775A9" />

export default PyPI
