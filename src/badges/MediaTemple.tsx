
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MediaTemple = (props: BadgeProps) => <Badge name="MediaTemple" {...props} backgroundColor="#000000" />

export default MediaTemple
