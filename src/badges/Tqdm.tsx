
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Tqdm = (props: BadgeProps) => <Badge name="tqdm" {...props} backgroundColor="#FFC107" />

export default Tqdm
