
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Tqdm = (props: BadgeProps) => <Badge name="tqdm" backgroundColor="#FFC107" {...props} />

export default Tqdm
