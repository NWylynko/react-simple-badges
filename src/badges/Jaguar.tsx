
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Jaguar = (props: BadgeProps) => <Badge name="Jaguar" backgroundColor="#FFFFFF" {...props} />

export default Jaguar
