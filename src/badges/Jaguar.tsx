
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Jaguar = (props: BadgeProps) => <Badge name="Jaguar" {...props} backgroundColor="#FFFFFF" />

export default Jaguar
