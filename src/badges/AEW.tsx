
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AEW = (props: BadgeProps) => <Badge name="AEW" {...props} backgroundColor="#000000" />

export default AEW
