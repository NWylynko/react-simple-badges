
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Polkadot = (props: BadgeProps) => <Badge name="Polkadot" {...props} backgroundColor="#E6007A" />

export default Polkadot
