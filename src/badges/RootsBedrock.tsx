
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const RootsBedrock = (props: BadgeProps) => <Badge name="Roots Bedrock" backgroundColor="#525DDC" {...props} />

export default RootsBedrock
