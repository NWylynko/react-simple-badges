
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const RootsBedrock = (props: BadgeProps) => <Badge name="Roots Bedrock" {...props} backgroundColor="#525DDC" />

export default RootsBedrock
