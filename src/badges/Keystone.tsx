
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Keystone = (props: BadgeProps) => <Badge name="Keystone" backgroundColor="#166BFF" {...props} />

export default Keystone
