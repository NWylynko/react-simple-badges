
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Keystone = (props: BadgeProps) => <Badge name="Keystone" {...props} backgroundColor="#166BFF" />

export default Keystone
