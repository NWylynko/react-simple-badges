
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Homebrew = (props: BadgeProps) => <Badge name="Homebrew" backgroundColor="#FBB040" {...props} />

export default Homebrew
