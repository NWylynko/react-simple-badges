
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Homebrew = (props: BadgeProps) => <Badge name="Homebrew" {...props} backgroundColor="#FBB040" />

export default Homebrew
