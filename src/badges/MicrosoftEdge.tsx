
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MicrosoftEdge = (props: BadgeProps) => <Badge name="Microsoft Edge" {...props} backgroundColor="#0078D7" />

export default MicrosoftEdge
