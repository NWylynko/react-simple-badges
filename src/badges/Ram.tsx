
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Ram = (props: BadgeProps) => <Badge name="Ram" {...props} backgroundColor="#000000" />

export default Ram
