
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Ram = (props: BadgeProps) => <Badge name="Ram" backgroundColor="#000000" {...props} />

export default Ram
