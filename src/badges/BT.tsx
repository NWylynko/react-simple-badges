
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const BT = (props: BadgeProps) => <Badge name="BT" {...props} backgroundColor="#6400AA" />

export default BT
