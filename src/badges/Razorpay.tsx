
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Razorpay = (props: BadgeProps) => <Badge name="Razorpay" backgroundColor="#0C2451" {...props} />

export default Razorpay
