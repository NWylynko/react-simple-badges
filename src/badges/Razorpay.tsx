
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Razorpay = (props: BadgeProps) => <Badge name="Razorpay" {...props} backgroundColor="#0C2451" />

export default Razorpay
