
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const AMG = (props: BadgeProps) => <Badge name="AMG" backgroundColor="#000000" {...props} />

export default AMG
