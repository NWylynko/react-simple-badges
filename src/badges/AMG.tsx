
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AMG = (props: BadgeProps) => <Badge name="AMG" {...props} backgroundColor="#000000" />

export default AMG
