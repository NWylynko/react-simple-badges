
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Codeberg = (props: BadgeProps) => <Badge name="Codeberg" {...props} backgroundColor="#2185D0" />

export default Codeberg
