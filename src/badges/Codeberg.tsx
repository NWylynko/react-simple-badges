
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Codeberg = (props: BadgeProps) => <Badge name="Codeberg" backgroundColor="#2185D0" {...props} />

export default Codeberg
