
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GitBook = (props: BadgeProps) => <Badge name="GitBook" {...props} backgroundColor="#3884FF" />

export default GitBook
