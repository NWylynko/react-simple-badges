
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const GitBook = (props: BadgeProps) => <Badge name="GitBook" backgroundColor="#3884FF" {...props} />

export default GitBook
