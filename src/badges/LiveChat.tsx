
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const LiveChat = (props: BadgeProps) => <Badge name="LiveChat" {...props} backgroundColor="#FFD000" />

export default LiveChat
