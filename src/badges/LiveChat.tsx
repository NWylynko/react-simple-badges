
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const LiveChat = (props: BadgeProps) => <Badge name="LiveChat" backgroundColor="#FFD000" {...props} />

export default LiveChat
