
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Prco = (props: BadgeProps) => <Badge name="pr.co" backgroundColor="#0080FF" {...props} />

export default Prco
