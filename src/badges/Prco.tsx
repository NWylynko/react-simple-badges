
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Prco = (props: BadgeProps) => <Badge name="pr.co" {...props} backgroundColor="#0080FF" />

export default Prco
