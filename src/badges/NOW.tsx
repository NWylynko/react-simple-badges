
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const NOW = (props: BadgeProps) => <Badge name="NOW" backgroundColor="#001211" {...props} />

export default NOW
