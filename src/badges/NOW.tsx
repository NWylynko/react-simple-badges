
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const NOW = (props: BadgeProps) => <Badge name="NOW" {...props} backgroundColor="#001211" />

export default NOW
