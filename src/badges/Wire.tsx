
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Wire = (props: BadgeProps) => <Badge name="Wire" backgroundColor="#000000" {...props} />

export default Wire
