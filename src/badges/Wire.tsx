
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Wire = (props: BadgeProps) => <Badge name="Wire" {...props} backgroundColor="#000000" />

export default Wire
