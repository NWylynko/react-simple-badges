
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Sencha = (props: BadgeProps) => <Badge name="Sencha" {...props} backgroundColor="#86BC40" />

export default Sencha
