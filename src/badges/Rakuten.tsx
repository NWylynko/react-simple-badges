
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Rakuten = (props: BadgeProps) => <Badge name="Rakuten" {...props} backgroundColor="#BF0000" />

export default Rakuten
