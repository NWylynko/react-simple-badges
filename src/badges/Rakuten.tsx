
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Rakuten = (props: BadgeProps) => <Badge name="Rakuten" backgroundColor="#BF0000" {...props} />

export default Rakuten
