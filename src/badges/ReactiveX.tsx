
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ReactiveX = (props: BadgeProps) => <Badge name="ReactiveX" {...props} backgroundColor="#B7178C" />

export default ReactiveX
