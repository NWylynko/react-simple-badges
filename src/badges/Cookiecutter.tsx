
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Cookiecutter = (props: BadgeProps) => <Badge name="Cookiecutter" {...props} backgroundColor="#D4AA00" />

export default Cookiecutter
