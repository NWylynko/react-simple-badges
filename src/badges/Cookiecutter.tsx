
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Cookiecutter = (props: BadgeProps) => <Badge name="Cookiecutter" backgroundColor="#D4AA00" {...props} />

export default Cookiecutter
