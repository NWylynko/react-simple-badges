
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Preact = (props: BadgeProps) => <Badge name="Preact" backgroundColor="#673AB8" {...props} />

export default Preact
