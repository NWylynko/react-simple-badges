
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Preact = (props: BadgeProps) => <Badge name="Preact" {...props} backgroundColor="#673AB8" />

export default Preact
