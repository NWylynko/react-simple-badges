
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const LaravelNova = (props: BadgeProps) => <Badge name="Laravel Nova" {...props} backgroundColor="#252D37" />

export default LaravelNova
