
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Foodpanda = (props: BadgeProps) => <Badge name="foodpanda" backgroundColor="#D70F64" {...props} />

export default Foodpanda
