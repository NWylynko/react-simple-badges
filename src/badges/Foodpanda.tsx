
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Foodpanda = (props: BadgeProps) => <Badge name="foodpanda" {...props} backgroundColor="#D70F64" />

export default Foodpanda
