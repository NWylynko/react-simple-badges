
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Gatsby = (props: BadgeProps) => <Badge name="Gatsby" {...props} backgroundColor="#663399" />

export default Gatsby
