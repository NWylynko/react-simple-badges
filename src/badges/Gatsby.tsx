
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Gatsby = (props: BadgeProps) => <Badge name="Gatsby" backgroundColor="#663399" {...props} />

export default Gatsby
