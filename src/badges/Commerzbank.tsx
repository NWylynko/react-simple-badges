
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Commerzbank = (props: BadgeProps) => <Badge name="Commerzbank" backgroundColor="#FFCC33" {...props} />

export default Commerzbank
