
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Commerzbank = (props: BadgeProps) => <Badge name="Commerzbank" {...props} backgroundColor="#FFCC33" />

export default Commerzbank
