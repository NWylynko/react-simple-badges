
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Merck = (props: BadgeProps) => <Badge name="Merck" backgroundColor="#007A73" {...props} />

export default Merck
