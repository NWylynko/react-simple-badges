
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Merck = (props: BadgeProps) => <Badge name="Merck" {...props} backgroundColor="#007A73" />

export default Merck
