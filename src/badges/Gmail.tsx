
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Gmail = (props: BadgeProps) => <Badge name="Gmail" backgroundColor="#EA4335" {...props} />

export default Gmail
