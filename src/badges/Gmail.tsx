
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Gmail = (props: BadgeProps) => <Badge name="Gmail" {...props} backgroundColor="#EA4335" />

export default Gmail
