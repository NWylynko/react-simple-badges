
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Alpinejs = (props: BadgeProps) => <Badge name="Alpine.js" {...props} backgroundColor="#8BC0D0" />

export default Alpinejs
