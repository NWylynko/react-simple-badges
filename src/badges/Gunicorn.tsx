
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Gunicorn = (props: BadgeProps) => <Badge name="Gunicorn" {...props} backgroundColor="#499848" />

export default Gunicorn
