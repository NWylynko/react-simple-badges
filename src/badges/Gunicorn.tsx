
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Gunicorn = (props: BadgeProps) => <Badge name="Gunicorn" backgroundColor="#499848" {...props} />

export default Gunicorn
