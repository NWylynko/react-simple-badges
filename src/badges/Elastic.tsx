
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Elastic = (props: BadgeProps) => <Badge name="Elastic" {...props} backgroundColor="#005571" />

export default Elastic
