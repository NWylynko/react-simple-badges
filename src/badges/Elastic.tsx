
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Elastic = (props: BadgeProps) => <Badge name="Elastic" backgroundColor="#005571" {...props} />

export default Elastic
