
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Ceph = (props: BadgeProps) => <Badge name="Ceph" {...props} backgroundColor="#EF5C55" />

export default Ceph
