
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Ceph = (props: BadgeProps) => <Badge name="Ceph" backgroundColor="#EF5C55" {...props} />

export default Ceph
