
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Treehouse = (props: BadgeProps) => <Badge name="Treehouse" {...props} backgroundColor="#5FCF80" />

export default Treehouse
