
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Treehouse = (props: BadgeProps) => <Badge name="Treehouse" backgroundColor="#5FCF80" {...props} />

export default Treehouse
