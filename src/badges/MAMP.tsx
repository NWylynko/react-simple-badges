
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const MAMP = (props: BadgeProps) => <Badge name="MAMP" backgroundColor="#02749C" {...props} />

export default MAMP
