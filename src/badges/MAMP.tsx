
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MAMP = (props: BadgeProps) => <Badge name="MAMP" {...props} backgroundColor="#02749C" />

export default MAMP
