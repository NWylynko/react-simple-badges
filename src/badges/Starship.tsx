
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Starship = (props: BadgeProps) => <Badge name="Starship" {...props} backgroundColor="#DD0B78" />

export default Starship
