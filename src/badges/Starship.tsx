
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Starship = (props: BadgeProps) => <Badge name="Starship" backgroundColor="#DD0B78" {...props} />

export default Starship
