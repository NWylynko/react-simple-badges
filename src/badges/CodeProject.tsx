
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CodeProject = (props: BadgeProps) => <Badge name="CodeProject" {...props} backgroundColor="#FF9900" />

export default CodeProject
