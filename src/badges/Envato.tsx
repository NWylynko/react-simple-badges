
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Envato = (props: BadgeProps) => <Badge name="Envato" {...props} backgroundColor="#81B441" />

export default Envato
