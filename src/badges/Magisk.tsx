
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Magisk = (props: BadgeProps) => <Badge name="Magisk" {...props} backgroundColor="#00AF9C" />

export default Magisk
