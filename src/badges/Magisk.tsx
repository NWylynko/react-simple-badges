
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Magisk = (props: BadgeProps) => <Badge name="Magisk" backgroundColor="#00AF9C" {...props} />

export default Magisk
