
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const OpsLevel = (props: BadgeProps) => <Badge name="OpsLevel" {...props} backgroundColor="#1890FF" />

export default OpsLevel
