
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PyCharm = (props: BadgeProps) => <Badge name="PyCharm" {...props} backgroundColor="#000000" />

export default PyCharm
