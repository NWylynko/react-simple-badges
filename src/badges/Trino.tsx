
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Trino = (props: BadgeProps) => <Badge name="Trino" backgroundColor="#DD00A1" {...props} />

export default Trino
