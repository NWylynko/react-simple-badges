
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Trino = (props: BadgeProps) => <Badge name="Trino" {...props} backgroundColor="#DD00A1" />

export default Trino
