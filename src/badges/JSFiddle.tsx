
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const JSFiddle = (props: BadgeProps) => <Badge name="JSFiddle" backgroundColor="#0084FF" {...props} />

export default JSFiddle
