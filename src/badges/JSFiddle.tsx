
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const JSFiddle = (props: BadgeProps) => <Badge name="JSFiddle" {...props} backgroundColor="#0084FF" />

export default JSFiddle
