
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Pingdom = (props: BadgeProps) => <Badge name="Pingdom" backgroundColor="#FFF000" {...props} />

export default Pingdom
