
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Pingdom = (props: BadgeProps) => <Badge name="Pingdom" {...props} backgroundColor="#FFF000" />

export default Pingdom
