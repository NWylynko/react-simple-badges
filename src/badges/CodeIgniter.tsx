
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CodeIgniter = (props: BadgeProps) => <Badge name="CodeIgniter" {...props} backgroundColor="#EF4223" />

export default CodeIgniter
