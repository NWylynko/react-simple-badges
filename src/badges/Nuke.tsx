
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Nuke = (props: BadgeProps) => <Badge name="Nuke" backgroundColor="#000000" {...props} />

export default Nuke
