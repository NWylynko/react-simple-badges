
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Nuke = (props: BadgeProps) => <Badge name="Nuke" {...props} backgroundColor="#000000" />

export default Nuke
