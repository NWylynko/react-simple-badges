
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AndroidStudio = (props: BadgeProps) => <Badge name="Android Studio" {...props} backgroundColor="#3DDC84" />

export default AndroidStudio
