
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ApacheNetBeansIDE = (props: BadgeProps) => <Badge name="Apache NetBeans IDE" {...props} backgroundColor="#1B6AC6" />

export default ApacheNetBeansIDE
