
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const ApacheNetBeansIDE = (props: BadgeProps) => <Badge name="Apache NetBeans IDE" backgroundColor="#1B6AC6" {...props} />

export default ApacheNetBeansIDE
