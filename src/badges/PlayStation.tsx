
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PlayStation = (props: BadgeProps) => <Badge name="PlayStation" {...props} backgroundColor="#003791" />

export default PlayStation
