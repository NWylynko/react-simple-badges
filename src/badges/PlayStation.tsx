
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const PlayStation = (props: BadgeProps) => <Badge name="PlayStation" backgroundColor="#003791" {...props} />

export default PlayStation
