
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Audacity = (props: BadgeProps) => <Badge name="Audacity" {...props} backgroundColor="#0000CC" />

export default Audacity
