
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Audacity = (props: BadgeProps) => <Badge name="Audacity" backgroundColor="#0000CC" {...props} />

export default Audacity
