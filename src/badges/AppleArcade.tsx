
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AppleArcade = (props: BadgeProps) => <Badge name="Apple Arcade" {...props} backgroundColor="#000000" />

export default AppleArcade
