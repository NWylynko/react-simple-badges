
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Fastly = (props: BadgeProps) => <Badge name="Fastly" backgroundColor="#FF282D" {...props} />

export default Fastly
