
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Fastly = (props: BadgeProps) => <Badge name="Fastly" {...props} backgroundColor="#FF282D" />

export default Fastly
