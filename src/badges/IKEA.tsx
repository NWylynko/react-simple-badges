
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const IKEA = (props: BadgeProps) => <Badge name="IKEA" {...props} backgroundColor="#0058A3" />

export default IKEA
