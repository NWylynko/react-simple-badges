
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SanDisk = (props: BadgeProps) => <Badge name="SanDisk" {...props} backgroundColor="#ED1C24" />

export default SanDisk
