
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const SanDisk = (props: BadgeProps) => <Badge name="SanDisk" backgroundColor="#ED1C24" {...props} />

export default SanDisk
