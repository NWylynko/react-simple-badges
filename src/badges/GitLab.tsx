
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GitLab = (props: BadgeProps) => <Badge name="GitLab" {...props} backgroundColor="#FCA121" />

export default GitLab
