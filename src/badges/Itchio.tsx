
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Itchio = (props: BadgeProps) => <Badge name="Itch.io" {...props} backgroundColor="#FA5C5C" />

export default Itchio
