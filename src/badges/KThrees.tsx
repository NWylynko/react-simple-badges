
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const KThrees = (props: BadgeProps) => <Badge name="K3s" backgroundColor="#FFC61C" {...props} />

export default KThrees
