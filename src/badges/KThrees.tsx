
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const KThrees = (props: BadgeProps) => <Badge name="K3s" {...props} backgroundColor="#FFC61C" />

export default KThrees
