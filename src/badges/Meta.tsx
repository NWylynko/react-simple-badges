
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Meta = (props: BadgeProps) => <Badge name="Meta" {...props} backgroundColor="#0467DF" />

export default Meta
