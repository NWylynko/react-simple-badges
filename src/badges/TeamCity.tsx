
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const TeamCity = (props: BadgeProps) => <Badge name="TeamCity" backgroundColor="#000000" {...props} />

export default TeamCity
