
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TeamCity = (props: BadgeProps) => <Badge name="TeamCity" {...props} backgroundColor="#000000" />

export default TeamCity
