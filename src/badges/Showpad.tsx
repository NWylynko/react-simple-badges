
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Showpad = (props: BadgeProps) => <Badge name="Showpad" {...props} backgroundColor="#2D2E83" />

export default Showpad
