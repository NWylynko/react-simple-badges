
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Showpad = (props: BadgeProps) => <Badge name="Showpad" backgroundColor="#2D2E83" {...props} />

export default Showpad
