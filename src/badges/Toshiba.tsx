
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Toshiba = (props: BadgeProps) => <Badge name="Toshiba" {...props} backgroundColor="#FF0000" />

export default Toshiba
