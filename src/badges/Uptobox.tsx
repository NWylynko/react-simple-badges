
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Uptobox = (props: BadgeProps) => <Badge name="Uptobox" {...props} backgroundColor="#5CE1E6" />

export default Uptobox
