
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Kong = (props: BadgeProps) => <Badge name="Kong" {...props} backgroundColor="#003459" />

export default Kong
