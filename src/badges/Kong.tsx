
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Kong = (props: BadgeProps) => <Badge name="Kong" backgroundColor="#003459" {...props} />

export default Kong
