
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Discogs = (props: BadgeProps) => <Badge name="Discogs" {...props} backgroundColor="#333333" />

export default Discogs
