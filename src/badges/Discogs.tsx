
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Discogs = (props: BadgeProps) => <Badge name="Discogs" backgroundColor="#333333" {...props} />

export default Discogs
