
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const FIFA = (props: BadgeProps) => <Badge name="FIFA" {...props} backgroundColor="#326295" />

export default FIFA
