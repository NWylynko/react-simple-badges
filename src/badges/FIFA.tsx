
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const FIFA = (props: BadgeProps) => <Badge name="FIFA" backgroundColor="#326295" {...props} />

export default FIFA
