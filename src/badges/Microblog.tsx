
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Microblog = (props: BadgeProps) => <Badge name="Micro.blog" {...props} backgroundColor="#FF8800" />

export default Microblog
