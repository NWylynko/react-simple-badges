
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Mixcloud = (props: BadgeProps) => <Badge name="Mixcloud" {...props} backgroundColor="#5000FF" />

export default Mixcloud
