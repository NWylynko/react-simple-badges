
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Mixcloud = (props: BadgeProps) => <Badge name="Mixcloud" backgroundColor="#5000FF" {...props} />

export default Mixcloud
