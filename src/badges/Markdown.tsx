
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Markdown = (props: BadgeProps) => <Badge name="Markdown" {...props} backgroundColor="#000000" />

export default Markdown
