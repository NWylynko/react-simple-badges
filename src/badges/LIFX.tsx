
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const LIFX = (props: BadgeProps) => <Badge name="LIFX" {...props} backgroundColor="#000000" />

export default LIFX
