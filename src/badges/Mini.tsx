
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Mini = (props: BadgeProps) => <Badge name="Mini" {...props} backgroundColor="#000000" />

export default Mini
