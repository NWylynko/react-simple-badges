
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Mini = (props: BadgeProps) => <Badge name="Mini" backgroundColor="#000000" {...props} />

export default Mini
