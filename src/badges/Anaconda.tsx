
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Anaconda = (props: BadgeProps) => <Badge name="Anaconda" {...props} backgroundColor="#44A833" />

export default Anaconda
