
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Epson = (props: BadgeProps) => <Badge name="Epson" {...props} backgroundColor="#003399" />

export default Epson
