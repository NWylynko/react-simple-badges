
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Relay = (props: BadgeProps) => <Badge name="Relay" {...props} backgroundColor="#F26B00" />

export default Relay
