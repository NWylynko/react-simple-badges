
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Relay = (props: BadgeProps) => <Badge name="Relay" backgroundColor="#F26B00" {...props} />

export default Relay
