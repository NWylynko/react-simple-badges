
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Staffbase = (props: BadgeProps) => <Badge name="Staffbase" {...props} backgroundColor="#00A4FD" />

export default Staffbase
