
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Staffbase = (props: BadgeProps) => <Badge name="Staffbase" backgroundColor="#00A4FD" {...props} />

export default Staffbase
