
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Scania = (props: BadgeProps) => <Badge name="Scania" {...props} backgroundColor="#041E42" />

export default Scania
