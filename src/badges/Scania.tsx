
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Scania = (props: BadgeProps) => <Badge name="Scania" backgroundColor="#041E42" {...props} />

export default Scania
