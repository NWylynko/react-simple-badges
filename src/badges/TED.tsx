
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TED = (props: BadgeProps) => <Badge name="TED" {...props} backgroundColor="#E62B1E" />

export default TED
