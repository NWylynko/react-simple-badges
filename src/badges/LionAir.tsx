
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const LionAir = (props: BadgeProps) => <Badge name="Lion Air" {...props} backgroundColor="#ED3237" />

export default LionAir
