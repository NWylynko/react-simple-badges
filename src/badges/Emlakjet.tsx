
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Emlakjet = (props: BadgeProps) => <Badge name="Emlakjet" {...props} backgroundColor="#0AE524" />

export default Emlakjet
