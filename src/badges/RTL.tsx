
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const RTL = (props: BadgeProps) => <Badge name="RTL" backgroundColor="#E9113B" {...props} />

export default RTL
