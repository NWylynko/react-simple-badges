
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const RTL = (props: BadgeProps) => <Badge name="RTL" {...props} backgroundColor="#E9113B" />

export default RTL
