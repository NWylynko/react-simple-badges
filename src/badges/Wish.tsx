
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Wish = (props: BadgeProps) => <Badge name="Wish" {...props} backgroundColor="#2FB7EC" />

export default Wish
