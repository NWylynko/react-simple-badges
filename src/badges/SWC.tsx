
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SWC = (props: BadgeProps) => <Badge name="SWC" {...props} backgroundColor="#FFFFFF" />

export default SWC
