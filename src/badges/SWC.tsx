
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const SWC = (props: BadgeProps) => <Badge name="SWC" backgroundColor="#FFFFFF" {...props} />

export default SWC
