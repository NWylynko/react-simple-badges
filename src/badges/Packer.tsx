
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Packer = (props: BadgeProps) => <Badge name="Packer" {...props} backgroundColor="#02A8EF" />

export default Packer
