
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const HomeAdvisor = (props: BadgeProps) => <Badge name="HomeAdvisor" {...props} backgroundColor="#F68315" />

export default HomeAdvisor
