
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SoundCloud = (props: BadgeProps) => <Badge name="SoundCloud" {...props} backgroundColor="#FF3300" />

export default SoundCloud
