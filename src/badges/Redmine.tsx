
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Redmine = (props: BadgeProps) => <Badge name="Redmine" backgroundColor="#B32024" {...props} />

export default Redmine
