
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Redmine = (props: BadgeProps) => <Badge name="Redmine" {...props} backgroundColor="#B32024" />

export default Redmine
