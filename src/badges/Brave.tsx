
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Brave = (props: BadgeProps) => <Badge name="Brave" {...props} backgroundColor="#FB542B" />

export default Brave
