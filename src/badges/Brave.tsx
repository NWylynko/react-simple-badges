
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Brave = (props: BadgeProps) => <Badge name="Brave" backgroundColor="#FB542B" {...props} />

export default Brave
