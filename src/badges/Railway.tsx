
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Railway = (props: BadgeProps) => <Badge name="Railway" backgroundColor="#0B0D0E" {...props} />

export default Railway
