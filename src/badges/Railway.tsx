
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Railway = (props: BadgeProps) => <Badge name="Railway" {...props} backgroundColor="#0B0D0E" />

export default Railway
