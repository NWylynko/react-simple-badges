
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const ApachePulsar = (props: BadgeProps) => <Badge name="Apache Pulsar" backgroundColor="#188FFF" {...props} />

export default ApachePulsar
