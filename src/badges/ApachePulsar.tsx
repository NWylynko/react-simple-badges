
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ApachePulsar = (props: BadgeProps) => <Badge name="Apache Pulsar" {...props} backgroundColor="#188FFF" />

export default ApachePulsar
