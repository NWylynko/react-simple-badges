
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Simplenote = (props: BadgeProps) => <Badge name="Simplenote" {...props} backgroundColor="#3361CC" />

export default Simplenote
