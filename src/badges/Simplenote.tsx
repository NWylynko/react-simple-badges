
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Simplenote = (props: BadgeProps) => <Badge name="Simplenote" backgroundColor="#3361CC" {...props} />

export default Simplenote
