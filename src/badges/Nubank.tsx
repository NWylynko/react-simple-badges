
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Nubank = (props: BadgeProps) => <Badge name="Nubank" {...props} backgroundColor="#820AD1" />

export default Nubank
