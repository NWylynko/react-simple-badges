
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Nubank = (props: BadgeProps) => <Badge name="Nubank" backgroundColor="#820AD1" {...props} />

export default Nubank
