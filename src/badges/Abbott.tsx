
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Abbott = (props: BadgeProps) => <Badge name="Abbott" {...props} backgroundColor="#008FC7" />

export default Abbott
