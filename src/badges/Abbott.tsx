
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Abbott = (props: BadgeProps) => <Badge name="Abbott" backgroundColor="#008FC7" {...props} />

export default Abbott
