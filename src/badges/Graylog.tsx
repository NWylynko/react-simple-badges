
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Graylog = (props: BadgeProps) => <Badge name="Graylog" backgroundColor="#FF3633" {...props} />

export default Graylog
