
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Graylog = (props: BadgeProps) => <Badge name="Graylog" {...props} backgroundColor="#FF3633" />

export default Graylog
