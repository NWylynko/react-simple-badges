
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Vuetify = (props: BadgeProps) => <Badge name="Vuetify" {...props} backgroundColor="#1867C0" />

export default Vuetify
