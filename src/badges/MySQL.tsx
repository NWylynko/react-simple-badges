
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MySQL = (props: BadgeProps) => <Badge name="MySQL" {...props} backgroundColor="#4479A1" />

export default MySQL
