
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const MySQL = (props: BadgeProps) => <Badge name="MySQL" backgroundColor="#4479A1" {...props} />

export default MySQL
