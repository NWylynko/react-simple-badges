
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Threema = (props: BadgeProps) => <Badge name="Threema" {...props} backgroundColor="#3FE669" />

export default Threema
