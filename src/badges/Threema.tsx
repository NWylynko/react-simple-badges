
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Threema = (props: BadgeProps) => <Badge name="Threema" backgroundColor="#3FE669" {...props} />

export default Threema
