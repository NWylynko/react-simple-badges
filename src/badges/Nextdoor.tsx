
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Nextdoor = (props: BadgeProps) => <Badge name="Nextdoor" {...props} backgroundColor="#8ED500" />

export default Nextdoor
