
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const OpenStreetMap = (props: BadgeProps) => <Badge name="OpenStreetMap" {...props} backgroundColor="#7EBC6F" />

export default OpenStreetMap
