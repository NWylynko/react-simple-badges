
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CMake = (props: BadgeProps) => <Badge name="CMake" {...props} backgroundColor="#064F8C" />

export default CMake
