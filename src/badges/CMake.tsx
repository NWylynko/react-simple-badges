
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const CMake = (props: BadgeProps) => <Badge name="CMake" backgroundColor="#064F8C" {...props} />

export default CMake
