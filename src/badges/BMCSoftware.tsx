
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const BMCSoftware = (props: BadgeProps) => <Badge name="BMC Software" {...props} backgroundColor="#FE5000" />

export default BMCSoftware
