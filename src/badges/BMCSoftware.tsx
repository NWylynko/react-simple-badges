
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const BMCSoftware = (props: BadgeProps) => <Badge name="BMC Software" backgroundColor="#FE5000" {...props} />

export default BMCSoftware
