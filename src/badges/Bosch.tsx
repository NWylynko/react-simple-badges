
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Bosch = (props: BadgeProps) => <Badge name="Bosch" {...props} backgroundColor="#EA0016" />

export default Bosch
