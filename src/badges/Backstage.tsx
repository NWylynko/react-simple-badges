
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Backstage = (props: BadgeProps) => <Badge name="Backstage" backgroundColor="#9BF0E1" {...props} />

export default Backstage
