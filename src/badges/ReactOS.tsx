
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ReactOS = (props: BadgeProps) => <Badge name="ReactOS" {...props} backgroundColor="#0088CC" />

export default ReactOS
