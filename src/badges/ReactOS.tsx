
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const ReactOS = (props: BadgeProps) => <Badge name="ReactOS" backgroundColor="#0088CC" {...props} />

export default ReactOS
