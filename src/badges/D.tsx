
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const D = (props: BadgeProps) => <Badge name="D" backgroundColor="#B03931" {...props} />

export default D
