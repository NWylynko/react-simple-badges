
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const D = (props: BadgeProps) => <Badge name="D" {...props} backgroundColor="#B03931" />

export default D
