
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Vapor = (props: BadgeProps) => <Badge name="Vapor" backgroundColor="#0D0D0D" {...props} />

export default Vapor
