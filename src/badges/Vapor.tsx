
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Vapor = (props: BadgeProps) => <Badge name="Vapor" {...props} backgroundColor="#0D0D0D" />

export default Vapor
