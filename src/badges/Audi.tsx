
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Audi = (props: BadgeProps) => <Badge name="Audi" backgroundColor="#BB0A30" {...props} />

export default Audi
