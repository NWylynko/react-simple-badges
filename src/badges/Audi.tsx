
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Audi = (props: BadgeProps) => <Badge name="Audi" {...props} backgroundColor="#BB0A30" />

export default Audi
