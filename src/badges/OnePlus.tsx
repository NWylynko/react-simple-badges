
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const OnePlus = (props: BadgeProps) => <Badge name="OnePlus" {...props} backgroundColor="#F5010C" />

export default OnePlus
