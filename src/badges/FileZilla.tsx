
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const FileZilla = (props: BadgeProps) => <Badge name="FileZilla" backgroundColor="#BF0000" {...props} />

export default FileZilla
