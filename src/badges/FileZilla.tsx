
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const FileZilla = (props: BadgeProps) => <Badge name="FileZilla" {...props} backgroundColor="#BF0000" />

export default FileZilla
