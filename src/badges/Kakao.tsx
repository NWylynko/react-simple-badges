
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Kakao = (props: BadgeProps) => <Badge name="Kakao" backgroundColor="#FFCD00" {...props} />

export default Kakao
