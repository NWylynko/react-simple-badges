
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Kakao = (props: BadgeProps) => <Badge name="Kakao" {...props} backgroundColor="#FFCD00" />

export default Kakao
