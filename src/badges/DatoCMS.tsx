
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const DatoCMS = (props: BadgeProps) => <Badge name="DatoCMS" {...props} backgroundColor="#FF7751" />

export default DatoCMS
