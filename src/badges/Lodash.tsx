
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Lodash = (props: BadgeProps) => <Badge name="Lodash" {...props} backgroundColor="#3492FF" />

export default Lodash
