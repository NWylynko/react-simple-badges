
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const KaliLinux = (props: BadgeProps) => <Badge name="Kali Linux" {...props} backgroundColor="#557C94" />

export default KaliLinux
