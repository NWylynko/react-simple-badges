
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const JRGroup = (props: BadgeProps) => <Badge name="JR Group" backgroundColor="#000000" {...props} />

export default JRGroup
