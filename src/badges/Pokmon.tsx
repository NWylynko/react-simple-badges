
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Pokmon = (props: BadgeProps) => <Badge name="Pokémon" backgroundColor="#FFCB05" {...props} />

export default Pokmon
