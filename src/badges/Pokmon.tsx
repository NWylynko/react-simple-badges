
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Pokmon = (props: BadgeProps) => <Badge name="Pokémon" {...props} backgroundColor="#FFCB05" />

export default Pokmon
