
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const EDEKA = (props: BadgeProps) => <Badge name="EDEKA" {...props} backgroundColor="#1B66B3" />

export default EDEKA
