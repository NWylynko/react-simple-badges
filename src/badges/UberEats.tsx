
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const UberEats = (props: BadgeProps) => <Badge name="Uber Eats" {...props} backgroundColor="#06C167" />

export default UberEats
