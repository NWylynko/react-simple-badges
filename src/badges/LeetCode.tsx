
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const LeetCode = (props: BadgeProps) => <Badge name="LeetCode" backgroundColor="#FFA116" {...props} />

export default LeetCode
