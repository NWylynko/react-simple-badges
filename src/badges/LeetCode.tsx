
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const LeetCode = (props: BadgeProps) => <Badge name="LeetCode" {...props} backgroundColor="#FFA116" />

export default LeetCode
