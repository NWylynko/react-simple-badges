
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const C = (props: BadgeProps) => <Badge name="C" {...props} backgroundColor="#A8B9CC" />

export default C
