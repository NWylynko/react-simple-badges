
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Zenodo = (props: BadgeProps) => <Badge name="Zenodo" {...props} backgroundColor="#1682D4" />

export default Zenodo
