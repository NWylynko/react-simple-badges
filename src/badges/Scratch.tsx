
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Scratch = (props: BadgeProps) => <Badge name="Scratch" {...props} backgroundColor="#4D97FF" />

export default Scratch
