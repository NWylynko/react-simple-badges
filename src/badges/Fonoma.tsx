
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Fonoma = (props: BadgeProps) => <Badge name="Fonoma" {...props} backgroundColor="#02B78F" />

export default Fonoma
