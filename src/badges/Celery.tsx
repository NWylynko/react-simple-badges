
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Celery = (props: BadgeProps) => <Badge name="Celery" backgroundColor="#37814A" {...props} />

export default Celery
