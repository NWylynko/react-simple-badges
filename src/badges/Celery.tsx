
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Celery = (props: BadgeProps) => <Badge name="Celery" {...props} backgroundColor="#37814A" />

export default Celery
