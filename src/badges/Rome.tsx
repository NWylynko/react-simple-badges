
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Rome = (props: BadgeProps) => <Badge name="Rome" {...props} backgroundColor="#27272A" />

export default Rome
