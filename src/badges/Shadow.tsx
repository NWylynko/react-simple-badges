
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Shadow = (props: BadgeProps) => <Badge name="Shadow" {...props} backgroundColor="#0A0C0D" />

export default Shadow
