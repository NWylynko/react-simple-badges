
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const TravisCI = (props: BadgeProps) => <Badge name="Travis CI" backgroundColor="#3EAAAF" {...props} />

export default TravisCI
