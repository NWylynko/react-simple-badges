
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TravisCI = (props: BadgeProps) => <Badge name="Travis CI" {...props} backgroundColor="#3EAAAF" />

export default TravisCI
