
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Arduino = (props: BadgeProps) => <Badge name="Arduino" backgroundColor="#00979D" {...props} />

export default Arduino
