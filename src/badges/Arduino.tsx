
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Arduino = (props: BadgeProps) => <Badge name="Arduino" {...props} backgroundColor="#00979D" />

export default Arduino
