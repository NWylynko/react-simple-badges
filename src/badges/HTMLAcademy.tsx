
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const HTMLAcademy = (props: BadgeProps) => <Badge name="HTML Academy" {...props} backgroundColor="#302683" />

export default HTMLAcademy
