
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const NBB = (props: BadgeProps) => <Badge name="NBB" {...props} backgroundColor="#FF7100" />

export default NBB
