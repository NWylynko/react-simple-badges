
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GoogleMaps = (props: BadgeProps) => <Badge name="Google Maps" {...props} backgroundColor="#4285F4" />

export default GoogleMaps
