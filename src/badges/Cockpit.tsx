
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Cockpit = (props: BadgeProps) => <Badge name="Cockpit" backgroundColor="#0066CC" {...props} />

export default Cockpit
