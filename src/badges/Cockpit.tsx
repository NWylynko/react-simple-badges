
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Cockpit = (props: BadgeProps) => <Badge name="Cockpit" {...props} backgroundColor="#0066CC" />

export default Cockpit
