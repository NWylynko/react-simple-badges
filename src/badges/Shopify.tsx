
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Shopify = (props: BadgeProps) => <Badge name="Shopify" {...props} backgroundColor="#7AB55C" />

export default Shopify
