
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Shopify = (props: BadgeProps) => <Badge name="Shopify" backgroundColor="#7AB55C" {...props} />

export default Shopify
