
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Directus = (props: BadgeProps) => <Badge name="Directus" {...props} backgroundColor="#263238" />

export default Directus
