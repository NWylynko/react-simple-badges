
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Directus = (props: BadgeProps) => <Badge name="Directus" backgroundColor="#263238" {...props} />

export default Directus
