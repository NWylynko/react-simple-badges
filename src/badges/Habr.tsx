
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Habr = (props: BadgeProps) => <Badge name="Habr" backgroundColor="#65A3BE" {...props} />

export default Habr
