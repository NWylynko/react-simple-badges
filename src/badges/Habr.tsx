
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Habr = (props: BadgeProps) => <Badge name="Habr" {...props} backgroundColor="#65A3BE" />

export default Habr
