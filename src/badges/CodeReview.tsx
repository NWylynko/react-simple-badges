
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CodeReview = (props: BadgeProps) => <Badge name="Code Review" {...props} backgroundColor="#485A62" />

export default CodeReview
