
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Amazon = (props: BadgeProps) => <Badge name="Amazon" {...props} backgroundColor="#FF9900" />

export default Amazon
