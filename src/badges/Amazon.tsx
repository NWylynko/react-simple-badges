
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Amazon = (props: BadgeProps) => <Badge name="Amazon" backgroundColor="#FF9900" {...props} />

export default Amazon
