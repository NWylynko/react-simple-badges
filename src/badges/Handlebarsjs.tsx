
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Handlebarsjs = (props: BadgeProps) => <Badge name="Handlebars.js" {...props} backgroundColor="#000000" />

export default Handlebarsjs
