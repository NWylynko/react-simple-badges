
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const WeChat = (props: BadgeProps) => <Badge name="WeChat" {...props} backgroundColor="#07C160" />

export default WeChat
