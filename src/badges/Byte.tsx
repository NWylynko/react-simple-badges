
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Byte = (props: BadgeProps) => <Badge name="byte" {...props} backgroundColor="#551DEF" />

export default Byte
