
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Byte = (props: BadgeProps) => <Badge name="byte" backgroundColor="#551DEF" {...props} />

export default Byte
