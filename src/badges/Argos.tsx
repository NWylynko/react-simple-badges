
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Argos = (props: BadgeProps) => <Badge name="Argos" backgroundColor="#DA291C" {...props} />

export default Argos
