
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Argos = (props: BadgeProps) => <Badge name="Argos" {...props} backgroundColor="#DA291C" />

export default Argos
