
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AmazonRDS = (props: BadgeProps) => <Badge name="Amazon RDS" {...props} backgroundColor="#527FFF" />

export default AmazonRDS
