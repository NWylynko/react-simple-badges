
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const AmazonRDS = (props: BadgeProps) => <Badge name="Amazon RDS" backgroundColor="#527FFF" {...props} />

export default AmazonRDS
