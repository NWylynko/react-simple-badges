
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Paytm = (props: BadgeProps) => <Badge name="Paytm" backgroundColor="#20336B" {...props} />

export default Paytm
