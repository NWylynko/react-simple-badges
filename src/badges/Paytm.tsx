
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Paytm = (props: BadgeProps) => <Badge name="Paytm" {...props} backgroundColor="#20336B" />

export default Paytm
