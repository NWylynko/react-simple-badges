
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Otto = (props: BadgeProps) => <Badge name="Otto" {...props} backgroundColor="#D4021D" />

export default Otto
