
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Quarkus = (props: BadgeProps) => <Badge name="Quarkus" {...props} backgroundColor="#4695EB" />

export default Quarkus
