
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Quarkus = (props: BadgeProps) => <Badge name="Quarkus" backgroundColor="#4695EB" {...props} />

export default Quarkus
