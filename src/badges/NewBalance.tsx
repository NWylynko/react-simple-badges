
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const NewBalance = (props: BadgeProps) => <Badge name="New Balance" {...props} backgroundColor="#CF0A2C" />

export default NewBalance
