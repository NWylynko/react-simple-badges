
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const NewBalance = (props: BadgeProps) => <Badge name="New Balance" backgroundColor="#CF0A2C" {...props} />

export default NewBalance
