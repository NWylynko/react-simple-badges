
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Expensify = (props: BadgeProps) => <Badge name="Expensify" {...props} backgroundColor="#0185FF" />

export default Expensify
