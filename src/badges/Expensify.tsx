
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Expensify = (props: BadgeProps) => <Badge name="Expensify" backgroundColor="#0185FF" {...props} />

export default Expensify
