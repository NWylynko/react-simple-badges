
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AppleTV = (props: BadgeProps) => <Badge name="Apple TV" {...props} backgroundColor="#000000" />

export default AppleTV
