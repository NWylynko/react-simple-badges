
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Testin = (props: BadgeProps) => <Badge name="Testin" {...props} backgroundColor="#007DD7" />

export default Testin
