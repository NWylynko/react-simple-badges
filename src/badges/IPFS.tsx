
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const IPFS = (props: BadgeProps) => <Badge name="IPFS" {...props} backgroundColor="#65C2CB" />

export default IPFS
