
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const IPFS = (props: BadgeProps) => <Badge name="IPFS" backgroundColor="#65C2CB" {...props} />

export default IPFS
