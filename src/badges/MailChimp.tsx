
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MailChimp = (props: BadgeProps) => <Badge name="MailChimp" {...props} backgroundColor="#FFE01B" />

export default MailChimp
