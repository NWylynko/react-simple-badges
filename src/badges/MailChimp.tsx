
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const MailChimp = (props: BadgeProps) => <Badge name="MailChimp" backgroundColor="#FFE01B" {...props} />

export default MailChimp
