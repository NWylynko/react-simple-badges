
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PhilipsHue = (props: BadgeProps) => <Badge name="Philips Hue" {...props} backgroundColor="#0065D3" />

export default PhilipsHue
