
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const KnowledgeBase = (props: BadgeProps) => <Badge name="KnowledgeBase" {...props} backgroundColor="#FFD000" />

export default KnowledgeBase
