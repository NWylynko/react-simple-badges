
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const MdBook = (props: BadgeProps) => <Badge name="mdBook" backgroundColor="#000000" {...props} />

export default MdBook
