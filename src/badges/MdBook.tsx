
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MdBook = (props: BadgeProps) => <Badge name="mdBook" {...props} backgroundColor="#000000" />

export default MdBook
