
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Redis = (props: BadgeProps) => <Badge name="Redis" {...props} backgroundColor="#DC382D" />

export default Redis
