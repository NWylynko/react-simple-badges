
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Redis = (props: BadgeProps) => <Badge name="Redis" backgroundColor="#DC382D" {...props} />

export default Redis
