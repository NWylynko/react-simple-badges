
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Keybase = (props: BadgeProps) => <Badge name="Keybase" backgroundColor="#33A0FF" {...props} />

export default Keybase
