
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Keybase = (props: BadgeProps) => <Badge name="Keybase" {...props} backgroundColor="#33A0FF" />

export default Keybase
