
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Aseprite = (props: BadgeProps) => <Badge name="Aseprite" {...props} backgroundColor="#7D929E" />

export default Aseprite
