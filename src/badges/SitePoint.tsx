
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const SitePoint = (props: BadgeProps) => <Badge name="SitePoint" backgroundColor="#258AAF" {...props} />

export default SitePoint
