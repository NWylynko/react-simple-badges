
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SitePoint = (props: BadgeProps) => <Badge name="SitePoint" {...props} backgroundColor="#258AAF" />

export default SitePoint
