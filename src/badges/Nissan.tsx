
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Nissan = (props: BadgeProps) => <Badge name="Nissan" {...props} backgroundColor="#C3002F" />

export default Nissan
