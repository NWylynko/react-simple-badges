
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Nissan = (props: BadgeProps) => <Badge name="Nissan" backgroundColor="#C3002F" {...props} />

export default Nissan
