
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Plotly = (props: BadgeProps) => <Badge name="Plotly" {...props} backgroundColor="#3F4F75" />

export default Plotly
