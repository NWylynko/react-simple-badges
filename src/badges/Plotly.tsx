
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Plotly = (props: BadgeProps) => <Badge name="Plotly" backgroundColor="#3F4F75" {...props} />

export default Plotly
