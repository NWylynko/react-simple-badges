
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PeerTube = (props: BadgeProps) => <Badge name="PeerTube" {...props} backgroundColor="#F1680D" />

export default PeerTube
