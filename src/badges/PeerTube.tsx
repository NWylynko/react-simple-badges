
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const PeerTube = (props: BadgeProps) => <Badge name="PeerTube" backgroundColor="#F1680D" {...props} />

export default PeerTube
