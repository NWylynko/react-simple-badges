
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ReasonStudios = (props: BadgeProps) => <Badge name="Reason Studios" {...props} backgroundColor="#FFFFFF" />

export default ReasonStudios
