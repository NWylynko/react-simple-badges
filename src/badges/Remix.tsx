
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Remix = (props: BadgeProps) => <Badge name="Remix" {...props} backgroundColor="#000000" />

export default Remix
