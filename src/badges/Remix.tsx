
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Remix = (props: BadgeProps) => <Badge name="Remix" backgroundColor="#000000" {...props} />

export default Remix
