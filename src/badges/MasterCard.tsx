
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MasterCard = (props: BadgeProps) => <Badge name="MasterCard" {...props} backgroundColor="#EB001B" />

export default MasterCard
