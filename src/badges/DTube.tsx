
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const DTube = (props: BadgeProps) => <Badge name="DTube" {...props} backgroundColor="#F01A30" />

export default DTube
