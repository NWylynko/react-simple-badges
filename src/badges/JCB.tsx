
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const JCB = (props: BadgeProps) => <Badge name="JCB" {...props} backgroundColor="#0B4EA2" />

export default JCB
