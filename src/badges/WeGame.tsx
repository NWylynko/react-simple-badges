
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const WeGame = (props: BadgeProps) => <Badge name="WeGame" backgroundColor="#FAAB00" {...props} />

export default WeGame
