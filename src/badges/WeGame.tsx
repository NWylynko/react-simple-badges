
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const WeGame = (props: BadgeProps) => <Badge name="WeGame" {...props} backgroundColor="#FAAB00" />

export default WeGame
