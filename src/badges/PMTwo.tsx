
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PMTwo = (props: BadgeProps) => <Badge name="PM2" {...props} backgroundColor="#2B037A" />

export default PMTwo
