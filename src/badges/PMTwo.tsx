
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const PMTwo = (props: BadgeProps) => <Badge name="PM2" backgroundColor="#2B037A" {...props} />

export default PMTwo
