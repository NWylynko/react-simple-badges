
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Tesla = (props: BadgeProps) => <Badge name="Tesla" {...props} backgroundColor="#CC0000" />

export default Tesla
