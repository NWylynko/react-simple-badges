
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Tesla = (props: BadgeProps) => <Badge name="Tesla" backgroundColor="#CC0000" {...props} />

export default Tesla
