
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Sahibinden = (props: BadgeProps) => <Badge name="Sahibinden" {...props} backgroundColor="#FFE800" />

export default Sahibinden
