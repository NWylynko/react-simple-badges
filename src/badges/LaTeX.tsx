
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const LaTeX = (props: BadgeProps) => <Badge name="LaTeX" {...props} backgroundColor="#008080" />

export default LaTeX
