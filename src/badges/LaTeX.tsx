
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const LaTeX = (props: BadgeProps) => <Badge name="LaTeX" backgroundColor="#008080" {...props} />

export default LaTeX
