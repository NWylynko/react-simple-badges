
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const UBlockOrigin = (props: BadgeProps) => <Badge name="uBlock Origin" backgroundColor="#800000" {...props} />

export default UBlockOrigin
