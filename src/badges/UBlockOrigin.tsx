
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const UBlockOrigin = (props: BadgeProps) => <Badge name="uBlock Origin" {...props} backgroundColor="#800000" />

export default UBlockOrigin
