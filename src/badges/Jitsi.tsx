
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Jitsi = (props: BadgeProps) => <Badge name="Jitsi" {...props} backgroundColor="#97979A" />

export default Jitsi
