
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Laragon = (props: BadgeProps) => <Badge name="Laragon" backgroundColor="#0E83CD" {...props} />

export default Laragon
