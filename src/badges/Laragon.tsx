
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Laragon = (props: BadgeProps) => <Badge name="Laragon" {...props} backgroundColor="#0E83CD" />

export default Laragon
