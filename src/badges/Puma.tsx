
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Puma = (props: BadgeProps) => <Badge name="Puma" {...props} backgroundColor="#242B2F" />

export default Puma
