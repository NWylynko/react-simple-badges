
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Flask = (props: BadgeProps) => <Badge name="Flask" {...props} backgroundColor="#000000" />

export default Flask
