
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Flask = (props: BadgeProps) => <Badge name="Flask" backgroundColor="#000000" {...props} />

export default Flask
