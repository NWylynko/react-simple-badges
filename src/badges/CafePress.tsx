
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CafePress = (props: BadgeProps) => <Badge name="CafePress" {...props} backgroundColor="#58A616" />

export default CafePress
