
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Loop = (props: BadgeProps) => <Badge name="Loop" {...props} backgroundColor="#F29400" />

export default Loop
