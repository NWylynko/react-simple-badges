
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Loop = (props: BadgeProps) => <Badge name="Loop" backgroundColor="#F29400" {...props} />

export default Loop
