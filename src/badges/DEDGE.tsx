
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const DEDGE = (props: BadgeProps) => <Badge name="D-EDGE" backgroundColor="#432975" {...props} />

export default DEDGE
