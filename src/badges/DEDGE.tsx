
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const DEDGE = (props: BadgeProps) => <Badge name="D-EDGE" {...props} backgroundColor="#432975" />

export default DEDGE
