
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Untappd = (props: BadgeProps) => <Badge name="Untappd" {...props} backgroundColor="#FFC000" />

export default Untappd
