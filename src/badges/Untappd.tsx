
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Untappd = (props: BadgeProps) => <Badge name="Untappd" backgroundColor="#FFC000" {...props} />

export default Untappd
