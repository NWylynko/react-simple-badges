
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Chemex = (props: BadgeProps) => <Badge name="Chemex" {...props} backgroundColor="#4D2B1A" />

export default Chemex
