
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Fossa = (props: BadgeProps) => <Badge name="Fossa" backgroundColor="#289E6D" {...props} />

export default Fossa
