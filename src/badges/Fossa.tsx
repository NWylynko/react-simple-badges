
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Fossa = (props: BadgeProps) => <Badge name="Fossa" {...props} backgroundColor="#289E6D" />

export default Fossa
