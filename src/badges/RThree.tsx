
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const RThree = (props: BadgeProps) => <Badge name="R3" {...props} backgroundColor="#EC1D24" />

export default RThree
