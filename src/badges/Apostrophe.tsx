
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Apostrophe = (props: BadgeProps) => <Badge name="Apostrophe" {...props} backgroundColor="#6236FF" />

export default Apostrophe
