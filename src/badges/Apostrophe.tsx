
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Apostrophe = (props: BadgeProps) => <Badge name="Apostrophe" backgroundColor="#6236FF" {...props} />

export default Apostrophe
