
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const ConstructThree = (props: BadgeProps) => <Badge name="Construct 3" backgroundColor="#00FFDA" {...props} />

export default ConstructThree
