
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ConstructThree = (props: BadgeProps) => <Badge name="Construct 3" {...props} backgroundColor="#00FFDA" />

export default ConstructThree
