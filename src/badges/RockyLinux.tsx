
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const RockyLinux = (props: BadgeProps) => <Badge name="Rocky Linux" {...props} backgroundColor="#10B981" />

export default RockyLinux
