
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const RockyLinux = (props: BadgeProps) => <Badge name="Rocky Linux" backgroundColor="#10B981" {...props} />

export default RockyLinux
