
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const FugaCloud = (props: BadgeProps) => <Badge name="Fuga Cloud" {...props} backgroundColor="#242F4B" />

export default FugaCloud
