
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const FugaCloud = (props: BadgeProps) => <Badge name="Fuga Cloud" backgroundColor="#242F4B" {...props} />

export default FugaCloud
