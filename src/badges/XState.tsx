
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const XState = (props: BadgeProps) => <Badge name="XState" {...props} backgroundColor="#2C3E50" />

export default XState
