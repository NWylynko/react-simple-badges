
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const XState = (props: BadgeProps) => <Badge name="XState" backgroundColor="#2C3E50" {...props} />

export default XState
