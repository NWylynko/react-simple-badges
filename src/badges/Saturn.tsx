
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Saturn = (props: BadgeProps) => <Badge name="Saturn" backgroundColor="#EB680B" {...props} />

export default Saturn
