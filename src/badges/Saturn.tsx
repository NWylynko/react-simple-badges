
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Saturn = (props: BadgeProps) => <Badge name="Saturn" {...props} backgroundColor="#EB680B" />

export default Saturn
