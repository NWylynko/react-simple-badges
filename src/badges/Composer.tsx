
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Composer = (props: BadgeProps) => <Badge name="Composer" {...props} backgroundColor="#885630" />

export default Composer
