
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Composer = (props: BadgeProps) => <Badge name="Composer" backgroundColor="#885630" {...props} />

export default Composer
