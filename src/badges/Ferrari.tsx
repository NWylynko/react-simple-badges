
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Ferrari = (props: BadgeProps) => <Badge name="Ferrari" {...props} backgroundColor="#D40000" />

export default Ferrari
