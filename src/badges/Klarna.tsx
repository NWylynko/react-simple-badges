
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Klarna = (props: BadgeProps) => <Badge name="Klarna" backgroundColor="#FFB3C7" {...props} />

export default Klarna
