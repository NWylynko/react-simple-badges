
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Klarna = (props: BadgeProps) => <Badge name="Klarna" {...props} backgroundColor="#FFB3C7" />

export default Klarna
