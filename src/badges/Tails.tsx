
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Tails = (props: BadgeProps) => <Badge name="Tails" {...props} backgroundColor="#56347C" />

export default Tails
