
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Tails = (props: BadgeProps) => <Badge name="Tails" backgroundColor="#56347C" {...props} />

export default Tails
