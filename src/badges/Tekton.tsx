
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Tekton = (props: BadgeProps) => <Badge name="Tekton" {...props} backgroundColor="#FD495C" />

export default Tekton
