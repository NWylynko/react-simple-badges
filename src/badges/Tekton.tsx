
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Tekton = (props: BadgeProps) => <Badge name="Tekton" backgroundColor="#FD495C" {...props} />

export default Tekton
