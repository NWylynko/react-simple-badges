
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const FedEx = (props: BadgeProps) => <Badge name="FedEx" {...props} backgroundColor="#4D148C" />

export default FedEx
