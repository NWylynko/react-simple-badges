
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const WeightsBiases = (props: BadgeProps) => <Badge name="Weights & Biases" {...props} backgroundColor="#FFBE00" />

export default WeightsBiases
