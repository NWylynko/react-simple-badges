
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const WeightsBiases = (props: BadgeProps) => <Badge name="Weights & Biases" backgroundColor="#FFBE00" {...props} />

export default WeightsBiases
