
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Webmin = (props: BadgeProps) => <Badge name="Webmin" {...props} backgroundColor="#7DA0D0" />

export default Webmin
