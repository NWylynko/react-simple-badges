
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Webmin = (props: BadgeProps) => <Badge name="Webmin" backgroundColor="#7DA0D0" {...props} />

export default Webmin
