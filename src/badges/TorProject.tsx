
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TorProject = (props: BadgeProps) => <Badge name="Tor Project" {...props} backgroundColor="#7E4798" />

export default TorProject
