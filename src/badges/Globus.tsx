
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Globus = (props: BadgeProps) => <Badge name="Globus" {...props} backgroundColor="#CA6201" />

export default Globus
