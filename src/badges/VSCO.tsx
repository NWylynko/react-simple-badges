
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const VSCO = (props: BadgeProps) => <Badge name="VSCO" backgroundColor="#000000" {...props} />

export default VSCO
