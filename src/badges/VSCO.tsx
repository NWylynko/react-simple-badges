
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const VSCO = (props: BadgeProps) => <Badge name="VSCO" {...props} backgroundColor="#000000" />

export default VSCO
