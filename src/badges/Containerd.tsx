
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Containerd = (props: BadgeProps) => <Badge name="containerd" backgroundColor="#575757" {...props} />

export default Containerd
