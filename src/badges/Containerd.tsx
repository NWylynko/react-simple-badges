
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Containerd = (props: BadgeProps) => <Badge name="containerd" {...props} backgroundColor="#575757" />

export default Containerd
