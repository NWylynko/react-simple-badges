
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const WALKMAN = (props: BadgeProps) => <Badge name="WALKMAN" {...props} backgroundColor="#000000" />

export default WALKMAN
