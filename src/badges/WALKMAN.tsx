
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const WALKMAN = (props: BadgeProps) => <Badge name="WALKMAN" backgroundColor="#000000" {...props} />

export default WALKMAN
