
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const BEM = (props: BadgeProps) => <Badge name="BEM" backgroundColor="#000000" {...props} />

export default BEM
