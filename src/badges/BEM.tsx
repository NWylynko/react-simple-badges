
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const BEM = (props: BadgeProps) => <Badge name="BEM" {...props} backgroundColor="#000000" />

export default BEM
