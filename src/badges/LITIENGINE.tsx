
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const LITIENGINE = (props: BadgeProps) => <Badge name="LITIENGINE" {...props} backgroundColor="#00A5BC" />

export default LITIENGINE
