
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const LITIENGINE = (props: BadgeProps) => <Badge name="LITIENGINE" backgroundColor="#00A5BC" {...props} />

export default LITIENGINE
