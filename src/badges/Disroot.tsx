
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Disroot = (props: BadgeProps) => <Badge name="Disroot" {...props} backgroundColor="#50162D" />

export default Disroot
