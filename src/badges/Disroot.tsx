
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Disroot = (props: BadgeProps) => <Badge name="Disroot" backgroundColor="#50162D" {...props} />

export default Disroot
