
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const DataStax = (props: BadgeProps) => <Badge name="DataStax" {...props} backgroundColor="#3A3A42" />

export default DataStax
