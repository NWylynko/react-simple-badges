
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const DataStax = (props: BadgeProps) => <Badge name="DataStax" backgroundColor="#3A3A42" {...props} />

export default DataStax
