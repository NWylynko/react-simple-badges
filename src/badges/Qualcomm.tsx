
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Qualcomm = (props: BadgeProps) => <Badge name="Qualcomm" {...props} backgroundColor="#3253DC" />

export default Qualcomm
