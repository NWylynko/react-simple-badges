
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Qualcomm = (props: BadgeProps) => <Badge name="Qualcomm" backgroundColor="#3253DC" {...props} />

export default Qualcomm
