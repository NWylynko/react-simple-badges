
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Lidl = (props: BadgeProps) => <Badge name="Lidl" {...props} backgroundColor="#0050AA" />

export default Lidl
