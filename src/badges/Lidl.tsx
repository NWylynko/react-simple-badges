
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Lidl = (props: BadgeProps) => <Badge name="Lidl" backgroundColor="#0050AA" {...props} />

export default Lidl
