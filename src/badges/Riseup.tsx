
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Riseup = (props: BadgeProps) => <Badge name="Riseup" {...props} backgroundColor="#FF0000" />

export default Riseup
