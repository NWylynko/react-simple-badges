
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Riseup = (props: BadgeProps) => <Badge name="Riseup" backgroundColor="#FF0000" {...props} />

export default Riseup
