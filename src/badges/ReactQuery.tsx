
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ReactQuery = (props: BadgeProps) => <Badge name="React Query" {...props} backgroundColor="#FF4154" />

export default ReactQuery
