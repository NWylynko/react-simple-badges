
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const ReactQuery = (props: BadgeProps) => <Badge name="React Query" backgroundColor="#FF4154" {...props} />

export default ReactQuery
