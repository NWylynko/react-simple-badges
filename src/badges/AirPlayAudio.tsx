
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AirPlayAudio = (props: BadgeProps) => <Badge name="AirPlay Audio" {...props} backgroundColor="#000000" />

export default AirPlayAudio
