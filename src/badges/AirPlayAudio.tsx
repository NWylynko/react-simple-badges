
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const AirPlayAudio = (props: BadgeProps) => <Badge name="AirPlay Audio" backgroundColor="#000000" {...props} />

export default AirPlayAudio
