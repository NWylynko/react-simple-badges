
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Smart = (props: BadgeProps) => <Badge name="smart" {...props} backgroundColor="#FABC0C" />

export default Smart
