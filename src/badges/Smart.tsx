
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Smart = (props: BadgeProps) => <Badge name="smart" backgroundColor="#FABC0C" {...props} />

export default Smart
