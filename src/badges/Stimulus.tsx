
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Stimulus = (props: BadgeProps) => <Badge name="Stimulus" {...props} backgroundColor="#77E8B9" />

export default Stimulus
