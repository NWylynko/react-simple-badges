
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Genius = (props: BadgeProps) => <Badge name="Genius" {...props} backgroundColor="#FFFF64" />

export default Genius
