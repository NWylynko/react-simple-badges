
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Launchpad = (props: BadgeProps) => <Badge name="Launchpad" backgroundColor="#F8C300" {...props} />

export default Launchpad
