
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Launchpad = (props: BadgeProps) => <Badge name="Launchpad" {...props} backgroundColor="#F8C300" />

export default Launchpad
