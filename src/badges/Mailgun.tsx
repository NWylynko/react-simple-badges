
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Mailgun = (props: BadgeProps) => <Badge name="Mailgun" {...props} backgroundColor="#F06B66" />

export default Mailgun
