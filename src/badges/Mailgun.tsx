
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Mailgun = (props: BadgeProps) => <Badge name="Mailgun" backgroundColor="#F06B66" {...props} />

export default Mailgun
