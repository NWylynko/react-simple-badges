
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Rasa = (props: BadgeProps) => <Badge name="Rasa" {...props} backgroundColor="#5A17EE" />

export default Rasa
