
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AstonMartin = (props: BadgeProps) => <Badge name="Aston Martin" {...props} backgroundColor="#000000" />

export default AstonMartin
