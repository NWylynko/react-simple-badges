
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AmazonGames = (props: BadgeProps) => <Badge name="Amazon Games" {...props} backgroundColor="#FF9900" />

export default AmazonGames
