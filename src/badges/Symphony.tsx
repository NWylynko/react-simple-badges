
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Symphony = (props: BadgeProps) => <Badge name="Symphony" {...props} backgroundColor="#0098FF" />

export default Symphony
