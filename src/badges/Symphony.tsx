
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Symphony = (props: BadgeProps) => <Badge name="Symphony" backgroundColor="#0098FF" {...props} />

export default Symphony
