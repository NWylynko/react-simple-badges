
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const RubySinatra = (props: BadgeProps) => <Badge name="Ruby Sinatra" backgroundColor="#000000" {...props} />

export default RubySinatra
