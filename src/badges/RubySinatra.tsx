
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const RubySinatra = (props: BadgeProps) => <Badge name="Ruby Sinatra" {...props} backgroundColor="#000000" />

export default RubySinatra
