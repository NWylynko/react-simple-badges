
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const SocietySix = (props: BadgeProps) => <Badge name="Society6" backgroundColor="#000000" {...props} />

export default SocietySix
