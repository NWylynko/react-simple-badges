
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SocietySix = (props: BadgeProps) => <Badge name="Society6" {...props} backgroundColor="#000000" />

export default SocietySix
