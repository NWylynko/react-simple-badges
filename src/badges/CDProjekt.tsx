
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CDProjekt = (props: BadgeProps) => <Badge name="CD Projekt" {...props} backgroundColor="#DC0D15" />

export default CDProjekt
