
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const CDProjekt = (props: BadgeProps) => <Badge name="CD Projekt" backgroundColor="#DC0D15" {...props} />

export default CDProjekt
