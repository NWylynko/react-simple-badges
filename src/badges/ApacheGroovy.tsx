
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const ApacheGroovy = (props: BadgeProps) => <Badge name="Apache Groovy" backgroundColor="#4298B8" {...props} />

export default ApacheGroovy
