
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ApacheGroovy = (props: BadgeProps) => <Badge name="Apache Groovy" {...props} backgroundColor="#4298B8" />

export default ApacheGroovy
