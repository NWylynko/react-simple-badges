
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Bitrise = (props: BadgeProps) => <Badge name="Bitrise" {...props} backgroundColor="#683D87" />

export default Bitrise
