
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Haskell = (props: BadgeProps) => <Badge name="Haskell" backgroundColor="#5D4F85" {...props} />

export default Haskell
