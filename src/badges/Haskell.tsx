
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Haskell = (props: BadgeProps) => <Badge name="Haskell" {...props} backgroundColor="#5D4F85" />

export default Haskell
