
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Npm = (props: BadgeProps) => <Badge name="npm" backgroundColor="#CB3837" {...props} />

export default Npm
