
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Npm = (props: BadgeProps) => <Badge name="npm" {...props} backgroundColor="#CB3837" />

export default Npm
