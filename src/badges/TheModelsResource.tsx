
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TheModelsResource = (props: BadgeProps) => <Badge name="The Models Resource" {...props} backgroundColor="#3A75BD" />

export default TheModelsResource
