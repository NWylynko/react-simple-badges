
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CloudBees = (props: BadgeProps) => <Badge name="CloudBees" {...props} backgroundColor="#1997B5" />

export default CloudBees
