
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Drupal = (props: BadgeProps) => <Badge name="Drupal" {...props} backgroundColor="#0678BE" />

export default Drupal
