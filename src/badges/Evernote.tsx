
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Evernote = (props: BadgeProps) => <Badge name="Evernote" {...props} backgroundColor="#00A82D" />

export default Evernote
