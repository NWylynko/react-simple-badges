
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Evernote = (props: BadgeProps) => <Badge name="Evernote" backgroundColor="#00A82D" {...props} />

export default Evernote
