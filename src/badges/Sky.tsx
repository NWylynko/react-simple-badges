
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Sky = (props: BadgeProps) => <Badge name="Sky" {...props} backgroundColor="#0072C9" />

export default Sky
