
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Sky = (props: BadgeProps) => <Badge name="Sky" backgroundColor="#0072C9" {...props} />

export default Sky
