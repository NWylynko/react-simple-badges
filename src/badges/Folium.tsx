
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Folium = (props: BadgeProps) => <Badge name="Folium" {...props} backgroundColor="#77B829" />

export default Folium
