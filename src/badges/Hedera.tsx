
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Hedera = (props: BadgeProps) => <Badge name="Hedera" {...props} backgroundColor="#222222" />

export default Hedera
