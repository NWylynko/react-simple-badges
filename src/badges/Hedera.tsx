
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Hedera = (props: BadgeProps) => <Badge name="Hedera" backgroundColor="#222222" {...props} />

export default Hedera
