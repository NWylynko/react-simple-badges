
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Coinbase = (props: BadgeProps) => <Badge name="Coinbase" {...props} backgroundColor="#0052FF" />

export default Coinbase
