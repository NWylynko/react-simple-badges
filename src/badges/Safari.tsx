
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Safari = (props: BadgeProps) => <Badge name="Safari" backgroundColor="#000000" {...props} />

export default Safari
