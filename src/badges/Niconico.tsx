
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Niconico = (props: BadgeProps) => <Badge name="niconico" {...props} backgroundColor="#231815" />

export default Niconico
