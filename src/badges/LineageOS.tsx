
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const LineageOS = (props: BadgeProps) => <Badge name="LineageOS" {...props} backgroundColor="#167C80" />

export default LineageOS
