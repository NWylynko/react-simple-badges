
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const VK = (props: BadgeProps) => <Badge name="VK" {...props} backgroundColor="#0077FF" />

export default VK
