
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const VK = (props: BadgeProps) => <Badge name="VK" backgroundColor="#0077FF" {...props} />

export default VK
