
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CSSThree = (props: BadgeProps) => <Badge name="CSS3" {...props} backgroundColor="#1572B6" />

export default CSSThree
