
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const OpenMined = (props: BadgeProps) => <Badge name="OpenMined" backgroundColor="#ED986C" {...props} />

export default OpenMined
