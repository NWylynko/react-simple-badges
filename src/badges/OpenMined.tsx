
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const OpenMined = (props: BadgeProps) => <Badge name="OpenMined" {...props} backgroundColor="#ED986C" />

export default OpenMined
