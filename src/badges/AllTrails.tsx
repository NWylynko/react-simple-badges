
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AllTrails = (props: BadgeProps) => <Badge name="AllTrails" {...props} backgroundColor="#428813" />

export default AllTrails
