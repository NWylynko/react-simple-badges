
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CashApp = (props: BadgeProps) => <Badge name="Cash App" {...props} backgroundColor="#00C244" />

export default CashApp
