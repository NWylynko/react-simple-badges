
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const CashApp = (props: BadgeProps) => <Badge name="Cash App" backgroundColor="#00C244" {...props} />

export default CashApp
