
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Cocos = (props: BadgeProps) => <Badge name="Cocos" {...props} backgroundColor="#55C2E1" />

export default Cocos
