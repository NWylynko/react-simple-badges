
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Sketch = (props: BadgeProps) => <Badge name="Sketch" {...props} backgroundColor="#F7B500" />

export default Sketch
