
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Sketch = (props: BadgeProps) => <Badge name="Sketch" backgroundColor="#F7B500" {...props} />

export default Sketch
