
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Thumbtack = (props: BadgeProps) => <Badge name="Thumbtack" {...props} backgroundColor="#009FD9" />

export default Thumbtack
