
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Thumbtack = (props: BadgeProps) => <Badge name="Thumbtack" backgroundColor="#009FD9" {...props} />

export default Thumbtack
