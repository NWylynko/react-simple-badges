
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const FiveM = (props: BadgeProps) => <Badge name="FiveM" {...props} backgroundColor="#F40552" />

export default FiveM
