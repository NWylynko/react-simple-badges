
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const FiveM = (props: BadgeProps) => <Badge name="FiveM" backgroundColor="#F40552" {...props} />

export default FiveM
