
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Python = (props: BadgeProps) => <Badge name="Python" backgroundColor="#3776AB" {...props} />

export default Python
