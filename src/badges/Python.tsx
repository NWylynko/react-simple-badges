
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Python = (props: BadgeProps) => <Badge name="Python" {...props} backgroundColor="#3776AB" />

export default Python
