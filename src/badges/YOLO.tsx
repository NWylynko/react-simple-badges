
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const YOLO = (props: BadgeProps) => <Badge name="YOLO" backgroundColor="#00FFFF" {...props} />

export default YOLO
