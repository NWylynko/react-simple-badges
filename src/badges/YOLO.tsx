
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const YOLO = (props: BadgeProps) => <Badge name="YOLO" {...props} backgroundColor="#00FFFF" />

export default YOLO
