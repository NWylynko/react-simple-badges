
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Shell = (props: BadgeProps) => <Badge name="Shell" {...props} backgroundColor="#FFD500" />

export default Shell
