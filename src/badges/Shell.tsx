
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Shell = (props: BadgeProps) => <Badge name="Shell" backgroundColor="#FFD500" {...props} />

export default Shell
