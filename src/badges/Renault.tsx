
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Renault = (props: BadgeProps) => <Badge name="Renault" backgroundColor="#FFCC33" {...props} />

export default Renault
