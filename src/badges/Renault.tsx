
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Renault = (props: BadgeProps) => <Badge name="Renault" {...props} backgroundColor="#FFCC33" />

export default Renault
