
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Untangle = (props: BadgeProps) => <Badge name="Untangle" {...props} backgroundColor="#68BD49" />

export default Untangle
