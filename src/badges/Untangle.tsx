
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Untangle = (props: BadgeProps) => <Badge name="Untangle" backgroundColor="#68BD49" {...props} />

export default Untangle
