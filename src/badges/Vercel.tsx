
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Vercel = (props: BadgeProps) => <Badge name="Vercel" backgroundColor="#000000" {...props} />

export default Vercel
