
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Vercel = (props: BadgeProps) => <Badge name="Vercel" {...props} backgroundColor="#000000" />

export default Vercel
