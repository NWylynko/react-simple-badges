
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Dunked = (props: BadgeProps) => <Badge name="Dunked" {...props} backgroundColor="#2DA9D7" />

export default Dunked
