
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Dunked = (props: BadgeProps) => <Badge name="Dunked" backgroundColor="#2DA9D7" {...props} />

export default Dunked
