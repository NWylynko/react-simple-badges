
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const PhpStorm = (props: BadgeProps) => <Badge name="PhpStorm" backgroundColor="#000000" {...props} />

export default PhpStorm
