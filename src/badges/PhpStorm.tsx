
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PhpStorm = (props: BadgeProps) => <Badge name="PhpStorm" {...props} backgroundColor="#000000" />

export default PhpStorm
