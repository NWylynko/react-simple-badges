
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const SEPA = (props: BadgeProps) => <Badge name="SEPA" backgroundColor="#2350A9" {...props} />

export default SEPA
