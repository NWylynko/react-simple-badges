
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SEPA = (props: BadgeProps) => <Badge name="SEPA" {...props} backgroundColor="#2350A9" />

export default SEPA
