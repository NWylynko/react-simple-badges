
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const BookStack = (props: BadgeProps) => <Badge name="BookStack" {...props} backgroundColor="#0288D1" />

export default BookStack
