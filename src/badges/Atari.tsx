
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Atari = (props: BadgeProps) => <Badge name="Atari" {...props} backgroundColor="#E4202E" />

export default Atari
