
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Atari = (props: BadgeProps) => <Badge name="Atari" backgroundColor="#E4202E" {...props} />

export default Atari
