
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Airtable = (props: BadgeProps) => <Badge name="Airtable" backgroundColor="#18BFFF" {...props} />

export default Airtable
