
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Airtable = (props: BadgeProps) => <Badge name="Airtable" {...props} backgroundColor="#18BFFF" />

export default Airtable
