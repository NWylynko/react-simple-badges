
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AldiNord = (props: BadgeProps) => <Badge name="Aldi Nord" {...props} backgroundColor="#2490D7" />

export default AldiNord
