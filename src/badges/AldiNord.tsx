
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const AldiNord = (props: BadgeProps) => <Badge name="Aldi Nord" backgroundColor="#2490D7" {...props} />

export default AldiNord
