
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Symfony = (props: BadgeProps) => <Badge name="Symfony" backgroundColor="#000000" {...props} />

export default Symfony
