
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Symfony = (props: BadgeProps) => <Badge name="Symfony" {...props} backgroundColor="#000000" />

export default Symfony
