
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Monkeytie = (props: BadgeProps) => <Badge name="monkey tie" {...props} backgroundColor="#1A52C2" />

export default Monkeytie
