
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Monkeytie = (props: BadgeProps) => <Badge name="monkey tie" backgroundColor="#1A52C2" {...props} />

export default Monkeytie
