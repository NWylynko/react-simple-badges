
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Anchor = (props: BadgeProps) => <Badge name="Anchor" {...props} backgroundColor="#5000B9" />

export default Anchor
