
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Contentful = (props: BadgeProps) => <Badge name="Contentful" backgroundColor="#2478CC" {...props} />

export default Contentful
