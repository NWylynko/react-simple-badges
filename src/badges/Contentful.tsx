
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Contentful = (props: BadgeProps) => <Badge name="Contentful" {...props} backgroundColor="#2478CC" />

export default Contentful
