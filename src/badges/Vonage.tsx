
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Vonage = (props: BadgeProps) => <Badge name="Vonage" backgroundColor="#FFFFFF" {...props} />

export default Vonage
