
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Vonage = (props: BadgeProps) => <Badge name="Vonage" {...props} backgroundColor="#FFFFFF" />

export default Vonage
