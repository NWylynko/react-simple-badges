
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const BBCiPlayer = (props: BadgeProps) => <Badge name="BBC iPlayer" {...props} backgroundColor="#F54997" />

export default BBCiPlayer
