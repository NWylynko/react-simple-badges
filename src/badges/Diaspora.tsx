
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Diaspora = (props: BadgeProps) => <Badge name="Diaspora" backgroundColor="#000000" {...props} />

export default Diaspora
