
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Diaspora = (props: BadgeProps) => <Badge name="Diaspora" {...props} backgroundColor="#000000" />

export default Diaspora
