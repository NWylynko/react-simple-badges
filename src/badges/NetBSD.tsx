
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const NetBSD = (props: BadgeProps) => <Badge name="NetBSD" backgroundColor="#FF6600" {...props} />

export default NetBSD
