
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const NetBSD = (props: BadgeProps) => <Badge name="NetBSD" {...props} backgroundColor="#FF6600" />

export default NetBSD
