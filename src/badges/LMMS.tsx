
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const LMMS = (props: BadgeProps) => <Badge name="LMMS" {...props} backgroundColor="#10B146" />

export default LMMS
