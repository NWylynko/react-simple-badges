
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Monoprix = (props: BadgeProps) => <Badge name="Monoprix" {...props} backgroundColor="#FB1911" />

export default Monoprix
