
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Openlayers = (props: BadgeProps) => <Badge name="Openlayers" {...props} backgroundColor="#1F6B75" />

export default Openlayers
