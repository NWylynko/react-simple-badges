
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Chase = (props: BadgeProps) => <Badge name="Chase" {...props} backgroundColor="#117ACA" />

export default Chase
