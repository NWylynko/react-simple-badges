
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Chase = (props: BadgeProps) => <Badge name="Chase" backgroundColor="#117ACA" {...props} />

export default Chase
