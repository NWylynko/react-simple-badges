
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ApacheHadoop = (props: BadgeProps) => <Badge name="Apache Hadoop" {...props} backgroundColor="#66CCFF" />

export default ApacheHadoop
