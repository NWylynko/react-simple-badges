
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const ApacheHadoop = (props: BadgeProps) => <Badge name="Apache Hadoop" backgroundColor="#66CCFF" {...props} />

export default ApacheHadoop
