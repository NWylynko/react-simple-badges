
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Baremetrics = (props: BadgeProps) => <Badge name="Baremetrics" backgroundColor="#6078FF" {...props} />

export default Baremetrics
