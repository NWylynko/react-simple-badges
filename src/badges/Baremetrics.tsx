
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Baremetrics = (props: BadgeProps) => <Badge name="Baremetrics" {...props} backgroundColor="#6078FF" />

export default Baremetrics
