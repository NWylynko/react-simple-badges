
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Kongregate = (props: BadgeProps) => <Badge name="Kongregate" backgroundColor="#990000" {...props} />

export default Kongregate
