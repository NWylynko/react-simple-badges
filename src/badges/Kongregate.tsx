
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Kongregate = (props: BadgeProps) => <Badge name="Kongregate" {...props} backgroundColor="#990000" />

export default Kongregate
