
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TypeScript = (props: BadgeProps) => <Badge name="TypeScript" {...props} backgroundColor="#3178C6" />

export default TypeScript
