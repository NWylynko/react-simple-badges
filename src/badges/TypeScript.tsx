
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const TypeScript = (props: BadgeProps) => <Badge name="TypeScript" backgroundColor="#3178C6" {...props} />

export default TypeScript
