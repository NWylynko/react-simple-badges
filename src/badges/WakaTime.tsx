
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const WakaTime = (props: BadgeProps) => <Badge name="WakaTime" {...props} backgroundColor="#000000" />

export default WakaTime
