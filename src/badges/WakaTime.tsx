
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const WakaTime = (props: BadgeProps) => <Badge name="WakaTime" backgroundColor="#000000" {...props} />

export default WakaTime
