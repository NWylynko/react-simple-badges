
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ProtonMail = (props: BadgeProps) => <Badge name="ProtonMail" {...props} backgroundColor="#8B89CC" />

export default ProtonMail
