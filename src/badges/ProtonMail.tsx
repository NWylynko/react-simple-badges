
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const ProtonMail = (props: BadgeProps) => <Badge name="ProtonMail" backgroundColor="#8B89CC" {...props} />

export default ProtonMail
