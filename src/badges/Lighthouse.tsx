
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Lighthouse = (props: BadgeProps) => <Badge name="Lighthouse" {...props} backgroundColor="#F44B21" />

export default Lighthouse
