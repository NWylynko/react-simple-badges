
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Metro = (props: BadgeProps) => <Badge name="Metro" backgroundColor="#EF4242" {...props} />

export default Metro
