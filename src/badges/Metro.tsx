
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Metro = (props: BadgeProps) => <Badge name="Metro" {...props} backgroundColor="#EF4242" />

export default Metro
