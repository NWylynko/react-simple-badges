
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PlayerFM = (props: BadgeProps) => <Badge name="Player FM" {...props} backgroundColor="#C8122A" />

export default PlayerFM
