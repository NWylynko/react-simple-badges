
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const PlayerFM = (props: BadgeProps) => <Badge name="Player FM" backgroundColor="#C8122A" {...props} />

export default PlayerFM
