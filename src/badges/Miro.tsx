
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Miro = (props: BadgeProps) => <Badge name="Miro" backgroundColor="#050038" {...props} />

export default Miro
