
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Miro = (props: BadgeProps) => <Badge name="Miro" {...props} backgroundColor="#050038" />

export default Miro
