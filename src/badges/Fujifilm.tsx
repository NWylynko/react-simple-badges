
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Fujifilm = (props: BadgeProps) => <Badge name="Fujifilm" {...props} backgroundColor="#ED1A3A" />

export default Fujifilm
