
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PodcastAddict = (props: BadgeProps) => <Badge name="Podcast Addict" {...props} backgroundColor="#F4842D" />

export default PodcastAddict
