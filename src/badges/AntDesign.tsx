
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AntDesign = (props: BadgeProps) => <Badge name="Ant Design" {...props} backgroundColor="#0170FE" />

export default AntDesign
