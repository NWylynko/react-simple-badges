
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const InfluxDB = (props: BadgeProps) => <Badge name="InfluxDB" {...props} backgroundColor="#22ADF6" />

export default InfluxDB
