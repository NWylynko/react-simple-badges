
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const InfluxDB = (props: BadgeProps) => <Badge name="InfluxDB" backgroundColor="#22ADF6" {...props} />

export default InfluxDB
