
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Elasticsearch = (props: BadgeProps) => <Badge name="Elasticsearch" {...props} backgroundColor="#005571" />

export default Elasticsearch
