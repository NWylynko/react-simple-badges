
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Liquibase = (props: BadgeProps) => <Badge name="Liquibase" {...props} backgroundColor="#2962FF" />

export default Liquibase
