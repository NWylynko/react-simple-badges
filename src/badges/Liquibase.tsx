
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Liquibase = (props: BadgeProps) => <Badge name="Liquibase" backgroundColor="#2962FF" {...props} />

export default Liquibase
