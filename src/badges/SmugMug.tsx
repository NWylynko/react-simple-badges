
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SmugMug = (props: BadgeProps) => <Badge name="SmugMug" {...props} backgroundColor="#6DB944" />

export default SmugMug
