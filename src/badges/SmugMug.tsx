
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const SmugMug = (props: BadgeProps) => <Badge name="SmugMug" backgroundColor="#6DB944" {...props} />

export default SmugMug
