
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Mocha = (props: BadgeProps) => <Badge name="Mocha" backgroundColor="#8D6748" {...props} />

export default Mocha
