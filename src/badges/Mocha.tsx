
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Mocha = (props: BadgeProps) => <Badge name="Mocha" {...props} backgroundColor="#8D6748" />

export default Mocha
