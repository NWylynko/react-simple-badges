
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Postmates = (props: BadgeProps) => <Badge name="Postmates" {...props} backgroundColor="#FFDF18" />

export default Postmates
