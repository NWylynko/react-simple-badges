
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Postmates = (props: BadgeProps) => <Badge name="Postmates" backgroundColor="#FFDF18" {...props} />

export default Postmates
