
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PlayStationFive = (props: BadgeProps) => <Badge name="PlayStation 5" {...props} backgroundColor="#003791" />

export default PlayStationFive
