
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Apache = (props: BadgeProps) => <Badge name="Apache" {...props} backgroundColor="#D22128" />

export default Apache
