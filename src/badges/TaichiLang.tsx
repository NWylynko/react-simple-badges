
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const TaichiLang = (props: BadgeProps) => <Badge name="Taichi Lang" backgroundColor="#000000" {...props} />

export default TaichiLang
