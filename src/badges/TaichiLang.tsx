
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TaichiLang = (props: BadgeProps) => <Badge name="Taichi Lang" {...props} backgroundColor="#000000" />

export default TaichiLang
