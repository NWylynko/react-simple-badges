
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Hackster = (props: BadgeProps) => <Badge name="Hackster" {...props} backgroundColor="#2E9FE6" />

export default Hackster
