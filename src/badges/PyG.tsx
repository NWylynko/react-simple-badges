
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PyG = (props: BadgeProps) => <Badge name="PyG" {...props} backgroundColor="#3C2179" />

export default PyG
