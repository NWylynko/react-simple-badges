
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Ray = (props: BadgeProps) => <Badge name="Ray" backgroundColor="#028CF0" {...props} />

export default Ray
