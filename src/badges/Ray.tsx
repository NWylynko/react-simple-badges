
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Ray = (props: BadgeProps) => <Badge name="Ray" {...props} backgroundColor="#028CF0" />

export default Ray
