
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AppleMusic = (props: BadgeProps) => <Badge name="Apple Music" {...props} backgroundColor="#FA243C" />

export default AppleMusic
