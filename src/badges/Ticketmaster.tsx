
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Ticketmaster = (props: BadgeProps) => <Badge name="Ticketmaster" {...props} backgroundColor="#026CDF" />

export default Ticketmaster
