
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Storyblok = (props: BadgeProps) => <Badge name="Storyblok" backgroundColor="#09B3AF" {...props} />

export default Storyblok
