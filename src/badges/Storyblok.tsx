
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Storyblok = (props: BadgeProps) => <Badge name="Storyblok" {...props} backgroundColor="#09B3AF" />

export default Storyblok
