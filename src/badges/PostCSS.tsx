
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PostCSS = (props: BadgeProps) => <Badge name="PostCSS" {...props} backgroundColor="#DD3A0A" />

export default PostCSS
