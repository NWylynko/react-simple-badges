
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Crowdsource = (props: BadgeProps) => <Badge name="Crowdsource" {...props} backgroundColor="#4285F4" />

export default Crowdsource
