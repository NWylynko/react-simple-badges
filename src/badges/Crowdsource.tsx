
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Crowdsource = (props: BadgeProps) => <Badge name="Crowdsource" backgroundColor="#4285F4" {...props} />

export default Crowdsource
