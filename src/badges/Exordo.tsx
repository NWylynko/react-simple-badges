
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Exordo = (props: BadgeProps) => <Badge name="Exordo" {...props} backgroundColor="#DAA449" />

export default Exordo
