
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ApacheCloudStack = (props: BadgeProps) => <Badge name="Apache CloudStack" {...props} backgroundColor="#2AA5DC" />

export default ApacheCloudStack
