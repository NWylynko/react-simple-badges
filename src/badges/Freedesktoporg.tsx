
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Freedesktoporg = (props: BadgeProps) => <Badge name="freedesktop.org" {...props} backgroundColor="#3B80AE" />

export default Freedesktoporg
