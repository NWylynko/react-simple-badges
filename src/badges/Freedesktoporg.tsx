
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Freedesktoporg = (props: BadgeProps) => <Badge name="freedesktop.org" backgroundColor="#3B80AE" {...props} />

export default Freedesktoporg
