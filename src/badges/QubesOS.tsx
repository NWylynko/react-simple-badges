
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const QubesOS = (props: BadgeProps) => <Badge name="Qubes OS" {...props} backgroundColor="#3874D8" />

export default QubesOS
