
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const QubesOS = (props: BadgeProps) => <Badge name="Qubes OS" backgroundColor="#3874D8" {...props} />

export default QubesOS
