
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Concourse = (props: BadgeProps) => <Badge name="Concourse" {...props} backgroundColor="#3398DC" />

export default Concourse
