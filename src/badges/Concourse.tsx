
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Concourse = (props: BadgeProps) => <Badge name="Concourse" backgroundColor="#3398DC" {...props} />

export default Concourse
