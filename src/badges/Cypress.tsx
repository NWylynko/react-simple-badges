
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Cypress = (props: BadgeProps) => <Badge name="Cypress" {...props} backgroundColor="#17202C" />

export default Cypress
