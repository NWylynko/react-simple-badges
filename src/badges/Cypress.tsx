
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Cypress = (props: BadgeProps) => <Badge name="Cypress" backgroundColor="#17202C" {...props} />

export default Cypress
