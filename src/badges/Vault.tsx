
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Vault = (props: BadgeProps) => <Badge name="Vault" {...props} backgroundColor="#000000" />

export default Vault
