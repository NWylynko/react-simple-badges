
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Vault = (props: BadgeProps) => <Badge name="Vault" backgroundColor="#000000" {...props} />

export default Vault
