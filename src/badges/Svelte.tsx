
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Svelte = (props: BadgeProps) => <Badge name="Svelte" {...props} backgroundColor="#FF3E00" />

export default Svelte
