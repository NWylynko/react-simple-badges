
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const IFTTT = (props: BadgeProps) => <Badge name="IFTTT" {...props} backgroundColor="#000000" />

export default IFTTT
