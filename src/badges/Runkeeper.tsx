
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Runkeeper = (props: BadgeProps) => <Badge name="Runkeeper" {...props} backgroundColor="#001E62" />

export default Runkeeper
