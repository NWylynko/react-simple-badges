
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Dart = (props: BadgeProps) => <Badge name="Dart" {...props} backgroundColor="#0175C2" />

export default Dart
