
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const AzureDevOps = (props: BadgeProps) => <Badge name="Azure DevOps" backgroundColor="#0078D7" {...props} />

export default AzureDevOps
