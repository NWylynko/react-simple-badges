
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AzureDevOps = (props: BadgeProps) => <Badge name="Azure DevOps" {...props} backgroundColor="#0078D7" />

export default AzureDevOps
