
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const BigBlueButton = (props: BadgeProps) => <Badge name="BigBlueButton" {...props} backgroundColor="#283274" />

export default BigBlueButton
