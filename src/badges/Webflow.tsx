
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Webflow = (props: BadgeProps) => <Badge name="Webflow" {...props} backgroundColor="#4353FF" />

export default Webflow
