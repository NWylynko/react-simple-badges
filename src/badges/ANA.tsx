
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ANA = (props: BadgeProps) => <Badge name="ANA" {...props} backgroundColor="#13448F" />

export default ANA
