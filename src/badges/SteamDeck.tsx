
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const SteamDeck = (props: BadgeProps) => <Badge name="Steam Deck" backgroundColor="#1A9FFF" {...props} />

export default SteamDeck
