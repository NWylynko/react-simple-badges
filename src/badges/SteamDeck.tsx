
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SteamDeck = (props: BadgeProps) => <Badge name="Steam Deck" {...props} backgroundColor="#1A9FFF" />

export default SteamDeck
