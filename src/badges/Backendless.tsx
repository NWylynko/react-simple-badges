
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Backendless = (props: BadgeProps) => <Badge name="Backendless" {...props} backgroundColor="#FFFFFF" />

export default Backendless
