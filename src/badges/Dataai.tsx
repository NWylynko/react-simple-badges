
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Dataai = (props: BadgeProps) => <Badge name="data.ai" {...props} backgroundColor="#000000" />

export default Dataai
