
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Dataai = (props: BadgeProps) => <Badge name="data.ai" backgroundColor="#000000" {...props} />

export default Dataai
