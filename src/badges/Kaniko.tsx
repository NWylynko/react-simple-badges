
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Kaniko = (props: BadgeProps) => <Badge name="Kaniko" backgroundColor="#FFA600" {...props} />

export default Kaniko
