
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Kaniko = (props: BadgeProps) => <Badge name="Kaniko" {...props} backgroundColor="#FFA600" />

export default Kaniko
