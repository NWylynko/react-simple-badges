
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Carto = (props: BadgeProps) => <Badge name="Carto" {...props} backgroundColor="#EB1510" />

export default Carto
