
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Less = (props: BadgeProps) => <Badge name="Less" {...props} backgroundColor="#1D365D" />

export default Less
