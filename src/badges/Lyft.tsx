
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Lyft = (props: BadgeProps) => <Badge name="Lyft" backgroundColor="#FF00BF" {...props} />

export default Lyft
