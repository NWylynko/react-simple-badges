
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Lyft = (props: BadgeProps) => <Badge name="Lyft" {...props} backgroundColor="#FF00BF" />

export default Lyft
