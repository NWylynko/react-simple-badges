
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Ethereum = (props: BadgeProps) => <Badge name="Ethereum" {...props} backgroundColor="#3C3C3D" />

export default Ethereum
