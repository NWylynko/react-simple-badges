
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Ethereum = (props: BadgeProps) => <Badge name="Ethereum" backgroundColor="#3C3C3D" {...props} />

export default Ethereum
