
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AlloCin = (props: BadgeProps) => <Badge name="AlloCiné" {...props} backgroundColor="#FECC00" />

export default AlloCin
