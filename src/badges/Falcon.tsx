
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Falcon = (props: BadgeProps) => <Badge name="Falcon" backgroundColor="#F0AD4E" {...props} />

export default Falcon
