
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Falcon = (props: BadgeProps) => <Badge name="Falcon" {...props} backgroundColor="#F0AD4E" />

export default Falcon
