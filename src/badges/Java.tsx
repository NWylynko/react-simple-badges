
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Java = (props: BadgeProps) => <Badge name="Java" {...props} backgroundColor="#007396" />

export default Java
