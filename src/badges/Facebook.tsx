
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Facebook = (props: BadgeProps) => <Badge name="Facebook" {...props} backgroundColor="#1877F2" />

export default Facebook
