
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Hibernate = (props: BadgeProps) => <Badge name="Hibernate" {...props} backgroundColor="#59666C" />

export default Hibernate
