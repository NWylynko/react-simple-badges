
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const PubMed = (props: BadgeProps) => <Badge name="PubMed" backgroundColor="#326599" {...props} />

export default PubMed
