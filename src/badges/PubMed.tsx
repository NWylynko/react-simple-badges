
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PubMed = (props: BadgeProps) => <Badge name="PubMed" {...props} backgroundColor="#326599" />

export default PubMed
