
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Abka = (props: BadgeProps) => <Badge name="Żabka" {...props} backgroundColor="#006420" />

export default Abka
