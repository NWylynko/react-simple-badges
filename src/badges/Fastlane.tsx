
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Fastlane = (props: BadgeProps) => <Badge name="Fastlane" backgroundColor="#00F200" {...props} />

export default Fastlane
