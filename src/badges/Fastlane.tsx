
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Fastlane = (props: BadgeProps) => <Badge name="Fastlane" {...props} backgroundColor="#00F200" />

export default Fastlane
