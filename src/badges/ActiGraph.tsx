
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const ActiGraph = (props: BadgeProps) => <Badge name="ActiGraph" backgroundColor="#0B2C4A" {...props} />

export default ActiGraph
