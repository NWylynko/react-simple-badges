
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ActiGraph = (props: BadgeProps) => <Badge name="ActiGraph" {...props} backgroundColor="#0B2C4A" />

export default ActiGraph
