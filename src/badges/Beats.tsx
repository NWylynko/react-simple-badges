
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Beats = (props: BadgeProps) => <Badge name="Beats" {...props} backgroundColor="#005571" />

export default Beats
