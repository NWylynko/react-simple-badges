
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Beats = (props: BadgeProps) => <Badge name="Beats" backgroundColor="#005571" {...props} />

export default Beats
