
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const RSS = (props: BadgeProps) => <Badge name="RSS" {...props} backgroundColor="#FFA500" />

export default RSS
