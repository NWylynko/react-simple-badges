
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const RSS = (props: BadgeProps) => <Badge name="RSS" backgroundColor="#FFA500" {...props} />

export default RSS
