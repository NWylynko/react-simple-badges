
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Asciidoctor = (props: BadgeProps) => <Badge name="Asciidoctor" backgroundColor="#E40046" {...props} />

export default Asciidoctor
