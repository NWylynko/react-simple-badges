
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Asciidoctor = (props: BadgeProps) => <Badge name="Asciidoctor" {...props} backgroundColor="#E40046" />

export default Asciidoctor
