
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Venmo = (props: BadgeProps) => <Badge name="Venmo" backgroundColor="#3D95CE" {...props} />

export default Venmo
