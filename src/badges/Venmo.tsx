
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Venmo = (props: BadgeProps) => <Badge name="Venmo" {...props} backgroundColor="#3D95CE" />

export default Venmo
