
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Webpack = (props: BadgeProps) => <Badge name="Webpack" {...props} backgroundColor="#8DD6F9" />

export default Webpack
