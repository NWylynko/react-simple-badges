
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Webpack = (props: BadgeProps) => <Badge name="Webpack" backgroundColor="#8DD6F9" {...props} />

export default Webpack
