
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GroupMe = (props: BadgeProps) => <Badge name="GroupMe" {...props} backgroundColor="#00AFF0" />

export default GroupMe
