
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const GroupMe = (props: BadgeProps) => <Badge name="GroupMe" backgroundColor="#00AFF0" {...props} />

export default GroupMe
