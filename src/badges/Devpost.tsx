
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Devpost = (props: BadgeProps) => <Badge name="Devpost" {...props} backgroundColor="#003E54" />

export default Devpost
