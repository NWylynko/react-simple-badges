
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Bit = (props: BadgeProps) => <Badge name="Bit" {...props} backgroundColor="#73398D" />

export default Bit
