
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Bit = (props: BadgeProps) => <Badge name="Bit" backgroundColor="#73398D" {...props} />

export default Bit
