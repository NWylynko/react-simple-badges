
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const WhiteSource = (props: BadgeProps) => <Badge name="WhiteSource" backgroundColor="#161D4E" {...props} />

export default WhiteSource
