
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const WhiteSource = (props: BadgeProps) => <Badge name="WhiteSource" {...props} backgroundColor="#161D4E" />

export default WhiteSource
