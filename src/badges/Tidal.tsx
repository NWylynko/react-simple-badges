
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Tidal = (props: BadgeProps) => <Badge name="Tidal" {...props} backgroundColor="#000000" />

export default Tidal
