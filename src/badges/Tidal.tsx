
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Tidal = (props: BadgeProps) => <Badge name="Tidal" backgroundColor="#000000" {...props} />

export default Tidal
