
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Engadget = (props: BadgeProps) => <Badge name="Engadget" backgroundColor="#000000" {...props} />

export default Engadget
