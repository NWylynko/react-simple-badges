
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Engadget = (props: BadgeProps) => <Badge name="Engadget" {...props} backgroundColor="#000000" />

export default Engadget
