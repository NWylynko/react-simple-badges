
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Stripe = (props: BadgeProps) => <Badge name="Stripe" {...props} backgroundColor="#008CDD" />

export default Stripe
