
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Stripe = (props: BadgeProps) => <Badge name="Stripe" backgroundColor="#008CDD" {...props} />

export default Stripe
