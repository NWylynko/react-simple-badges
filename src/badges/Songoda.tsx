
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Songoda = (props: BadgeProps) => <Badge name="Songoda" {...props} backgroundColor="#FC494A" />

export default Songoda
