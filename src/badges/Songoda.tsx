
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Songoda = (props: BadgeProps) => <Badge name="Songoda" backgroundColor="#FC494A" {...props} />

export default Songoda
