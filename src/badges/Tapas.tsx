
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Tapas = (props: BadgeProps) => <Badge name="Tapas" {...props} backgroundColor="#FFCE00" />

export default Tapas
