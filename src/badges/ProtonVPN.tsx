
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const ProtonVPN = (props: BadgeProps) => <Badge name="ProtonVPN" backgroundColor="#56B366" {...props} />

export default ProtonVPN
