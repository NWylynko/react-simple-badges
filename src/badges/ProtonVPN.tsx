
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ProtonVPN = (props: BadgeProps) => <Badge name="ProtonVPN" {...props} backgroundColor="#56B366" />

export default ProtonVPN
