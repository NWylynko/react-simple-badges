
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const NZXT = (props: BadgeProps) => <Badge name="NZXT" {...props} backgroundColor="#000000" />

export default NZXT
