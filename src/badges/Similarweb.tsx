
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Similarweb = (props: BadgeProps) => <Badge name="Similarweb" {...props} backgroundColor="#092540" />

export default Similarweb
