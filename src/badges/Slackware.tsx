
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Slackware = (props: BadgeProps) => <Badge name="Slackware" {...props} backgroundColor="#000000" />

export default Slackware
