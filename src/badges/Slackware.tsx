
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Slackware = (props: BadgeProps) => <Badge name="Slackware" backgroundColor="#000000" {...props} />

export default Slackware
