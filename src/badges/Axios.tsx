
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Axios = (props: BadgeProps) => <Badge name="Axios" {...props} backgroundColor="#5A29E4" />

export default Axios
