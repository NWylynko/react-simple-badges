
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Erlang = (props: BadgeProps) => <Badge name="Erlang" backgroundColor="#A90533" {...props} />

export default Erlang
