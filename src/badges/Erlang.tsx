
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Erlang = (props: BadgeProps) => <Badge name="Erlang" {...props} backgroundColor="#A90533" />

export default Erlang
