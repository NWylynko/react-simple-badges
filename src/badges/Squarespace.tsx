
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Squarespace = (props: BadgeProps) => <Badge name="Squarespace" {...props} backgroundColor="#000000" />

export default Squarespace
