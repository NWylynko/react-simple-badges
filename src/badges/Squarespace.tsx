
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Squarespace = (props: BadgeProps) => <Badge name="Squarespace" backgroundColor="#000000" {...props} />

export default Squarespace
