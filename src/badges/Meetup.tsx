
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Meetup = (props: BadgeProps) => <Badge name="Meetup" {...props} backgroundColor="#ED1C40" />

export default Meetup
