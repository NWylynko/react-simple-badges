
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const NGINX = (props: BadgeProps) => <Badge name="NGINX" {...props} backgroundColor="#009639" />

export default NGINX
