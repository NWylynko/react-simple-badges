
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const NGINX = (props: BadgeProps) => <Badge name="NGINX" backgroundColor="#009639" {...props} />

export default NGINX
