
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Analogue = (props: BadgeProps) => <Badge name="Analogue" {...props} backgroundColor="#1A1A1A" />

export default Analogue
