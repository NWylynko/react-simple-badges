
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GitHub = (props: BadgeProps) => <Badge name="GitHub" {...props} backgroundColor="#181717" />

export default GitHub
