
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Feedly = (props: BadgeProps) => <Badge name="Feedly" {...props} backgroundColor="#2BB24C" />

export default Feedly
