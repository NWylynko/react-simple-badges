
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Feedly = (props: BadgeProps) => <Badge name="Feedly" backgroundColor="#2BB24C" {...props} />

export default Feedly
