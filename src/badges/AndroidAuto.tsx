
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AndroidAuto = (props: BadgeProps) => <Badge name="Android Auto" {...props} backgroundColor="#3DDC84" />

export default AndroidAuto
