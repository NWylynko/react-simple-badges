
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const AndroidAuto = (props: BadgeProps) => <Badge name="Android Auto" backgroundColor="#3DDC84" {...props} />

export default AndroidAuto
