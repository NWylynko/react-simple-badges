
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Siemens = (props: BadgeProps) => <Badge name="Siemens" {...props} backgroundColor="#009999" />

export default Siemens
