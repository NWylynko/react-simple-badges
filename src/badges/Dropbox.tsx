
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Dropbox = (props: BadgeProps) => <Badge name="Dropbox" {...props} backgroundColor="#0061FF" />

export default Dropbox
