
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ReSharper = (props: BadgeProps) => <Badge name="ReSharper" {...props} backgroundColor="#000000" />

export default ReSharper
