
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const ReSharper = (props: BadgeProps) => <Badge name="ReSharper" backgroundColor="#000000" {...props} />

export default ReSharper
