
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Zoiper = (props: BadgeProps) => <Badge name="Zoiper" {...props} backgroundColor="#F47920" />

export default Zoiper
