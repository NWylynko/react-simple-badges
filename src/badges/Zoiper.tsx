
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Zoiper = (props: BadgeProps) => <Badge name="Zoiper" backgroundColor="#F47920" {...props} />

export default Zoiper
