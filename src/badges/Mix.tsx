
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Mix = (props: BadgeProps) => <Badge name="Mix" {...props} backgroundColor="#FF8126" />

export default Mix
