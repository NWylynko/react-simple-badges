
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Mix = (props: BadgeProps) => <Badge name="Mix" backgroundColor="#FF8126" {...props} />

export default Mix
