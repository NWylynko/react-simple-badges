
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Porsche = (props: BadgeProps) => <Badge name="Porsche" {...props} backgroundColor="#B12B28" />

export default Porsche
