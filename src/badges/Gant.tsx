
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Gant = (props: BadgeProps) => <Badge name="Géant" {...props} backgroundColor="#DD1F26" />

export default Gant
