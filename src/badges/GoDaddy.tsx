
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GoDaddy = (props: BadgeProps) => <Badge name="GoDaddy" {...props} backgroundColor="#1BDBDB" />

export default GoDaddy
