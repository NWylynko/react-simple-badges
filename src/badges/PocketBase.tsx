
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PocketBase = (props: BadgeProps) => <Badge name="PocketBase" {...props} backgroundColor="#B8DBE4" />

export default PocketBase
