
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const WarnerBros = (props: BadgeProps) => <Badge name="Warner Bros." backgroundColor="#004DB4" {...props} />

export default WarnerBros
