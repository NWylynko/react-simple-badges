
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const WarnerBros = (props: BadgeProps) => <Badge name="Warner Bros." {...props} backgroundColor="#004DB4" />

export default WarnerBros
