
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Shikimori = (props: BadgeProps) => <Badge name="Shikimori" {...props} backgroundColor="#343434" />

export default Shikimori
