
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Protoio = (props: BadgeProps) => <Badge name="Proto.io" backgroundColor="#34A7C1" {...props} />

export default Protoio
