
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Protoio = (props: BadgeProps) => <Badge name="Proto.io" {...props} backgroundColor="#34A7C1" />

export default Protoio
