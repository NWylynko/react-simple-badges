
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Dblp = (props: BadgeProps) => <Badge name="dblp" {...props} backgroundColor="#004F9F" />

export default Dblp
