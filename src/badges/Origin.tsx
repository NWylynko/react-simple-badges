
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Origin = (props: BadgeProps) => <Badge name="Origin" backgroundColor="#F56C2D" {...props} />

export default Origin
