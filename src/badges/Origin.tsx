
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Origin = (props: BadgeProps) => <Badge name="Origin" {...props} backgroundColor="#F56C2D" />

export default Origin
