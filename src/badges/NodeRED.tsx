
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const NodeRED = (props: BadgeProps) => <Badge name="Node-RED" {...props} backgroundColor="#8F0000" />

export default NodeRED
