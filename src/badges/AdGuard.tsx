
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const AdGuard = (props: BadgeProps) => <Badge name="AdGuard" backgroundColor="#68BC71" {...props} />

export default AdGuard
