
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AdGuard = (props: BadgeProps) => <Badge name="AdGuard" {...props} backgroundColor="#68BC71" />

export default AdGuard
