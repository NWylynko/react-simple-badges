
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Convertio = (props: BadgeProps) => <Badge name="Convertio" backgroundColor="#FF3333" {...props} />

export default Convertio
