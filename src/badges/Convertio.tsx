
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Convertio = (props: BadgeProps) => <Badge name="Convertio" {...props} backgroundColor="#FF3333" />

export default Convertio
