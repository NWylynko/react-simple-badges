
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Databricks = (props: BadgeProps) => <Badge name="Databricks" {...props} backgroundColor="#FF3621" />

export default Databricks
