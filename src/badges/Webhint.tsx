
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Webhint = (props: BadgeProps) => <Badge name="webhint" backgroundColor="#4700A3" {...props} />

export default Webhint
