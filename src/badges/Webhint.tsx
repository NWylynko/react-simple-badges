
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Webhint = (props: BadgeProps) => <Badge name="webhint" {...props} backgroundColor="#4700A3" />

export default Webhint
