
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const DuckDB = (props: BadgeProps) => <Badge name="DuckDB" {...props} backgroundColor="#FFF000" />

export default DuckDB
