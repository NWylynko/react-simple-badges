
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const DuckDB = (props: BadgeProps) => <Badge name="DuckDB" backgroundColor="#FFF000" {...props} />

export default DuckDB
