
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const NintendoNetwork = (props: BadgeProps) => <Badge name="Nintendo Network" {...props} backgroundColor="#FF7D00" />

export default NintendoNetwork
