
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const EdX = (props: BadgeProps) => <Badge name="edX" {...props} backgroundColor="#02262B" />

export default EdX
