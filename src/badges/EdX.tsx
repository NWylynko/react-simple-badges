
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const EdX = (props: BadgeProps) => <Badge name="edX" backgroundColor="#02262B" {...props} />

export default EdX
