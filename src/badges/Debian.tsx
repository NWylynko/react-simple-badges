
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Debian = (props: BadgeProps) => <Badge name="Debian" backgroundColor="#A81D33" {...props} />

export default Debian
