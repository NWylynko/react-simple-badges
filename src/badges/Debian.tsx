
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Debian = (props: BadgeProps) => <Badge name="Debian" {...props} backgroundColor="#A81D33" />

export default Debian
