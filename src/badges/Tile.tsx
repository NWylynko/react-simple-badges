
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Tile = (props: BadgeProps) => <Badge name="Tile" {...props} backgroundColor="#000000" />

export default Tile
