
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Tile = (props: BadgeProps) => <Badge name="Tile" backgroundColor="#000000" {...props} />

export default Tile
