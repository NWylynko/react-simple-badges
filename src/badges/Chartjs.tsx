
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Chartjs = (props: BadgeProps) => <Badge name="Chart.js" backgroundColor="#FF6384" {...props} />

export default Chartjs
