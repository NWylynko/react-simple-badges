
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Chartjs = (props: BadgeProps) => <Badge name="Chart.js" {...props} backgroundColor="#FF6384" />

export default Chartjs
