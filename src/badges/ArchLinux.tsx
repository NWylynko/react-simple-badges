
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ArchLinux = (props: BadgeProps) => <Badge name="Arch Linux" {...props} backgroundColor="#1793D1" />

export default ArchLinux
