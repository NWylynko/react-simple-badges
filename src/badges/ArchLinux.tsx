
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const ArchLinux = (props: BadgeProps) => <Badge name="Arch Linux" backgroundColor="#1793D1" {...props} />

export default ArchLinux
