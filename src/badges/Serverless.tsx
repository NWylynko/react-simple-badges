
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Serverless = (props: BadgeProps) => <Badge name="Serverless" backgroundColor="#FD5750" {...props} />

export default Serverless
