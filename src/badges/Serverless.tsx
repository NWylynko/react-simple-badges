
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Serverless = (props: BadgeProps) => <Badge name="Serverless" {...props} backgroundColor="#FD5750" />

export default Serverless
