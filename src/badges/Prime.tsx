
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Prime = (props: BadgeProps) => <Badge name="Prime" {...props} backgroundColor="#00A8E1" />

export default Prime
