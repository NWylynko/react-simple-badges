
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Org = (props: BadgeProps) => <Badge name="Org" {...props} backgroundColor="#77AA99" />

export default Org
