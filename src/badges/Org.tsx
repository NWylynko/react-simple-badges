
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Org = (props: BadgeProps) => <Badge name="Org" backgroundColor="#77AA99" {...props} />

export default Org
