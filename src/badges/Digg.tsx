
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Digg = (props: BadgeProps) => <Badge name="Digg" {...props} backgroundColor="#000000" />

export default Digg
