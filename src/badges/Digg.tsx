
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Digg = (props: BadgeProps) => <Badge name="Digg" backgroundColor="#000000" {...props} />

export default Digg
