
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Veritas = (props: BadgeProps) => <Badge name="Veritas" backgroundColor="#B1181E" {...props} />

export default Veritas
