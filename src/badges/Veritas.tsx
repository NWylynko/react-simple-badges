
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Veritas = (props: BadgeProps) => <Badge name="Veritas" {...props} backgroundColor="#B1181E" />

export default Veritas
