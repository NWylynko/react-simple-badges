
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Bugatti = (props: BadgeProps) => <Badge name="Bugatti" {...props} backgroundColor="#BE0030" />

export default Bugatti
