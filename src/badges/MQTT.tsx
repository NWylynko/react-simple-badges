
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const MQTT = (props: BadgeProps) => <Badge name="MQTT" backgroundColor="#660066" {...props} />

export default MQTT
