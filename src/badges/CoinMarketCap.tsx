
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CoinMarketCap = (props: BadgeProps) => <Badge name="CoinMarketCap" {...props} backgroundColor="#17181B" />

export default CoinMarketCap
