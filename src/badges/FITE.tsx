
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const FITE = (props: BadgeProps) => <Badge name="FITE" {...props} backgroundColor="#CA0404" />

export default FITE
