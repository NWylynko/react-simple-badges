
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const FITE = (props: BadgeProps) => <Badge name="FITE" backgroundColor="#CA0404" {...props} />

export default FITE
