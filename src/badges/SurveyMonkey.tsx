
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SurveyMonkey = (props: BadgeProps) => <Badge name="SurveyMonkey" {...props} backgroundColor="#00BF6F" />

export default SurveyMonkey
