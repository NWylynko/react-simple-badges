
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Tubi = (props: BadgeProps) => <Badge name="Tubi" backgroundColor="#000000" {...props} />

export default Tubi
