
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Tubi = (props: BadgeProps) => <Badge name="Tubi" {...props} backgroundColor="#000000" />

export default Tubi
