
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AmazonAlexa = (props: BadgeProps) => <Badge name="Amazon Alexa" {...props} backgroundColor="#00CAFF" />

export default AmazonAlexa
