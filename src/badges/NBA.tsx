
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const NBA = (props: BadgeProps) => <Badge name="NBA" {...props} backgroundColor="#253B73" />

export default NBA
