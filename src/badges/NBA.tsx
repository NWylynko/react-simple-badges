
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const NBA = (props: BadgeProps) => <Badge name="NBA" backgroundColor="#253B73" {...props} />

export default NBA
