
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const GitHubPages = (props: BadgeProps) => <Badge name="GitHub Pages" backgroundColor="#222222" {...props} />

export default GitHubPages
