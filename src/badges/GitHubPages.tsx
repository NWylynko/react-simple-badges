
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GitHubPages = (props: BadgeProps) => <Badge name="GitHub Pages" {...props} backgroundColor="#222222" />

export default GitHubPages
