
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const NVIDIA = (props: BadgeProps) => <Badge name="NVIDIA" {...props} backgroundColor="#76B900" />

export default NVIDIA
