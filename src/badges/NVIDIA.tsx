
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const NVIDIA = (props: BadgeProps) => <Badge name="NVIDIA" backgroundColor="#76B900" {...props} />

export default NVIDIA
