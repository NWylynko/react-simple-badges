
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const YouTubeMusic = (props: BadgeProps) => <Badge name="YouTube Music" {...props} backgroundColor="#FF0000" />

export default YouTubeMusic
