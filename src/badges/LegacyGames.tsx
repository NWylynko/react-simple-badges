
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const LegacyGames = (props: BadgeProps) => <Badge name="Legacy Games" {...props} backgroundColor="#144B9E" />

export default LegacyGames
