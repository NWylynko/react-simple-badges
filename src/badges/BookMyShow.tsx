
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const BookMyShow = (props: BadgeProps) => <Badge name="BookMyShow" {...props} backgroundColor="#C4242B" />

export default BookMyShow
