
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Buildkite = (props: BadgeProps) => <Badge name="Buildkite" {...props} backgroundColor="#14CC80" />

export default Buildkite
