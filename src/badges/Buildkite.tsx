
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Buildkite = (props: BadgeProps) => <Badge name="Buildkite" backgroundColor="#14CC80" {...props} />

export default Buildkite
