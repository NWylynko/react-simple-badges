
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const KDE = (props: BadgeProps) => <Badge name="KDE" {...props} backgroundColor="#1D99F3" />

export default KDE
