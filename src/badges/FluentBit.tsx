
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const FluentBit = (props: BadgeProps) => <Badge name="Fluent Bit" backgroundColor="#49BDA5" {...props} />

export default FluentBit
