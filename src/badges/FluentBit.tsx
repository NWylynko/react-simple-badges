
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const FluentBit = (props: BadgeProps) => <Badge name="Fluent Bit" {...props} backgroundColor="#49BDA5" />

export default FluentBit
