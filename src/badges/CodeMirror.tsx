
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CodeMirror = (props: BadgeProps) => <Badge name="CodeMirror" {...props} backgroundColor="#D30707" />

export default CodeMirror
