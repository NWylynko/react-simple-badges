
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Vim = (props: BadgeProps) => <Badge name="Vim" {...props} backgroundColor="#019733" />

export default Vim
