
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Vim = (props: BadgeProps) => <Badge name="Vim" backgroundColor="#019733" {...props} />

export default Vim
