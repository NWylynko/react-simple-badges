
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Rhinoceros = (props: BadgeProps) => <Badge name="Rhinoceros" backgroundColor="#801010" {...props} />

export default Rhinoceros
