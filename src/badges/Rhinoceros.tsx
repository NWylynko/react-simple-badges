
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Rhinoceros = (props: BadgeProps) => <Badge name="Rhinoceros" {...props} backgroundColor="#801010" />

export default Rhinoceros
