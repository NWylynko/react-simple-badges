
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Chai = (props: BadgeProps) => <Badge name="Chai" {...props} backgroundColor="#A30701" />

export default Chai
