
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const FACEIT = (props: BadgeProps) => <Badge name="FACEIT" backgroundColor="#FF5500" {...props} />

export default FACEIT
