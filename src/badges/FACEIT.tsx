
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const FACEIT = (props: BadgeProps) => <Badge name="FACEIT" {...props} backgroundColor="#FF5500" />

export default FACEIT
