
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Twitter = (props: BadgeProps) => <Badge name="Twitter" {...props} backgroundColor="#1DA1F2" />

export default Twitter
