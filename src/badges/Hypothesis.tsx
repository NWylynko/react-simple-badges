
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Hypothesis = (props: BadgeProps) => <Badge name="Hypothesis" {...props} backgroundColor="#BD1C2B" />

export default Hypothesis
