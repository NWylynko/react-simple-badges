
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Gradle = (props: BadgeProps) => <Badge name="Gradle" {...props} backgroundColor="#02303A" />

export default Gradle
