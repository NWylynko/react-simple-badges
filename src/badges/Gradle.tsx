
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Gradle = (props: BadgeProps) => <Badge name="Gradle" backgroundColor="#02303A" {...props} />

export default Gradle
