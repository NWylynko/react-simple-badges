
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const VMware = (props: BadgeProps) => <Badge name="VMware" backgroundColor="#607078" {...props} />

export default VMware
