
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const VMware = (props: BadgeProps) => <Badge name="VMware" {...props} backgroundColor="#607078" />

export default VMware
