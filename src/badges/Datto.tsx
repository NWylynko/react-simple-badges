
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Datto = (props: BadgeProps) => <Badge name="Datto" {...props} backgroundColor="#199ED9" />

export default Datto
