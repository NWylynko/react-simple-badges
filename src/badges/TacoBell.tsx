
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TacoBell = (props: BadgeProps) => <Badge name="Taco Bell" {...props} backgroundColor="#38096C" />

export default TacoBell
