
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Handshake = (props: BadgeProps) => <Badge name="Handshake" {...props} backgroundColor="#FF2F1C" />

export default Handshake
