
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Handshake = (props: BadgeProps) => <Badge name="Handshake" backgroundColor="#FF2F1C" {...props} />

export default Handshake
