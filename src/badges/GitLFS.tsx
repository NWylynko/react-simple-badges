
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GitLFS = (props: BadgeProps) => <Badge name="Git LFS" {...props} backgroundColor="#F64935" />

export default GitLFS
