
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const GitLFS = (props: BadgeProps) => <Badge name="Git LFS" backgroundColor="#F64935" {...props} />

export default GitLFS
