
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Observable = (props: BadgeProps) => <Badge name="Observable" backgroundColor="#353E58" {...props} />

export default Observable
