
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Observable = (props: BadgeProps) => <Badge name="Observable" {...props} backgroundColor="#353E58" />

export default Observable
