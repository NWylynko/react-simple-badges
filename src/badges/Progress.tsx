
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Progress = (props: BadgeProps) => <Badge name="Progress" {...props} backgroundColor="#5CE500" />

export default Progress
