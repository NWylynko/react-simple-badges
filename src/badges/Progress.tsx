
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Progress = (props: BadgeProps) => <Badge name="Progress" backgroundColor="#5CE500" {...props} />

export default Progress
