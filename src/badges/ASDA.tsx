
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ASDA = (props: BadgeProps) => <Badge name="ASDA" {...props} backgroundColor="#68A51C" />

export default ASDA
