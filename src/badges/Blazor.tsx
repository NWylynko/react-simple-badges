
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Blazor = (props: BadgeProps) => <Badge name="Blazor" backgroundColor="#512BD4" {...props} />

export default Blazor
