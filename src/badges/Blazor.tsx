
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Blazor = (props: BadgeProps) => <Badge name="Blazor" {...props} backgroundColor="#512BD4" />

export default Blazor
