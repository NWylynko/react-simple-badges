
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const CodeClimate = (props: BadgeProps) => <Badge name="Code Climate" backgroundColor="#000000" {...props} />

export default CodeClimate
