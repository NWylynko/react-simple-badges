
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CodeClimate = (props: BadgeProps) => <Badge name="Code Climate" {...props} backgroundColor="#000000" />

export default CodeClimate
