
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Weasyl = (props: BadgeProps) => <Badge name="Weasyl" {...props} backgroundColor="#990000" />

export default Weasyl
