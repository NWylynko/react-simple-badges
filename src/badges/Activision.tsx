
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Activision = (props: BadgeProps) => <Badge name="Activision" {...props} backgroundColor="#000000" />

export default Activision
