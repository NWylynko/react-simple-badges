
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Activision = (props: BadgeProps) => <Badge name="Activision" backgroundColor="#000000" {...props} />

export default Activision
