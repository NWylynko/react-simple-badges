
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Discover = (props: BadgeProps) => <Badge name="Discover" backgroundColor="#FF6000" {...props} />

export default Discover
