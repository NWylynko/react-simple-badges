
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Discover = (props: BadgeProps) => <Badge name="Discover" {...props} backgroundColor="#FF6000" />

export default Discover
