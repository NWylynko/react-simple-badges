
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Headspace = (props: BadgeProps) => <Badge name="Headspace" {...props} backgroundColor="#F47D31" />

export default Headspace
