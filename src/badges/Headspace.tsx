
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Headspace = (props: BadgeProps) => <Badge name="Headspace" backgroundColor="#F47D31" {...props} />

export default Headspace
