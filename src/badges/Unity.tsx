
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Unity = (props: BadgeProps) => <Badge name="Unity" backgroundColor="#FFFFFF" {...props} />

export default Unity
