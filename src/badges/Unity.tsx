
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Unity = (props: BadgeProps) => <Badge name="Unity" {...props} backgroundColor="#FFFFFF" />

export default Unity
