
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const RootsSage = (props: BadgeProps) => <Badge name="Roots Sage" backgroundColor="#525DDC" {...props} />

export default RootsSage
