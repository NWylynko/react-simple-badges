
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const RootsSage = (props: BadgeProps) => <Badge name="Roots Sage" {...props} backgroundColor="#525DDC" />

export default RootsSage
