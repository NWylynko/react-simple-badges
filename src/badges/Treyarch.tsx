
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Treyarch = (props: BadgeProps) => <Badge name="Treyarch" {...props} backgroundColor="#000000" />

export default Treyarch
