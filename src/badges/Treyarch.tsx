
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Treyarch = (props: BadgeProps) => <Badge name="Treyarch" backgroundColor="#000000" {...props} />

export default Treyarch
