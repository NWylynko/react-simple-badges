
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Zendesk = (props: BadgeProps) => <Badge name="Zendesk" backgroundColor="#03363D" {...props} />

export default Zendesk
