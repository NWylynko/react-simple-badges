
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Zendesk = (props: BadgeProps) => <Badge name="Zendesk" {...props} backgroundColor="#03363D" />

export default Zendesk
