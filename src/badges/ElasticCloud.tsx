
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ElasticCloud = (props: BadgeProps) => <Badge name="Elastic Cloud" {...props} backgroundColor="#005571" />

export default ElasticCloud
