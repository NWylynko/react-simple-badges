
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Bitcoin = (props: BadgeProps) => <Badge name="Bitcoin" {...props} backgroundColor="#F7931A" />

export default Bitcoin
