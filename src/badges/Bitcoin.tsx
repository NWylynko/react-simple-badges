
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Bitcoin = (props: BadgeProps) => <Badge name="Bitcoin" backgroundColor="#F7931A" {...props} />

export default Bitcoin
