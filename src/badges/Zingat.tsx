
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Zingat = (props: BadgeProps) => <Badge name="Zingat" backgroundColor="#009CFB" {...props} />

export default Zingat
