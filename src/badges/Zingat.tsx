
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Zingat = (props: BadgeProps) => <Badge name="Zingat" {...props} backgroundColor="#009CFB" />

export default Zingat
