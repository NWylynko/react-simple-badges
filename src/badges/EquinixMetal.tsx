
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const EquinixMetal = (props: BadgeProps) => <Badge name="Equinix Metal" {...props} backgroundColor="#ED2224" />

export default EquinixMetal
