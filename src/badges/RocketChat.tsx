
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const RocketChat = (props: BadgeProps) => <Badge name="Rocket.Chat" {...props} backgroundColor="#F5455C" />

export default RocketChat
