
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const RocketChat = (props: BadgeProps) => <Badge name="Rocket.Chat" backgroundColor="#F5455C" {...props} />

export default RocketChat
