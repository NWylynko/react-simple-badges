
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const IEEE = (props: BadgeProps) => <Badge name="IEEE" {...props} backgroundColor="#00629B" />

export default IEEE
