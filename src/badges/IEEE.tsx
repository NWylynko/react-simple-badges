
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const IEEE = (props: BadgeProps) => <Badge name="IEEE" backgroundColor="#00629B" {...props} />

export default IEEE
