
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const FARFETCH = (props: BadgeProps) => <Badge name="FARFETCH" backgroundColor="#000000" {...props} />

export default FARFETCH
