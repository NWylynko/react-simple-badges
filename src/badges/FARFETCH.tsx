
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const FARFETCH = (props: BadgeProps) => <Badge name="FARFETCH" {...props} backgroundColor="#000000" />

export default FARFETCH
