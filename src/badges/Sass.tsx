
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Sass = (props: BadgeProps) => <Badge name="Sass" {...props} backgroundColor="#CC6699" />

export default Sass
