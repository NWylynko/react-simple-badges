
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Sass = (props: BadgeProps) => <Badge name="Sass" backgroundColor="#CC6699" {...props} />

export default Sass
