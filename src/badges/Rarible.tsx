
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Rarible = (props: BadgeProps) => <Badge name="Rarible" {...props} backgroundColor="#FEDA03" />

export default Rarible
