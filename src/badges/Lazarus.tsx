
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Lazarus = (props: BadgeProps) => <Badge name="Lazarus" {...props} backgroundColor="#000000" />

export default Lazarus
