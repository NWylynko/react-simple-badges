
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Lazarus = (props: BadgeProps) => <Badge name="Lazarus" backgroundColor="#000000" {...props} />

export default Lazarus
