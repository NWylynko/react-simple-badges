
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Packagist = (props: BadgeProps) => <Badge name="Packagist" {...props} backgroundColor="#F28D1A" />

export default Packagist
