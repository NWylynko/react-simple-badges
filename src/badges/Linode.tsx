
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Linode = (props: BadgeProps) => <Badge name="Linode" backgroundColor="#00A95C" {...props} />

export default Linode
