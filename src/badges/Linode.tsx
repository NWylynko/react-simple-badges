
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Linode = (props: BadgeProps) => <Badge name="Linode" {...props} backgroundColor="#00A95C" />

export default Linode
