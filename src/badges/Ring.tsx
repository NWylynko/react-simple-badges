
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Ring = (props: BadgeProps) => <Badge name="Ring" {...props} backgroundColor="#1C9AD6" />

export default Ring
