
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Ring = (props: BadgeProps) => <Badge name="Ring" backgroundColor="#1C9AD6" {...props} />

export default Ring
