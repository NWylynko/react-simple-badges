
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const ArtixLinux = (props: BadgeProps) => <Badge name="Artix Linux" backgroundColor="#10A0CC" {...props} />

export default ArtixLinux
