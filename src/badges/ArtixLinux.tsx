
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ArtixLinux = (props: BadgeProps) => <Badge name="Artix Linux" {...props} backgroundColor="#10A0CC" />

export default ArtixLinux
