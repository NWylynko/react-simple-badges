
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SPDX = (props: BadgeProps) => <Badge name="SPDX" {...props} backgroundColor="#4398CC" />

export default SPDX
