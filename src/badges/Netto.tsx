
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Netto = (props: BadgeProps) => <Badge name="Netto" {...props} backgroundColor="#FFE500" />

export default Netto
