
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Netto = (props: BadgeProps) => <Badge name="Netto" backgroundColor="#FFE500" {...props} />

export default Netto
