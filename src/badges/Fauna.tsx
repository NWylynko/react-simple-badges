
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Fauna = (props: BadgeProps) => <Badge name="Fauna" {...props} backgroundColor="#3A1AB6" />

export default Fauna
