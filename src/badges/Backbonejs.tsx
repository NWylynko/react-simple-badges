
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Backbonejs = (props: BadgeProps) => <Badge name="Backbone.js" {...props} backgroundColor="#0071B5" />

export default Backbonejs
