
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Betfair = (props: BadgeProps) => <Badge name="Betfair" {...props} backgroundColor="#FFB80B" />

export default Betfair
