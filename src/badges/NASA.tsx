
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const NASA = (props: BadgeProps) => <Badge name="NASA" {...props} backgroundColor="#E03C31" />

export default NASA
