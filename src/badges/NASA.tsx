
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const NASA = (props: BadgeProps) => <Badge name="NASA" backgroundColor="#E03C31" {...props} />

export default NASA
