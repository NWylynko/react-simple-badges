
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const LibraryThing = (props: BadgeProps) => <Badge name="LibraryThing" {...props} backgroundColor="#251A15" />

export default LibraryThing
