
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const UnderArmour = (props: BadgeProps) => <Badge name="Under Armour" backgroundColor="#1D1D1D" {...props} />

export default UnderArmour
