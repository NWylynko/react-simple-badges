
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const UnderArmour = (props: BadgeProps) => <Badge name="Under Armour" {...props} backgroundColor="#1D1D1D" />

export default UnderArmour
