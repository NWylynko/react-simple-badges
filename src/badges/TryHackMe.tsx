
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TryHackMe = (props: BadgeProps) => <Badge name="TryHackMe" {...props} backgroundColor="#212C42" />

export default TryHackMe
