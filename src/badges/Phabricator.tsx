
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Phabricator = (props: BadgeProps) => <Badge name="Phabricator" {...props} backgroundColor="#4A5F88" />

export default Phabricator
