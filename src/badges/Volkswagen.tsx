
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Volkswagen = (props: BadgeProps) => <Badge name="Volkswagen" {...props} backgroundColor="#151F5D" />

export default Volkswagen
