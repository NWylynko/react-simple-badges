
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Volkswagen = (props: BadgeProps) => <Badge name="Volkswagen" backgroundColor="#151F5D" {...props} />

export default Volkswagen
