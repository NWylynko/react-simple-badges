
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Trakt = (props: BadgeProps) => <Badge name="Trakt" backgroundColor="#ED1C24" {...props} />

export default Trakt
