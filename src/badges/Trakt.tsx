
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Trakt = (props: BadgeProps) => <Badge name="Trakt" {...props} backgroundColor="#ED1C24" />

export default Trakt
