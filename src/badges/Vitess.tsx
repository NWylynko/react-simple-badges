
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Vitess = (props: BadgeProps) => <Badge name="Vitess" {...props} backgroundColor="#F16728" />

export default Vitess
