
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Vitess = (props: BadgeProps) => <Badge name="Vitess" backgroundColor="#F16728" {...props} />

export default Vitess
