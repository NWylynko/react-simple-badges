
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Octave = (props: BadgeProps) => <Badge name="Octave" backgroundColor="#0790C0" {...props} />

export default Octave
