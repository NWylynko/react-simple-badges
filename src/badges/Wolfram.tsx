
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Wolfram = (props: BadgeProps) => <Badge name="Wolfram" {...props} backgroundColor="#DD1100" />

export default Wolfram
