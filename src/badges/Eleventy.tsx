
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Eleventy = (props: BadgeProps) => <Badge name="Eleventy" {...props} backgroundColor="#000000" />

export default Eleventy
