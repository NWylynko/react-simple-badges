
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Eleventy = (props: BadgeProps) => <Badge name="Eleventy" backgroundColor="#000000" {...props} />

export default Eleventy
