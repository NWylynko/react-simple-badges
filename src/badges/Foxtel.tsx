
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Foxtel = (props: BadgeProps) => <Badge name="Foxtel" {...props} backgroundColor="#EB5205" />

export default Foxtel
