
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const ReactRouter = (props: BadgeProps) => <Badge name="React Router" backgroundColor="#CA4245" {...props} />

export default ReactRouter
