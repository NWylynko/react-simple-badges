
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ReactRouter = (props: BadgeProps) => <Badge name="React Router" {...props} backgroundColor="#CA4245" />

export default ReactRouter
