
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const IBM = (props: BadgeProps) => <Badge name="IBM" {...props} backgroundColor="#052FAD" />

export default IBM
