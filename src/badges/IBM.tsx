
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const IBM = (props: BadgeProps) => <Badge name="IBM" backgroundColor="#052FAD" {...props} />

export default IBM
