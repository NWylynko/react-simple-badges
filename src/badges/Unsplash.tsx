
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Unsplash = (props: BadgeProps) => <Badge name="Unsplash" {...props} backgroundColor="#000000" />

export default Unsplash
