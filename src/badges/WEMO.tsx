
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const WEMO = (props: BadgeProps) => <Badge name="WEMO" {...props} backgroundColor="#72D44C" />

export default WEMO
