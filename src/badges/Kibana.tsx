
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Kibana = (props: BadgeProps) => <Badge name="Kibana" backgroundColor="#005571" {...props} />

export default Kibana
