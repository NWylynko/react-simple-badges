
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Kibana = (props: BadgeProps) => <Badge name="Kibana" {...props} backgroundColor="#005571" />

export default Kibana
