
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CRYENGINE = (props: BadgeProps) => <Badge name="CRYENGINE" {...props} backgroundColor="#000000" />

export default CRYENGINE
