
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Commodore = (props: BadgeProps) => <Badge name="Commodore" {...props} backgroundColor="#1E2A4E" />

export default Commodore
