
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const OpenSearch = (props: BadgeProps) => <Badge name="OpenSearch" {...props} backgroundColor="#005EB8" />

export default OpenSearch
