
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AirCanada = (props: BadgeProps) => <Badge name="Air Canada" {...props} backgroundColor="#F01428" />

export default AirCanada
