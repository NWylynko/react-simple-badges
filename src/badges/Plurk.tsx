
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Plurk = (props: BadgeProps) => <Badge name="Plurk" {...props} backgroundColor="#FF574D" />

export default Plurk
