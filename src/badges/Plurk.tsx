
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Plurk = (props: BadgeProps) => <Badge name="Plurk" backgroundColor="#FF574D" {...props} />

export default Plurk
