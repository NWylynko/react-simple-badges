
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Jamstack = (props: BadgeProps) => <Badge name="Jamstack" {...props} backgroundColor="#F0047F" />

export default Jamstack
