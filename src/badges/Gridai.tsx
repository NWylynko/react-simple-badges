
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Gridai = (props: BadgeProps) => <Badge name="Grid.ai" {...props} backgroundColor="#78FF96" />

export default Gridai
