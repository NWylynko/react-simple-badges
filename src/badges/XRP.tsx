
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const XRP = (props: BadgeProps) => <Badge name="XRP" {...props} backgroundColor="#25A768" />

export default XRP
