
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const XRP = (props: BadgeProps) => <Badge name="XRP" backgroundColor="#25A768" {...props} />

export default XRP
