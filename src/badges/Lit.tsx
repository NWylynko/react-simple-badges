
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Lit = (props: BadgeProps) => <Badge name="Lit" backgroundColor="#324FFF" {...props} />

export default Lit
