
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Lit = (props: BadgeProps) => <Badge name="Lit" {...props} backgroundColor="#324FFF" />

export default Lit
