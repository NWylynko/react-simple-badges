
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Asciinema = (props: BadgeProps) => <Badge name="asciinema" {...props} backgroundColor="#D40000" />

export default Asciinema
