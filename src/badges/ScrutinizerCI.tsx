
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ScrutinizerCI = (props: BadgeProps) => <Badge name="Scrutinizer CI" {...props} backgroundColor="#8A9296" />

export default ScrutinizerCI
