
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Qzone = (props: BadgeProps) => <Badge name="Qzone" {...props} backgroundColor="#FECE00" />

export default Qzone
