
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Qzone = (props: BadgeProps) => <Badge name="Qzone" backgroundColor="#FECE00" {...props} />

export default Qzone
