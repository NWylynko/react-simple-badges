
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Checkmarx = (props: BadgeProps) => <Badge name="Checkmarx" {...props} backgroundColor="#54B848" />

export default Checkmarx
