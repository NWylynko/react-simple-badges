
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Asana = (props: BadgeProps) => <Badge name="Asana" {...props} backgroundColor="#273347" />

export default Asana
