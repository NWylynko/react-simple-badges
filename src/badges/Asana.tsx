
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Asana = (props: BadgeProps) => <Badge name="Asana" backgroundColor="#273347" {...props} />

export default Asana
