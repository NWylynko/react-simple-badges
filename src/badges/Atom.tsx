
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Atom = (props: BadgeProps) => <Badge name="Atom" backgroundColor="#66595C" {...props} />

export default Atom
