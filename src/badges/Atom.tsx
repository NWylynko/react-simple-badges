
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Atom = (props: BadgeProps) => <Badge name="Atom" {...props} backgroundColor="#66595C" />

export default Atom
