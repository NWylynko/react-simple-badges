
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TraefikMesh = (props: BadgeProps) => <Badge name="Traefik Mesh" {...props} backgroundColor="#9D0FB0" />

export default TraefikMesh
