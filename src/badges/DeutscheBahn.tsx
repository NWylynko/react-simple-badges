
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const DeutscheBahn = (props: BadgeProps) => <Badge name="Deutsche Bahn" {...props} backgroundColor="#F01414" />

export default DeutscheBahn
