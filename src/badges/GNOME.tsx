
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const GNOME = (props: BadgeProps) => <Badge name="GNOME" backgroundColor="#4A86CF" {...props} />

export default GNOME
