
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const GNOME = (props: BadgeProps) => <Badge name="GNOME" {...props} backgroundColor="#4A86CF" />

export default GNOME
