
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Enpass = (props: BadgeProps) => <Badge name="Enpass" {...props} backgroundColor="#0D47A1" />

export default Enpass
