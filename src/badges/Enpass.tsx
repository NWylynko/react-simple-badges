
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Enpass = (props: BadgeProps) => <Badge name="Enpass" backgroundColor="#0D47A1" {...props} />

export default Enpass
