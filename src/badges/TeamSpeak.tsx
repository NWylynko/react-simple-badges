
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TeamSpeak = (props: BadgeProps) => <Badge name="TeamSpeak" {...props} backgroundColor="#2580C3" />

export default TeamSpeak
