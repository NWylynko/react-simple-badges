
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const TeamSpeak = (props: BadgeProps) => <Badge name="TeamSpeak" backgroundColor="#2580C3" {...props} />

export default TeamSpeak
