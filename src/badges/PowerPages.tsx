
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PowerPages = (props: BadgeProps) => <Badge name="Power Pages" {...props} backgroundColor="#A493E7" />

export default PowerPages
