
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const SketchUp = (props: BadgeProps) => <Badge name="SketchUp" backgroundColor="#005F9E" {...props} />

export default SketchUp
