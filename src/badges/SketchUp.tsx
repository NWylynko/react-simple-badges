
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SketchUp = (props: BadgeProps) => <Badge name="SketchUp" {...props} backgroundColor="#005F9E" />

export default SketchUp
