
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const NounProject = (props: BadgeProps) => <Badge name="Noun Project" {...props} backgroundColor="#000000" />

export default NounProject
