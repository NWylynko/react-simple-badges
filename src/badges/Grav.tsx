
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Grav = (props: BadgeProps) => <Badge name="Grav" backgroundColor="#221E1F" {...props} />

export default Grav
