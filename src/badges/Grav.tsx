
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Grav = (props: BadgeProps) => <Badge name="Grav" {...props} backgroundColor="#221E1F" />

export default Grav
