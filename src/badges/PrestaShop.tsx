
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const PrestaShop = (props: BadgeProps) => <Badge name="PrestaShop" {...props} backgroundColor="#DF0067" />

export default PrestaShop
