
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const PrestaShop = (props: BadgeProps) => <Badge name="PrestaShop" backgroundColor="#DF0067" {...props} />

export default PrestaShop
