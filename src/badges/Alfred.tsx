
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Alfred = (props: BadgeProps) => <Badge name="Alfred" {...props} backgroundColor="#5C1F87" />

export default Alfred
