
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Bookmeter = (props: BadgeProps) => <Badge name="Bookmeter" {...props} backgroundColor="#64BC4B" />

export default Bookmeter
