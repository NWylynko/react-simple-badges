
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const IconsEight = (props: BadgeProps) => <Badge name="Icons8" {...props} backgroundColor="#1FB141" />

export default IconsEight
