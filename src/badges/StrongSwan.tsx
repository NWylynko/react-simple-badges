
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const StrongSwan = (props: BadgeProps) => <Badge name="strongSwan" {...props} backgroundColor="#E00033" />

export default StrongSwan
