
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Roots = (props: BadgeProps) => <Badge name="Roots" {...props} backgroundColor="#525DDC" />

export default Roots
