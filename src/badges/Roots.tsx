
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Roots = (props: BadgeProps) => <Badge name="Roots" backgroundColor="#525DDC" {...props} />

export default Roots
