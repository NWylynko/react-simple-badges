
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Deezer = (props: BadgeProps) => <Badge name="Deezer" {...props} backgroundColor="#FEAA2D" />

export default Deezer
