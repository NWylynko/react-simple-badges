
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Deezer = (props: BadgeProps) => <Badge name="Deezer" backgroundColor="#FEAA2D" {...props} />

export default Deezer
