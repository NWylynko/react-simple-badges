
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SemaphoreCI = (props: BadgeProps) => <Badge name="Semaphore CI" {...props} backgroundColor="#19A974" />

export default SemaphoreCI
