
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Bugcrowd = (props: BadgeProps) => <Badge name="Bugcrowd" backgroundColor="#F26822" {...props} />

export default Bugcrowd
