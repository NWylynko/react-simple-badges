
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Bugcrowd = (props: BadgeProps) => <Badge name="Bugcrowd" {...props} backgroundColor="#F26822" />

export default Bugcrowd
