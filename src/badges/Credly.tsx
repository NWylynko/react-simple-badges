
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Credly = (props: BadgeProps) => <Badge name="Credly" backgroundColor="#FF6B00" {...props} />

export default Credly
