
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Credly = (props: BadgeProps) => <Badge name="Credly" {...props} backgroundColor="#FF6B00" />

export default Credly
