
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const MLB = (props: BadgeProps) => <Badge name="MLB" backgroundColor="#041E42" {...props} />

export default MLB
