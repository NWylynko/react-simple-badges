
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MLB = (props: BadgeProps) => <Badge name="MLB" {...props} backgroundColor="#041E42" />

export default MLB
