
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Sessionize = (props: BadgeProps) => <Badge name="Sessionize" {...props} backgroundColor="#1AB394" />

export default Sessionize
