
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Sessionize = (props: BadgeProps) => <Badge name="Sessionize" backgroundColor="#1AB394" {...props} />

export default Sessionize
