
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const MTR = (props: BadgeProps) => <Badge name="MTR" backgroundColor="#AC2E45" {...props} />

export default MTR
