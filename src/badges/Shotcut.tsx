
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Shotcut = (props: BadgeProps) => <Badge name="Shotcut" backgroundColor="#115C77" {...props} />

export default Shotcut
