
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Shotcut = (props: BadgeProps) => <Badge name="Shotcut" {...props} backgroundColor="#115C77" />

export default Shotcut
