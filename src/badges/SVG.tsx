
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const SVG = (props: BadgeProps) => <Badge name="SVG" backgroundColor="#FFB13B" {...props} />

export default SVG
