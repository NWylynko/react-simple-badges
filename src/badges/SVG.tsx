
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SVG = (props: BadgeProps) => <Badge name="SVG" {...props} backgroundColor="#FFB13B" />

export default SVG
