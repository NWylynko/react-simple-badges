
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Macys = (props: BadgeProps) => <Badge name="Macy's" backgroundColor="#E21A2C" {...props} />

export default Macys
