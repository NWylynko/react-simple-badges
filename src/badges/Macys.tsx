
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Macys = (props: BadgeProps) => <Badge name="Macy's" {...props} backgroundColor="#E21A2C" />

export default Macys
