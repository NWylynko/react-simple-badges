
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ONLYOFFICE = (props: BadgeProps) => <Badge name="ONLYOFFICE" {...props} backgroundColor="#444444" />

export default ONLYOFFICE
