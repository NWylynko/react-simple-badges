
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Target = (props: BadgeProps) => <Badge name="Target" backgroundColor="#CC0000" {...props} />

export default Target
