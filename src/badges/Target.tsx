
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Target = (props: BadgeProps) => <Badge name="Target" {...props} backgroundColor="#CC0000" />

export default Target
