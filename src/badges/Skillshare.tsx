
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Skillshare = (props: BadgeProps) => <Badge name="Skillshare" backgroundColor="#00FF84" {...props} />

export default Skillshare
