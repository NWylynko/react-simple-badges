
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Skillshare = (props: BadgeProps) => <Badge name="Skillshare" {...props} backgroundColor="#00FF84" />

export default Skillshare
