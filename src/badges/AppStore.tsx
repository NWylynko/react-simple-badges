
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const AppStore = (props: BadgeProps) => <Badge name="App Store" {...props} backgroundColor="#0D96F6" />

export default AppStore
