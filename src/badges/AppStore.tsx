
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const AppStore = (props: BadgeProps) => <Badge name="App Store" backgroundColor="#0D96F6" {...props} />

export default AppStore
