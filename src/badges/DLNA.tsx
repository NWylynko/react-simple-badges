
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const DLNA = (props: BadgeProps) => <Badge name="DLNA" backgroundColor="#48A842" {...props} />

export default DLNA
