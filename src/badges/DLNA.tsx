
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const DLNA = (props: BadgeProps) => <Badge name="DLNA" {...props} backgroundColor="#48A842" />

export default DLNA
