
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Unilever = (props: BadgeProps) => <Badge name="Unilever" backgroundColor="#1F36C7" {...props} />

export default Unilever
