
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Unilever = (props: BadgeProps) => <Badge name="Unilever" {...props} backgroundColor="#1F36C7" />

export default Unilever
