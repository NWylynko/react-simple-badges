
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Abstract = (props: BadgeProps) => <Badge name="Abstract" {...props} backgroundColor="#191A1B" />

export default Abstract
