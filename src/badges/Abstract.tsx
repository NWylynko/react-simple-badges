
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Abstract = (props: BadgeProps) => <Badge name="Abstract" backgroundColor="#191A1B" {...props} />

export default Abstract
