
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Percy = (props: BadgeProps) => <Badge name="Percy" backgroundColor="#9E66BF" {...props} />

export default Percy
