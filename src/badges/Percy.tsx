
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Percy = (props: BadgeProps) => <Badge name="Percy" {...props} backgroundColor="#9E66BF" />

export default Percy
