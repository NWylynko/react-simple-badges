
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Eagle = (props: BadgeProps) => <Badge name="Eagle" {...props} backgroundColor="#0072EF" />

export default Eagle
