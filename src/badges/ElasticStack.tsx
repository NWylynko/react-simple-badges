
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ElasticStack = (props: BadgeProps) => <Badge name="Elastic Stack" {...props} backgroundColor="#005571" />

export default ElasticStack
