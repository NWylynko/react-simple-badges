
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Funimation = (props: BadgeProps) => <Badge name="Funimation" {...props} backgroundColor="#5B0BB5" />

export default Funimation
