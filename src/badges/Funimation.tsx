
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Funimation = (props: BadgeProps) => <Badge name="Funimation" backgroundColor="#5B0BB5" {...props} />

export default Funimation
