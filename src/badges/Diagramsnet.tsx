
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Diagramsnet = (props: BadgeProps) => <Badge name="diagrams.net" {...props} backgroundColor="#F08705" />

export default Diagramsnet
