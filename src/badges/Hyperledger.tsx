
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Hyperledger = (props: BadgeProps) => <Badge name="Hyperledger" {...props} backgroundColor="#2F3134" />

export default Hyperledger
