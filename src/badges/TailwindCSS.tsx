
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const TailwindCSS = (props: BadgeProps) => <Badge name="Tailwind CSS" {...props} backgroundColor="#06B6D4" />

export default TailwindCSS
