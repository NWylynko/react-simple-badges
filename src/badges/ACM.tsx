
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const ACM = (props: BadgeProps) => <Badge name="ACM" backgroundColor="#0085CA" {...props} />

export default ACM
