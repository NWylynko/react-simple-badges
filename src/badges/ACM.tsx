
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const ACM = (props: BadgeProps) => <Badge name="ACM" {...props} backgroundColor="#0085CA" />

export default ACM
