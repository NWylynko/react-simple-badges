
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const CloudCannon = (props: BadgeProps) => <Badge name="CloudCannon" backgroundColor="#407AFC" {...props} />

export default CloudCannon
