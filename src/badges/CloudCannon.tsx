
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CloudCannon = (props: BadgeProps) => <Badge name="CloudCannon" {...props} backgroundColor="#407AFC" />

export default CloudCannon
