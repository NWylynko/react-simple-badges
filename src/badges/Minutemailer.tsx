
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Minutemailer = (props: BadgeProps) => <Badge name="Minutemailer" {...props} backgroundColor="#30B980" />

export default Minutemailer
