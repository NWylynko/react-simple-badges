
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Dovecot = (props: BadgeProps) => <Badge name="Dovecot" {...props} backgroundColor="#54BCAB" />

export default Dovecot
