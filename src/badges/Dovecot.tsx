
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Dovecot = (props: BadgeProps) => <Badge name="Dovecot" backgroundColor="#54BCAB" {...props} />

export default Dovecot
