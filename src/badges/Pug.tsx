
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Pug = (props: BadgeProps) => <Badge name="Pug" {...props} backgroundColor="#A86454" />

export default Pug
