
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Coda = (props: BadgeProps) => <Badge name="Coda" backgroundColor="#F46A54" {...props} />

export default Coda
