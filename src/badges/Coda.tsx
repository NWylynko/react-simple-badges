
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Coda = (props: BadgeProps) => <Badge name="Coda" {...props} backgroundColor="#F46A54" />

export default Coda
