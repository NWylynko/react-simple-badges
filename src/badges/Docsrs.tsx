
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Docsrs = (props: BadgeProps) => <Badge name="Docs.rs" backgroundColor="#000000" {...props} />

export default Docsrs
