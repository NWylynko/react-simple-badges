
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Docsrs = (props: BadgeProps) => <Badge name="Docs.rs" {...props} backgroundColor="#000000" />

export default Docsrs
