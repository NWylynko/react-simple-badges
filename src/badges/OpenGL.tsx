
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const OpenGL = (props: BadgeProps) => <Badge name="OpenGL" backgroundColor="#5586A4" {...props} />

export default OpenGL
