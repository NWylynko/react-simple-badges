
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const OpenGL = (props: BadgeProps) => <Badge name="OpenGL" {...props} backgroundColor="#5586A4" />

export default OpenGL
