
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const BigCartel = (props: BadgeProps) => <Badge name="Big Cartel" {...props} backgroundColor="#222222" />

export default BigCartel
