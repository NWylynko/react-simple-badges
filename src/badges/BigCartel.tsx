
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const BigCartel = (props: BadgeProps) => <Badge name="Big Cartel" backgroundColor="#222222" {...props} />

export default BigCartel
