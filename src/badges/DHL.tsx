
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const DHL = (props: BadgeProps) => <Badge name="DHL" {...props} backgroundColor="#FFCC00" />

export default DHL
