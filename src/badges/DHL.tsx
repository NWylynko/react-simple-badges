
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const DHL = (props: BadgeProps) => <Badge name="DHL" backgroundColor="#FFCC00" {...props} />

export default DHL
