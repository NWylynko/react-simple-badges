
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const EditorConfig = (props: BadgeProps) => <Badge name="EditorConfig" {...props} backgroundColor="#FEFEFE" />

export default EditorConfig
