
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const EditorConfig = (props: BadgeProps) => <Badge name="EditorConfig" backgroundColor="#FEFEFE" {...props} />

export default EditorConfig
