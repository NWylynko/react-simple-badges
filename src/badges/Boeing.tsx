
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Boeing = (props: BadgeProps) => <Badge name="Boeing" {...props} backgroundColor="#1D439C" />

export default Boeing
