
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Boeing = (props: BadgeProps) => <Badge name="Boeing" backgroundColor="#1D439C" {...props} />

export default Boeing
