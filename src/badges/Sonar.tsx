
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Sonar = (props: BadgeProps) => <Badge name="Sonar" {...props} backgroundColor="#FD3456" />

export default Sonar
