
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Sonar = (props: BadgeProps) => <Badge name="Sonar" backgroundColor="#FD3456" {...props} />

export default Sonar
