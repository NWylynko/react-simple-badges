
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Disqus = (props: BadgeProps) => <Badge name="Disqus" {...props} backgroundColor="#2E9FFF" />

export default Disqus
