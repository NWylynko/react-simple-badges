
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Pexels = (props: BadgeProps) => <Badge name="Pexels" {...props} backgroundColor="#05A081" />

export default Pexels
