
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const LinkedIn = (props: BadgeProps) => <Badge name="LinkedIn" {...props} backgroundColor="#0A66C2" />

export default LinkedIn
