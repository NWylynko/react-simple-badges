
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const MakerBot = (props: BadgeProps) => <Badge name="MakerBot" backgroundColor="#FF1E0D" {...props} />

export default MakerBot
