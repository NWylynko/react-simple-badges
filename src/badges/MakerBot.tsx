
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const MakerBot = (props: BadgeProps) => <Badge name="MakerBot" {...props} backgroundColor="#FF1E0D" />

export default MakerBot
