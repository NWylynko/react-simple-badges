
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Todoist = (props: BadgeProps) => <Badge name="Todoist" {...props} backgroundColor="#E44332" />

export default Todoist
