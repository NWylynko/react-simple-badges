
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Todoist = (props: BadgeProps) => <Badge name="Todoist" backgroundColor="#E44332" {...props} />

export default Todoist
