
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const CBS = (props: BadgeProps) => <Badge name="CBS" {...props} backgroundColor="#033963" />

export default CBS
