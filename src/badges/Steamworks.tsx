
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const Steamworks = (props: BadgeProps) => <Badge name="Steamworks" backgroundColor="#1E1E1E" {...props} />

export default Steamworks
