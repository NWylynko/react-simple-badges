
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Steamworks = (props: BadgeProps) => <Badge name="Steamworks" {...props} backgroundColor="#1E1E1E" />

export default Steamworks
