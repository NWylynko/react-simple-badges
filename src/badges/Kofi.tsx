
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Kofi = (props: BadgeProps) => <Badge name="Ko-fi" {...props} backgroundColor="#FF5E5B" />

export default Kofi
