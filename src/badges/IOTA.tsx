
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const IOTA = (props: BadgeProps) => <Badge name="IOTA" {...props} backgroundColor="#131F37" />

export default IOTA
