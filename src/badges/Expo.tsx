
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Expo = (props: BadgeProps) => <Badge name="Expo" {...props} backgroundColor="#000020" />

export default Expo
