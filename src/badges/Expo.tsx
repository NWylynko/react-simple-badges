
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const Expo = (props: BadgeProps) => <Badge name="Expo" backgroundColor="#000020" {...props} />

export default Expo
