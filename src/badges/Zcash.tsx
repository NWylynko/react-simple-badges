
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Zcash = (props: BadgeProps) => <Badge name="Zcash" {...props} backgroundColor="#F4B728" />

export default Zcash
