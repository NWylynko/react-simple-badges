
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge"

const SMRT = (props: BadgeProps) => <Badge name="SMRT" backgroundColor="#EE2E24" {...props} />

export default SMRT
