
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const SMRT = (props: BadgeProps) => <Badge name="SMRT" {...props} backgroundColor="#EE2E24" />

export default SMRT
