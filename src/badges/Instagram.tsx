
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const Instagram = (props: BadgeProps) => <Badge name="Instagram" {...props} backgroundColor="#E4405F" />

export default Instagram
