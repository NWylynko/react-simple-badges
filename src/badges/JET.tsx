
import React from "react"
import { Badge } from "../badge.js"
import type { BadgeProps } from "../badge.js"

const JET = (props: BadgeProps) => <Badge name="JET" backgroundColor="#FBBA00" {...props} />

export default JET
