
import React from "react"
import { Badge } from "../badge"
import type { BadgeProps } from "../badge"

const JET = (props: BadgeProps) => <Badge name="JET" {...props} backgroundColor="#FBBA00" />

export default JET
